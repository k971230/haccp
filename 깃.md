# Git 인수인계 — CCP 가열공정 작성 예시 (Git Bash)

> **대상:** 신규·기존 팀원 (화면 하나 고쳐서 운영까지 올리는 절차)  
> **터미널:** **Git Bash 전용** (아래 명령을 그대로 복사)  
> **저장소 루트 예:** `/d/haccp` (본인 PC 경로에 맞게 바꿀 것)  
> **금지:** `main`에 직접 커밋·푸시 · `.env` 커밋 · Build Now 없이 “반영됐다”고 가정

이 문서는 **시나리오 하나**로 끝까지 보여 준다.

| 항목 | 값 |
|------|-----|
| 화면 이름 | CCP 가열공정 작성 |
| 로컬 URL | `http://localhost:4173/haccp/draft/ccp-monitoring/ccp-htg` |
| 운영 URL (예) | `http://180.71.58.87/haccp/` 아래 동일 화면 (Path에 **`/haccp`** 접두). **`https` 로 열지 않는다** — `README.md` · `운영.md` 10.2 |
| 브랜치 이름 | `feature/ccp-htg-judge-required` |
| 배포 Job | Jenkins **`haccp-deploy`** → **Build Now** |
| 안 쓰는 Job | `haccp-audit` (야간 규약 감시, 배포 아님) |

개발 루프 정본: [`개발.md`](개발.md) · 환경: [`환경구축.md`](환경구축.md) · 운영: [`운영.md`](운영.md)

---

## 0. 반드시 기억할 순서

```text
main 최신화
  → feature/ccp-htg-judge-required 생성
  → 코드 수정 + 로컬 4173 확인
  → tsc 검증
  → 커밋
  → push + PR
  → main 머지
  → Jenkins haccp-deploy Build Now
  → 운영 URL 확인
```

**강조:** GitHub에 `main` 머지만 하면 **서버는 안 바뀐다.**  
반드시 Jenkins에서 **Build Now**를 눌러야 운영에 올라간다.

---

## 1. main 맞추고 브랜치 만들기

Git Bash:

```bash
cd /d/haccp

git checkout main
git pull origin main

# CCP 가열공정 작성 작업 브랜치 — 이 이름을 그대로 써도 된다
git checkout -b feature/ccp-htg-judge-required

# 확인: main 이면 안 된다
git branch --show-current
# → feature/ccp-htg-judge-required
```

다른 화면이면 브랜치만 바꾼다. 예: `feature/hyg-process-label`  
긴급 장애만 `hotfix/...` 사용.

---

## 2. 어디를 고치나 (CCP 가열공정 작성)

| 역할 | 경로 |
|------|------|
| 화면 등록 | `frontend/haccp-web/src/shell/screenRegistry.tsx` (`ccp-htg`) |
| 화면 구현 | `frontend/haccp-web/src/pages/draft/ccp-monitoring/CcpHtgDraftPage.tsx` |
| BE/SP | 필요 시 `backend/haccp-api/` · `db_sasshaccp/` |

인수인계 주석(FE·BE 동일 밀도)은 [`.cursor/rules/05-handoff-comments.mdc`](.cursor/rules/05-handoff-comments.mdc).

---

## 3. 로컬에서 화면 띄우기 (Git Bash · 터미널 2개)

**터미널 A — API (7070)**

```bash
cd /d/haccp/backend/haccp-api
./mvnw spring-boot:run
```

**터미널 B — 웹 (4173)**

```bash
cd /d/haccp/frontend/haccp-web
npm run dev
```

브라우저:

```text
http://localhost:4173/haccp/draft/ccp-monitoring/ccp-htg
```

`5174`가 뜨면 잘못된 설정/옛 프로세스다. 현행은 **4173**이다.  
API CORS는 `http://localhost:4173` 이어야 한다 ([`환경구축.md`](환경구축.md)).

---

## 4. 커밋 전 검증

```bash
cd /d/haccp/frontend/haccp-web
npx tsc --noEmit
npm run lint
npm test -- --run
```

백엔드도 건드렸으면:

```bash
cd /d/haccp/backend/haccp-api
./mvnw -q -DskipTests compile
```

시크릿 실수 방지:

```bash
cd /d/haccp
bash scripts/pre-commit-check-secrets.sh
```

---

## 5. 커밋 (Conventional Commits)

```bash
cd /d/haccp

git status
git diff

# .env 가 안 들어갔는지 확인한 뒤
git add frontend/haccp-web/src/...

git commit -m "$(cat <<'EOF'
feat(fe): CCP 가열공정 작성 화면 수정

EOF
)"
```

메시지 형식:

```text
<type>(<scope>): <subject>
```

이 시나리오 예:

```text
feat(fe): CCP 가열공정 작성 저장 버튼 라벨 수정
fix(fe): CCP 가열공정 작성 목록 조회 조건 보정
fix(be): CCP 가열공정 관련 SP 조회 버그 수정
```

`type`: `feat` · `fix` · `chore` · `docs` · …  
`scope`: `fe` · `be` · `db` · `infra` · `docs`

---

## 6. push + PR

```bash
cd /d/haccp

git push -u origin HEAD
```

PR 생성 (`gh` 있을 때):

```bash
gh pr create --base main \
  --title "[STEP nn] feat(fe): CCP 가열공정 작성 화면 수정" \
  --body "$(cat <<'EOF'
## Summary
- CCP 가열공정 작성(`ccp-htg`) 변경 요약

## Test plan
- [ ] http://localhost:4173/haccp/draft/ccp-monitoring/ccp-htg
- [ ] npx tsc --noEmit
- [ ] main 머지 후 haccp-deploy Build Now
- [ ] 운영 /haccp 경로에서 동일 화면 확인

EOF
)"
```

`gh` 없으면 push 후 GitHub이 준  
`https://github.com/k971230/haccp/pull/new/feature/ccp-htg-judge-required` 링크를 연다.

**PR base는 항상 `main`.**

---

## 7. main 머지

**작성자가 스스로 머지하지 않는다.** 리뷰어 없는 self-merge 는 금지다
(`.cursor/rules/03-branching.mdc`). 리뷰어 승인이 붙은 뒤 리뷰어나 관리자가 머지한다.

```bash
# 승인이 붙었는지 먼저 본다
gh pr view <번호> --json reviewDecision -q .reviewDecision
# → APPROVED 여야 한다

# 머지는 리뷰어·관리자가 친다
gh pr merge <번호> --merge --delete-branch
```

로컬을 다시 main에 맞춤:

```bash
git checkout main
git pull origin main
```

이 시점 = **코드가 GitHub main에만** 올라간 상태.  
**운영 서버 이미지는 아직 이전 버전일 수 있다.**

---

## 8. 운영 반영 (팀원에게 가장 강조)

1. Jenkins 접속  
2. Job 이름: **`haccp-deploy`**  
3. 버튼: **Build Now**  
4. 콘솔 로그에서 실패 없는지 · `TAG=1.0.x` 확인  
5. 브라우저에서 운영 Path로 CCP 가열공정 작성 확인  

| 하면 됨 | 하면 안 됨 |
|---------|-----------|
| `haccp-deploy` Build Now | `haccp-audit` 만 돌리고 배포됐다고 착각 |
| 운영 URL에 `/haccp` 포함 여부 확인 | 로컬 4173만 보고 운영 반영 완료로 판단 |
| 실패 시 콘솔·Credentials·SSH 확인 ([`운영.md`](운영.md)) | `main`에 핫픽스 직커밋 |

---

## 9. 팀원 FAQ

**Q. 브랜치 이름을 꼭 `feature/ccp-htg-judge-required`로 해야 하나?**  
A. CCP 가열공정이면 이 이름을 쓰면 인수인계가 쉽다. 다른 작업은 `feature/<작업-영문-slug>`.

**Q. PR 머지했는데 현장이 안 바뀌어요.**  
A. **Build Now를 안 누른 것.** 또는 빌드 실패. `haccp-deploy` 콘솔을 본다.

**Q. `haccp-audit`는 언제 쓰나?**  
A. 배포와 무관. 밤에 규약(DELETE·문서링크 등) 검사만 한다. 화면 배포 흐름에 넣지 말 것.

**Q. main에서 바로 고치면 안 되나?**  
A. **안 된다.** 항상 feature → PR → main → Build Now.

**Q. Windows에서 `&&` 쓰나요?**  
A. Git Bash에서는 아래처럼 `;` 또는 줄 나눔을 쓴다. 이 문서 명령을 그대로 쓰면 된다.

---

## 10. 한 장 치트시트 (복사용)

```bash
cd /d/haccp
git checkout main
git pull origin main
git checkout -b feature/ccp-htg-judge-required

# ... 코드 수정 ...
# API:  cd backend/haccp-api && ./mvnw spring-boot:run
# Web:  cd frontend/haccp-web && npm run dev
# URL:  http://localhost:4173/haccp/draft/ccp-monitoring/ccp-htg

cd /d/haccp/frontend/haccp-web
npx tsc --noEmit

cd /d/haccp
git add <파일>
git commit -m "feat(fe): CCP 가열공정 작성 …"
git push -u origin HEAD
gh pr create --base main --title "[STEP nn] feat(fe): CCP 가열공정 작성 …" --body "…"
# PR 머지 후
git checkout main
git pull origin main
# → Jenkins: haccp-deploy → Build Now
```

---

작성·공유용. 스택/화면 규약 상세는 [`개발.md`](개발.md), Jenkins 설치는 [`환경구축.md`](환경구축.md) §11.
