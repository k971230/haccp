# E2E 수행 결과

> 일자: 2026-08-25 (2회차 8-26 · 3회차 8-27 · **4회차 8-27**) · 대상: 메뉴 28화면 전수 · 계정: `admin` / `e2erw`(USER) / `smoke`(VIEWER)
> 실행: `cd frontend/haccp-web ; npx playwright test`
> 최신 결과: **4회차 151건 전부 통과 · 실패 0 · 건너뜀 0** — 발견·수정한 결함 누적 **14건** (→ [E2E_ERRORS.md](E2E_ERRORS.md))
>
> 1회차(8-25) 68건·결함 7건 → 2회차(8-26, 배포 전 정본화) 105건·결함 7건 추가
> → 3회차(8-27, 배포 서버 상대) 150건 → **4회차(8-27, 실무 검증 반영) 151건**. 아래 본문은 2회차(105건) 기준으로 쓰였고
> 3회차는 [4-1 절](#4-1-3회차-2026-08-27--배포-서버-상대-150건), 4회차는 [4-2 절](#4-2-4회차-2026-08-27--실무-검증에서-나온-결함을-고치고-다시-돌렸다) 이다.
> (3 절 표는 1회차 68건 기준이다 — 회차 기록이라 덮지 않았다.)
> 2회차 계획·결과는 [`배포전_최종검증_계획.md`](배포전_최종검증_계획.md) ·
> [`배포전_최종검증_결과.md`](배포전_최종검증_결과.md).

---

## 1. 무엇을 어떻게 봤는가

화면이 열리는지만 보는 스모크와 다르다. 이번 회차의 기준은 셋이다.

| 기준 | 뜻 |
|---|---|
| **DB 로 판정한다** | 화면이 「저장했습니다」라고 해도 믿지 않는다. `tbl_*` 를 직접 읽어 확인한다 |
| **막히는지도 본다** | 정상 경로만 보면 반쪽이다. 중복키·필수값 누락·권한 없음·상태 역행을 일부러 시도한다 |
| **이어 붙여 본다** | 화면별로 다 통과해도 층 사이에서 끊긴다. 업무 하나를 끝까지 돌린다 |

첫 번째 기준이 이번 회차의 핵심이었다. **E2E-001(결재 전 경로 500)** 은 화면·스모크·단위 테스트를
전부 통과하면서 결재만 죽어 있었고, DB 를 직접 보지 않았다면 못 잡았다.

DB 조회는 `e2e/helpers.ts` 의 `dbOne` / `dbRows` 가 맡는다.
로컬 `tools/`(git 미포함)가 필요하고, **없으면 그 시험은 skip** 된다 — 빈 값으로 조용히 통과시키지 않는다.

---

## 2. 대상 28화면

| 대분류 | 중분류 | 화면 | 스펙 |
|---|---|---|---|
| — | — | 오늘 할 일·대시보드 | `doc-cycle` · `scenario` |
| 양식작성 | HTML 양식 | 일반위생·공정점검 작성 | `draft-all` |
| | | CCP 검증점검표 작성 | `draft-all` |
| | CCP 공정모니터링 | CCP 포장공정 작성 | `draft-all` |
| | | CCP 가열공정 작성 | `draft-all` · `document-flow` · `flow-approval` · `scenario` |
| | | CCP 금속검출공정 작성 | `draft-all` |
| | HWP 양식 | HWP 작성 | `draft-all` · `scenario` |
| 문서 현황·결재 | 결재 | 결재첨부 | `document-flow` |
| | | 결재대기 | `flow-approval` · `scenario` |
| | | 결재완료 | `flow-approval` |
| | 문서함 | 문서함 | `flow-approval` · `scenario` |
| | 이탈·개선조치 | 이탈·개선조치 | `draft-all` · `scenario` |
| 문서 | 작성 문서·주기 | 문서주기관리 | `doc-cycle` · `schedule-cycle-management` |
| | HWP·양식 원본 | 사용양식관리 | `docs-hwp-template` |
| | HTML·양식 원본 | 일반위생·공정점검 양식관리 | `docs-html-template` |
| | | CCP 검증점검표 양식관리 | `docs-html-template` |
| | | CCP 포장공정 일지관리 | `docs-html-template` |
| | | CCP 가열공정 일지관리 | `docs-html-template` |
| | | CCP 금속검출공정 일지관리 | `docs-html-template` |
| 시스템 | 권한·사용자·코드 | 공통코드관리 | `sys-common-code` |
| | | 메뉴관리 | `sys-master` |
| | | 권한그룹관리 | `sys-master` |
| | | 부서관리 | `sys-master` · `security-constraints` |
| | | 사용자관리 | `sys-user` · `scenario` |
| | | 결재선관리 | `sys-approval-line` |
| | 이력·통계 | 로그인이력 | `sys-logs` |
| | | 화면통계 | `sys-logs` |
| | | 감사로그 | `sys-logs` |

28화면 전체는 `screens.smoke.spec.ts` 가 매 회차 열어 보며 콘솔·네트워크 오류가 없는지도 본다.

---

## 3. 스펙별 결과 (1회차 68건 기준)

최신 전량은 [4-1 절](#4-1-3회차-2026-08-27--배포-서버-상대-150건) 을 본다.

| 스펙 | 건수 | 무엇을 보는가 |
|---|---:|---|
| `screens.smoke` | 1 | 28화면이 열리고 콘솔·네트워크 오류가 없다 |
| `sys-common-code` | 3 | 세부코드 CRUD · 중복 세부코드 차단 · 코드명 필수 |
| `sys-master` | 4 | 부서·권한그룹 등록 · 부서명 필수 · 메뉴명 수정 |
| `sys-user` | 2 | 사용자 등록과 **BCrypt 저장** · 사용자 ID 중복 차단 |
| `sys-approval-line` | 2 | 결재선 등록 시 1~3단계 자동 생성 · 감사 기록 · 기본선 검토 OFF |
| `sys-logs` | 5 | 세 화면 조회 전용 · 로그인 성공·**실패** 기록 · 화면통계 적재 · 감사 적재 |
| `doc-cycle` | 9 | 주기 7종(D/W/M/Q/H/Y/E) · 사용안함 시 정리 · 오늘 할 일 연동 |
| `schedule-cycle-management` | 5 | 검색 3조건 · 단일 폼 · 삭제 2단계 · 본문 테넌트 위조 방어 · 조회전용 403 |
| `docs-hwp-template` | 2 | **13.5MB 실제 HWP 업로드** · 시스템 양식 삭제 차단 |
| `docs-html-template` | 7 | 5화면 표준 존재 · **5화면 각각** 표준 복사(항목 동반) · 표준 삭제 차단 |
| `draft-all` | 8 | 작성 6화면 문서 생성 · 지면 값 영속 · 이탈→개선조치 |
| `document-flow` | 1 | 작성 → 전송 → 승인 → 보관 한 바퀴 (회귀 고정) |
| `flow-approval` | 3 | 승인 · 문서함 조회전용 · 반려(사유 필수) |
| `security-constraints` | 11 | 인증 · 권한 · 테넌트 · 상태 전이 · 입력 검증 |
| `scenario` | 5 | 통합 시나리오 A~E |
| **합계** | **68** | |

---

## 4. 통합 시나리오

| # | 시나리오 | 확인한 것 |
|---|---|---|
| A | 신규 업체 세팅 | 부서 → 사용자 생성 후 **그 계정으로 실제 로그인**되고 부서가 붙어 있다 |
| B | 정상 결재 | 작성 → 전송(REQ) → 승인(APV) → 문서함. 승인자·승인시각이 남는다 |
| C | 반려 후 재상신 | 반려(RJT) → 작성화면 복귀 → 재전송(REQ) → 승인(APV). **재상신 시 이전 반려 사유가 지워진다** |
| D | 이탈 발생 | 작성에서 이탈 ON → `tbl_corrective_action` 생성 + 개선조치번호 채번 → 개선조치 화면 조회 |
| E | 주기 변경 | 매일 → 매년으로 바꾸면 예정일 수가 실제로 줄고, 오늘 할 일이 그것을 읽는다 |

---

## 4-1. 3회차 (2026-08-27) — 배포 서버 상대 150건

2회차까지는 **로컬 `vite preview`(4173)** 를 봤다. 3회차는 **배포 서버**를 그대로 두들겼다.
로컬에서 초록이던 것이 배포본에서도 초록인지는 별개 문제이기 때문이다.

```sh
cd frontend/haccp-web
E2E_BASE_URL=http://180.71.58.87/haccp/ E2E_API_BASE_URL=http://180.71.58.87 E2E_DB_NAME=sasshaccp E2E_ALLOW_PROD_WRITES=1 npx playwright test
```

**결과: 150건 전부 통과 · 실패 0 · 건너뜀 0 · 10분.**

### 2회차(105) → 3회차(150) 로 늘어난 45건

| 스펙 | 건수 | 무엇을 더 봤는가 |
|---|---:|---|
| `grid-features` | **35** | 정렬·다중정렬·필터·검색·열 숨김/너비/순서/틀고정 · CSV 내용 · 가상스크롤 · **아이디별 열 저장** · 편집 키보드 · 단축키 예의 |
| `draft-judge-default` | **10** | 작성 5화면 판정 기본값 적합 · 「모두 적합」 · 금속검출만 미리 안 칠하는 것 |

### 3회차 스펙 전량 (23본 · 150건)

> 2026-08-27 4회차에서 `flow-corrective` 에 한 건을 더해 **151건**이 됐다 —
> 조치일을 달력으로 고르면 DB 에 8자리로 들어가는지 본다 (아래 4-2 절).

| 스펙 | 건수 | 스펙 | 건수 |
|---|---:|---|---:|
| `grid-features` | 35 | `sys-master` | 4 |
| `docs-html-template` | 12 | `docs-hwp-template` | 4 |
| `security-constraints` | 11 | `sys-common-code` | 3 |
| `draft-judge-default` | 10 | `screen-permission` | 3 |
| `doc-cycle` | 9 | `flow-attach` | 3 |
| `today-tasks` | 8 | `flow-approval` | 3 |
| `draft-all` | 8 | `sys-user` | 2 |
| `company-provisioning` | 6 | `sys-approval-line` | 2 |
| `sys-logs` | 5 | `document-flow` | 1 |
| `shell-grid` | 5 | `screens.smoke` | 1 |
| `schedule-cycle-management` | 5 | | |
| `scenario` | 5 | | |
| `flow-corrective` | 6 | **합계** | **151** |

### 배포 서버로 돌리며 걸린 것 — 시험 쪽 결함 셋 (PULL REQUEST #51·#52)

앱 결함이 아니라 **시험이 배포 규모를 못 견딘 것**이다. 그대로 적어 둔다.

| | 무엇이 터졌나 | 왜 |
|---|---|---|
| DB 이름 | 22건이 「자료가 없다」로 정지 | 브라우저는 운영(`sasshaccp`)을 보는데 검증은 로컬 `.env` 의 시험 DB 를 읽었다. `tools/q.mjs` 가 `E2E_DB_NAME` 을 보게 고쳤다 |
| 운영 규모 SQL | 코드 목록을 `IN (…)` 에 박아 명령줄 길이를 넘겨 잘렸다 | 부분질의를 넘기게 고쳤다 |
| 앞 회차 잔재 | 회차가 끊기면 뒷정리가 안 돌아 다음 회차가 깨졌다 | **시작할 때도** 지운다 |

**`E2E_ALLOW_PROD_WRITES=1` 은 운영 문서를 지운다. 운영 개시 전에만 쓴다.**

## 4-2. 4회차 (2026-08-27) — 실무 검증에서 나온 결함을 고치고 다시 돌렸다

실사용자 관점의 검증 보고 두 본을 받아 재현하고, **재현된 것만** 고쳤다.

### 고친 것

| | 무엇 | 어디 |
|---|---|---|
| 1 | 결재를 승인해도 **지면 도장칸에 결재자가 안 찍혔다** | `sp_tbl_hyg_process_r_001` · `sp_ccp_verify_r_001` — 승인자·서명을 `tbl_document_approval` 에서 먼저 읽는다 |
| 2 | 지면 승인자 칸에 **사람 이름이 아닌 값**이 저장됐다 (`'3'`) | 지면 서명 SP 두 본이 `tbl_user` 에 없는 이름을 `45000` 으로 막는다 |
| 3 | 금속검출 일지가 **저장·전송 전부 실패**했다 | `tbl_ccp_metal_sens_row.check_time` `varchar(4)` → `varchar(10)`. 화면이 보내는 `09:10` 이 5자였다 |
| 4 | 개선조치 **조치일을 저장할 수 없었다** | `MesEditableGrid` 가 `type:"date"` 셀을 `YYYYMMDD` 로 되돌린다 |
| 5 | 같은 코드가 화면마다 다른 이름 (`OPEN` = 진행 / 미조치) | 개선조치 화면이 `CA_STATUS` 공통코드를 읽는다. 저장 불가였던 `CANCEL`(6자)도 같이 사라졌다 |
| 6 | 오늘 할 일 마감이 `20260827 1800` 생숫자 | 기존 `toDisplayDate`·`toInputTime` 재사용 |

### 이어서 고친 것 — 미뤄 뒀던 자리

| | 무엇 | 어디 |
|---|---|---|
| 7 | **전송 필수값이 그 칸으로 안 갔다** — 항목 수십 개에서 빈칸을 눈으로 뒤져야 했다 | `firstInvalidTarget` 이 막은 자리를 같이 준다. 지면 행에 `data-item-cd`·`data-log-seq`, 작성 화면이 스크롤·포커스 |
| 8 | **양식 선택 팝업 일자가 전 행 `Invalid Date`** | `fmtDate` 가 저장형 `YYYYMMDD` 를 못 읽었다. 조회 그리드 date 열 전부가 이 함수를 탄다 |
| 9 | **탭이 스무 개 넘으면 활성 탭이 화면 밖에 남았다** | 탭바는 스크롤되는데 따라오지 않았다 (`scrollLeft=0`, 활성 탭 `inView:false`). `ShellTabBar` 가 활성 탭을 끌어온다 |
| 10 | 결재첨부 결재선이 **「1차 … 3차 …」로 건너뛰었다** | 꺼 둔 검토 단계가 없어서다. 결재선 단계번호가 아니라 **실제 거치는 순서**로 센다 |
| 11 | 「목록 접기」가 두 줄로 접혀 세로로 깨졌다 (`h=32`) | `shrink-0 whitespace-nowrap` |
| 12 | 지면 제목이 잘려 전체를 못 봤다 | 말줄임은 그대로 두고 `title` 로 전체를 붙였다 |
| 13 | 「모두 적합」이 온도를 안 채워 전송이 막혔다 | **동작은 그대로 둔다** — 측정값을 자동으로 채우면 기록을 지어내는 것이다. 무엇을 채우는 버튼인지 `title` 로 밝혔다 |

### 여기서도 재현 안 된 것

| 보고 | 실측 |
|---|---|
| 그리드 「결과 내 검색」이 목록을 안 줄인다 | 공통코드 18행 → `CYCLE` 입력 → **1행.** 비우면 18행 |
| 금속검출 표준 양식이 한계기준·주기·방법을 늦게 채운다 | 600ms~5초 내내 **처음부터 채워져 있다** |
| 사이드바가 하위를 누르다 접힌다 | 대분류 클릭마다 9 → 12 → 15 → 17 로 **펼쳐지기만 한다** |

### 더한 시험 — 깨뜨려서 물었다

| 어디 | 무엇 | 깨뜨려 보니 |
|---|---|---|
| `flow-corrective` (E2E) | 조치일을 달력으로 고르면 DB 에 8자리로 들어간다 | 되돌리는 변환을 빼고 **다시 빌드해** 돌리자 그 시험만 실패했다 |
| `htmlFormDraftShared.test` | 막은 자리의 항목코드·`rowSeq` 를 같이 준다 (2건) | 자리를 안 돌려주게 하자 두 건이 함께 실패했다 |
| `utils/date.test` | `YYYYMMDD` 를 읽고 `Invalid Date` 를 안 흘린다 (5건) | 새로 만든 파일이다 |

**시험은 통과를 보려고 쓰는 게 아니라 깨질 때 물으라고 쓴다.** 셋 다 실제로 물었다.

### 이번 회차에 **안 고친 것**

실무 보고에서 「P0·치명적」으로 올라왔지만 **재현되지 않은 것들이다.**
로컬과 배포 서버 양쪽에서 좌표 클릭·요소 클릭으로 다시 쟀다.

| 보고 | 실측 |
|---|---|
| 행 선택이 3줄 위로 어긋난다 | 4화면 좌표 클릭 — 누른 행과 선택된 행이 **전부 일치** |
| 오늘 할 일 더블클릭이 이동 안 한다 | 좌표·요소 더블클릭 둘 다 작성 화면으로 **이동함** |
| 결재첨부·HWP·결재대기 클릭이 안 먹는다 | 배포 서버에서 **한 번 클릭에 열림** |
| CCP 4화면 행추가가 막힌다 | **네 화면 다 정상.** 그 토스트 문구는 공통코드 화면에만 있다 |
| 양식 선택 팝업 검색 결과를 못 고른다 | 검색 3건으로 걸러진 뒤 더블클릭 → **기안 행에 반영됨** |
| HWP 전송 버튼이 비활성 | 행을 클릭하면 **활성으로 바뀐다** |

**그리드 클릭 코드는 건드리지 않았다.** 고쳤다면 통과 중인 `grid-features` 35건을 깨면서
없는 병을 고치는 셈이었다.

## 5. 제약·권한 위반 시험

| 분류 | 시도 | 기대 | 결과 |
|---|---|---|---|
| 인증 | 토큰 없이 API 호출 | 401/403 | 통과 |
| 인증 | 서명이 틀린 토큰 | 401/403 | 통과 |
| 인증 | 잘못된 비밀번호 | 토큰 미발급 + 이력 기록 | 통과 |
| 권한 | VIEWER 로 저장 API | 거절 + DB 무변화 | 통과 |
| 권한 | 삭제권한 없는 USER 로 삭제 API | 거절 | 통과 |
| 테넌트 | 본문에 `coCd:"9999"` 위조 | JWT 값(0000)으로 덮어씀 | 통과 |
| 상태 | 전송 안 한 문서를 승인 | 거절 + 상태 유지(WRK) | 통과 |
| 상태 | 없는 문서(idx 999999999) 승인 | 400 (500 아님) | 통과 |
| 상태 | 모르는 행위(`DROP_TABLE`) | 400 (500 아님) | 통과 |
| 입력 | 필수값 빈 채로 저장 API | 거절 + DB 무변화 | 통과 |
| 입력 | 300자 초과 부서명 | 400 `BAD_INPUT` (500 아님) | **E2E-006 수정 후** 통과 |
| 업무키 | 같은 세부코드 재등록 | 차단 + 건수 불변 | 통과 |
| 업무키 | 같은 사용자 ID 재등록 | 차단 + 건수 불변 | 통과 |
| 보존 | 시스템 제공 HWP 양식 삭제 | 차단 | 통과 |
| 보존 | HTML 표준 양식 삭제 | 선택 자체가 불가 | 통과 |
| 보존 | 문서함에서 수정·삭제 | 버튼 자체가 없음 | 통과 |

---

## 6. DB 대조 (수행 후)

| 표 | 건수 | 비고 |
|---|---:|---|
| `tbl_screen` | 28 | 메뉴 28화면과 1:1. 고아 75건을 이번 회차에 정리했다 |
| `tbl_menu` | 43 | 대·중분류 노드 포함 |
| `tbl_code` | 86 | 18개 그룹, 전부 UPPER_SNAKE |
| `tbl_role_screen` | 84 | 3그룹 × 28화면. 고아 0건 |
| `tbl_template` / `tbl_company_template` | 48 / 47 | 삭제된 화면 양식 27종을 걷어냈다 |
| 표 / SP | 53 / 152 | `db_sasshaccp/` 6본이 만드는 것과 같다 (소스가 안 쓰는 표 18개 제거) |

`tools/reset_test_documents.sql` 로 시험 문서를 비우므로 문서 표는 회차마다 초기화된다.
기준정보(부서·사용자·양식)는 시험이 만든 것을 각 스펙의 `afterAll` 이 되돌린다.

---

## 7. 이번 회차에 정리한 것

- **결재 전 경로 복구** — `sign_path` 잔재로 전송·검토·승인·반려·결재취소가 모두 500 이었다 (E2E-001)
- **SP 본 재적용 가능** — `01_sp.sql` 152본을 `CREATE OR REPLACE` 로 (E2E-002)
- **결재선 감사 기록** — 형제 5화면과 달리 혼자 빠져 있었다 (E2E-003)
- **HWP 이탈 처리 복구** — 체크가 눌리지도, 서버로 가지도 않았다 (E2E-004·005)
- **입력 오류를 400 으로** — 길이·NOT NULL·참조·형식 위반이 전부 500 이었다 (E2E-006)
- **HTML 양식 복사 복구** — 5화면 중 2화면의 표준 원본을 잘못 지웠던 것을 되살렸다 (E2E-007)
- **고아 화면 정리** — `tbl_screen` 103 → 28, 씨앗 파일도 같이 정리
- **고아 표·양식 정리** — 표 71→53, 양식 73→48. 소스·SP 어디서도 안 쓰고 FK 참조도 없던 것들
- **신규 업체 개설 정본** — `06_company_seed.sql`. 0001 을 실제로 만들어 로그인·문서작성까지 확인
- **하드코딩 제거** — SP 안 양식코드 정규식 2곳을 `doc_kind` 기준으로. `hwp_sys_028`~`038` 11종이 다시 보인다

---

## 8. 다시 돌리는 법

```sh
cd frontend/haccp-web

# 전체
npx playwright test

# 한 스펙만
npx playwright test e2e/scenario.spec.ts

# 실패만 다시
npx playwright test --last-failed
```

필요한 환경변수 (`.env` 또는 셸):

| 변수 | 기본 | 뜻 |
|---|---|---|
| `E2E_USER` / `E2E_PASS` | — | 관리자 계정 |
| `E2E_RO_USER` / `E2E_RO_PASS` | — | 조회 전용 계정. 없으면 권한 시험을 건너뛴다 |
| `E2E_API_BASE_URL` | `http://localhost:7070` | 백엔드 |

DB 대조와 문서 초기화는 로컬 `tools/`(git 미포함)를 쓴다. 없으면 그 시험은 skip 된다 —
CI 에 붙이는 법은 [`frontend/haccp-web/e2e/README.md`](frontend/haccp-web/e2e/README.md) 「CI 에서 DB 대조하기」.
