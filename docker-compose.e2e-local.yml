# local-only override — prod 와 동일하게 api listen 7070 (8081 로 덮지 않음)
# 용도: 로컬에서 compose 로 API 를 띄울 때 healthcheck·포트 정합 확인용
services:
  api:
    environment:
      SERVER_PORT: "7070"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:7070/actuator/health"]
