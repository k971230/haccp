# 배포 전 최종 검증 계획

작성 2026-08-26 · 대상 28화면 · 끝나면 main 머지

말씀하시면 0단계부터 순서대로 돈다. 단계마다 **관문**이 있고, 관문을 못 넘으면
다음 단계로 안 간다. 아래층(DB)이 틀린 채로 위층(E2E)을 돌리면 실패가 무엇 때문인지
구분이 안 되기 때문이다.

---

## 지금 실측 상태

| 항목 | 수 |
|---|---|
| 화면 (`SCREEN_PATH`) | **28** |
| SP · 함수 (`01_sp.sql`) | 154 (프로시저 69 · 함수 85) |
| 매퍼 XML | 29 · 참조 SP 150 |
| 자바 소스 | 138 · 컨트롤러 27 |
| FE 소스 | 210 |
| E2E | spec 16본 · test 블록 56 · 실행 71건 |
| 사용자 수정 중 | **15 파일** (미커밋) |

이미 확인한 것: **참조하는데 정의 없는 SP 0건.** 정의만 있고 매퍼가 안 쓰는 4건은
모두 다른 SP 안에서 불린다 — 고아 아니다.

---

## 0단계 — 진행 중 작업 확정 (관문)

말씀해 주신 두 묶음 중 **1번(방향키·초록 헤더)은 이미 커밋**돼 있다
(`e82bdc0` — `mesSec.ts` · `mesSec.test.ts` · `MesDataGrid.tsx` 포함).
미커밋은 **2번(오늘 할 일) 16파일 · +612 / -61** 이다.

```
DocumentService.java          db_sasshaccp/01_sp.sql
doc-cycle.spec.ts             scenario.spec.ts
taskWorkflowApi.ts            documentNav.ts (+66)
DocumentApprovalToolbar.tsx   MesEditableGrid.tsx
lib/docStatus.ts              DocumentBoxPage.tsx
HtmlFormDraftPage.tsx         tsk/TodayTasksPage.tsx
tsk/TodayTasksRule.ts (+225)  tsk/TodayTasksRule.test.ts
tsk/README.md                 styles/global.css
```

할 일

1. 16파일 `git diff` 를 읽어 3단계 기대값에 반영한다
2. `01_sp.sql` 은 **로컬 적용 완료**라 하셨으니 운영 재적용만 5단계에서 확인한다
3. `npx tsc --noEmit` · `npx vitest run` · `./mvnw -q -o test` · 백엔드 재기동

**관문: 컴파일 0 · 단위 통과 · 기동 성공.**

---

## 0-1 — 이번 변경이 계획을 바꾸는 세 곳

### (가) 클릭 제스처가 바뀌었다 — E2E 다수가 깨진다

「한 번 클릭 = 선택, 더블클릭 = 이동」으로 바꾸셨다.
지금 E2E 는 **단일 클릭으로 이동하던 곳이 17군데**고, `dblclick` 은 2군데뿐이다.

```
document-flow.spec.ts:78,100,156    flow-approval.spec.ts:58,109
scenario.spec.ts:63                 draft-all.spec.ts:91
schedule-cycle-management.spec.ts:35,66
sys-common-code.spec.ts:47,64,91,117
docs-hwp-template.spec.ts:65,103    sys-user.spec.ts:47
helpers.ts:292                      ← 공용 헬퍼
```

`helpers.ts:292` 가 공용이라 여기부터 본다. **행 선택**과 **행 열기**를 헬퍼에서
갈라 두면 화면별 spec 을 하나씩 안 고쳐도 된다.

- `selectRow()` — 클릭. 선택만
- `openRow()` — 더블클릭. 이동

3단계에서 격자를 채울 때 이 두 개로 통일한다.

### (나) `docStatus` APV 표기가 바뀌었다

APV = 승인완료(초록)로 바꾸셨다. E2E 배지 문구 단언과
`doc-cycle` · `scenario` 의 KPI 문구를 이미 맞추셨다 하니, 3단계에서
**나머지 spec 에도 같은 문구가 남아 있는지** 훑는다.

### (다) `sp_tbl_today_task_r_000` 는 반환 컬럼이 늘었다

`tmpl_cd` · `base_dt` 가 붙었다. `RETURNS TABLE` 이 바뀌면
`CREATE OR REPLACE FUNCTION` 만으로는 **재적용이 실패**한다.
확인해 보니 앞에 `DROP FUNCTION IF EXISTS` 를 넣어 두셨다 — 이건 안전하다.

다만 **`01_sp.sql` 154건 중 DROP 가드가 있는 건 이 하나뿐**이다.
다른 함수의 반환 형이 바뀐 적이 있으면 같은 자리에서 터진다.
1단계에 "빈 DB 아닌 **현재 DB 에 01_sp.sql 재적용 → 오류 0**" 을 넣는다.

### 추가 E2E 대상 (이번 변경으로 생긴 것)

| 무엇 | 왜 봐야 하나 |
|---|---|
| KPI 5장 | 4장→5장. 카드 문구·이동 대상 |
| 「미완료 과제만 보기」 | APV·조치완료 CA 가 숨나 |
| 기한경과 빨강·최상단 | `.mes-row-overdue` · 정렬 |
| 더블클릭 분기 4종 | 예정 / 문서있음 / CA / 문서없는 지연 |
| `?add=1&tmplCd=&baseDt=` | 행 1개만 추가되고 `add` 만 지워지나. **새로고침에 또 안 생기나** |
| 방향키 행 이동 | 로그 3화면 (비제어) · 문서주기관리 (제어) |
| 패널 초록 헤더 | 그리드·트리·단패널·중첩분할 4케이스. `data-mes-sec` |

`TodayTasksRule.test.ts` 24건은 단위라 규칙만 본다.
**화면에서 실제로 그렇게 도는지는 3단계 E2E 가 본다.**

---

## 1단계 — SP 내용 정합 (154건 전수)

이름은 맞는 걸 확인했다. 이제 **속**을 본다. E2E-001(`sign_path`→`sign_img`)이
바로 이 층에서 났고, 스모크도 단위도 못 잡았다.

축 4개를 전수로 돌린다.

| 축 | 무엇을 보나 | 방법 |
|---|---|---|
| **컬럼 실재** | SP 가 읽고 쓰는 컬럼이 `00_ddl.sql` 에 있나 | SP 본문 식별자 → `information_schema.columns` 대조 |
| **인자 일치** | 매퍼 XML 이 넘기는 파라미터 수·순서·형이 SP 시그니처와 같나 | XML `#{}` 개수 ↔ `pg_proc.pronargs` |
| **결과 형** | `resultType` 필드가 SP 반환 컬럼과 같나 | 커서 반환 컬럼 ↔ 자바 DTO 필드 |
| **회사 격리** | 모든 SP 가 `co_cd` 를 인자로 받고 WHERE 에 쓰나 | 안 쓰는 SP 목록 = 남의 회사 자료 노출 후보 |
| **재적용** | **이미 있는 DB** 에 154건을 다시 밀어도 오류 0 인가 | 반환 형이 바뀐 함수는 `DROP` 가드가 없으면 터진다 |

산출: `db_sasshaccp/SP_점검표.md` — 154행, 축별 OK/수정.
**안 맞는 건 그 자리에서 고친다.** 고치면 DB 재적용 + 재기동.

---

## 2단계 — URL · 주석 전수 (소스층)

### 2-1 URL 정합 — 이미 어긋난 것 1건 잡았다

규칙은 **URL = DB = 폴더 = 패키지**. 컨트롤러 27개를 `SCREEN_PATH` 28행과 맞댄다.

확인된 어긋남:

```
UserController      @RequestMapping("/api/v1/sys")
                    @GetMapping("/code/user-management/list")     ← 경로가 메서드에 흩어짐
형제 5화면          @RequestMapping("/api/v1/sys/code/{화면}")
                    @GetMapping("/list")                          ← 정상
```

형제와 다르면 한쪽만 고쳐진다. `UserController` 를 형제 형태로 맞춘다.
(같은 파일의 `/users/me/sign` 계열은 화면 API 가 아니라 공용이라 그대로 둔다 — 주석에 적는다.)

`docs/documents` · `docs/templates` 는 화면이 아니라 **여러 화면이 함께 쓰는 축**이다.
지울 게 아니라 "왜 화면 경로가 아닌가"를 주석·README 에 남긴다.

### 2-2 주석 전수

| 층 | 헤더 있음 | 대상 |
|---|---|---|
| 자바 | **137 / 138** | `ErrorResponse.java` 1건 |
| FE | **200 / 210** | 10건 (아래) |

FE 누락 10건 — 타입 선언·테스트 파일이라 규칙 적용 여부부터 정한다.

```
GridErrorBoundary.tsx      shell/gridRules/index.ts    utils/date.ts
gridFilterNormalize.test.ts  shell/gridRules/types.ts   vite-env.d.ts
gridPref.test.ts           shell/useSection.ts
tanstack-table.d.ts        types/editable.ts
```

판단: `*.d.ts` · `vite-env.d.ts` 는 타입 선언이라 헤더를 안 붙인다 (규칙에 예외로 명시).
나머지 6건은 붙인다.

헤더 유무만이 아니라 **내용**도 본다 — 개발자·일자·코멘트 1)2)3) 형식,
인자마다 한 줄 설명, `05-handoff-comments.mdc` 밀도. 자바·FE 같은 밀도여야 한다.

### 2-3 하드코딩

회사코드 `0000`·`0001`, 사용자 `admin`, URL 호스트, 매직 문자열을 훑는다.
E2E·seed 안의 것은 정상. **운영 코드 안의 것만** 뽑아 상수·env 로 뺀다.

---

## 3단계 — E2E 28화면 전수 (본편)

아래 두 층이 맞은 뒤에야 의미가 있다.

### 지금 덮는 깊이

| 깊이 | 화면 수 |
|---|---|
| 조회·저장·삭제까지 왕복 | 약 14 |
| 열리는 것만 (스모크) | 약 14 |

`screens.smoke.spec.ts` 가 28화면을 다 열어 보지만 **여는 것까지**다.

### 채울 격자 — 28화면 × 5축

| 축 | 무엇 |
|---|---|
| **R** 조회 | 열리고, 그리드가 뜨고, 행이 있다 |
| **CU** 저장 | 행 추가·수정 → 저장 → **DB 에서 확인** → 새로고침 후 남아 있다 |
| **D** 삭제 | `validate-delete` → `delete` 2단계. 참조 있으면 막힌다 |
| **X** 위반 | 길이·NOT NULL·FK·형식 위반이 **400** 이지 500 이 아니다 |
| **P** 권한 | 권한 없는 계정으로 열면 닫히고 오늘 할 일로 간다 |

UI 만 보고 통과시키지 않는다. **저장했으면 DB 를 조회해 대조한다** (`dbRows()`).
E2E-001 이 UI 초록·DB 빨강이었다.

### 화면별 계획

| 묶음 | 화면 | 지금 | 할 것 |
|---|---|---|---|
| 랜딩 | today-tasks | R | CU 없음(읽기 전용) · P 추가 |
| 문서기준 | schedule-cycle-management | R CU D | X · P |
| | hwp-template-management | R CU | D · X · P |
| | html-form 양식 5본 | R CU | D · X · P (5화면 각각) |
| 양식작성 | hyg-process · ccp-verify | R CU | D · X · P |
| | ccp-pkg · htg · mtl | R CU | D · X · P |
| | hwp-write | R CU | D · X · P |
| 결재 | attach · sign-ready · sign-ok | R CU | 반려·재상신 · X · P |
| | document-inbox | R | 상태 전이 4종 · P |
| | corrective-action-management | R | CU · D · X · P |
| 시스템 | 코드 6화면 | R CU D | X · P |
| | 로그 3화면 | R | 기간 필터 · 내보내기 · P |

### 추가로 도는 시나리오

1. **문서 한 바퀴** — 작성 → 전송 → 반려 → 재작성 → 재전송 → 결재 → 확정.
   단계마다 `DOC_STATUS` 를 DB 에서 확인
2. **계정 전환** — 권한 있는 계정 로그인 → 화면 염 → 로그아웃 → 권한 없는 계정 →
   그 화면이 닫히나 (이미 3건 있음, 28화면으로 넓힌다)
3. **신규 회사** — `06_company_seed.sql` 로 `0002` 를 만들고 로그인해서
   메뉴 43·화면권한 84·양식 38 이 그대로 붙나
4. **회사 격리** — `0001` 계정으로 `0000` 자료가 안 보이나

### 관문

```sh
cd frontend/haccp-web
npx playwright test          # 목표: 실패 0
```

**실패는 그 자리에서 고친다.** 넘기지 않는다.
목표 규모: 71건 → **약 150건**.

---

## 4단계 — 문서 최종 정본

3단계까지 고친 게 문서에 반영돼야 정본이다. 그래서 **맨 뒤**다.

| 대상 | 할 것 |
|---|---|
| `docs/` 8본 | 3단계에서 바뀐 내용 반영 · 죽은 링크 0 재확인 |
| `docs/3_화면_지도.md` | DB·`tabRoute`·컨트롤러에서 **다시 생성** (28행) |
| `docs/5_PIPELINE_색인.md` | 소스 태그에서 다시 생성. **결함 있음** — 아래 |
| BE·FE `PIPELINE.md` | 2단계 URL 변경 반영 |
| README 전수 | 폴더마다 1본 있나 · 최근 변경란 갱신 |
| `db_sasshaccp/README.md` | 1단계 SP 점검 결과 반영 |
| `requirements.txt` | 최신화 |
| `.cursor/rules/` | 2-2 에서 정한 주석 예외를 규칙에 명시 |

### 잡아 둔 문서 결함 — PIPELINE 태그가 1:N 인데 색인이 1:1 이다

`HF74` 를 다는 파일이 **5개**인데 색인은 `main.tsx` 한 줄만 싣는다.

```
authCrossTab.ts  authPaths.ts  authSession.ts  mesSec.ts  useActiveGrid.ts
                        ↓
docs/5_PIPELINE_색인.md:56  | HF74 | main.tsx | 연관 모듈 |
```

이번에 넣으신 `mesSec.ts` 도 색인에서 사라져 있다.
생성기가 태그당 파일 하나만 남긴다 — 여러 줄을 싣도록 고친다.

검사: 링크 죽은 것 0 · 이모지 0 · 폴더당 README 1본 · **PIPELINE 태그 전건이 색인에 있나**.

---

## 5단계 — main 머지

앞서 시뮬레이션한 결과가 있다. 8개 미머지 브랜치 중

- 새 코드가 들어오는 것 **1개** (우리 브랜치)
- 변경 0 **2개** (이미 main 에 있음)
- 지운 파일을 되살리려는 것 **5개** (충돌 267개)

순서

```
1. 커밋 · 푸시 · PR #39 갱신
2. PR #39 머지
3. bash db_sasshaccp/apply-all.sh      표 53 · 양식 48 반영
4. Jenkins Build Now                    사용자가 버튼
5. scripts/prod_smoke.sh                운영 확인
6. 브랜치 31개 삭제                      머지됨 24 + 대체됨 7
```

5개 브랜치를 굳이 다 올리라 하시면 올린다. 다만 충돌 267개를 다 풀고 나면
지금 상태와 같아진다 — 그 판단만 말씀해 주시면 된다.

---

## 산출물

| 파일 | 내용 |
|---|---|
| `db_sasshaccp/SP_점검표.md` | SP 154 × 축 4 |
| `배포전_최종검증_결과.md` | 화면 28 × 축 5 격자, 고친 것 전부 |
| `docs/` 8본 갱신 | |
| E2E spec | 71 → 약 150건 |

## 순서를 이렇게 잡은 까닭

```
0 수정분 확정 ─▶ 1 SP ─▶ 2 소스 ─▶ 3 E2E ─▶ 4 문서 ─▶ 5 머지
   무엇이         아래층    중간층    위층      결과를    확정
   바뀌는지        먼저      다음     마지막    적는다
```

거꾸로 하면 E2E 실패가 SP 탓인지 소스 탓인지 진행 중 수정 탓인지 못 가린다.
