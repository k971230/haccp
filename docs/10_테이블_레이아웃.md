# 10. 테이블 레이아웃

> 개발자: 박승우 · 일자: 2026-09-07
> `db_sasshaccp/00_ddl.sql` 에서 뽑았다. **손으로 고치지 않는다.**

표 **56개** · 컬럼 **809개**

```sh
node scripts/gen_table_layout.mjs           # 다시 만든다
node scripts/gen_table_layout.mjs --check   # 어긋나면 실패한다 (CI)
```

엑셀본은 이 파일을 뽑을 때 `10_테이블_레이아웃.xls` 로 같이 나온다 —
첫 시트가 표제, 둘째가 목차, 셋째부터 표 하나에 시트 하나다.

**`.xls` 는 git 에 안 올린다** (`.gitignore` 가 `docs/` 에서 번호 붙은 `.md` 만 추적한다).
필요하면 위 명령으로 그때 뽑는다 — 내용은 이 문서와 같다.

## 목차

### 기초코드 (13)

회사·사람·권한·코드. 업무가 시작되기 전에 있어야 하는 값이다

| # | 표 | 컬럼 | 무엇을 담는가 |
|---:|---|---:|---|
| 1 | `tbl_company` | 26 | 회사(테넌트) 마스터 — SaaS 가입 업체 1행. |
| 2 | `tbl_dept` | 11 | 부서 — 업체별 조직도. 문서 결재란·작성자 부서 표기에 사용 |
| 3 | `tbl_user` | 23 | 사용자 — 구 bap1100 재설계. user_id 전역 UNIQUE로 아이디만으로 소속 회사 결정 |
| 4 | `tbl_role` | 10 | 권한그룹 — HACCP팀장/모니터링담당자/일반작업자 등 |
| 5 | `tbl_role_screen` | 13 | 권한그룹별 화면 권한 — SideMenu 노출과 그리드 CRUD 잠금(useGridAccess)의 근거 |
| 6 | `tbl_screen` | 11 | 화면 마스터 — 플랫폼 전역. 메뉴·권한·UV/PV 통계의 공통 차원 |
| 7 | `tbl_menu` | 12 | 업체별 메뉴 트리 — SideMenu 3단 구성. 미사용 양식은 use_yn=N으로 숨긴다 |
| 8 | `tbl_code` | 14 | 공통코드 — sub_cd=* 행이 그룹 헤더 |
| 9 | `tbl_approval_line` | 9 | 결재선 헤더 — 업체별 결재 경로 정의. 템플릿별로 다른 결재선 지정 가능 |
| 10 | `tbl_approval_line_step` | 13 | 결재선 단계 — 작성자·승인자 순번 정의 |
| 11 | `tbl_doc_no_rule` | 13 | 문서번호 채번 규칙 — 사용자에게 보이는 업무번호(doc_no) 생성. idx와 별개 |
| 12 | `tbl_grid_pref` | 10 | 사용자별 그리드 열 설정 — 숨김·순서·너비만 저장. 정렬·필터는 세션 보관 |
| 13 | `tbl_user_noti_pref` | 9 | 사용자별 알림 수신 설정 — bap1100 알림 컬럼 12종의 정규화 대체 |

### 기초문서 (18)

양식이 무엇이고 어떤 항목을 갖는가. 기록을 담는 그릇의 정의다

| # | 표 | 컬럼 | 무엇을 담는가 |
|---:|---|---:|---|
| 1 | `tbl_template` | 19 | 표준 템플릿 카탈로그 — 플랫폼이 배포하는 31종 + 추가 12종 메타. 업체는 복사해서 쓴다 |
| 2 | `tbl_company_template` | 18 | 업체 사용양식 — 표준 템플릿을 업체가 복사·조정한 결과 |
| 3 | `tbl_company_template_file` | 11 | 양식 파일 이력 — 업로드·기본제공 파일 1행씩. 불러오기(과거 버전 적용)·초기화의 원천 |
| 4 | `tbl_check_item` | 16 | 템플릿별 표준 점검항목 — 일일위생 15항목, 용수관리 11항목 등 표준기준서 문구 그대로 |
| 5 | `tbl_html_form_ver` | 12 | - |
| 6 | `tbl_html_form_ver_item` | 15 | - |
| 7 | `tbl_html_hyg_prc_ver` | 12 | 일반위생·공정점검 자사 양식 버전 — 예시는 html_hyg_prc_000 가상 |
| 8 | `tbl_html_hyg_prc_ver_item` | 15 | 일반위생·공정점검 자사 양식 항목 |
| 9 | `tbl_html_ccp_chk_ver` | 12 | CCP 검증점검 자사 양식 버전 — 예시는 html_ccp_chk_000 가상 |
| 10 | `tbl_html_ccp_chk_ver_item` | 15 | CCP 검증점검 자사 양식 항목 |
| 11 | `tbl_html_ccp_pkg_ver` | 12 | CCP-1B 포장 모니터링일지 자사 양식 버전 — 예시는 html_ccp_pkg_000 가상 |
| 12 | `tbl_html_ccp_pkg_ver_item` | 15 | CCP-1B 포장 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·개선조치 |
| 13 | `tbl_html_ccp_htg_ver` | 12 | CCP-2B 가열 모니터링일지 자사 양식 버전 — 예시는 html_ccp_htg_000 가상 |
| 14 | `tbl_html_ccp_htg_ver_item` | 15 | CCP-2B 가열 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·개선조치 |
| 15 | `tbl_html_ccp_mtl_ver` | 12 | CCP-3P 금속검출 모니터링일지 자사 양식 버전 — 예시는 html_ccp_mtl_000 가상 |
| 16 | `tbl_html_ccp_mtl_ver_item` | 15 | CCP-3P 금속검출 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·감도열 해당없음·개선조치 |
| 17 | `tbl_schedule_rule` | 18 | 작성주기 규칙 — 오늘 할 일 생성 배치의 입력. 템플릿별 여러 규칙 허용 |
| 18 | `tbl_schedule_rule_detail` | 7 | 작성주기 반복 상세 — 요일·실행일·말일·분기월·반기월. 저장 시 양식 단위로 전량 교체된다 |

### 업무 (20)

현장이 실제로 쓰는 기록과 그 결재. 법정 서류가 여기 쌓인다

| # | 표 | 컬럼 | 무엇을 담는가 |
|---:|---|---:|---|
| 1 | `tbl_document` | 26 | 문서 공통 관리정보 — 모든 문서 1행. 사용자는 목록에서 DB형·문서형을 구분하지 않는다 |
| 2 | `tbl_document_approval` | 15 | 문서 결재 이력 — A4 결재란 3칸의 실제 데이터 원천 |
| 3 | `tbl_document_file` | 11 | 문서 첨부·원본·PDF — 경로만 저장. 실제 파일은 업체별 볼륨 경로에 보관 |
| 4 | `tbl_document_version` | 9 | 문서 버전 스냅샷 — 승인 후 수정 시 직전 상태 보존. 감사 대응 필수 |
| 5 | `tbl_hyg_process` | 21 | - |
| 6 | `tbl_hyg_process_item` | 16 | - |
| 7 | `tbl_ccp_verify_check` | 22 | CCP 검증점검표 헤더 — 표준기준서 관리번호 3. 월 1회 작성 |
| 8 | `tbl_ccp_verify_item` | 23 | CCP 검증 항목별 결과 — 예/아니오 응답과 근거 기록. 일지 건수는 자동 집계 |
| 9 | `tbl_ccp_pkg_monitor` | 16 | CCP 포장 모니터링 헤더 — 표준기준서 포장공정 기록 |
| 10 | `tbl_ccp_pkg_monitor_row` | 17 | CCP 포장 점검 행 — 시간별 측정·판정·점검자 |
| 11 | `tbl_ccp_pkg_monitor_cell` | 11 | CCP 포장 측정 셀 — 수치 또는 텍스트 값과 항목별 판정 |
| 12 | `tbl_ccp_htg_monitor` | 16 | CCP 가열 모니터링 헤더 — 표준기준서 가열공정 기록 |
| 13 | `tbl_ccp_htg_monitor_row` | 17 | CCP 가열 점검 행 — 시간별 측정·판정·점검자 |
| 14 | `tbl_ccp_htg_monitor_cell` | 11 | CCP 가열 측정 셀 — 수치 또는 텍스트 값과 항목별 판정 |
| 15 | `tbl_ccp_metal_monitor` | 13 | CCP 금속검출 모니터링 일지 헤더 — 표준기준서 관리번호 2-2 |
| 16 | `tbl_ccp_metal_sens_row` | 22 | 금속검출기 감도 모니터링 행 — 시편 통과 시험 결과 |
| 17 | `tbl_ccp_metal_pass_row` | 14 | 금속검출기 제품 통과 실적 — 통과량·검출량 기록 |
| 18 | `tbl_corrective_action` | 22 | 이탈·개선조치 — 전 점검표 공통. 미완료 건은 대시보드에 노출되고 결재 상신을 막는다 |
| 19 | `tbl_schedule_task` | 16 | 작성 과제 — 일 1회 배치 생성 + 로그인 시 온디맨드 보정. 오늘 할 일·누락 알림의 원천 |
| 20 | `tbl_workday_override` | 5 | 영업일 전환 — 행이 있으면 그 날(주말·공휴일)을 회사 영업일로 취급한다 |

### 로그·통계 (5)

누가 언제 무엇을 했는가. 업무를 만들지 않고 뒤에서 쌓인다

| # | 표 | 컬럼 | 무엇을 담는가 |
|---:|---|---:|---|
| 1 | `tbl_login_log` | 12 | 로그인 이력 — 성공·실패·잠금 전수 기록. 감사 대응 및 계정 도용 추적용 |
| 2 | `tbl_view_log` | 12 | 화면 조회 원시 이벤트 — PV 1건 = 1행. 보존기간 경과분은 정리 배치로 삭제 |
| 3 | `tbl_view_stat_daily` | 14 | 화면별 일자 집계 — UV/PV/세션수/평균체류. 일 1회 배치 upsert, 영구 보존 |
| 4 | `tbl_audit_log` | 12 | 변경 감사 로그 — HACCP 기록의 사후 수정 추적. 삭제하지 않는다 |
| 5 | `tbl_notification` | 11 | 알림함 — 수신 여부는 tbl_user_noti_pref를 따르고, 발송된 알림만 이 테이블에 쌓인다 |

---

## 삭제 의미

전면 `del_yn` 전환은 하지 않는다. 새 표는 이 표에 한 줄 없이 못 넣는다.

| 표 | 삭제 의미 | 차단 SP | 복구 |
|---|---|---|---|
| `tbl_company` | use_yn | - | 가능 — use_yn=Y |
| `tbl_dept` | use_yn | `sp_department_management_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_user` | use_yn | `sp_user_management_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_role` | use_yn | `sp_role_management_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_role_screen` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_screen` | use_yn | - | 가능 — use_yn=Y |
| `tbl_menu` | use_yn | `sp_menu_management_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_code` | use_yn | `sp_common_code_management_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_approval_line` | use_yn | `sp_tbl_approval_line_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_approval_line_step` | use_yn | - | 가능 — use_yn=Y |
| `tbl_doc_no_rule` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_grid_pref` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_user_noti_pref` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_template` | use_yn | - | 가능 — use_yn=Y |
| `tbl_company_template` | use_yn | `sp_tbl_company_template_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_company_template_file` | del_yn | - | 가능 — del_yn=N |
| `tbl_check_item` | use_yn | - | 가능 — use_yn=Y |
| `tbl_html_form_ver` | use_yn | - | 가능 — use_yn=Y |
| `tbl_html_form_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_html_hyg_prc_ver` | use_yn | `sp_tbl_html_hyg_prc_ver_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_html_hyg_prc_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_html_ccp_chk_ver` | use_yn | `sp_tbl_html_ccp_chk_ver_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_html_ccp_chk_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_html_ccp_pkg_ver` | use_yn | `sp_tbl_html_ccp_pkg_ver_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_html_ccp_pkg_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_html_ccp_htg_ver` | use_yn | `sp_tbl_html_ccp_htg_ver_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_html_ccp_htg_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_html_ccp_mtl_ver` | use_yn | `sp_tbl_html_ccp_mtl_ver_delete_blocker_r_000` | 가능 — use_yn=Y |
| `tbl_html_ccp_mtl_ver_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_schedule_rule` | use_yn | - | 가능 — use_yn=Y |
| `tbl_schedule_rule_detail` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_document` | del_yn | `sp_tbl_document_delete_blocker_r_000` | 가능 — del_yn=N |
| `tbl_document_approval` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_document_file` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_document_version` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_hyg_process` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_hyg_process_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_verify_check` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_verify_item` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_pkg_monitor` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_pkg_monitor_row` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_pkg_monitor_cell` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_htg_monitor` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_htg_monitor_row` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_htg_monitor_cell` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_metal_monitor` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_metal_sens_row` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_ccp_metal_pass_row` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_corrective_action` | 물리 DELETE | `sp_tbl_corrective_action_delete_blocker_r_000` | 불가 — 백업·감사 로그 |
| `tbl_schedule_task` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_workday_override` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_login_log` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_view_log` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_view_stat_daily` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_audit_log` | 물리 DELETE | - | 불가 — 백업·감사 로그 |
| `tbl_notification` | 물리 DELETE | - | 불가 — 백업·감사 로그 |

---

## tbl_company

**기초코드** — 회사(테넌트) 마스터 — SaaS 가입 업체 1행.

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키. JWT LoginUser.coCd 및 전 SP p_co_cd 와 1:1 |
| `co_nm` | character varying(100) | N |  |  | 회사명 — 전 문서 A4 헤더에 출력 |
| `co_nm_en` | character varying(100) | Y |  |  | 회사명(영문) |
| `biz_no` | character varying(20) | Y |  |  | 사업자등록번호 |
| `co_no` | character varying(20) | Y |  |  | 법인등록번호 |
| `co_gbn` | character varying(10) | Y | `'1'::character varying` |  | 법인구분 — 1:법인, 2:개인 (tbl_code CO_GBN) |
| `ceo_nm` | character varying(50) | Y |  |  | 대표자명 — 회수 안내문·성적서 등에 출력 |
| `tel_no` | character varying(20) | Y |  |  | 대표 전화번호 |
| `fax_no` | character varying(20) | Y |  |  | 팩스번호 — 회수 시 거래처 통보에 사용 |
| `zip_no` | character varying(10) | Y |  |  | 우편번호 |
| `addr_h` | character varying(200) | Y |  |  | 주소 |
| `addr_d` | character varying(200) | Y |  |  | 상세주소 |
| `open_dt` | character varying(8) | Y |  |  | 개업일 YYYYMMDD |
| `haccp_type` | character varying(20) | Y | `'MEAT_PACK'::character varying` |  | HACCP 업종유형 — MEAT_PACK(식육포장처리업) 등. 표준 템플릿 패키지 선택 기준 |
| `lic_no` | character varying(50) | Y |  |  | 영업허가(신고)번호 |
| `logo_path` | character varying(300) | Y |  |  | 회사 로고 파일 경로 — A4 문서 헤더 출력용 |
| `retention_month` | integer | N | `24` |  | 기본 문서 보존 개월수 — HACCP 기준 최소 24(2년) |
| `plan_cd` | character varying(20) | Y |  |  | 요금제 코드 (tbl_code PLAN_CD) |
| `svc_st_dt` | character varying(8) | Y |  |  | 서비스 시작일 YYYYMMDD |
| `svc_fn_dt` | character varying(8) | Y |  |  | 서비스 종료일 YYYYMMDD — 경과 시 로그인 차단 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_company_co_cd`: `co_cd`

## tbl_dept

**기초코드** — 부서 — 업체별 조직도. 문서 결재란·작성자 부서 표기에 사용

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `dept_cd` | character varying(20) | N |  | UQ | 부서코드 — 업체 내 유일 |
| `dept_nm` | character varying(100) | N |  |  | 부서명 |
| `h_dept_cd` | character varying(20) | Y |  |  | 상위 부서코드 — NULL이거나 공백일 때(= 최상위 노드) 트리 루트 |
| `sort_no` | integer | N | `0` |  | 정렬순서 — 같은 상위 안에서의 표시 순서 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_dept_co_dept`: `co_cd, dept_cd`

## tbl_user

**기초코드** — 사용자 — 구 bap1100 재설계. user_id 전역 UNIQUE로 아이디만으로 소속 회사 결정

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `user_id` | character varying(20) | N |  | UQ | 로그인 ID — 전 테넌트 통틀어 유일(ux_tbl_user_user_id). 로그인 시 회사 선택 불필요 |
| `co_cd` | character varying(10) | N |  | UQ | 소속 회사코드 — 로그인 성공 시 JWT coCd 클레임으로 주입되어 전 SP 테넌트 범위를 결정 |
| `emp_cd` | character varying(20) | Y |  | UQ | 사번 — 업체 내 유일(NULL 허용, PG는 NULL 중복 허용) |
| `user_nm` | character varying(50) | N |  |  | 사용자명 — 문서 작성자·점검자·결재자란에 출력 |
| `user_pw` | character varying(300) | N |  |  | 비밀번호 해시 — PasswordHasher 산출값. 평문 저장 금지 |
| `usrgrp_cd` | character varying(20) | N |  |  | 권한그룹코드 — tbl_role.usrgrp_cd. PLATFORM일 때(= 플랫폼 관리자) 업체 전환 가능 |
| `dept_cd` | character varying(20) | Y |  |  | 부서코드 — tbl_dept.dept_cd |
| `email` | character varying(100) | Y |  |  | 이메일 — 알림 발송 주소 |
| `mobile` | character varying(20) | Y |  |  | 휴대전화번호 |
| `gridsave_yn` | character varying(1) | N | `'Y'::character varying` |  | 그리드 열 설정 저장여부 Y/N — tbl_grid_pref 사용 여부 |
| `last_login_dt` | timestamp without time zone | Y |  |  | 최종 로그인 일시 |
| `pw_upd_dt` | timestamp without time zone | Y |  |  | 비밀번호 최종 변경일시 — 주기적 변경 안내 기준 |
| `login_fail_cnt` | integer | N | `0` |  | 연속 로그인 실패 횟수 — 성공 시 0으로 초기화 |
| `lock_yn` | character varying(1) | N | `'N'::character varying` |  | 계정 잠금여부 Y/N — Y일 때(= 실패 임계 초과) 로그인 차단 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N — N일 때(= 퇴사·비활성) 로그인 차단 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `sign_img` | bytea | Y |  |  | 서명 이미지 바이너리 — PNG/JPG 원본. 결재·점검자 서명란에 삽입 |
| `sign_mime` | character varying(50) | Y |  |  | 서명 이미지 MIME — image/png 또는 image/jpeg |
| `sign_nm` | character varying(255) | Y |  |  | 서명 이미지 원본 파일명 — 다운로드 시 Content-Disposition에 사용 |

- 기본키: `idx`
- 유일키 `ux_tbl_user_co_emp`: `co_cd, emp_cd`
- 유일키 `ux_tbl_user_user_id`: `user_id`
- 인덱스 `ix_tbl_user_co`: `co_cd, use_yn`

## tbl_role

**기초코드** — 권한그룹 — HACCP팀장/모니터링담당자/일반작업자 등

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키. 0000일 때(= 플랫폼 예약 회사) 플랫폼 공통 권한그룹 |
| `usrgrp_cd` | character varying(20) | N |  | UQ | 권한그룹코드 — 업체 내 유일. PLATFORM은 플랫폼 관리자 예약어 |
| `usrgrp_nm` | character varying(100) | N |  |  | 권한그룹명 |
| `desc_rmk` | character varying(300) | Y |  |  | 설명 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_role_co_grp`: `co_cd, usrgrp_cd`

## tbl_role_screen

**기초코드** — 권한그룹별 화면 권한 — SideMenu 노출과 그리드 CRUD 잠금(useGridAccess)의 근거

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `usrgrp_cd` | character varying(20) | N |  | UQ | 권한그룹코드 — tbl_role.usrgrp_cd |
| `scrn_cd` | character varying(30) | N |  | UQ | 화면코드 — tbl_screen.scrn_cd |
| `read_yn` | character varying(1) | N | `'Y'::character varying` |  | 조회 권한 Y/N — N일 때(= 메뉴 자체 숨김) |
| `write_yn` | character varying(1) | N | `'N'::character varying` |  | 등록(신규행 추가) 권한 Y/N |
| `modify_yn` | character varying(1) | N | `'N'::character varying` |  | 수정(기존행 편집) 권한 Y/N |
| `delete_yn` | character varying(1) | N | `'N'::character varying` |  | 삭제 권한 Y/N |
| `print_yn` | character varying(1) | N | `'Y'::character varying` |  | 출력(A4 인쇄·PDF 다운로드) 권한 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_role_screen`: `co_cd, usrgrp_cd, scrn_cd`
- 인덱스 `ix_tbl_role_screen_grp`: `co_cd, usrgrp_cd`

## tbl_screen

**기초코드** — 화면 마스터 — 플랫폼 전역. 메뉴·권한·UV/PV 통계의 공통 차원

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `scrn_cd` | character varying(30) | N |  | UQ | 화면 식별자 — 역할 기반 kebab-case. FE screenRegistry 키와 1:1 |
| `scrn_nm` | character varying(100) | N |  |  | 화면명 — 탭 제목·통계 표시명 |
| `module_cd` | character varying(10) | N |  |  | 모듈코드 — SYS/BAS/CCP/HYG/FAC/INV/VER/DOC/TSK. URL 첫 세그먼트 |
| `tmpl_cd` | character varying(40) | Y |  |  | 연결 템플릿코드 — tbl_template.tmpl_cd. 점검표 화면일 때만 값이 있다 |
| `sort_no` | integer | N | `0` |  | 정렬순서 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_screen_scrn_cd`: `scrn_cd`

## tbl_menu

**기초코드** — 업체별 메뉴 트리 — SideMenu 3단 구성. 미사용 양식은 use_yn=N으로 숨긴다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `menu_cd` | character varying(40) | N |  | UQ | 메뉴코드 — 업체 내 유일 |
| `menu_nm` | character varying(100) | N |  |  | 메뉴명 |
| `h_menu_cd` | character varying(40) | Y |  |  | 상위 메뉴코드 — NULL이거나 공백일 때(= 대분류) 트리 루트 |
| `scrn_cd` | character varying(30) | Y |  |  | 화면코드 — tbl_screen.scrn_cd. 값이 있을 때(= leaf 노드) 클릭 시 탭이 열린다 |
| `sort_no` | integer | N | `0` |  | 정렬순서 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_menu_co_menu`: `co_cd, menu_cd`
- 인덱스 `ix_tbl_menu_co`: `co_cd, use_yn, sort_no`

## tbl_code

**기초코드** — 공통코드 — sub_cd=* 행이 그룹 헤더

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키. 0000일 때(= 플랫폼 표준코드) 전 업체 공용 |
| `main_cd` | character varying(20) | N |  | UQ | 대분류 코드 |
| `sub_cd` | character varying(20) | N |  | UQ | 세부 코드 — *일 때(= 그룹 헤더행) code_nm이 그룹명 |
| `code_nm` | character varying(100) | N |  |  | 코드명 |
| `sort_no` | integer | N | `0` |  | 정렬순서 |
| `ref1` | character varying(100) | Y |  |  | 참조값1 — 단위·기준값 등 코드별 부가정보 |
| `ref2` | character varying(100) | Y |  |  | 참조값2 |
| `sys_yn` | character varying(1) | N | `'N'::character varying` |  | 시스템코드 여부 Y/N — Y일 때(= 플랫폼 고정코드) 업체 수정·삭제 불가 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_code`: `co_cd, main_cd, sub_cd`
- 인덱스 `ix_tbl_code_main`: `co_cd, main_cd, use_yn, sort_no`

## tbl_approval_line

**기초코드** — 결재선 헤더 — 업체별 결재 경로 정의. 템플릿별로 다른 결재선 지정 가능

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `appr_line_cd` | character varying(20) | N |  | UQ | 결재선 코드 — 업체 내 유일 |
| `appr_line_nm` | character varying(100) | N |  |  | 결재선명 — 일반 점검표 결재선 등 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_approval_line`: `co_cd, appr_line_cd`

## tbl_approval_line_step

**기초코드** — 결재선 단계 — 작성자·승인자 순번 정의

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `appr_line_cd` | character varying(20) | N |  | UQ | 결재선 코드 — tbl_approval_line.appr_line_cd |
| `step_no` | integer | N |  | UQ | 단계 순번 — 1부터. 낮은 번호부터 순차 결재 |
| `role_cd` | character varying(20) | N |  |  | 역할 — WRITE:작성자, APPROVE:승인자 |
| `approver_id` | character varying(20) | Y |  |  | 결재자 로그인 ID — NULL이면(= 직위 기준 지정) dept_cd·pos_cd로 대상자를 찾는다 |
| `dept_cd` | character varying(20) | Y |  |  | 결재 부서코드 — approver_id 미지정 시 사용 |
| `pos_cd` | character varying(20) | Y |  |  | 결재 직위코드 — approver_id 미지정 시 사용 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 단계 사용여부 Y/N — N이면 상신 스냅샷에서 빠진다 |

- 기본키: `idx`
- 유일키 `ux_tbl_approval_line_step`: `co_cd, appr_line_cd, step_no`

## tbl_doc_no_rule

**기초코드** — 문서번호 채번 규칙 — 사용자에게 보이는 업무번호(doc_no) 생성. idx와 별개

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — tbl_template.tmpl_cd |
| `prefix` | character varying(20) | Y |  |  | 문서번호 접두 — CCP-B 등 |
| `date_fmt` | character varying(10) | Y | `'YYYYMMDD'::character varying` |  | 일자 형식 — YYYYMMDD / YYYYMM / YYYY. 공백이면 일자 미포함 |
| `seq_len` | integer | N | `3` |  | 일련번호 자릿수 — 3이면 001부터 |
| `reset_cycle` | character varying(1) | N | `'Y'::character varying` |  | 일련번호 리셋 주기 — D:일, M:월, Y:년, N:리셋 없음 |
| `last_reset_key` | character varying(10) | Y |  |  | 마지막 리셋 기준키 — reset_cycle에 맞춘 일자 문자열. 값이 바뀌면 last_seq를 0으로 되돌린다 |
| `last_seq` | integer | N | `0` |  | 마지막 채번 일련번호 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_doc_no_rule`: `co_cd, tmpl_cd`

## tbl_grid_pref

**기초코드** — 사용자별 그리드 열 설정 — 숨김·순서·너비만 저장. 정렬·필터는 세션 보관

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `user_id` | character varying(20) | N |  | UQ | 로그인 ID — tbl_user.user_id |
| `scrn_cd` | character varying(30) | N |  | UQ | 화면코드 — tbl_screen.scrn_cd |
| `grid_id` | character varying(30) | N |  | UQ | 그리드 식별자 — MesEditableGrid persistId (한 화면에 그리드가 여럿일 때 구분) |
| `pref_json` | text | N |  |  | 열 설정 JSON — gridPref v2 직렬화 문자열 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_grid_pref`: `user_id, scrn_cd, grid_id`

## tbl_user_noti_pref

**기초코드** — 사용자별 알림 수신 설정 — bap1100 알림 컬럼 12종의 정규화 대체

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `user_id` | character varying(20) | N |  | UQ | 로그인 ID — tbl_user.user_id |
| `noti_type_cd` | character varying(30) | N |  | UQ | 알림 유형 — DOC_DUE(작성기한), DOC_LATE(미작성), APPROVAL(결재요청), CA_DUE(개선조치기한), CALIB_DUE(검교정도래), EDU_DUE(교육예정) 등 |
| `recv_yn` | character varying(1) | N | `'Y'::character varying` |  | 수신여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_user_noti_pref`: `user_id, noti_type_cd`

## tbl_template

**기초문서** — 표준 템플릿 카탈로그 — 플랫폼이 배포하는 31종 + 추가 12종 메타. 업체는 복사해서 쓴다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — CCP_COLD, CCP_METAL, DAILY_HYG 등 |
| `tmpl_nm` | character varying(200) | N |  |  | 표준 문서명 — CCP 냉장보관 모니터링 일지 등(회사별 CCP 코드는 포함하지 않음) |
| `mng_no` | character varying(20) | Y |  |  | 표준기준서 관리번호 — HA-HYG-01, HA-CCP-06-01 등 |
| `doc_kind` | character varying(10) | N |  |  | 문서 유형 — HTML:전용 화면 + DB 저장, HWP:rhwp 문서작성형. 저장값은 대문자다 |
| `category_cd` | character varying(20) | Y |  |  | 분류 — CCP, HYG, FAC, INV, VER, EDU, DOC |
| `scrn_cd` | character varying(30) | Y |  |  | 연결 화면코드 — doc_kind=HTML일 때(= 전용 화면 보유) tbl_screen.scrn_cd |
| `form_path` | character varying(300) | Y |  |  | 표준 원본 HWP 상대경로 — HaccpTemplates/{tmpl_cd}/{파일명}. html 양식은 물리 원본이 없어 NULL |
| `default_cycle_cd` | character varying(10) | Y |  |  | 기본 작성주기 — D:일, W:주, M:월, Y:년, E:수시(이벤트) |
| `default_retention_month` | integer | N | `24` |  | 기본 보존 개월수 — HACCP 기준 최소 24 |
| `ver_no` | integer | N | `1` |  | 템플릿 버전 — 표의 전체 구조 변경 시 증가 |
| `impl_yn` | character varying(1) | N | `'N'::character varying` |  | 구현여부 Y/N — N일 때(= 카탈로그 등록만, 화면 미개발) 업체 배포 대상에서 제외 |
| `sort_no` | integer | N | `0` |  | 정렬순서 — 표준기준서 관리번호 순 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `co_cd` | character varying(10) | N | `'0000'::character varying` | UQ | 카탈로그 소유 회사 — 0000이면(= 플랫폼 공용) 전 업체 배포 대상, 그 외는 해당 업체가 직접 등록한 자사 양식 |

- 기본키: `idx`
- 유일키 `ux_tbl_template`: `co_cd, tmpl_cd`
- 유일키 `ux_tbl_template`: `co_cd, tmpl_cd`

## tbl_company_template

**기초문서** — 업체 사용양식 — 표준 템플릿을 업체가 복사·조정한 결과

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — tbl_template.tmpl_cd |
| `tmpl_nm_ovr` | character varying(200) | Y |  |  | 문서명 오버라이드 — NULL이면(= 미지정) tbl_template.tmpl_nm 사용 |
| `appr_line_cd` | character varying(20) | Y |  |  | 적용 결재선 코드 — tbl_approval_line.appr_line_cd |
| `cycle_cd` | character varying(10) | Y |  |  | 작성주기 오버라이드 — NULL이면 표준 주기 사용 |
| `retention_month` | integer | Y |  |  | 보존 개월수 오버라이드 — NULL이면 표준값 사용 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N — N일 때(= 우리 업체 미해당 양식) 메뉴·일정에서 제외 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `base_use_yn` | character varying(1) | N | `'Y'::character varying` |  | 기본 양식 사용여부 — Y일 때(= 플랫폼 기본), N일 때(= 활성 자사 양식) 사용 |
| `co_form_idx` | bigint | Y |  |  | 활성 자사 양식 idx — base_use_yn=N일 때 tbl_company_form.idx |
| `sys_yn` | character varying(10) | N | `'Y'::character varying` |  | 시스템유무 — sys=플랫폼, usr=사용자(공통코드 sys-yn) |
| `form_path` | character varying(300) | Y |  |  | 회사 전용 HWP 원본 상대경로 — CustomTemplates/{co_cd}/{tmpl_cd}/{파일명}. NULL이면 tbl_template.form_path |
| `default_file_idx` | bigint | Y |  |  | 기본 제공 파일 idx — 시스템 양식은 표준 원본, 자사양식은 최초 등록본. 초기화 대상. NULL이면 초기화 불가 |
| `current_file_idx` | bigint | Y |  |  | 현재 적용 파일 idx — 업로드·불러오기로만 바뀐다. form_path 와 항상 같은 파일을 가리킨다 |

- 기본키: `idx`
- 유일키 `ux_tbl_company_template`: `co_cd, tmpl_cd`

## tbl_company_template_file

**기초문서** — 양식 파일 이력 — 업로드·기본제공 파일 1행씩. 불러오기(과거 버전 적용)·초기화의 원천

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 — default_file_idx/current_file_idx가 이 값을 가리킨다 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키. 다른 회사 파일이 섞이지 않는다 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 양식코드 — tbl_company_template.tmpl_cd |
| `file_seq` | integer | N |  | UQ | 버전 순번 — 업로드 순서. 물리 파일명 접미(_v{seq})와 같다 |
| `file_nm` | character varying(300) | N |  |  | 표시 파일명 — 업로드 원본명(번호 접두 제거) |
| `form_path` | character varying(300) | N |  |  | APP_FILE_ROOT 기준 상대 경로 — 표준은 HaccpTemplates/{tmpl_cd}/, 자사는 CustomTemplates/{co_cd}/{tmpl_cd}/ |
| `file_size` | bigint | Y |  |  | 바이트 크기 — 표시·검증용. 기본제공 시딩분은 NULL |
| `src_ty` | character varying(10) | N | `'usr'::character varying` |  | 출처 — sys:프로그램 기본 제공본, usr:회사 업로드본 |
| `del_yn` | character varying(1) | N | `'N'::character varying` |  | 삭제여부 Y/N — Y일 때(= 양식 삭제됨) 불러오기 목록에서 제외하고 파일은 남긴다 |
| `ins_id` | character varying(20) | Y |  |  | 업로드자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 업로드 일시 — 불러오기 목록 표시 기준 |

- 기본키: `idx`
- 유일키 `ux_tbl_company_template_file`: `co_cd, tmpl_cd, file_seq`
- 인덱스 `ix_tbl_company_template_file_01`: `co_cd, tmpl_cd, del_yn, file_seq DESC`

## tbl_check_item

**기초문서** — 템플릿별 표준 점검항목 — 일일위생 15항목, 용수관리 11항목 등 표준기준서 문구 그대로

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — tbl_template.tmpl_cd |
| `item_cd` | character varying(20) | N |  | UQ | 항목코드 — 템플릿 내 유일 |
| `grp_cd` | character varying(20) | Y |  |  | 항목 구분코드 — BEFORE(작업 전), DURING(작업 중), AFTER(작업 후), TANK(저장탱크) 등 |
| `grp_nm` | character varying(100) | Y |  |  | 항목 구분명 — A4 표 좌측 병합 셀에 출력 |
| `item_nm` | text | N |  |  | 점검항목 문구 — 표준기준서 원문 |
| `input_type` | character varying(20) | N | `'OX'::character varying` |  | 입력유형 — OX(양호/불량), YN(예/아니오), JUDGE(적합/부적합), NUM(수치), TEXT(서술), TIME(시각) |
| `unit_nm` | character varying(20) | Y |  |  | 단위 — input_type=NUM일 때 표기 |
| `method_nm` | character varying(100) | Y |  |  | 점검방법 — 육안 등. 시설·설비 점검표처럼 표에 방법 열이 있는 양식에서만 사용 |
| `cycle_nm` | character varying(50) | Y |  |  | 점검주기 — 1회/일 등. 방법 열과 함께 출력 |
| `sort_no` | integer | N | `0` |  | 정렬순서 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_check_item`: `tmpl_cd, item_cd`

## tbl_html_form_ver

**기초문서** — (표 주석 없음)

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | 대리키 PK — identity |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ | 표시 순번 — 1부터. 0은 표준 가상행. 항목·작성 문서가 참조하므로 재사용 금지 |
| `ver_nm` | character varying(100) | N |  |  | 버전명 — 유니크 아님 |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  | 작성 신규 적용. 없으면(전부 N) 표준 |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 — N=소프트 삭제 |
| `ver_cd` | character varying(20) | N |  |  | 버전코드 — 표준 가상행 0.1. 활성(use_yn=Y)만 업체+양식당 유니크. 삭제 후 재사용 가능 |

- 기본키: `idx`
- 유일키 `ux_tbl_html_form_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_form_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_form_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_form_ver_item

**기초문서** — (표 주석 없음)

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'radio'::character varying` |  | html-input-ty — radio / radio-num / radio-text / num / text |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_form_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_html_hyg_prc_ver

**기초문서** — 일반위생·공정점검 자사 양식 버전 — 예시는 html_hyg_prc_000 가상

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `ver_cd` | character varying(20) | N |  |  |  |
| `ver_nm` | character varying(100) | N |  |  |  |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_hyg_prc_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_hyg_prc_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_hyg_prc_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_hyg_prc_ver_item

**기초문서** — 일반위생·공정점검 자사 양식 항목

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'radio'::character varying` |  |  |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_hyg_prc_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_html_ccp_chk_ver

**기초문서** — CCP 검증점검 자사 양식 버전 — 예시는 html_ccp_chk_000 가상

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `ver_cd` | character varying(20) | N |  |  |  |
| `ver_nm` | character varying(100) | N |  |  |  |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_chk_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_ccp_chk_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_ccp_chk_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_ccp_chk_ver_item

**기초문서** — CCP 검증점검 자사 양식 항목

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'radio'::character varying` |  |  |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_chk_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_html_ccp_pkg_ver

**기초문서** — CCP-1B 포장 모니터링일지 자사 양식 버전 — 예시는 html_ccp_pkg_000 가상

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `ver_cd` | character varying(20) | N |  |  |  |
| `ver_nm` | character varying(100) | N |  |  |  |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_pkg_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_ccp_pkg_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_ccp_pkg_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_ccp_pkg_ver_item

**기초문서** — CCP-1B 포장 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·개선조치

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'text'::character varying` |  |  |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_pkg_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_html_ccp_htg_ver

**기초문서** — CCP-2B 가열 모니터링일지 자사 양식 버전 — 예시는 html_ccp_htg_000 가상

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `ver_cd` | character varying(20) | N |  |  |  |
| `ver_nm` | character varying(100) | N |  |  |  |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_htg_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_ccp_htg_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_ccp_htg_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_ccp_htg_ver_item

**기초문서** — CCP-2B 가열 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·개선조치

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'text'::character varying` |  |  |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_htg_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_html_ccp_mtl_ver

**기초문서** — CCP-3P 금속검출 모니터링일지 자사 양식 버전 — 예시는 html_ccp_mtl_000 가상

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `ver_cd` | character varying(20) | N |  |  |  |
| `ver_nm` | character varying(100) | N |  |  |  |
| `apply_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_mtl_ver`: `co_cd, tmpl_cd, ver_no`
- 인덱스 `ux_tbl_html_ccp_mtl_ver_apply` (유일): `co_cd, tmpl_cd` — 조건 `(apply_yn)::text = 'Y'::text`
- 인덱스 `ux_tbl_html_ccp_mtl_ver_cd` (유일): `co_cd, tmpl_cd, ver_cd` — 조건 `(use_yn)::text = 'Y'::text`

## tbl_html_ccp_mtl_ver_item

**기초문서** — CCP-3P 금속검출 모니터링일지 자사 양식 항목 — 한계기준·주기·방법·감도열 해당없음·개선조치

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  | UQ |  |
| `tmpl_cd` | character varying(40) | N |  | UQ |  |
| `ver_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  | UQ |  |
| `sort_no` | integer | N | `0` |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | N |  |  |  |
| `input_type` | character varying(20) | N | `'text'::character varying` |  |  |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_html_ccp_mtl_ver_item`: `co_cd, tmpl_cd, ver_no, item_cd`

## tbl_schedule_rule

**기초문서** — 작성주기 규칙 — 오늘 할 일 생성 배치의 입력. 템플릿별 여러 규칙 허용

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — tbl_template.tmpl_cd |
| `rule_seq` | integer | N | `1` | UQ | 규칙 순번 — 양식당 1건 제약(ux_tbl_schedule_rule_tmpl) 이후로는 항상 1. 레거시 유니크 유지용 |
| `cycle_cd` | character varying(1) | N |  |  | 주기 — D:매일, W:매주, M:매월, Q:분기, H:반기, Y:매년, E:수시(이벤트 발생 시) |
| `week_days` | character varying(20) | Y |  |  | 요일 — cycle_cd=W일 때. 1(월)~7(일) 쉼표 구분 |
| `month_day` | integer | Y |  |  | 기준일 — cycle_cd=M/Y일 때 해당 월의 일자 |
| `month_no` | integer | Y |  |  | 기준월 — cycle_cd=Y일 때 1~12 |
| `due_time` | character varying(4) | Y | `'1800'::character varying` |  | 마감시각 HHMM — 경과 시 지연(LATE) 처리 |
| `dept_cd` | character varying(20) | Y |  |  | 담당 부서코드 — 오늘 할 일 배정 대상 |
| `user_id` | character varying(20) | Y |  |  | 담당자 로그인 ID — 지정 시 개인에게 직접 배정 |
| `use_yn` | character varying(1) | N | `'Y'::character varying` |  | 사용여부 Y/N |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `base_dt` | character varying(8) | Y |  |  | 관리 시작일 yyyyMMdd — 이 날짜 이전 예정일은 만들지 않는다 |
| `nonwork_rule` | character varying(10) | N | `'keep'::character varying` |  | 비영업일 처리 — keep:그대로, prev:이전 영업일, next:다음 영업일. 토·일에 걸린 예정일을 어디로 옮길지 |

- 기본키: `idx`
- 유일키 `ux_tbl_schedule_rule`: `co_cd, tmpl_cd, rule_seq`
- 유일키 `ux_tbl_schedule_rule_tmpl`: `co_cd, tmpl_cd`

## tbl_schedule_rule_detail

**기초문서** — 작성주기 반복 상세 — 요일·실행일·말일·분기월·반기월. 저장 시 양식 단위로 전량 교체된다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 양식코드 — tbl_schedule_rule.tmpl_cd (양식당 주기 1건이라 rule 키와 같다) |
| `seq` | integer | N |  | UQ | 입력 순번 — 화면 표시 순서. 업무 의미는 없다 |
| `detail_ty` | character varying(20) | N |  |  | 상세 유형 — week-day:요일, month-day:실행일, month-end:말일, quarter-month:분기내 실행월, half-month:반기내 실행월, year-month:실행월 |
| `val1` | integer | Y |  |  | 값1 — 요일 1(월)~7(일) / 실행일 1~31 / 분기·반기 내 월 순번 / 실행월 1~12. month-end 는 NULL |
| `val2` | integer | Y |  |  | 값2 — 월 지정과 함께 쓰는 실행일 1~31. 요일·실행일 단독일 때는 NULL |

- 기본키: `idx`
- 유일키 `ux_tbl_schedule_rule_detail`: `co_cd, tmpl_cd, seq`
- 인덱스 `ix_tbl_schedule_rule_detail_01`: `co_cd, tmpl_cd, seq`

## tbl_document

**업무** — 문서 공통 관리정보 — 모든 문서 1행. 사용자는 목록에서 DB형·문서형을 구분하지 않는다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 — 업무 테이블이 doc_idx로 참조 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  |  | 템플릿 코드 — tbl_template.tmpl_cd |
| `doc_kind` | character varying(10) | N |  |  | 문서 유형 — HTML:전용 화면 + DB 저장, HWP:rhwp 문서작성형. 저장값은 대문자다 |
| `doc_no` | character varying(50) | N |  | UQ | 문서번호 — tbl_doc_no_rule로 채번한 사용자 표기용 번호 |
| `base_dt` | character varying(8) | N |  |  | 기준일자 YYYYMMDD — 작성일 또는 점검일. 일정·검색의 기준 |
| `base_dt_to` | character varying(8) | Y |  |  | 기준종료일자 YYYYMMDD — 용수관리처럼 기간 단위 문서일 때만 사용 |
| `title` | character varying(300) | Y |  |  | 문서 제목 — 미입력 시 템플릿명 + 기준일자로 자동 생성 |
| `status` | character varying(3) | N | `'WRK'::character varying` |  | 문서상태 — WRK:작성중, REQ:승인요청, APV:승인완료, RJT:반려 |
| `appr_line_cd` | character varying(20) | Y |  |  | 적용 결재선 — 작성 시점의 tbl_company_template.appr_line_cd 스냅샷 |
| `writer_id` | character varying(20) | Y |  |  | 작성자 로그인 ID |
| `write_dt` | timestamp without time zone | Y |  |  | 작성(상신) 일시 |
| `approver_id` | character varying(20) | Y |  |  | 승인자 로그인 ID |
| `approve_dt` | timestamp without time zone | Y |  |  | 승인 일시 |
| `reject_reason` | character varying(500) | Y |  |  | 반려 사유 — 줄바꿈으로 쌓는다. 최신이 맨 위. 재전송해도 유지 |
| `ver_no` | integer | N | `1` |  | 현재 버전 — 승인 후 수정 시 증가하고 직전 내용은 tbl_document_version에 스냅샷 |
| `retention_until` | character varying(8) | Y |  |  | 보존 만료일 YYYYMMDD — base_dt + 보존 개월수. 경과 전 삭제 차단 |
| `del_yn` | character varying(1) | N | `'N'::character varying` |  | 삭제여부 Y/N — 물리 삭제 대신 논리 삭제. 승인 문서는 Y로도 바꿀 수 없다 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `form_src` | character varying(10) | N | `'BASE'::character varying` |  | 작성 시점 양식 출처 — BASE 기본양식, CUSTOM 자사양식 |
| `co_form_idx` | bigint | Y |  |  | 작성 시점 자사 양식 idx — CUSTOM일 때만 값 보관 |
| `remark` | character varying(500) | Y |  |  | 비고 — 상신자가 결재자에게 남기는 문서 단위 메모. 결재완료(APV) 전까지만 수정 가능 |
| `cancel_reason` | character varying(500) | Y |  |  | 결재 취소 사유 — 줄바꿈으로 쌓는다. 최신이 맨 위. 재전송해도 유지 |

- 기본키: `idx`
- 유일키 `ux_tbl_document_doc_no`: `co_cd, doc_no`
- 인덱스 `ix_tbl_document_retain`: `co_cd, retention_until`
- 인덱스 `ix_tbl_document_search`: `co_cd, tmpl_cd, base_dt DESC`
- 인덱스 `ix_tbl_document_status`: `co_cd, status, base_dt DESC`
- 인덱스 `ix_tbl_document_writer`: `co_cd, writer_id, base_dt DESC`

## tbl_document_approval

**업무** — 문서 결재 이력 — A4 결재란 3칸의 실제 데이터 원천

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `doc_idx` | bigint | N |  | UQ | 문서 idx — tbl_document.idx |
| `step_no` | integer | N |  | UQ | 단계 순번 — 결재선 step_no 스냅샷 |
| `role_cd` | character varying(20) | N |  |  | 역할 — WRITE:작성자, APPROVE:승인자 |
| `approver_id` | character varying(20) | Y |  |  | 결재자 로그인 ID |
| `approver_nm` | character varying(50) | Y |  |  | 결재자명 — 결재 시점 스냅샷. 이후 사용자명이 바뀌어도 문서는 당시 이름을 유지 |
| `result_cd` | character varying(1) | N | `'W'::character varying` |  | 결재 결과 — W:대기, A:승인, R:반려 |
| `opinion` | character varying(500) | Y |  |  | 결재 의견 — 반려(R)일 때 필수 |
| `act_dt` | timestamp without time zone | Y |  |  | 결재 처리 일시 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `sign_img` | bytea | Y |  |  | 서명 이미지 바이너리 — 결재 시점 tbl_user.sign_img 스냅샷 |

- 기본키: `idx`
- 유일키 `ux_tbl_document_approval`: `doc_idx, step_no`
- 인덱스 `ix_tbl_document_approval_doc`: `doc_idx, step_no`
- 인덱스 `ix_tbl_document_approval_usr`: `co_cd, approver_id, result_cd`

## tbl_document_file

**업무** — 문서 첨부·원본·PDF — 경로만 저장. 실제 파일은 업체별 볼륨 경로에 보관

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키. 파일 저장 경로의 첫 세그먼트이기도 하다 |
| `doc_idx` | bigint | N |  |  | 문서 idx — tbl_document.idx |
| `file_kind` | character varying(10) | N |  |  | 파일 종류 — HWP_SRC:한글 원본, PDF:완료본, ATTACH:일반첨부, PHOTO:사진 |
| `file_nm` | character varying(300) | N |  |  | 원본 파일명 — 사용자가 올린 그대로. 다운로드 시 이 이름으로 내려준다 |
| `file_path` | character varying(500) | N |  |  | 저장 경로 — {root}/{co_cd}/{yyyy}/{mm}/{idx}_{원본명} |
| `file_size` | bigint | Y |  |  | 파일 크기(byte) |
| `mime_type` | character varying(100) | Y |  |  | MIME 타입 |
| `sort_no` | integer | N | `0` |  | 정렬순서 — 사진 첨부 표시 순서 |
| `ins_id` | character varying(20) | Y |  |  | 업로더 로그인 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 업로드 일시 |

- 기본키: `idx`
- 인덱스 `ix_tbl_document_file_doc`: `doc_idx, file_kind`

## tbl_document_version

**업무** — 문서 버전 스냅샷 — 승인 후 수정 시 직전 상태 보존. 감사 대응 필수

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `doc_idx` | bigint | N |  | UQ | 문서 idx — tbl_document.idx |
| `ver_no` | integer | N |  | UQ | 스냅샷 버전 — 이 행이 보존하는 직전 버전 번호 |
| `snap_json` | jsonb | Y |  |  | 본문 스냅샷 JSON — DB형일 때(= 헤더+상세 전체 직렬화) |
| `file_path` | character varying(500) | Y |  |  | 원본 파일 스냅샷 경로 — 문서형일 때(= HWPX/PDF 사본) |
| `change_reason` | character varying(500) | Y |  |  | 변경 사유 — 승인 문서 수정 시 필수 입력 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_document_version`: `doc_idx, ver_no`

## tbl_hyg_process

**업무** — (표 주석 없음)

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `doc_idx` | bigint | N |  | UQ |  |
| `base_dt` | character varying(8) | N |  |  |  |
| `checker_nm` | character varying(50) | Y |  |  |  |
| `ver_no` | integer | N | `0` |  |  |
| `special_note` | text | Y |  |  |  |
| `improve_note` | text | Y |  |  |  |
| `action_nm` | text | Y |  |  |  |
| `confirm_nm` | text | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |
| `checker_id` | character varying(20) | Y |  |  | 점검자 로그인 ID — 저장 시 이름 매칭 |
| `checker_sign_img` | bytea | Y |  |  | 점검자 서명 스냅샷 — 없으면 이름만 |
| `approver_id` | character varying(20) | Y |  |  |  |
| `approver_nm` | character varying(50) | Y |  |  | 승인자명 — 헤더 스냅샷 |
| `approver_sign_img` | bytea | Y |  |  | 승인자 서명 스냅샷 — 없으면 이름만 |
| `confirm_id` | character varying(20) | Y |  |  |  |
| `confirm_sign_img` | bytea | Y |  |  | 확인 서명 스냅샷 — 없으면 confirm_nm만 |

- 기본키: `idx`
- 유일키 `ux_tbl_hyg_process`: `doc_idx`

## tbl_hyg_process_item

**업무** — (표 주석 없음)

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `hdr_idx` | bigint | N |  | UQ |  |
| `sort_no` | integer | N |  | UQ |  |
| `item_cd` | character varying(20) | N |  |  |  |
| `cycle_nm` | character varying(50) | Y |  |  |  |
| `grp_nm` | character varying(100) | Y |  |  |  |
| `item_nm` | text | Y |  |  |  |
| `input_type` | character varying(20) | N | `'radio'::character varying` |  | html-input-ty — radio / radio-num / radio-text / num / text |
| `unit_nm` | character varying(20) | Y |  |  |  |
| `yn` | character varying(1) | Y |  |  |  |
| `val_nm` | text | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | Y | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_hyg_process_item`: `hdr_idx, sort_no`

## tbl_ccp_verify_check

**업무** — CCP 검증점검표 헤더 — 표준기준서 관리번호 3. 월 1회 작성

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `doc_idx` | bigint | N |  | UQ | 문서 idx — tbl_document.idx와 1:1 |
| `base_dt` | character varying(8) | N |  |  | 점검일자 YYYYMMDD |
| `checker_id` | character varying(20) | Y |  |  | 점검자 로그인 ID |
| `checker_nm` | character varying(50) | Y |  |  | 점검자명 — 작성 시점 스냅샷 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `monitor_chk_rmk` | text | Y |  |  | 모니터링 일지 확인 — SPAN 입력 |
| `ver_no` | integer | N | `0` |  | 작성에 쓴 자사 양식 버전 — tbl_html_ccp_chk_ver.ver_no. 0이면 표준 |
| `checker_sign_img` | bytea | Y |  |  |  |
| `approver_id` | character varying(20) | Y |  |  |  |
| `approver_nm` | character varying(50) | Y |  |  | 승인자명 스냅샷 — 지면 결재란 |
| `approver_sign_img` | bytea | Y |  |  |  |
| `confirm_id` | character varying(20) | Y |  |  |  |
| `confirm_nm` | character varying(50) | Y |  |  | 확인자명 스냅샷 — 지면 하단 확인칸 |
| `confirm_sign_img` | bytea | Y |  |  |  |
| `special_note` | text | Y |  |  | 특이사항 — 개행 보존 |
| `improve_note` | text | Y |  |  |  |
| `action_nm` | text | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_verify_check`: `doc_idx`

## tbl_ccp_verify_item

**업무** — CCP 검증 항목별 결과 — 예/아니오 응답과 근거 기록. 일지 건수는 자동 집계

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `hdr_idx` | bigint | N |  | UQ | 헤더 idx — tbl_ccp_verify_check.idx |
| `row_seq` | integer | N |  | UQ | 행 순번 |
| `proc_cd` | character varying(20) | Y |  |  | 공정 코드 — 원료육 냉장보관, 금속검출, 완제품 냉장보관 |
| `proc_nm` | character varying(100) | Y |  |  | 공정명 — A4 표 좌측 병합 셀 |
| `item_cd` | character varying(20) | Y |  |  | 항목코드 — tbl_check_item.item_cd |
| `verify_desc` | character varying(500) | N |  |  | 검증 내용 — 표준기준서 질문 문구 |
| `answer_cd` | character varying(1) | Y |  |  | 응답 — Y:예, N:아니오. N일 때(= 미준수) 개선조치 필수 |
| `record_desc` | character varying(500) | Y |  |  | 기록 내용 — 확인 근거 서술. 자동 집계 항목은 SP가 문구를 생성해 채운다 |
| `ref_tmpl_cd` | character varying(20) | Y |  |  | 자동 집계 대상 템플릿 코드 — CCP_COLD, CCP_METAL 등. NULL이면(= 수동 입력 항목) |
| `ref_from_dt` | character varying(8) | Y |  |  | 집계 시작일 YYYYMMDD |
| `ref_to_dt` | character varying(8) | Y |  |  | 집계 종료일 YYYYMMDD |
| `ref_total_cnt` | integer | Y |  |  | 집계 결과 총 작성 건수 |
| `ref_ok_cnt` | integer | Y |  |  | 집계 결과 정상 건수 |
| `ref_ng_cnt` | integer | Y |  |  | 집계 결과 이탈 건수 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `cycle_nm` | character varying(50) | Y |  |  | 점검주기 문구 스냅샷 — 양식 항목에서 복사 |
| `input_type` | character varying(20) | N | `'radio'::character varying` |  | 입력유형 html-input-ty — 지면 렌더·필수값 판정 기준 |
| `unit_nm` | character varying(20) | Y |  |  | 단위 — 숫자 입력 항목 표시용 |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_verify_item`: `hdr_idx, row_seq`
- 인덱스 `ix_tbl_ccp_verify_item_hdr`: `hdr_idx`

## tbl_ccp_pkg_monitor

**업무** — CCP 포장 모니터링 헤더 — 표준기준서 포장공정 기록

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `doc_idx` | bigint | N |  | UQ |  |
| `base_dt` | character varying(8) | N |  |  |  |
| `tmpl_cd` | character varying(40) | N |  |  |  |
| `ccp_cd` | character varying(30) | Y |  |  |  |
| `diary_no` | character varying(20) | Y |  |  |  |
| `limit_item_kind` | character varying(30) | Y |  |  |  |
| `mng_user_id` | character varying(20) | Y |  |  |  |
| `mng_nm` | character varying(50) | Y |  |  |  |
| `form_src` | character varying(10) | N | `'BASE'::character varying` |  | 작성 양식 출처 — BASE 또는 CUSTOM. 과거 양식 이력 보존 |
| `co_form_idx` | bigint | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_pkg_monitor_doc`: `doc_idx`

## tbl_ccp_pkg_monitor_row

**업무** — CCP 포장 점검 행 — 시간별 측정·판정·점검자

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `monitor_idx` | bigint | N |  | UQ |  |
| `row_seq` | integer | N |  | UQ |  |
| `check_time` | character varying(10) | Y |  |  |  |
| `judge_cd` | character varying(1) | Y |  |  |  |
| `judge_mod_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `checker_id` | character varying(20) | Y |  |  |  |
| `checker_nm` | character varying(50) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |
| `equip_nm` | character varying(100) | Y |  |  |  |
| `product_nm` | character varying(100) | Y |  |  |  |
| `phase_cd` | character varying(20) | Y |  |  |  |
| `sign_img` | bytea | Y |  |  | 행 서명 이미지 바이너리 — 서명 적용 시점 tbl_user.sign_img 스냅샷 |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_pkg_monitor_row`: `monitor_idx, row_seq`

## tbl_ccp_pkg_monitor_cell

**업무** — CCP 포장 측정 셀 — 수치 또는 텍스트 값과 항목별 판정

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `row_idx` | bigint | N |  | UQ |  |
| `item_cd` | character varying(30) | N |  | UQ | 한계항목 코드 — limit_item_kind 프리셋의 열 식별자 |
| `num_val` | numeric(14,3) | Y |  |  |  |
| `txt_val` | character varying(500) | Y |  |  |  |
| `judge_cd` | character varying(1) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_pkg_monitor_cell`: `row_idx, item_cd`

## tbl_ccp_htg_monitor

**업무** — CCP 가열 모니터링 헤더 — 표준기준서 가열공정 기록

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `doc_idx` | bigint | N |  | UQ |  |
| `base_dt` | character varying(8) | N |  |  |  |
| `tmpl_cd` | character varying(40) | N |  |  |  |
| `ccp_cd` | character varying(30) | Y |  |  |  |
| `diary_no` | character varying(20) | Y |  |  |  |
| `limit_item_kind` | character varying(30) | Y |  |  |  |
| `mng_user_id` | character varying(20) | Y |  |  |  |
| `mng_nm` | character varying(50) | Y |  |  |  |
| `form_src` | character varying(10) | N | `'BASE'::character varying` |  | 작성 양식 출처 — BASE 또는 CUSTOM. 과거 양식 이력 보존 |
| `co_form_idx` | bigint | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_htg_monitor_doc`: `doc_idx`

## tbl_ccp_htg_monitor_row

**업무** — CCP 가열 점검 행 — 시간별 측정·판정·점검자

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `monitor_idx` | bigint | N |  | UQ |  |
| `row_seq` | integer | N |  | UQ |  |
| `check_time` | character varying(10) | Y |  |  |  |
| `judge_cd` | character varying(1) | Y |  |  |  |
| `judge_mod_yn` | character varying(1) | N | `'N'::character varying` |  |  |
| `checker_id` | character varying(20) | Y |  |  |  |
| `checker_nm` | character varying(50) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |
| `equip_nm` | character varying(100) | Y |  |  |  |
| `product_nm` | character varying(100) | Y |  |  |  |
| `phase_cd` | character varying(20) | Y |  |  |  |
| `sign_img` | bytea | Y |  |  | 행 서명 이미지 바이너리 — 서명 적용 시점 tbl_user.sign_img 스냅샷 |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_htg_monitor_row`: `monitor_idx, row_seq`

## tbl_ccp_htg_monitor_cell

**업무** — CCP 가열 측정 셀 — 수치 또는 텍스트 값과 항목별 판정

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK |  |
| `co_cd` | character varying(10) | N |  |  |  |
| `row_idx` | bigint | N |  | UQ |  |
| `item_cd` | character varying(30) | N |  | UQ | 한계항목 코드 — limit_item_kind 프리셋의 열 식별자 |
| `num_val` | numeric(14,3) | Y |  |  |  |
| `txt_val` | character varying(500) | Y |  |  |  |
| `judge_cd` | character varying(1) | Y |  |  |  |
| `ins_id` | character varying(20) | Y |  |  |  |
| `ins_dt` | timestamp without time zone | N | `now()` |  |  |
| `upd_id` | character varying(20) | Y |  |  |  |
| `upd_dt` | timestamp without time zone | Y |  |  |  |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_htg_monitor_cell`: `row_idx, item_cd`

## tbl_ccp_metal_monitor

**업무** — CCP 금속검출 모니터링 일지 헤더 — 표준기준서 관리번호 2-2

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `doc_idx` | bigint | N |  | UQ | 문서 idx — tbl_document.idx와 1:1 |
| `base_dt` | character varying(8) | N |  |  | 작성일 YYYYMMDD |
| `ccp_cd` | character varying(20) | N |  |  |  |
| `fe_size` | numeric(4,1) | Y |  |  | Fe 시편 규격(mm) — 작성 시점 한계기준 스냅샷. A4 한계기준란에 출력 |
| `sts_size` | numeric(4,1) | Y |  |  | STS 시편 규격(mm) — 작성 시점 한계기준 스냅샷 |
| `mng_user_id` | character varying(20) | Y |  |  | 담당자 로그인 ID |
| `mng_nm` | character varying(50) | Y |  |  | 담당자명 — 작성 시점 스냅샷 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_metal_monitor`: `doc_idx`

## tbl_ccp_metal_sens_row

**업무** — 금속검출기 감도 모니터링 행 — 시편 통과 시험 결과

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `hdr_idx` | bigint | N |  | UQ | 헤더 idx — tbl_ccp_metal_monitor.idx |
| `row_seq` | integer | N |  | UQ | 행 순번 |
| `phase_cd` | character varying(10) | N |  |  | 점검 시점 — BEFORE:작업 전, DURING:작업 중, AFTER:작업 후 |
| `product_cd` | character varying(30) | Y |  |  |  |
| `product_nm` | character varying(200) | Y |  |  | 품명 — 작성 시점 스냅샷 |
| `check_time` | character varying(10) | Y |  |  | 점검 시각 HH:MM — 다른 CCP 표와 같은 자리 폭(10) |
| `fe_only_cd` | character varying(1) | Y |  |  | Fe 시편만 통과 결과 — O:검출, X:불검출 |
| `sts_only_cd` | character varying(1) | Y |  |  | STS 시편만 통과 결과 — O:검출, X:불검출 |
| `prod_only_cd` | character varying(1) | Y |  |  | 제품만 통과 결과 — O:검출, X:불검출. 정상이면 X(미검출)여야 한다 |
| `fe_prod_cd` | character varying(1) | Y |  |  | Fe 시편 + 제품 통과 결과 — O:검출, X:불검출 |
| `sts_prod_cd` | character varying(1) | Y |  |  | STS 시편 + 제품 통과 결과 — O:검출, X:불검출 |
| `judge_cd` | character varying(1) | Y |  |  | 행 판정 — P:적합, F:부적합. 시편 시험 4종이 모두 검출(O)이면 적합 |
| `judge_mod_yn` | character varying(1) | N | `'N'::character varying` |  | 판정 수동변경 여부 Y/N |
| `checker_id` | character varying(20) | Y |  |  | 점검자 로그인 ID |
| `checker_nm` | character varying(50) | Y |  |  | 점검자명 — 서명란 표기용 스냅샷 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `place_nm` | character varying(100) | Y |  |  | 위치(비고) |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_metal_sens_row`: `hdr_idx, row_seq`
- 인덱스 `ix_tbl_ccp_metal_sens_row_hdr`: `hdr_idx`

## tbl_ccp_metal_pass_row

**업무** — 금속검출기 제품 통과 실적 — 통과량·검출량 기록

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `hdr_idx` | bigint | N |  | UQ | 헤더 idx — tbl_ccp_metal_monitor.idx |
| `row_seq` | integer | N |  | UQ | 행 순번 |
| `product_cd` | character varying(30) | Y |  |  |  |
| `product_nm` | character varying(200) | Y |  |  | 품명 — 작성 시점 스냅샷 |
| `pass_qty` | numeric(15,3) | Y |  |  | 통과량 — 금속검출기를 통과한 제품 수량 |
| `detect_qty` | numeric(15,3) | Y |  |  | 검출량 — 금속이 검출되어 배출된 수량. 0보다 클 때(= 이탈 발생) 개선조치 필수 |
| `unit_nm` | character varying(20) | Y |  |  | 단위 |
| `remark` | character varying(500) | Y |  |  | 특이사항 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |

- 기본키: `idx`
- 유일키 `ux_tbl_ccp_metal_pass_row`: `hdr_idx, row_seq`
- 인덱스 `ix_tbl_ccp_metal_pass_row_hdr`: `hdr_idx`

## tbl_corrective_action

**업무** — 이탈·개선조치 — 전 점검표 공통. 미완료 건은 대시보드에 노출되고 결재 상신을 막는다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `ca_no` | character varying(50) | N |  | UQ | 개선조치 번호 — 업체 내 유일 |
| `src_tmpl_cd` | character varying(40) | Y |  |  | 발생 출처 템플릿 코드 — 어느 점검표에서 나왔는지 |
| `src_doc_idx` | bigint | Y |  |  | 발생 출처 문서 idx — tbl_document.idx |
| `src_row_idx` | bigint | Y |  |  | 발생 출처 상세행 idx — 어느 행의 부적합인지 특정 |
| `occur_dt` | character varying(8) | N |  |  | 발생일자 YYYYMMDD |
| `occur_place` | character varying(200) | Y |  |  | 발생장소 — 작업장·보관고·설비명 |
| `deviation_desc` | text | N |  |  | 이탈내용 — 기준을 벗어난 사실 서술 |
| `action_desc` | text | Y |  |  | 개선조치 및 결과 — 조치 내용과 결과 서술 |
| `action_user_id` | character varying(20) | Y |  |  | 조치자 로그인 ID |
| `action_dt` | character varying(8) | Y |  |  | 조치일자 YYYYMMDD |
| `confirm_user_id` | character varying(20) | Y |  |  | 확인자 로그인 ID — A4 조치자 확인란 |
| `confirm_dt` | character varying(8) | Y |  |  | 확인일자 YYYYMMDD |
| `due_dt` | character varying(8) | Y |  |  | 조치 기한 YYYYMMDD — 경과 시 알림 |
| `status` | character varying(4) | N | `'OPEN'::character varying` |  | 상태 — OPEN:미조치, ING:조치중, DONE:완료 |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `action_user_nm` | character varying(50) | Y |  |  | 조치자 표시명 — A4 푸터 서명란 자유입력 |
| `confirm_user_nm` | character varying(50) | Y |  |  | 확인자 표시명 — A4 푸터 확인란 자유입력 |

- 기본키: `idx`
- 유일키 `ux_tbl_corrective_action`: `co_cd, ca_no`
- 인덱스 `ix_tbl_corrective_action_src`: `src_doc_idx`
- 인덱스 `ix_tbl_corrective_action_st`: `co_cd, status, due_dt`

## tbl_schedule_task

**업무** — 작성 과제 — 일 1회 배치 생성 + 로그인 시 온디맨드 보정. 오늘 할 일·누락 알림의 원천

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `tmpl_cd` | character varying(40) | N |  | UQ | 템플릿 코드 — tbl_template.tmpl_cd |
| `base_dt` | character varying(8) | N |  | UQ | 기준일자 YYYYMMDD — 작성해야 하는 대상 일자 |
| `due_dt` | character varying(8) | N |  |  | 마감일자 YYYYMMDD |
| `due_time` | character varying(4) | Y |  |  | 마감시각 HHMM |
| `status` | character varying(4) | N | `'TODO'::character varying` |  | 상태 — TODO:미작성, ING:작성중, APV:승인완료, LATE:기한경과 |
| `doc_idx` | bigint | Y |  |  | 연결 문서 idx — 작성 시작 시 tbl_document.idx로 채워진다 |
| `dept_cd` | character varying(20) | Y |  |  | 담당 부서코드 |
| `user_id` | character varying(20) | Y |  |  | 담당자 로그인 ID |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID — 배치 실행 주체 |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `alarm_dt` | timestamp without time zone | Y |  |  | 알림 시각 — 마감(due_dt+due_time) 에서 app.schedule.alarm-before-minutes 만큼 앞선 시점 |
| `alarm_send_yn` | character varying(1) | N | `'N'::character varying` |  | 알림 발송여부 Y/N — Y일 때(= 이미 보냄) 다시 보내지 않는다 |

- 기본키: `idx`
- 유일키 `ux_tbl_schedule_task`: `co_cd, tmpl_cd, base_dt`
- 인덱스 `ix_tbl_schedule_task_alarm`: `alarm_send_yn, alarm_dt`
- 인덱스 `ix_tbl_schedule_task_due`: `co_cd, due_dt, status`
- 인덱스 `ix_tbl_schedule_task_user`: `co_cd, user_id, status`

## tbl_workday_override

**업무** — 영업일 전환 — 행이 있으면 그 날(주말·공휴일)을 회사 영업일로 취급한다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY | Y |  |  | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `ymd` | character varying(8) | N |  |  | 대상일 YYYYMMDD — 주말·공휴일을 영업일로 바꾼 날 |
| `ins_id` | character varying(20) | Y |  |  | 전환 저장자 ID |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 전환 저장 시각 |


## tbl_login_log

**로그·통계** — 로그인 이력 — 성공·실패·잠금 전수 기록. 감사 대응 및 계정 도용 추적용

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | Y |  |  | 회사코드 — 실패 시 아이디가 존재하지 않으면 NULL이 될 수 있다 |
| `user_id` | character varying(20) | N |  |  | 시도한 로그인 ID — 존재하지 않는 아이디도 그대로 기록 |
| `sid` | character varying(36) | Y |  |  | 세션 UUID — 성공 시에만 발급. JWT sid 클레임 및 tbl_view_log.sid와 조인 |
| `login_dt` | timestamp without time zone | N | `now()` |  | 로그인 시도 일시 |
| `logout_dt` | timestamp without time zone | Y |  |  | 로그아웃 일시 — 명시적 로그아웃 또는 토큰 만료 시 갱신. NULL이면(= 미종료 세션) |
| `result_cd` | character varying(1) | N |  |  | 결과 — S:성공, F:실패(비밀번호 불일치·미존재·미사용), L:잠금(실패 임계 초과) |
| `fail_reason` | character varying(200) | Y |  |  | 실패 사유 — 서버 로그용 기술 문구. 사용자에게는 노출하지 않는다 |
| `ip_addr` | character varying(45) | Y |  |  | 접속 IP — IPv6 대비 45자 |
| `user_agent` | character varying(500) | Y |  |  | 브라우저 User-Agent 원문 |
| `device_gbn` | character varying(10) | Y |  |  | 기기구분 — PC / MOBILE / TABLET |
| `token_exp_dt` | timestamp without time zone | Y |  |  | 발급 토큰 만료 예정일시 |

- 기본키: `idx`
- 인덱스 `ix_tbl_login_log_co`: `co_cd, login_dt DESC`
- 인덱스 `ix_tbl_login_log_user`: `user_id, login_dt DESC`

## tbl_view_log

**로그·통계** — 화면 조회 원시 이벤트 — PV 1건 = 1행. 보존기간 경과분은 정리 배치로 삭제

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `user_id` | character varying(20) | N |  |  | 로그인 ID — UV 산출의 distinct 기준 |
| `sid` | character varying(36) | Y |  |  | 세션 UUID — tbl_login_log.sid와 조인. 세션수(sess_cnt) 산출 기준 |
| `scrn_cd` | character varying(30) | N |  |  | 화면코드 — tbl_screen.scrn_cd |
| `enter_dt` | timestamp without time zone | N |  |  | 화면 진입 일시 — 탭이 활성으로 전환된 시각 |
| `leave_dt` | timestamp without time zone | Y |  |  | 화면 이탈 일시 — 다른 탭으로 전환·탭 닫기·페이지 종료 시각. NULL이면(= 미종료 이벤트) |
| `stay_sec` | integer | Y |  |  | 체류 시간(초) — leave_dt - enter_dt. 미종료 이벤트는 NULL |
| `ref_scrn_cd` | character varying(30) | Y |  |  | 직전 화면코드 — 화면 간 이동 경로 분석용 |
| `ip_addr` | character varying(45) | Y |  |  | 접속 IP |
| `user_agent` | character varying(500) | Y |  |  | 브라우저 User-Agent 원문 |
| `ins_dt` | timestamp without time zone | N | `now()` |  | 서버 수집 일시 — 배치 전송이라 enter_dt와 차이가 날 수 있다 |

- 기본키: `idx`
- 인덱스 `ix_tbl_view_log_agg`: `co_cd, enter_dt, scrn_cd`
- 인덱스 `ix_tbl_view_log_sid`: `sid, enter_dt`

## tbl_view_stat_daily

**로그·통계** — 화면별 일자 집계 — UV/PV/세션수/평균체류. 일 1회 배치 upsert, 영구 보존

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  | UQ | 회사코드 — 테넌트 키 |
| `stat_dt` | character varying(8) | N |  | UQ | 집계일자 YYYYMMDD |
| `scrn_cd` | character varying(30) | N |  | UQ | 화면코드 — tbl_screen.scrn_cd |
| `pv_cnt` | integer | N | `0` |  | PV — 해당 일자·화면의 조회 이벤트 건수 |
| `uv_cnt` | integer | N | `0` |  | UV — 해당 일자·화면을 조회한 서로 다른 사용자 수(distinct user_id) |
| `sess_cnt` | integer | N | `0` |  | 세션수 — 서로 다른 sid 수. 같은 사용자가 여러 번 로그인하면 분리 집계 |
| `avg_stay_sec` | numeric(10,1) | Y |  |  | 평균 체류시간(초) — stay_sec이 있는 이벤트만 대상 |
| `max_stay_sec` | integer | Y |  |  | 최대 체류시간(초) |
| `ins_id` | character varying(20) | Y |  |  | 최초입력자 ID — 배치 실행 주체 |
| `ins_dt` | timestamp without time zone | Y | `now()` |  | 최초입력일시 |
| `upd_id` | character varying(20) | Y |  |  | 최종수정자 ID — 재집계 실행 주체 |
| `upd_dt` | timestamp without time zone | Y |  |  | 최종수정일시 |
| `ip_cnt` | integer | N | `0` |  | IP수 — 해당 일자·화면의 서로 다른 ip_addr 수 |

- 기본키: `idx`
- 유일키 `ux_tbl_view_stat_daily`: `co_cd, stat_dt, scrn_cd`
- 인덱스 `ix_tbl_view_stat_dt`: `co_cd, stat_dt DESC`

## tbl_audit_log

**로그·통계** — 변경 감사 로그 — HACCP 기록의 사후 수정 추적. 삭제하지 않는다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `user_id` | character varying(20) | N |  |  | 행위자 로그인 ID |
| `scrn_cd` | character varying(30) | N | `''::character varying` |  | 행위 화면코드 — tbl_screen.scrn_cd. 적재 시점에 남긴다. 조회는 이 값으로 메뉴 트리를 가른다 |
| `tbl_nm` | character varying(50) | N |  |  | 대상 테이블명 — tbl_ 접두 포함 |
| `tgt_idx` | bigint | Y |  |  | 대상 행의 idx |
| `action_cd` | character varying(20) | N |  |  | 행위 — I:등록, U:수정, D:삭제, REQ:상신, REV:검토, APV:승인, RJT:반려, CANCEL:상신취소, UNDO:결재취소 |
| `before_json` | jsonb | Y |  |  | 변경 전 값 JSON — 등록(I)일 때는 NULL |
| `after_json` | jsonb | Y |  |  | 변경 후 값 JSON — 삭제(D)일 때는 NULL |
| `reason` | character varying(500) | Y |  |  | 사유 — 결재 반려·결재취소 시 입력값 |
| `ip_addr` | character varying(45) | Y |  |  | 행위자 IP |
| `ins_dt` | timestamp without time zone | N | `now()` |  | 기록 일시 |

- 기본키: `idx`
- 인덱스 `ix_tbl_audit_log_tgt`: `co_cd, tbl_nm, tgt_idx, ins_dt DESC`
- 인덱스 `ix_tbl_audit_log_scrn`: `co_cd, scrn_cd, ins_dt DESC`

## tbl_notification

**로그·통계** — 알림함 — 수신 여부는 tbl_user_noti_pref를 따르고, 발송된 알림만 이 테이블에 쌓인다

| 컬럼 | 자료형 | 널 | 기본값 | 키 | 설명 |
|---|---|:-:|---|:-:|---|
| `idx` | bigint | N |  | PK | PK 자동 채번 대리키 |
| `co_cd` | character varying(10) | N |  |  | 회사코드 — 테넌트 키 |
| `noti_type_cd` | character varying(30) | N |  |  | 알림 유형 — tbl_user_noti_pref.noti_type_cd와 동일 체계 |
| `user_id` | character varying(20) | N |  |  | 수신자 로그인 ID |
| `title` | character varying(200) | N |  |  | 알림 제목 |
| `content` | character varying(500) | Y |  |  | 알림 본문 |
| `link_scrn_cd` | character varying(30) | Y |  |  | 바로가기 화면코드 — 클릭 시 열 탭 |
| `link_doc_idx` | bigint | Y |  |  | 바로가기 문서 idx — tbl_document.idx |
| `read_yn` | character varying(1) | N | `'N'::character varying` |  | 읽음여부 Y/N |
| `read_dt` | timestamp without time zone | Y |  |  | 읽은 일시 |
| `ins_dt` | timestamp without time zone | N | `now()` |  | 발송 일시 |

- 기본키: `idx`
- 인덱스 `ix_tbl_notification_user`: `co_cd, user_id, read_yn, ins_dt DESC`

## 관련

- SP → 표: [`9_SP_색인.md`](9_SP_색인.md)
- 이름·값의 모양: [`4_명명과_경로.md`](4_명명과_경로.md)
- DB 규칙: [`../db_sasshaccp/README.md`](../db_sasshaccp/README.md)
