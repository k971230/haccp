# 8. 결정 이력

> 개발자: 박승우 · 일자: 2026-08-26
> **왜 이렇게 돼 있는지**를 적는다. 지금 상태는 나머지 문서에 있다.

같은 논의를 두 번 하지 않으려고 남긴다. 결정을 뒤집을 수는 있지만,
**뒤집기 전에 왜 그렇게 정했는지는 읽어야 한다.**

---

## 화면을 28개로 줄였다 — 2026-08-25

**전:** 화면 100개 이상. 대부분 껍데기이거나 한 번도 안 쓰인 것.

**왜:** 쓰지 않는 화면이 메뉴·권한·문서·시험을 전부 무겁게 했다.
「이 화면 살아 있나」를 매번 확인해야 했다.

**한 것:** 실제로 쓰는 28화면만 남기고 나머지 화면·표·SP·메뉴를 지웠다.
`tbl_screen` 103 → 28, 표 71 → 53, 양식 73 → 48.

**대가:** 나중에 그 기능이 필요하면 다시 만들어야 한다.
git 이력에는 남아 있다.

---

## 업무 SQL 을 전부 SP 에 둔다

**왜:** 화면이 늘어도 서버 코드는 「SP 를 부르고 결과를 담아 넘기는」 모양이 안 바뀐다.
SQL 이 자바와 XML 에 흩어지면 같은 조건이 여러 곳에 복제된다.

**규칙:** 매퍼 XML 에 직접 `SELECT` 를 적지 않는다.

**실제로 어겼을 때:** 삭제차단 SQL 이 매퍼 5곳에 복제돼 있었다.
상태 코드가 하나 늘면 여섯 곳을 고쳐야 했다 — 2026-08-26 에 공용 SP 로 내렸다.

---

## SP 를 `CREATE OR REPLACE` 로 쓴다 — 2026-08-26

**왜:** `CREATE PROCEDURE` 로 쓰면 두 번째 적용부터 실패한다.
`apply-all.sh` 는 「몇 번을 돌려도 결과가 같다」고 주장하는데 실제로는 아니었다.
운영에서 SP 하나 고치려면 손으로 DROP 해야 했다(E2E-002).

---

## 양식코드를 SP 안에 나열하지 않는다 — 2026-08-26

**왜:** 정규식 화이트리스트(`^hwp_sys_0(0[1-9]|1[0-9]|2[0-7])$`)가 SP 두 곳에 있었다.
`hwp_sys_028` 부터는 등록해도 목록에 안 떴다 — **열한 종이 조용히 숨어 있었다.**

**대신:** `tbl_template.doc_kind` 처럼 값으로 판단한다.

**뒷이야기 (2026-08-31):** 숨어 있던 그 열한 종은 결국 **지웠다.**
목록에 나오게 만들어 놓고 보니 **볼륨에 실물 `.hwp` 가 없어** 골라도 안 열렸다 —
등록만 되고 파일은 한 번도 올라간 적이 없는 껍데기였다(문서 0건).
지금 시스템 제공 HWP 는 `hwp_sys_001`~`027` 27종이다.
**교훈은 그대로다** — 목록에서 숨기는 것과 지우는 것은 다른 결정이고,
숨겨 두면 그게 살아 있는 줄 알게 된다.

---

## 검토(REVIEW) 단계를 기본으로 끈다 — **폐기, 아래 「2단」이 정본**

옛 결정: 검토를 `use_yn=N` 으로 남겨 켜 둘 수 있게 한다.
**2026-09-03 에 검토 단계를 지웠다.** 아래 「결재는 기안·결재 2단이다」가 정본이다.

---

## 목록 배지는 3단계다 — 저장값은 네 코드

DB 는 `WRK`·`REQ`·`APV`·`RJT` 네 코드다 (`REV` 는 시드에서 지웠다).
화면 배지는 전송대기(`WRK`·`RJT`)·전송(`REQ`)·결재완료(`APV`) 셋이다.
반려는 배지 대신 행 색(`mes-row-rejected`)이다. 전송대기로 다시 고칠 수 있다.

---

## 공통코드 `main_cd`·`sub_cd` 를 UPPER_SNAKE 로 — 2026-08-25

**왜:** 표기가 섞여 있어 콤보가 비는 일이 잦았다.

**핵심:** `sub_cd` 는 **업무 표에 저장되는 값과 같은 표기**다.
`tbl_template.doc_kind = 'HTML'` 이면 `sub_cd` 도 `HTML` 이다.
코드만 바꾸고 데이터를 안 바꾸면 라벨이 안 붙는다 — 항상 같이 옮긴다.

**지켜지지 않은 칸 하나:** `tbl_schedule_rule.nonwork_rule` 은 저장이 소문자다 —
저장 SP(`01_sp.sql` 의 `sp_schedule_cycle_management_c_000`)가 `lower()` 로 접는데
공통코드 `sub_cd` 는 `KEEP|PREV|NEXT` 대문자다. FE·Java 가 비교 전에 대문자로 올려서 붙는다.
어느 쪽도 혼자 고치지 않는다 (`.cursor/rules/07-haccp-db.mdc`).

**대가:** 저장된 데이터까지 마이그레이션해야 했다(`04_migrate_code_upper.sql`).

---

## 신규 업체를 0000 복제로 연다 — 2026-08-26

**전:** 신규 업체 개설 경로가 아예 없었다. 메뉴·권한·부서·관리자가
0000 전용 덤프에만 있어 다른 회사는 로그인해도 빈 화면이었다.

**왜 복제인가:** 메뉴 43줄·화면권한 84줄을 `06_company_seed.sql` 에 손으로 적으면
화면이 하나 늘 때마다 두 곳을 고쳐야 한다. 0000 을 원본으로 삼으면
`02_seed.sql` 한 곳만 정본이 된다.

**안 주는 것:** `usr`(그 업체가 직접 만든 양식)은 복제하지 않는다. 남의 것이다.

**조회는 복제가 아니다 — 2026-09-07.** 개설 시드가 `:src_co`(기본 0000)에서 베끼는 것과
런타임 `co_cd = p_co_cd` 는 다른 결정이다. 공통코드·양식 조회에 0000 폴백이 없다.
`tbl_code.co_cd = 0000` 은 플랫폼·데모 업체 행이지 전 업체 공용 카탈로그가 아니다.

---

## DB 를 배포 파이프라인에 넣지 않는다

**왜:** 스키마 변경은 되돌리기 어렵고, 배포와 같은 트랜잭션으로 묶을 수 없다.
실패하면 반쪽 상태가 남는다.

**대신:** 배포 담당이 `bash db_sasshaccp/apply-all.sh` 를 따로 돌린다.

당시 "6본 모두 재실행 안전하게 만들어 두었다" 고 적었으나 `00_ddl` 은 그대로다.
2026-09-07 에 `apply-all.sh` 가 스키마가 있으면 `00_ddl`·`02_seed` 를 건너뛰게 바꿨다.
`00_ddl` 53표 IF NOT EXISTS 개작은 하지 않는다. 아래 「apply-all 은 스키마가 있으면」이 정본이다.

---

## E2E 가 DB 를 직접 읽는다 — 2026-08-25

**왜:** 화면이 「저장했습니다」를 띄우고 서버가 실패를 삼킨 사고가 있었다.
결재 전 경로가 500 인데 스모크·단위 테스트가 전부 통과했다(E2E-001).

**대가:** 로컬 도구(`tools/`, git 미포함)가 필요하고 CI 에서는 그 시험이 skip 된다.
**빈 값을 돌려 조용히 통과시키지 않는다** — skip 이 낫다.

---

## 감사자료 내보내기 API 를 지운다 — 2026-09-03

`/api/v1/docs/audit-export/**` 는 화면이 없고 프론트도 안 불렀다.
예전에 「규제 대비로 경로만 남긴다」고 적혀 있었으나, 부르는 곳이 없어 입구·SP·미리보기까지 지웠다.
`tbl_audit_log`(감사로그 화면)와는 다른 기능이었다.

---

## 문서관계 표는 지운다 — 2026-09-03

화면이 안 불렀다. `tbl_document_relation` · SP 2본 · relations API 를 지웠다.

---

## 문서를 25본에서 8본으로 — 2026-08-26

**왜:** 문서가 늘면 아무도 안 고치고, 안 고친 문서는 거짓말을 한다.
실제로 25본 중 다수가 삭제된 화면·표를 가리키고 있었다.

**버린 것:** FE/BE 로 쪼개져 겹치던 것, `.cursor/rules` 로 옮겨간 규칙,
지난달 스냅샷(완성도 판정·후속 STEP), 코드가 정본인 것(파일 트리·스모크 매트릭스).

**남긴 원칙:** 표에 화면·파일 이름을 적을 거면 **실물에서 뽑는다.**
`3_화면_지도.md` 와 `5_PIPELINE_색인.md` 는 스크립트로 생성한다.

---

## 이모지를 쓰지 않는다

터미널·diff·검색에서 깨지고, 뜻이 사람마다 다르다.
소스 주석·규칙·커밋·UI 문구·문서 전부 해당한다.

---

## 화면 API 는 화면 자기 경로에 둔다 — 2026-08-26

**왜:** 한 컨트롤러가 base 가 다른 두 갈래를 담고 있으면, 형제 화면을 보고
`@GetMapping("/list")` 를 적었을 때 엉뚱한 URL 이 된다.

- `UserController` → 화면(`/sys/code/user-management`)과 서명(`/sys/users`)으로 **갈랐다**
- 개선조치관리 API 를 `com.haccp.tsk` 에서 `com.haccp.flow.ca` 로 **옮겼다**.
  URL 이 `/flow/ca/...` 인데 패키지가 `tsk` 여서 「URL = 폴더 = 패키지」가 깨져 있었다

**남은 예외:** 여러 화면이 함께 쓰는 축은 화면 경로가 아니다 —
문서 허브(`docs/documents`) · 양식 허브(`docs/templates`) · 서명(`sys/users`).
그건 결함이 아니라 설계다. README 에 왜인지 적었다.

---

## 없는 경로는 404 다 — 2026-08-26

`NoResourceFoundException` 이 `@ExceptionHandler(Exception.class)` 로 떨어져
**500 UNKNOWN + 스택트레이스**가 나가고 있었다.

잘못된 주소는 서버 결함이 아니다. 404 로 내리고 스택트레이스를 남기지 않는다.

**이 결함이 다른 결함을 가렸다.** HWP 양식 삭제가 죽은 URL 을 부르고 있었는데
500 이라 「서버 오류」로 보였다. 404 였으면 훨씬 빨리 드러났다.

---

## 시험은 부정만 보지 않는다 — 2026-08-26

「시스템 양식은 지워지지 않는다」만 보는 시험은 **삭제 기능이 통째로 죽어도 통과한다.**
실제로 그랬다 — 삭제 API 가 없어 500 이 났는데 「안 지워졌다」가 참이 됐다.

원인이 하나 더 있었다. 삭제 확인창의 긍정 버튼 글자가 **「확인」이 아니라 「삭제」**라
`btn(page, "확인")` 이 아무것도 못 찾고 조용히 지나갔다.

**규칙:** 막는 것을 볼 때는 **되는 것도 같이 본다.**
그리고 확인창을 실제로 눌렀는지 응답 코드로 확인한다.

---

## 생성 문서에는 생성기를 둔다 — 2026-08-26

`3_화면_지도.md` 와 `5_PIPELINE_색인.md` 는 「손으로 고치지 않는다」고 적혀 있었지만
**뽑는 스크립트가 없었다.** 그래서 색인은 태그 하나에 파일 하나만 싣고 있었고
(태그는 1:N 인데 표는 1:1), 새로 넣은 `mesSec.ts` 가 색인에서 빠져 있었다.

```sh
node scripts/gen_screen_map.mjs         # 화면 지도
node scripts/gen_pipeline_index.mjs     # PIPELINE 색인
    --check                             # 어긋나면 실패 (CI)
```

Node 만 있으면 된다 — FE 빌드에 이미 필요하고, DB 접속이 없어도 돈다
(메뉴 이름은 `02_seed.sql`, 화면 컴포넌트는 `screenRegistry.tsx` 에서 읽는다).

---

## 문서 검수는 md 만 고친다 — 2026-09-03

**전:** 같은 날 두 세션이 갈렸다. 한쪽은 「검수는 md 만, 코드는 보고만」을 메모리에
고정했고, 다른쪽은 검수 순서를 승인·진행하다가 코드까지 고쳤다. 작업 트리에
검수 문서와 기능 변경이 섞였다.

**왜 md 만인가:** 검수는 「문서가 실물을 맞추는가」를 가리는 일이다. 코드를 같이
고치면 (1) 검수가 자기가 바꾼 코드를 다시 근거로 삼고 (2) 커밋이 갈라지지 않으며
(3) 생성기(`docs/5`·`docs/9`)가 아직 안 올라간 소스에 맞춰져 CI 가 깨진다.

**규칙:**

| 단계 | 고치나 |
|---|---|
| `/doc-audit` · Critic · Defender · Supervisor | **문서만.** 코드는 읽고 지적만 |
| 코드 패치 | 검수 보고를 본 사용자가 **따로** 지시할 때. 다른 커밋 |

서브는 `Read`·`Grep`·`Glob` 만 쓴다. 생성기를 돌리거나 소스를 쓰는 손은 이 루프에 없다.
절차 정본: `.claude/commands/doc-audit.md`.

---

## 오늘 할 일을 board 아래로 두고 캘린더를 붙인다 — 2026-09-03

**전:** `today-tasks` 가 평탄 leaf 였다. 회사 전체 일정을 한눈에 보는 화면이 없었다.

**왜:** 담당·밀림·완료를 월 칸에서 보고, 주말·공휴일을 영업일로 바꿀 일이 생겼다.
비밀번호 변경도 푸터에서 본인만 하게 했다.

**한 것:** URL·패키지·매퍼를 `/board/{scrnCd}` 로 맞춘다. `scrnCd`·`persistId` 는 그대로.
캘린더 더블클릭은 오늘 할 일 담당 규칙(없음·본인·mine). 완료는 문서가 있을 때만 조회.
최근 문서는 `writer_id` = JWT.

**대가:** 라이브·로컬은 `01_sp.sql` 의 해당 SP 만 `CREATE OR REPLACE`. `apply-all` 은 스키마가 있으면 `00_ddl` 을 건너뛴다. 반환 타입이 바뀐 SP 는 DROP 후 CREATE.

---

## HTML 양식코드는 `html_` — 2026-09-03

**전:** CCP HTML 4계열(검증점검·포장·가열·금속검출)이 `tml_ccp_*` 로 채번돼 있었다.
표·SP 도 `tbl_tml_ccp_*` · `sp_tbl_tml_ccp_*`.

**왜:** 공정점검은 이미 `html_hyg_prc_*` 다. 같은 HTML 양식인데 접두만 다르면
채번·검색·문서가 갈린다. `tml_` 은 템플릿 약자였는데 HTML 종류를 안 말한다.

**한 것:** 행은 지우지 않는다. `tml_ccp_*` → `html_ccp_*` 로 UPDATE/RENAME.
운영·시험에 적용한 뒤 1회용 스크립트는 지웠다. 빈 DB 는 00+01+02 가 처음부터 `html_`.

**안 바꿈:** `html_sys_*` · `html_hyg_prc_*` · `hwp_sys_*` · `scrnCd` · `persistId` · 이미 발급된 `doc_no`.

**대가:** 옛 접두로 남은 백업을 복구하면 정본과 안 맞는다. 다시 옮기려면 git 이력의 `08_rename_tml_html.sql` 을 본다.

---

## HTML 사용양식은 지면 버전이 있어야 한다 — 2026-09-03

**전:** `02_seed.sql` 이 회사 카탈로그(`tbl_template` · `tbl_company_template` · `tbl_doc_no_rule`)만
넣고 지면 버전(`tbl_html_*_ver`)은 빼 두었다. 문서주기는 카탈로그를 읽고, 양식관리는 버전을 읽는다.

**왜:** 주기 목록에만 뜨고 양식관리에는 없어 화면에서 지울 수도 없다.
버전 행을 만들어 붙이면 없는 지면이 작성 화면에 나온다. 이 프로그램은 그렇게 동작하지 않는다.

**한 것:** 문서 없는 고아 카탈로그를 시험 DB 에서 지운다. 시드 INSERT 도 뺀다.
주기 목록 SP 는 HTML 에 사용 중인 버전이 있을 때만 보여 준다. HWP 는 그대로.
복사 원본 `*_000` · `html_sys_001` · `html_sys_006` 은 남긴다.

**회사 지면은 `07_company_forms.sql` 이 표준에서 복사한다.** 시드에 회사 HTML 코드를 미리 넣지 않는다.

---

## 결재는 기안·결재 2단이다 — 2026-09-03

**전:** 결재선이 작성·검토·승인 3단이었다. 검토는 기본 `use_yn=N` 으로 꺼 두었는데,
코드·공통코드·SP·화면에 `REVIEW`/`REV` 가 남아 있었다.

**왜:** 현장은 검토를 쓰지 않는다. 꺼 둔 단계가 스냅샷에 섞이면 결재가 멈춘다.
결재첨부 화면은 HWP 본문(`HWP_SRC`)을 첨부 목록에 그려 「0개」 밑에 본문 파일이 보였다.

**한 것:** 검토 단계와 `DOC_STATUS.REV`·`APPR_ROLE.REVIEW`·`APPR_ACTION.REVIEW` 를 시드·SP·화면에서 지웠다.
기본 결재선은 `WRITE` → `APPROVE`. 「승인」은 항상 `APPROVE`.
결재첨부는 원본 파일(다운로드만)과 첨부 파일(추가·삭제)을 나눈다.
운영·시험 DB 에 적용한 뒤 `08_remove_review.sql` 은 지웠다. 정본은 `00_ddl`·`01_sp`·시드다.

**대가:** 검토를 다시 넣으려면 결재선·전이 SP·화면을 새로 짠다.

---

## 결재 4화면 이름·잔재를 맞춘다 — 2026-09-03

**전:** 화면코드는 `sign-ready`/`sign-ok` 인데 API 는 `/approval-inbox`/`/approval-history`,
SP 는 `sp_tbl_document_appr_inbox_r_000`/`_hist_r_000` 이었다.
`tbl_document.reviewer_id`·`review_dt` 와 status DEFAULT `TMP` 가 남아 있었다.
전송 버튼은 결재첨부가 툴바와 따로 구현돼 확인 문구가 갈렸다.

**왜:** 같은 흐름을 세 가지 이름으로 부르면 고칠 때 빠뜨린다. 검토 컬럼은 읽히지 않는데 상세 SP 가 계속 SELECT 했다.

**한 것:**
- API `GET /sign-ready` · `GET /sign-ok`, SP `sp_sign_ready_r_000` · `sp_sign_ok_r_000`
- reviewer 컬럼 삭제, status DEFAULT `WRK`
- 꺼 둔 `REVIEW` 결재선 단계를 지우고 승인 순번을 2 로 당김
- 전송/전송취소를 `DocumentApprovalToolbar` 로 일원화. 섹션 제목·사유·첨부 카드·스테퍼 색을 공용 컴포넌트로 모음
- 운영·시험 DB 에 적용한 뒤 `09_rename_appr.sql` 은 지웠다. 정본은 `00_ddl`·`01_sp` 다.

**안 바꾼 것:** `scrnCd`·그리드 `persistId`(`doc-approval-inbox` 등). 열 너비가 날아간다.

---

## JSON SP 는 넓은 화면 1개 CUD 만 — 2026-09-07

**전:** HACCP 시정조치가 jsonb payload 를 쓴 뒤, 같은 모양이 복제되기 쉬웠다.
규약이 없었다.

**왜:** 칸이 많은 화면은 인자 목록이 길어진다. 그렇다고 전 SP 를 `(p_co_cd, p_user_id, p_json)` 로
통일하면 계약이 문자열이 된다. 오타·타입은 런타임 SP 파싱에서만 터진다.

**한 것:** 허용 기준을 여기 못 박는다.

- **허용:** 화면 1개 전용 CUD 이고 컬럼이 많아 인자 목록이 깨질 때 jsonb 1개
- **금지:** 기준정보(코드·사용자·부서·메뉴·권한) typed 인자를 jsonb 로 바꾸기
- **금지:** 전 화면 통일. 새 화면은 기본 typed

이미 jsonb 인 것(`sp_tbl_hyg_process_c_000` · `sp_ccp_verify_c_000` · 시정조치)은 유지한다.

---

## HTML 출력만 살균한다 — 2026-09-07

**전:** 전역 `<>` strip·Lucy 필터를 넣을지 논의했다.

**왜:** `<10℃` 와 양식 원문을 파괴한다. XSS 는 출력 문제다.

**한 것:** 전역 strip 금지. 프로덕션 `dangerouslySetInnerHTML` 0건을 유지한다.
HTML 을 **렌더하는 한 지점**이 생기면 그 싱크에만 DOMPurify 를 붙인다.
미리보기·내보내기가 생기기 전에는 의존성을 넣지 않는다.

---

## 변경 감사는 fail-closed, 조회로그는 fail-open — 2026-09-07

**왜:** 규제 문서의 변경 이력이 비면 저장만 되고 감사가 빈다.
조회/통계 로그 락이 본업을 막으면 현장이 멈춘다.

**한 것:**
- 변경 감사(`AuditWriter`)는 같은 TX. 저장이 롤백되면 이력도 같이 사라진다
- 조회로그(`useViewLog`)는 실패를 삼킨다

감사도 삼키라는 처방은 기각한다.

---

## SP 이름 세 번째 갈래 금지 — 2026-09-07

**정본:** 테이블 공유 `sp_tbl_*_{r|c|d|u}_000` · 화면 전용 `sp_{화면명}_{r|c|d|u}_000`.

**금지:** 화면이 아닌 `sp_foo_save` 같은 세 번째 갈래. 156본 일괄 개명은 하지 않는다.
지금 일탈은 `sp_hwp_template_management_ensure_default_000` 한 본 — 손댈 때 고친다.
`scripts/gen_sp_index.mjs --check` 가 새 일탈을 실패로 낸다.

---

## 셸 조회는 직행, Pref save 만 Service — 2026-09-07

**왜:** 조회 한 줄에 빈 Service 를 두면 칸만 맞고 경계는 안 생긴다.
`08-haccp-backend.mdc` 도 셸 API 직결을 이미 연다. Menu list 가 그 정답이다.

**한 것:** `PrefController.save` 의 `@Transactional` 을 `PrefService.save` 로 옮겼다.
list 는 Mapper 직행 유지. MenuService 같은 빈 클래스는 만들지 않는다.

---

## HTML 양식 다섯 표는 포트, 화면별 Service 금지 — 2026-09-07

**왜:** `HtmlTemplateService` 의 가족 if 가 여덟 메서드에 복붙돼 있었다.
화면별 Service 다섯 개로 쪼개면 검증(표준 수정 금지·화면 일치·Double Check)이 다섯 번 복사된다.

**한 것:** `HtmlFormFamilyStore` + 다섯 매퍼 어댑터. `storeFor(tmpl)` 한 번.
Service 검증은 한 곳. 항목 JSON 본문은 Map 유지.

---

## apply-all 은 스키마가 있으면 00_ddl 을 건너뛴다 — 2026-09-07

**전:** `apply-all.sh` 는 빈 DB 전용이라고 스스로 적었다. 다시 돌리면 `00_ddl` 이 42P06 으로 죽었다.

**하지 않은 것:** `00_ddl` 표를 `IF NOT EXISTS` 로 개작. Flyway.

**한 것:** 스키마 `sasshaccp` 가 있으면 `00_ddl` 을 건너뛰고 `01_sp` 부터 돌린다.
`02_seed` 는 화면 시드가 있으면 건너뛴다(ON CONFLICT 0건). `03`·`05` 는 이미 upsert,
`06`·`07` 은 이미 재실행 안전.

---

## 포장·가열은 각자 표다 — 2026-09-07

**전:** 포장·가열이 `tbl_ccp_generic_monitor` 한 벌을 접두로 갈라 썼다.

**왜:** 한계·셀 모양이 다른데 한 표에 두면 SP·매퍼가 `tmpl_cd` 접두로 분기한다.
한쪽을 고치면 다른 쪽이 조용히 깨진다.

**한 것:** `tbl_ccp_pkg_*` · `tbl_ccp_htg_*` 와 각자 SP. generic 표는 없다.
금속검출은 원래 별 표라 그대로 둔다.
URL·`scrnCd`·지면 `cells` 키는 안 바꿨다 — 화면이 같은 경로로 같은 칸을 보낸다.

---

## 검증 참조집계 8칸을 지운다 — 2026-09-07

**전:** `tbl_ccp_verify_item` 에 공정·집계 7칸, `tbl_ccp_verify_check` 에 `monitor_chk_rmk`.
화면이 안 쓰고 값도 0이었다.

**한 것:** 값이 0일 때만 DROP. 정본은 `00_ddl` — 빈 DB 에도 이 칸이 없다.

---

## 1회성 10·11·12 는 적용한 뒤 지운다 — 2026-09-07

**왜:** 마이그레이션 본이 남으면 apply-all 이 그걸 다시 찾고, 정본 7본과 갈라진다.

**한 것:** 시험·운영에 돌린 뒤 파일을 지웠다. `apply-all.sh` 는 7본만 돈다.

---

## 운영·정책으로 두고 고치지 않기로 한 것 — 2026-09-08

**왜:** 아래 6건은 코드 결함이 아니라 정책·운영 판단이다. 재논의하려면 이 절부터 읽는다.

| 항목 | 왜 두나 | 근거 |
|---|---|---|
| 운영 HTTP + 기본 비밀번호 `1234` | 운영 환경·시크릿 정책. 코드로 막을 자리가 아니다 | 배포 env · `04.배포.md` · `docs/7_보안과_파일.md` · `UserService.DEFAULT_NEW_USER_PW` |
| 동시 저장 | `seenUpdDt` 충돌과 HWP `hwp/save` 경합은 알고 둔다 | `DocumentService` 저장 · HTML/CCP `seenUpdDt` |
| 자기승인 | 결재선 작성자=승인자 허용. 소규모 사업장 흐름 | `sp_tbl_document_approval_c_000` APPROVE |
| 서명 GET 화이트리스트 | 서명 이미지는 로그인 사용자가 연다. 공개 URL 이 아니다 | `UserController` 서명 조회 · `docs/7_보안과_파일.md` |
| `lockYn` stale 해제 | 잠금 플래그가 남아 화면이 잠긴 것처럼 보이는 잔여. 운영으로 푼다 | 작성 화면 `lockYn` |
| JWT ADMIN 8시간 | 관리자 토큰 TTL. 정책 값이다 | JWT 설정 · `application.yml` `JWT_EXPIRE_MINUTES` |

**한 것:** 재발할 때마다 다시 논의하지 않도록 한 표로 남긴다.

---

## 외래키는 스냅샷·앱상수만 빼고 건다 — 2026-09-10

**전:** 2026-09-09 에 「외래키를 안 쓴다」고 적었다. 표 56본에 CONSTRAINT 가 없었다.
이유는 맞다 — `23503` 은 무엇이 참조인지 사용자 말로 못 알려 주고,
`validate-delete` 가 먼저 막아야 한다.

**왜 뒤집나:** 못 거는 이유가 스냅샷·앱상수가 아니라 시드 순서·센티널·순환·빠진 `co_cd` 였다.
그건 설계를 고칠 자리이지, CONSTRAINT 를 포기할 자리가 아니다.

**안 거는 칸 (이 둘만):**
- 스냅샷 — 결재·작성 ID, 로그 `user_id`/`tgt_idx`, 개선조치 `src_doc_idx`(DONE 원문이 사라져도 조치 기록은 남는다)
- 앱상수 — CCP 셀 `item_cd` (`temp`/`min`/`sec`/`time`). 부모 표를 만들지 않는다

**한 것:** 나머지는 부모→자식 일방향 CONSTRAINT. `co_cd` 는 `tbl_company`.
`00_alter` 가 고아를 지운 뒤 건다. 시드는 회사 먼저(`02_seed` 맨 위, 새 업체는 `06` → `03` → `05`).
부모 삭제는 그대로 `validate-delete` 가 먼저 알리고, FK `23503` 은
이미 있는 문구 `"다른 자료가 참조 중입니다. 참조를 먼저 정리하세요."` 가 받는다.
목록은 [`10_테이블_레이아웃.md`](10_테이블_레이아웃.md) FK 분석.

**남긴 것:** 삭제 차단 SP · `assertDeletable` Double Check. FK 는 차단 SP 를 빠뜨렸을 때의 마지막 장치다.

---

## purgeCompany 는 E2E 전용이다 — 2026-09-10

`frontend/haccp-web/e2e/helpers.ts` `purgeCompany` 는 `assertTestDb` 가 시험 DB 만 허용한다.

## 테넌트 트리 역순 삭제 SP — 2026-09-11

요청이 와서 `sp_tbl_company_purge_d_000` 을 만들었다. 화면은 없다.
`POST /api/v1/sys/company/validate-delete` → `POST .../delete`, body `[{ coCd }]`.
플랫폼(`0000`) `ADMIN`·`HACCP_MASTER` 만. `0000`·로그인 회사는 거절.
E2E `purgeCompany` 는 같은 SP 를 시험 DB 에서 직접 부른다.

---

## 앱 계층 가드 — 2026-09-10

1. **초안 삭제**는 작성자 또는 같은 회사 `ADMIN`·`HACCP_MASTER`. 저장은 이미 `writer_id` 가드. 퇴사자 초안이 좀비가 되지 않게 관리자만 우회한다. `isAdmin()` 은 `ADMIN` 만이라 SP 우회에 `HACCP_MASTER` 를 같이 둔다.
2. **시계**는 UTC 가 아니라 서울 락다운. JVM `-Duser.timezone=Asia/Seoul`, JDBC `options=-c TimeZone=Asia/Seoul`, `ALTER DATABASE sasshaccp SET timezone`. `ins_dt`/`write_dt`/`approve_dt` 는 KST 벽시계, 감사는 서버 `now()`.
3. **업로드 `fileKind=PDF` 금지.** 변환 API 만 PDF 를 등록한다.
4. **결재첨부**는 문서함과 같은 `useLatestOnly`. 툴바는 안에서 `useAsyncAction` `approval` 잠금.
5. **`assert_seen`** — 문서가 있는데 스탬프가 비면 거절. 빈 값 skip 은 경합을 못 본다.
6. **조회 감사 화이트리스트.** `audit_sp_cocd.mjs` 쓰기는 예외 없다. SELECT 는 마스터·플랫폼·배치 전사만 통과. `idx`·`file_idx`·`doc_no`·`tmpl_cd` 단건 누수를 잡는다.
7. **문서함 GET 은 회사 공유.** 목록·상세·다운로드는 허브 읽기 + 테넌트. 첨부 C/D 는 문서 헤더 `writer_id`(또는 `ADMIN`·`HACCP_MASTER`). 파일 `ins_id` 는 지우기 권한이 아니다.

## 일부러 안 한 것 — 2026-09-08

고치지 않은 **이유**가 있어서 남긴다. 다음에 손댈 때는 한 건씩, 요지를 그대로 따른다.
새 훅·새 권한 모델을 먼저 만들지 말 것.

1. **권한그룹 조회 하나 = 쓰기·수정·삭제·인쇄 다섯.** `RoleMgmtService` 가 `readYn` 만 보고 나머지 4칸을 같은 값으로 채운다. SP·DTO 는 이미 5칸이다. FE 5체크 + 서버가 그대로 넘기면 된다. 기존 DB 행(YYYYY) 마이그레이션은 그때 정한다.
2. **문서 허브 OR 권한.** `/api/v1/docs/documents/**` 는 묶음 화면 중 하나라도 해당 칸이 Y 면 통과한다. 문서마다 AND 로 묶으면 결재자 흐름이 막혀 과설계로 판단했다. 손댈 때는 허브 쓰기만 호출 화면(`X-Haccp-Screen`) 칸을 보고, 조회는 OR, CUD 만 헤더 화면 AND — 이 절충도 그때 묻는다.

---

## 관련

- 지금 상태: [`README.md`](../README.md) 의 「어디를 보나」
- 규칙 정본: `.cursor/rules/`
