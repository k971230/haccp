# 22 — 코드 kebab-case · 양식 매핑 정본 (47종)

개발자: 박승우  
일자: 2026-08-12

## 전역 규약

**코드성 값은 전부 kebab-case(소문자·하이픈)다.**

| 종류 | 정본 예 |
|------|---------|
| 양식 `tmpl_cd` | `tmpl_prp-hygiene-daily` |
| 화면 `scrn_cd` | `schedule-cycle-management` |
| 공통코드 main | `sys-yn`, `tmpl-ty`, `use-yn`, `nonwork-rule`, `src-ty` |
| 시스템유무 | `sys` / `usr` |
| 양식출처 | `sys` 기본양식 / `usr` 사용자양식 (`src-ty`, 파일 이력) |
| 양식 타입 | `html` / `hwp` |
| 사용여부 | `y` / `n` |

## 중분류 (양식)

`ccp` · `prp` · `logis` · `admin`

## 메뉴 대·중·소 (사이드바 IA)

적용: `db_sasshaccp/52_migrate_menu_tier3_kebab.sql` · FE `SideMenu` 3단.

**열림 규칙:** `scrn_cd`가 있고 FE `SCREEN_REGISTRY`에 있으면 클릭 가능. 없으면 폴더(아코디언)만.

| 단 | 규칙 | 예 |
|----|------|-----|
| 대 | `h_menu_cd` NULL, 보통 `scrn_cd` NULL | `menu-doc-write` |
| 중 | 대 아래, 보통 `scrn_cd` NULL | `menu-write-ccp` |
| 소 | 중 아래, `scrn_cd` 필수, `menu_cd` = `menu-{scrn_cd}` | `menu-ccp-heat-monitor` |

### 대분류

| sort | menu_cd | 표시명 | scrn |
|------|---------|--------|------|
| 10 | `today-tasks` | 오늘 할 일 | `today-tasks` (최상위 leaf) |
| 100 | `menu-doc-write` | 문서 작성 | — |
| 200 | `menu-doc-flow` | 문서 현황·결재 | — |
| 300 | `menu-doc-master` | 문서 기준관리 | — |
| 400 | `menu-base` | 기초정보 | — |
| 900 | `menu-sys` | 시스템 | — |

### 문서 작성 (`menu-doc-write`)

| 중 menu_cd | 표시 | 소 scrn_cd (요약) |
|------------|------|-------------------|
| `menu-write-ccp` | CCP(공정) | `ccp-*-monitor`, `ccp-verification-check`, `process-hwp` |
| `menu-write-prp` | PRP(위생·설비) | `daily-hygiene-check`, `pest-control-check`, `health-cert-record`, 위생·검교정·검증·성적서 HWP, `equipment-history` 등 |
| `menu-write-logis` | 물류 | inventory/receive/shipment/vehicle/submat HWP |
| `menu-write-admin` | 운영·법정 | edu/claim/handover/visitor/recall/eval/bad-product |

### 문서 현황·결재 (`menu-doc-flow`)

| 중 | 소 scrn |
|----|---------|
| `menu-flow-appr` | `approval-inbox`, `approval-history` |
| `menu-flow-box` | `document-inbox`, `legal-document-upload` |
| `menu-flow-ca` | `corrective-action-management` |

### 문서 기준관리 (`menu-doc-master`)

| 중 | 역할 | 소 scrn |
|----|------|---------|
| `menu-master-doc` | 작성 문서·주기 | `schedule-cycle-management` |
| `menu-master-form` | HWP·양식 원본 | `hwp-template-management` |
| `menu-master-item` | 점검항목/한계 | `daily-hyg-item-admin`, `ccp-*-limit-admin`, `ccp-verify-standard-admin`, `facility-check-item-admin` 등 |
| `menu-master-appr` | 결재선 | `approval-line-management` |

### 기초정보 / 시스템

| 대 | 중 | 소 (요약) |
|----|-----|-----------|
| `menu-base` | `menu-base-master` | 회사·부서·사용자·거래처·제품·원부재료·보관고·계측기·차량·작업장 |
| `menu-sys` | `menu-sys-auth` | 권한그룹·메뉴·공통코드 |
| `menu-sys` | `menu-sys-log` | 로그인이력·화면통계·감사로그 |

구 대메뉴 `MWRK`/`MAPR`/`MFRM`/`MCOD`/`MSYS` 및 `menu_cd = scrn_cd` 구 leaf는 migrate에서 삭제(`53`).

### 메뉴 sort_no 인코딩

`sort_no = 대(1~9) × 1000 + 중(0~9) × 100 + 소(0~99)` → 범위 약 `1001`~`9999`.

| 대 | 의미 | 대 폴더 sort |
|----|------|--------------|
| 1 | 오늘 할 일 | `1001` (최상위 leaf) |
| 2 | 문서 작성 | `2000` |
| 3 | 문서 현황·결재 | `3000` |
| 4 | 문서 기준관리 | `4000` |
| 5 | 기초정보 | `5000` |
| 6 | 시스템 | `6000` |

중 폴더는 `대*1000 + 중*100` (예: CCP `2100`), 소는 그 아래 `+1`~`+99` (예: `2101`).  
조회·메뉴관리 목록은 **이 sort_no 순**만 사용한다 (`sp_tbl_menu_sort_encode_u_000`).

## 표준 양식 47종

| # | 한글명 | 최종 코드 | 중분류 |
|---|--------|-----------|--------|
| 1 | 업무 인수인계서 | `tmpl_admin-handover-doc` | admin |
| 2 | CCP 냉장·냉동 보관 모니터링 일지 | `tmpl_ccp-cold-log` | ccp |
| 3 | CCP 금속검출 모니터링 일지 | `tmpl_ccp-metal-log` | ccp |
| 4 | 중요관리점(CCP) 검증점검표 | `tmpl_ccp-verify-check` | ccp |
| 5 | 연간 검증계획서 | `tmpl_prp-verify-plan` | prp |
| 6 | 검증 점검표 | `tmpl_prp-verify-check` | prp |
| 7 | 검증결과 보고서 | `tmpl_prp-verify-report` | prp |
| 8 | 검증 개선조치 결과보고서 | `tmpl_prp-verify-action` | prp |
| 9 | 연간 교육·훈련 계획서 | `tmpl_admin-edu-plan` | admin |
| 10 | 교육일지 | `tmpl_admin-edu-log` | admin |
| 11 | 일일 위생 점검일지 | `tmpl_prp-hygiene-daily` | prp |
| 12 | 개인 위생관리 점검표 | `tmpl_prp-hygiene-personal` | prp |
| 13 | 작업장 환경위생관리 점검표 | `tmpl_prp-hygiene-area` | prp |
| 14 | 방충·방서 점검표 | `tmpl_prp-pest-check` | prp |
| 15 | 시설·설비·처리도구 점검표 | `tmpl_prp-facility-check` | prp |
| 16 | 검·교정 대상 | `tmpl_prp-calib-target` | prp |
| 17 | 자체 검·교정 일지(온도계) | `tmpl_prp-calib-temp` | prp |
| 18 | 자체 검·교정 일지(저울·표준분동) | `tmpl_prp-calib-weight` | prp |
| 19 | 자체 검·교정 일지(저울·표준저울) | `tmpl_prp-calib-scale` | prp |
| 20 | 시설·설비 이력카드 | `tmpl_prp-equip-card` | prp |
| 21 | 폐기물 처리 점검표 | `tmpl_prp-waste-check` | prp |
| 22 | 입·출고 및 재고 점검표 | `tmpl_logis-inventory-check` | logis |
| 23 | 입고검사 일지 | `tmpl_logis-receive-inspect` | logis |
| 24 | 제품검사 성적서 | `tmpl_prp-test-product` | prp |
| 25 | 표면오염도 검사 성적서 | `tmpl_prp-test-surface` | prp |
| 26 | 부적합제품 관리 점검표 | `tmpl_admin-bad-product` | admin |
| 27 | 용수관리 점검표 | `tmpl_prp-water-check` | prp |
| 28 | 클레임 관리 일지 | `tmpl_admin-claim-log` | admin |
| 29 | 공정관리 점검표 | `tmpl_ccp-process-check` | ccp |
| 30 | 차량운행일지 | `tmpl_logis-vehicle-log` | logis |
| 31 | 가열 모니터링 일지 | `tmpl_ccp-heat-log` | ccp |
| 32 | 멸균 모니터링 일지 | `tmpl_ccp-sanitize-log` | ccp |
| 33 | 여과 모니터링 일지 | `tmpl_ccp-filter-log` | ccp |
| 34 | 보건증관리 | `tmpl_admin-law-health` | admin |
| 35 | 원료수불대장관리 | `tmpl_logis-material-ledger` | logis |
| 36 | 건축물대장관리 | `tmpl_admin-building-ledger` | admin |
| 37 | 생산대장관리 | `tmpl_admin-production-ledger` | admin |
| 38 | 영업등록증관리 | `tmpl_admin-license-manage` | admin |
| 39 | 자가품질검사관리 | `tmpl_admin-self-test` | admin |
| 40 | 수료증관리 | `tmpl_admin-cert-manage` | admin |
| 41 | 입출입대장 | `tmpl_admin-visitor-log` | admin |
| 42 | 육안검사기준 | `tmpl_prp-visual-inspect` | prp |
| 43 | 부자재입고검수점검표 | `tmpl_logis-submat-receive` | logis |
| 44 | 외부검교정기록부 | `tmpl_prp-calib-ext` | prp |
| 45 | 제품출고관리일지 | `tmpl_logis-shipment-log` | logis |
| 46 | 회수결과보고서 | `tmpl_admin-recall-report` | admin |
| 47 | 실시상황평가표 | `tmpl_admin-eval-check` | admin |

## 사용양식 관리 편집 규칙 (`hwp-template-management`)

| 컬럼 | 추가 | 이후 |
|------|------|------|
| 양식코드 | 수정 가능 | 변경 불가 |
| 양식명 | 가능 | 가능 |
| 타입 | hwp 고정 | 변경 불가 |
| 구분(`sys_yn`) | `usr` 고정 — 화면 콤보 없음(badge 표시) | 변경 불가 |
| 사용여부 | 가능 | 가능 |

- 삭제는 자사양식(`usr`)만. 시스템양식은 FE·Service·SP 3중 차단(문구 통일: *"시스템에서 제공하는 양식은 삭제할 수 없습니다."*)
- 파일 기능(업로드·내보내기·불러오기·초기화)은 구분과 무관하게 허용
- 업로드는 덮어쓰지 않고 `tbl_company_template_file` 에 버전 1건을 쌓는다 — 파일명 뒤에 `_yyyyMMddHHmmss`
- 초기화 = `default_file_idx` 복원(시스템양식은 배포 원본, 자사양식은 최초 등록본)

## 문서주기 (`schedule-cycle-management`)

- **양식 1개 = 주기 0..1건** — `tbl_schedule_rule UNIQUE(co_cd, tmpl_cd)`
- 주기 `cycle_cd`: 매일/매주/매월/분기/반기/매년 = `D`/`W`/`M`/`Q`/`H`/`Y` (기존 대문자 도메인 유지, `E` 수시는 화면 제외)
- 반복 상세 `tbl_schedule_rule_detail.detail_ty`: `week-day` / `month-day` / `month-end` / `quarter-month` / `half-month` / `year-month`
- 비영업일 처리 `nonwork_rule`: `keep`(그대로) / `prev`(이전 영업일) / `next`(다음 영업일) — 현재 토·일만 비영업일
- 관리 시작일 `base_dt`: `yyyyMMdd` 저장, 화면은 `input type="date"`
- 마감시간 `due_time`: `HHMM` 저장, 화면은 `input type="time"`. 알림은 `alarm_dt = 마감 - app.schedule.alarm-before-minutes`
- 담당부서·담당자는 **코드·ID로만 저장**(`dept_cd`·`user_id`), 이름은 조회 시 조인 표시
- 예정일 계산은 Java `CycleScheduleGenerator` 한 곳 — 31일 지정은 말일로 당기고, 시작일 이전은 만들지 않는다
