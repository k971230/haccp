# 배포 후 개선점

작성 2026-08-26 · 배포 `1.0.42` · main `a4e2aa9` 이후

배포하고 실제로 돌려 보면서 모은 것. **고친 것과 남은 것을 나눠 적는다.**
남은 것은 판단이 필요하거나 코드로 못 고치는 것이다.

---

## 고친 것

| # | 무엇 | 어디 |
|---|---|---|
| 1 | 지연 과제가 더블클릭으로 행추가가 안 됐다 | PR #42 |
| 2 | 스모크가 없어진 화면(`approval-history` 등)을 쳤다 | PR #43 |
| 3 | 생성기 `--check` 가 줄바꿈 차이로 실패했다 | PR #40 |
| 4 | E2E DB 헬퍼가 질의마다 JVM 을 띄웠다 | 아래 4-1 |
| 5 | **운영 DB 와 시험 DB 가 같았다** | 아래 A |
| 6 | **시드만으로는 HTML 작성 화면에 고를 양식이 없다** | 아래 F |
| 7 | 로그인 배경이 1.5MB PNG 였다 | 아래 C |
| 8 | lint 경고 16 → 12 | 아래 D |
| 9 | 시험 도구를 git 에 올릴 수 있게 됐다 | 아래 E |
| 10 | 자료에 매인 시험 3건 | 아래 G |
| 11 | **백업은 있는데 되돌려 본 적이 없었다** | 아래 H |
| 12 | **파일 볼륨이 백업에서 빠져 있었다** | 아래 H |
| 13 | 백엔드 단위 시험 23 → 49건 | 아래 I |

### 4-1. E2E DB 헬퍼 — JVM 을 걷어냈다

배포 서버를 상대로 105건을 돌리자 22건에서 **30분 넘게 멈춰 섰다.**

가른 결과

```
DB     잠금 0 · 대기 질의 0 · 접속 36/100     서버는 멀쩡
Java   CPU 0 으로 정지 · 내 IP idle 세션 20개  클라이언트가 물림
호출   1회당 1.9초 (JVM 기동 + 원격 접속)
```

`dbOne()` 이 **질의 하나에 JVM 을 하나씩** 띄웠다. 로컬 DB 에서는 티가 안 났는데
원격 DB 를 상대로는 호출마다 2초가 붙고 가끔 물렸다.

`execFileSync` 는 동기라 물리면 **Node 이벤트 루프째 막혀** Playwright 의 시험
타임아웃(120초)도 못 살린다 — 실패가 아니라 정지가 된다.

타임아웃 60초를 넣어 봤지만 **Windows 에서는 그게 JVM 을 못 죽인다**(자식 stdio
핸들이 남아 계속 대기한다). 그래서 JVM 자체를 걷어냈다.

| | 전 | 후 |
|---|---|---|
| 호출 1회 | 1.9초 | **0.32초** |
| 양식 복사 시험 | 25.9초 | **12.9초** |
| 물림 | 30분+ 정지 | 없음 |
| **배포 서버 상대 105건** | 22건에서 정지 | **전부 통과 · 10분** |

`tools/q.mjs`(Node `pg`)가 `tools/Q.java` 를 대신한다. 출력 형식이 같아
`helpers.ts` 파싱은 그대로다. 접속·질의에 각각 타임아웃이 있어 **멈추지 않고 실패한다.**

> 영원히 멈추는 시험 도구는 실패하는 도구보다 나쁘다 — 원인을 못 보게 만든다.

---

## 남은 것 — 판단이 필요하다

### A. 운영 DB 와 시험 DB 를 갈랐다 (완료)

전에는 같은 DB 였다 — E2E 5본이 `resetDocuments()` 로 `tbl_document` 를 전량 지우므로
한 번 돌릴 때마다 운영 문서가 사라졌다.

**한 것**

| | |
|---|---|
| `sasshaccp_test` DB 신설 | 같은 서버, 다른 DB |
| `tools/seed_test_db.mjs` | 시드 6본을 순서대로 넣는다. psql 없이 돈다 |
| `backend/haccp-api/.env` | `DB_NAME=sasshaccp_test` — 로컬 백엔드와 `q.mjs` 가 함께 옮겨 간다 |
| **안전장치** | 자료를 지우는 헬퍼가 DB 이름을 보고 막는다 |

안전장치는 이름으로 가른다. 시험용 DB 는 `_test` 로 끝난다.

```
resetDocuments · purgeCompany · seedCompany
  → DB 이름이 _test 가 아니면 시험을 멈춘다

Error: 문서 전량 삭제 은(는) 시험용 DB 에서만 한다. 지금 DB 는 "sasshaccp" 이다.
```

정말 필요하면 `E2E_ALLOW_PROD_WRITES=1` 로 열 수 있다 — **손으로 켜야 한다.**
운영 DB 를 겨눈 채 돌려 실제로 막히는 것을 확인했다.

### B. 자체서명 인증서 — rhwp 편집기 ServiceWorker 가 등록 안 된다

```
subject = CN = 180.71.58.87
issuer  = CN = 180.71.58.87    자기가 자기를 발급
```

브라우저는 **인증서 오류가 있는 연결에서 ServiceWorker 등록을 거부한다.**
「고급 → 계속」으로 화면을 열어도 안 풀린다. `/rhwp/` 와 `/rhwp/sw.js` 는 둘 다 200 이라
프록시 잘못이 아니다. **코드로는 못 고친다.**

도메인 + Let's Encrypt 가 정공법이다 — `HACCP_SERVER_NAME` 이 이미 도메인을 받게 돼 있다.
자세한 건 `운영.md` 10.2.

**HWP 열기·저장이 실제로 되는지 한 번 해 보고** 판단할 일이다. 되면 콘솔 경고일 뿐이다.

---

## 남은 것 — 작지만 쌓인다

### C. 로그인 배경 1.5MB → 84KB (완료)

```
전   index.js 693KB + css 78KB + login_bg.png 1565KB  =  약 2.3MB
후   index.js 693KB + css 78KB + login_bg.jpg   84KB  =  약 0.87MB
```

1376x768 사진을 PNG 로 저장해 두었다. **전 화소가 불투명**인 것을 확인하고
JPEG(q88)로 다시 인코딩했다 — 알파를 잃지 않는다.

**번들 693KB 는 그대로다.** `React.lazy` 로 화면을 나누면 더 줄지만
`screenRegistry` 가 컴포넌트를 한꺼번에 가져오는 구조라 손이 더 든다. 급하지 않다.

### D. lint 경고 16 → 12 (부분 완료)

```
전   any 12 · exhaustive-deps 1 · unused 3      16건
후   any 12                                     12건
```

- `CommonCodePage` 가 자기만의 `stripMeta` 를 갖고 있었다 — 형제 4화면이 쓰는
  공용 `stripRowMeta` 로 바꿨다. **경고가 준 게 아니라 중복이 사라졌다**
- `useGridAccess` 의 `exhaustive-deps` 는 **끄는 게 맞다.** 화면이 매 렌더마다
  객체 리터럴을 넘기므로 `ctx` 를 통째로 넣으면 메모가 죽는다. 까닭을 주석에 적고 껐다

남은 `any` 12건은 그리드가 제네릭이라 그렇다. 없애려면 그리드 타입을 다시 짜야 한다.

### G. 자료에 매인 시험 — 운영에서 우연히 통과하던 것 (완료)

시험 DB 를 새로 깔자 두 건이 드러났다. **운영에서는 자료가 우연히 맞아 지나갔다.**

| 시험 | 무엇을 가정했나 | 어떻게 고쳤나 |
|---|---|---|
| 문서 흐름 | 작성 화면에 고를 양식이 있다 | 위 F — 시험 DB 에 표준 복사를 깔았다 |
| 문서주기 삭제 2단계 | **첫 행에 주기가 걸려 있다** | 주기가 걸린 양식을 DB 에서 집는다 |
| 로그 3화면 방향키 | **화면 이용 통계에 행이 있다** | 일일 배치가 부르는 집계 SP 를 시험이 먼저 부른다 |

주기가 안 걸린 양식을 고르면 지울 게 없어 화면이 API 를 아예 안 부른다.
운영에서는 `hwp_sys_001` 이 첫 행이라 우연히 통과했다.

화면 이용 통계는 **집계 표**(`tbl_view_stat_daily`)를 읽는다. 갓 시드한 DB 는
일일 배치가 돈 적이 없어 0행이다 — 운영에는 500행이 쌓여 있었다.

> 「우연히 통과하는 시험」은 통과해도 아무것도 보증하지 않는다.
> 자료를 가정하지 말고 **필요한 자료를 시험이 직접 집거나 깔아야 한다.**

### E. tools/ 를 git 에 올렸다 (완료)

`q.mjs` 로 바꾸면서 필요한 게 Node 와 `pg` 뿐이 됐다(전에는 JDK·JDBC jar).
접속정보는 `backend/.env` 에서 읽으므로 **파일 자체에는 비밀이 없다.**

올린 것 넷

```
tools/q.mjs                   SQL 실행·표 출력
tools/seed_test_db.mjs        시험용 DB 에 시드 6본
tools/seed_test_forms.sql     양식 표준 복사 (F 참조)
tools/reset_test_documents.sql
```

`Q.java`·`ApplyOneSql.java`·`out/` 은 계속 뺀다 — `q.mjs` 가 대신하고 빌드 산출물이다.
`.gitignore` 는 `/tools/*` + 예외 넷으로 바꿨다. 디렉터리를 통째로 무시하면
안쪽 예외가 안 먹는다.

**주의 하나를 같이 넣었다.** `hasDbTools()` 가 도구만 보고 판정하면 CI 에서
건너뛰던 시험이 **실패로 바뀐다** — CI 에는 `backend/.env` 가 없기 때문이다.
그래서 **도구와 접속정보가 둘 다 있을 때만** 참으로 본다.

CI 에 DB 접속정보를 주면 그때부터 CI 도 「저장했다」가 아니라 DB 로 판정한다.
그 전까지는 지금처럼 건너뛴다.

### F. 시드만으로는 HTML 작성 화면이 빈 목록이다 (문서로 고침)

시험용 DB 를 새로 만들어 시드 6본을 넣고 E2E 를 돌리자 **문서 흐름이 실패했다.**
작성 화면에서 고를 양식이 하나도 없었다.

```
tbl_tml_ccp_htg_ver   운영 1건   갓 시드한 DB 0건
```

시드는 **표준 지면**(`tbl_check_item`)까지만 깐다. HTML 작성 5화면은
**회사 지면 버전**(`tbl_*_ver`)을 읽는데 그건 안 만든다.
운영에는 사람이 양식 원본 화면에서 「행추가(표준 복사)」를 눌러 만들어 둔 것이 있었다.

**동작은 안 바꿨다.** 업체마다 지면이 달라 표준을 그대로 주면 안 되는 게 설계다.
문제는 **그 다음 단계를 아무 데도 안 적어 둔 것**이었다. 게다가 `06_company_seed.sql`
주석은 「HTML 자사본도 물려준다」고 했는데 바로 아래 `WHERE` 가 `usr` 을 뺀다.

고친 것

| | |
|---|---|
| `06_company_seed.sql` | 주석 정정 + 확인 질의에 `HTML지면버전` 추가 + **다음 단계 5줄** 명시 |
| `db_sasshaccp/README.md` | 「10. 다음 단계 — 시드가 안 해 주는 것」 |
| `tools/seed_test_forms.sql` | 시험용 DB 를 「양식을 복사해 둔 업체」 상태로 만든다 |

> 갓 개설한 업체는 HTML 작성 화면이 빈 목록으로 뜬다. 그게 정상이고,
> 양식 원본 5화면에서 행추가를 눌러야 한다 — 이제 시드 출력이 그렇게 말한다.

### H. 백업 — 되돌려 보고, 빠진 것을 채웠다 (완료)

**되돌려 본 적 없는 백업은 백업이 아니다.** 실제로 해봤다.

시험용 DB 를 통째로 지우고 덤프로 되살렸다.

| | |
|---|---|
| 덤프 | 777 KB (`pg_dump -Fc`, 운영과 같은 방식) |
| 복구 | **17초** |
| 결과 | 표 53 · SP 152 · 문서 1 · 사용자 5 — **원본과 같음** |
| 앱 | 3.3초 기동 · 로그인·메뉴·문서·작성양식 API **전부 200** |

**겪은 것** — 접속이 하나라도 남아 있으면 `DROP DATABASE` 가 거절된다.

```
database "sasshaccp_test" is being accessed by other users     (JDBC 세션 10개)
```

실제 장애 때 제일 먼저 부딪힐 자리다. `scripts/db_restore.sh` 가 먼저 끊는다.

**빠져 있던 것 — 파일 볼륨**

compose 주석은 `haccp-files` 를 「유일하게 백업이 필요한 볼륨」이라 적어 두었는데
`backup` 프로파일은 **DB 만** 떴다. 업로드 원본·생성 PDF 가 통째로 빠져 있었다.

> DB 만 되돌리면 문서 행은 살아나고 그 문서의 첨부는 사라진다.
> 규제 문서를 다루는 시스템에서 그건 복구가 아니다.

`backup-files` 서비스를 붙였다. DB 덤프와 같은 볼륨에 같은 시각 규칙으로 tar 를 뜬다.

**아직 남은 것** — 덤프가 `haccp-db-backup` 볼륨, 즉 **같은 호스트 안**에 쌓인다.
호스트가 통째로 죽으면 백업도 같이 죽는다. 밖으로 빼는 것은 아직 손으로 한다
(`운영.md` 12.1 에 명령을 적어 두었다).

### I. 백엔드 단위 시험 23 → 104건 (완료)

서비스 24본 중 시험이 닿는 건 1본이었다. 값이 큰 곳부터 넣었다.

| 새 시험 | 왜 여기부터인가 |
|---|---|
| `DeleteValidationTest` (12) | **전 화면 삭제가 지나는 길.** 여기가 무르면 스물여덟 화면이 같이 무른다 |
| `HwpTemplateServiceDeleteTest` (7) | **한 번도 동작한 적 없던 경로** (E2E-008). 되는 것과 막는 것을 같이 본다 |
| `CorrectiveActionServiceTest` (7) | 2026-08-26 에 옮긴 서비스. 옮길 때 시험이 없었다 |

**시험이 진짜 무는지 확인했다.** 운영 코드를 일부러 깨뜨려 봤다.

| 깨뜨린 것 | 결과 |
|---|---|
| 사용양식 차단 검사 제거 | **1건 실패** |
| 감사 기록 제거 | **1건 실패** |
| `requireItems` 무력화 | **3개 시험 4건 동시 실패** |

전부 되돌렸고 운영 코드는 그대로다. 통과만 보고 넘기지 않았다.

#### 2026-08-27 — 값이 큰 넷을 더 넣었다 (58 → 104건)

분기가 많은 순으로 골랐다. 「서비스가 몇 본 덮였나」가 아니라
**「무르면 무엇이 조용히 망가지나」**를 기준으로 봤다.

| 새 시험 | 무르면 생기는 일 |
|---|---|
| `AuthServiceGuardTest` (9) | 잠긴·정지된·계약 끝난 계정이 **비밀번호만 맞으면 들어온다.** 아이디 존재 여부가 문구로 새어 나간다 |
| `UserServiceSignTest` (10) | 아래 J 참조 — 올린 쪽이 준 Content-Type 이 그대로 우리 도메인에서 되돌아 나갔다 |
| `HtmlTemplateServiceGuardTest` (13) | 한 회사가 고친 **표준 양식이 전 회사 지면에 번진다.** 양식코드 배정이 어긋나면 오류 없이 엉뚱한 표에 쌓인다 |
| `CcpLogDraftServiceSaveTest` (14) | **부적합(F) 행이 있는데 이탈이 안 생긴다.** 한계 이탈이 기록 없이 지나간다 — HACCP 에서 제일 큰 사고다 |

**전부 깨뜨려 확인했다.** 운영 코드를 22가지로 일부러 망가뜨려 봤고, **22건 모두 시험이 물었다.**

| 깨뜨린 것 | 물었나 |
|---|---|
| 잠금·정지·만료 관문을 통째로 제거 | 물었다 |
| 없는 아이디를 「존재하지 않는 아이디」로 따로 알림 | 물었다 |
| 잠금 임계 비교를 하나 늦춤 (`>=` → `>`) | 물었다 |
| 구독 만료 경계를 하루 당김 (`<` → `<=`) | 물었다 |
| 서명 MIME 을 다시 올린 쪽 값으로 (옛 코드) | 물었다 |
| 서명 확장자·타입 검사를 「둘 중 하나」로 (옛 코드) | 물었다 |
| 10MB 경계를 하나 당김 | 물었다 |
| 표준 양식 보호 제거 | 물었다 |
| 회사순번 0(표준 자리) 열어 줌 | 물었다 |
| 가열 양식을 금속검출 표로 보냄 | 물었다 |
| 옛 양식코드 매핑 삭제 | 물었다 |
| 사용여부를 Y/N 로 안 고르고 원값 그대로 | 물었다 |
| **부적합 판정을 항상 거짓으로** | 물었다 |
| **부적합을 대소문자 구분으로** (`f` 를 놓친다) | 물었다 |
| **전 행이 부적합일 때만 이탈 생성** (`anyMatch` → `allMatch`) | 물었다 |
| 빈 셀을 숫자로 취급 | 물었다 |
| 숫자칸·글자칸 뒤바꿈 | 물었다 |
| 화면이 준 행 순번 무시 | 물었다 |
| 저장 결과 0 을 성공으로 봄 | 물었다 |
| 일자 여덟 자리 검사 제거 | 물었다 |
| 양식명 공백 검사 제거 | 물었다 |
| 서명 대상 아이디 공백 검사 제거 | 물었다 |

전부 되돌렸고 운영 코드는 그대로다.

**서비스 24본 중 9본**에 시험이 닿는다. 남은 15본은 대부분 SP 를 그대로 부르는
얇은 통로다 — E2E 105건이 그 길을 지난다.

---

### J. 서명 이미지가 우리 도메인에서 실행될 수 있었다 (완료 · 2026-08-27)

**단위 시험을 쓰다가 찾았다.**

`uploadSign` 이 **올린 쪽이 보낸 `Content-Type` 을 그대로 저장**했고,
조회는 그 값을 그대로 응답 `Content-Type` 에 실어 **`inline` 으로** 되돌려 줬다.
검사도 「확장자 **또는** 타입」이라 한쪽만 맞으면 통과했다.

`sign.png` 를 `Content-Type: text/html` 로 올리면 우리 도메인에서 열리는 HTML 이 된다.
같은 출처라 화면의 토큰까지 닿는다. `nosniff` 도 어디에도 없었다.

| 고친 것 | 어떻게 |
|---|---|
| 저장 MIME | 올린 쪽 값을 **안 쓴다** — 확장자에서만 정한다 (`guessImageMime`) |
| 검사 | 「확장자 **또는** 타입」 → 「확장자 **그리고** 타입」 |
| 겹으로 | `SecurityHeadersFilter` — 전 응답에 `X-Content-Type-Options: nosniff` · `Referrer-Policy: same-origin` |

파일을 되돌려 주는 곳이 넷이라(문서첨부·양식·서명·감사PDF) 헤더는 **한 곳에서** 건다.
네 군데에 따로 붙이면 다섯 번째가 생겼을 때 빠진다.

실서버로 확인했다.

```
확장자 png + Content-Type text/html  →  HTTP 400   (예전엔 통과)
정상 png                              →  HTTP 200
되돌아오는 Content-Type               →  image/png
                                          X-Content-Type-Options: nosniff
```

---

### K. 커스텀 그리드 전수 점검 — 결함 1건 (완료 · 2026-08-27)

그리드는 스물여덟 화면이 **같은 것 하나**를 쓴다. 그런데 시험이 닿는 건
「방향키로 행이 옮겨가나」 5건뿐이었다. 34건으로 늘리면서 **결함 하나를 찾았다.**

#### 찾은 결함 — Escape 를 누르면 키보드가 죽는다

편집 중 `Escape` 를 누르면 포커스가 `body` 로 빠졌다.
그 뒤 **방향키·F2·Delete·Backspace 가 전부 먹지 않는다** — 다시 마우스로 눌러야 살아났다.
키보드로 기록을 치는 현장에서 손을 멈추게 하는 종류다.

| | |
|---|---|
| 원인 | `setEditCell(null)` 만 하고 포커스를 안 되돌렸다. 셀 선택까지 같이 날아갔다 |
| 고침 | `cancelEdit` — 셀 선택은 **비편집 상태로 유지**하고 포커스를 그리드로 되돌린다 |
| 한 번 더 | `requestAnimationFrame` 으로 미뤘더니 한 프레임이 비어 **그 사이 Delete·F2 가 새 나갔다**. `useLayoutEffect` 로 당겨 틈을 없앴다 |

#### 확인한 것

| 갈래 | 본 것 |
|---|---|
| 조회 | 정렬 · 다중 정렬(Shift, 순번) · Shift 없이 누르면 기준 교체 · 컬럼별 필터 · 결과 내 검색 · 열 숨김/복원 · **저장된 열 순서 적용** · 틀 고정 · **CSV 내용**(숨긴 열 빠짐 · 거른 행만 · 엑셀용 BOM) · **가상 스크롤**(550건 중 24행만 그린다) · 스크롤 시 재렌더 |
| **아이디별 저장** | 숨김·순서·너비가 `tbl_grid_pref` 에 v2 로 남는다 · 새로고침해도 유지 · **다른 사용자 행을 안 건드린다** · 한 화면의 그리드 셋이 각자 칸을 쓴다 · 편집 그리드도 저장한다 · **정렬·필터는 저장 안 한다**(표 주석대로) |
| 편집 | 행 이동 · **끝에서 순환 안 함**(클램프) · Tab/Shift+Tab 셀 이동 · F2·Enter 편집 열기 · Escape · Delete·Backspace 비우기 · 변경 표시 · 편집 중 방향키 차단 · 검색칸 타이핑 중 차단 |
| 단축키 예의 | Ctrl+R·S·F·P·L·C·V·A·T·W **열 개 모두 안 가로챈다** (조회·편집 양쪽) · Backspace 는 입력칸에서 글자만 지운다 · **Tab 이 그리드를 가두지 않는다**(편집·비편집 양쪽) |

**단축키는 애초에 잘 골라져 있었다.** `Ctrl`·`Meta` 조합이 코드에 **하나도 없다** —
새로고침·저장·검색·인쇄·주소창·복사·붙여넣기를 뺏을 수 없는 구조다.
쓰는 키는 화살표·Tab·Enter·F2·Delete·Backspace·Escape 뿐이고,
`F2`(엑셀의 셀 편집)는 브라우저가 안 쓰는 키다.

**전부 깨뜨려 확인했다.** 12가지로 망가뜨려 12건 모두 시험이 물었다 —
클램프 제거(순환), 입력칸 안에서도 키 가로채기, Tab 가두기, 편집 중 방향키 통과,
Ctrl 조합 가로채기, 열 설정 저장 안 하기, 너비를 50 미만으로 저장, Escape 포커스 되돌림 제거,
저장된 열 순서 무시, CSV 가 숨긴 열까지 내보내기, CSV 가 거르기 전 전부 내보내기.

세 번은 **시험이 아니라 시험 코드가 틀려서** 잡혔다 — 그것도 적어 뒀다
([`docs/6_테스트.md`](docs/6_테스트.md) 「시험이 빠지기 쉬운 자리」).
특히 **E2E 는 `dist/` 를 본다** — 소스만 고치고 돌리면 고치기 전 화면을 시험한다.

#### 안 고치고 남긴 것 — Escape 는 「취소」가 아니다

엑셀과 달리 이 그리드는 타이핑이 곧바로 행 버퍼로 들어가고, `Escape` 는 편집칸만 닫는다.
**친 값은 남고 행은 「변경」으로 표시된다.** 그대로 「저장」을 누르면 들어간다.

되돌리려면 편집을 열 때 원래 값을 떠 놓아야 한다 — 동작이 바뀌는 일이라
지금은 **실제 동작을 시험으로 못 박고** 문서에 적어 두었다
([`frontend/haccp-web/src/components/grid/README.md`](frontend/haccp-web/src/components/grid/README.md)).
바꿀지는 판단이 필요하다.

---

### L. 작성 5화면 — 판정 기본값 적합 · 「모두 적합」 (완료 · 2026-08-27)

현장에서 치는 기록은 대부분이 적합이다. 그런데 판정이 빈 값으로 시작해서
**행마다 라디오를 한 번씩 더 눌러야 했다.** 적합으로 깔고 부적합만 눌러 고치게 바꿨다.

| | |
|---|---|
| 지면을 열 때 | **비어 있는 판정만** 적합으로 채운다 (`fillBlankLogJudges` · `fillBlankItemJudges`) |
| 행추가 | 새 기록행은 적합으로 시작한다 (`appendLogRow` 기본값) |
| **「모두 적합」 버튼** | 지면 4본에 달았다 — 부적합으로 바꿔 놓은 것까지 한 번에 되돌린다 |

**저장된 판정은 절대 안 건드린다.** 사람이 남긴 부적합을 적합으로 덮으면
판정을 지우는 것이다. 빈 것만 채운다.

지면은 넷이면 다섯 화면이 다 덮인다 — `ccp-verify` 가 `HygPrcPaper` 를 그대로 쓴다.

#### 금속검출만 다르다 — 화면이 미리 칠하지 않는다

`sp_tbl_ccp_metal_monitor_c_000` 이 **감도 5칸으로 판정을 계산한다.**
`feOnly='O' AND stsOnly='O' AND prodOnly='X' AND feProd='O' AND stsProd='O'` 라야 적합이다.

화면이 미리 적합으로 칠하면 저장하는 순간 서버가 부적합으로 뒤집어
**「보이는 값」과 「저장되는 값」이 달라진다.** 그리고 감도 확인도 안 한 행이
적합으로 남는다 — HACCP 에서 하면 안 되는 일이다. 그래서 금속만 기본값을 뺐다.

**「모두 적합」은 금속에도 있다.** 대신 `judgeModYn: "Y"` 를 같이 남긴다 —
「사람이 정한 판정」이라는 뜻이고, SP 는 그때만 자동 계산을 누르고 그 값을 쓴다.
행마다 적합 라디오를 직접 누르는 것과 똑같고, 감사에도 그렇게 남는다.

#### 스스로 걸린 것

처음엔 「신규 문서(docIdx == null)일 때만 깐다」로 짰다. **안 먹었다.**
작성 화면은 좌측을 먼저 저장해야 우측 지면이 열려서, 지면을 처음 여는 시점에는
이미 `docIdx` 가 있다. 변이 시험에서 그 줄을 뒤집었는데 결과가 안 바뀌어 알았다 —
**시험이 코드가 아니라 내 가정을 잡아 줬다.**

시험은 단위 14건(`htmlFormJudgeDefault.test.ts`) + E2E 10건(`draft-judge-default.spec.ts`).
E2E 는 화면 문구가 아니라 **라디오의 checked 상태**로 판정한다.

#### 같이 바뀐 것 — 전송 필수값 시험

`document-flow` 가 「판정을 비우고 전송하면 **판정을 선택하세요** 가 뜬다」를 보고 있었다.
이제 판정이 깔려 나오므로 그 문구로는 안 막힌다. 그 자리를 **「판정이 다 깔렸는지」**
확인으로 바꾸고, 여전히 필수인 **시각**으로 전송이 막히는지를 본다.

기능이 바뀌면 그 기능을 보던 시험도 같이 바뀌어야 한다 —
전체 E2E 를 돌려서 알았다. 그 한 건이 안 깨졌으면 오히려 이상한 것이다.

---

### M. Jenkins 가 시험을 소스보다 많이 셌다 (완료 · 2026-08-27)

로컬 `mvnw test` 는 **104건**인데 Jenkins JUnit 리포트는 **117건**으로 찍혔다.
**실패는 0건** — 숫자만 달랐다.

원인은 파이프라인이 `clean` 을 안 한 것이다. Jenkins 워크스페이스는 빌드마다
지워지지 않아서, 시험 클래스를 **옮기거나 이름을 바꾸면 `target/test-classes` 에
옛 `.class` 가 그대로 남는다.** surefire 는 소스가 아니라 클래스를 훑으므로
그 옛 시험까지 찾아서 돌린다.

이 저장소에서 실제로 옮긴 시험이 셋 있다 (패키지를 URL 과 맞추면서).

| 옛 자리 | 지금 자리 |
|---|---|
| `com.haccp.hwp.doccycle.CycleScheduleGeneratorTest` | `com.haccp.docs.sch.…` |
| `com.haccp.docs.doccycle.CycleScheduleGeneratorTest` | 〃 |
| `com.haccp.draft.hwp.HwpDraftServiceDetailTest` | `com.haccp.draft.hwpdoc.…` |

**왜 나쁜가** — 리포트 건수가 부풀 뿐 아니라 **이미 없는 시험이 「통과」로 잡힌다.**
지운 기능을 보던 시험이 초록으로 남아 있으면, 그 기능이 죽어도 아무도 모른다.

```
./mvnw -q -B -DskipITs clean test    # clean 을 붙였다
```

---

## 배포 서버 검증 결과

| | |
|---|---|
| **E2E 105건** | **배포 서버 상대로 전부 통과 · 실패 0 · 건너뜀 0 · 10분** |
| `prod_smoke.sh` | 9단계 **OK** |
| 화면 API 18 + 공용 허브 8 | **26 / 26 = 200** |
| 이번에 고친 결함 | 404 · HWP삭제 · 개선조치URL · recent-docs 전부 운영에서 확인 |
| 시험 잔여물 | 업체 9099 · E2E 양식 · E2E 개선조치 **전부 0** |

돌린 것

```
E2E_BASE_URL=http://180.71.58.87/haccp/ E2E_API_BASE_URL=http://180.71.58.87 npx playwright test
```

화면(배포된 번들) → edge → API → SP → DB 가 한 줄로 꿰였다.
그중엔 13.5MB HWP 실파일 업로드, 신규 업체 개설, 문서 한 바퀴(작성→전송→승인→보관),
계정 전환 시 탭 정리가 들어 있다.
