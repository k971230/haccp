# 배포 전 최종 검증 결과

시작 2026-08-26 · 대상 28화면 · 계획: [`배포전_최종검증_계획.md`](배포전_최종검증_계획.md)

---

## 0단계 — 진행 중 작업 확정 (완료)

### 관문

| 검사 | 결과 |
|---|---|
| `npx tsc --noEmit` | 오류 0 |
| `npx vitest run` | **130건 통과** |
| `npx eslint src e2e` | 오류 0 (경고 17 — 기존) |
| `./mvnw -o test` | **통과** |
| 백엔드 기동 | **성공** 4.4초 |

미커밋 16파일(오늘 할 일 KPI 5장·더블클릭 이동·`?add=1`)은 위 관문을 모두 통과했다.
1번 묶음(방향키·패널 초록)은 이미 `e82bdc0` 로 커밋돼 있었다.

### 잡은 결함 2건

#### 결함 1 — HWP 사용양식 삭제가 통째로 죽어 있었다 (심각)

화면에서 양식을 지우면 **500** 이 났다. 한 번도 동작한 적이 없다.

```
화면  HwpTemplateManagementPage.tsx:411
  → validateDeleteCompanyTemplates()
  → POST /api/v1/bas/company-templates/validate-delete
  → 서빙하는 컨트롤러 없음 → NoResourceFoundException → 500
```

왜 아무도 못 잡았나

| | |
|---|---|
| 이 URL 을 서빙하던 `MasterController` 가 `190fe6d`(28화면 정리)에서 지워졌다 | 컴파일에 안 잡힌다 — FE 문자열이라 |
| `company-templates` 는 그 컨트롤러의 `MasterType` enum 에 **애초에 없었다** | 지워지기 전에도 `지원하지 않는 기준정보입니다` 로 실패했다 |
| E2E 가 이 화면의 삭제를 안 봤다 | 조회·저장까지만 있었다 |
| 주석·README 3곳이 "법적서류와 공유해서 Workflow 에 잔류" 라고 적어 두었다 | 그 Workflow 도 없었다 |

**고친 것** — 형제 5화면과 같은 형태로 화면 자기 경로에 붙였다.

| 층 | 무엇 |
|---|---|
| DB | `sp_tbl_company_template_delete_blocker_r_000` **신설** — 시스템 제공분·작성된 문서 차단 |
| DB | `sp_tbl_master_delete_blocker_r_000` **제거** — 지운 표 15본을 참조하는 죽은 SP |
| BE | `HwpTemplateMapper` `.java`·`.xml` — `selectDeleteBlocker` · `deleteHwpTemplate` |
| BE | `HwpTemplateService` — `validateDelete` · `delete` · `assertDeletable` · **AuditWriter** |
| BE | `HwpTemplateController` — `POST /validate-delete` · `POST /delete` |
| BE | `ScreenAuthResolver` — 죽은 `/api/v1/bas` 예외 맵 제거 |
| FE | `hwpTemplateApi.ts` — `${BASE}/validate-delete` · `${BASE}/delete` |
| 테스트 | `ScreenAuthResolverTest` · `ScreenAuthInterceptorTest` 2본 |
| 문서 | README 5본 |

**검증 — 실제 호출 + DB 대조**

```
자사양식 생성            → 200   DB: hwp_e2e_del_01 sys=usr
validate-delete         → 200
delete                  → 200
DB tbl_company_template → 0행
DB tbl_template         → 0행   (자사 카탈로그 함께 정리)
tbl_audit_log           → D tbl_company_template
시스템양식 삭제 시도      → 400   "'hwp_sys_001'이(가) 시스템 제공 양식에서 참조 중"
빈 목록                  → 400   "삭제할 양식을 선택하세요."
```

#### 결함 2 — 없는 경로가 500 으로 나왔다

`NoResourceFoundException` 이 `@ExceptionHandler(Exception.class)` 로 떨어져
**500 UNKNOWN + 스택트레이스**가 됐다. 잘못된 주소는 서버 결함이 아니다.

**고친 것** — `GlobalExceptionHandler` 에 404 핸들러 추가. 스택트레이스를 남기지 않는다.

```
전  POST /api/v1/bas/... → 500 {"code":"UNKNOWN"}  + 스택트레이스
후  POST /api/v1/bas/... → 404 {"code":"NOT_FOUND","message":"요청하신 경로를 찾을 수 없습니다."}
```

이 결함이 결함 1을 가렸다. 404 였으면 훨씬 빨리 드러났다.

### 계획 정정

계획 문서에 "참조하는데 정의 없는 SP 0건 · 고아 4건은 다른 SP 안에서 불린다"고 적었다.
**그 측정이 느슨했다** — 매퍼 XML 의 *주석*에 적힌 SP 이름을 호출로 셌다.
`sp_tbl_company_template_d_000` 은 주석에만 있었고 실제로는 아무도 부르지 않았다.

1단계 SP 점검은 주석을 뺀 실제 호출문만 세는 것으로 한다.

---

## 1단계 — SP 전수 점검 (완료)

**154 → 152본.** 죽은 SP 2본을 더 걷어냈다(결함 1에서 1본, 여기서 2본).

### 축별 결과

| 축 | 결과 |
|---|---|
| **참조 정합** | 매퍼가 부르는데 정의 없음 **0건** (주석 제외 실측) |
| **인자 일치** | 호출문 **167건 전건 일치** (기본값 인자 고려) |
| **컬럼 실재** | 표 53 · 컬럼 767 대조 — 없는 컬럼 **0건** |
| **회사 격리** | 위반 **0건** |
| **재적용** | 운영 DB 에 261문 재적용 **오류 0** |
| **DB·파일 일치** | DB 152 = 파일 152, 어긋남 **0** |

### 걷어낸 죽은 SP 2본

| SP | 왜 죽었나 |
|---|---|
| `sp_tbl_company_code_copy_c_000` | `03_code_seed.sql` 이 `-v co_cd` 로 같은 일을 한다. 두 갈래면 한쪽만 고쳐진다 |
| `sp_tbl_menu_sort_encode_u_000` | 없어진 메뉴(`bas`·`ccp`·`prp`·`logis`·`admin`)의 정렬값을 쓴다. **돌리면 순서가 어긋난다** |

`01_sp.sql` 맨 위에 「물러난 SP 정리」 절을 두고 `DROP ... IF EXISTS` 3줄로 명시했다.
파일에서 정의만 빼면 이미 있는 운영 DB 에는 남기 때문이다.

### 회사 격리 — 걸러낸 14건은 모두 정상

기계 검사가 14건을 올렸지만 전수로 보니 위반이 아니다.

| 갈래 | 건수 | 왜 정상인가 |
|---|---|---|
| `_c_000` INSERT 계열 | 9 | `co_cd` 로 **거르는** 게 아니라 **쓴다** |
| 로그인 SP 3본 | 3 | 로그인은 회사를 알기 전이다. `user_id` 로 찾아 `co_cd` 를 돌려준다 |
| `sp_tbl_schedule_rule_active_r_000` | 1 | 회사 전체를 도는 **일일 배치** 전용. `DailyTaskGenerationJob` 만 부른다 — 화면에서 안 닿는다 |
| `sp_tbl_notification_task_c_000` | 1 | 같은 배치의 알림 적재 |

`*_ver_copy_c_000` 5본도 확인했다 — 원본이 **표준 양식**(`html_ccp_pkg_000` 등)이지
남의 회사 자료가 아니다. 쓸 때는 `p_co_cd` 를 넣는다.

### 계획보다 강하게 본 것

「이미 있는 DB 에 재적용」을 넣은 판단이 맞았다. `sp_tbl_today_task_r_000` 처럼
반환 형이 바뀐 함수는 `CREATE OR REPLACE` 만으로 터지는데, **261문이 오류 0** 으로
지나갔다 — 반환 형이 바뀐 다른 함수는 없다는 뜻이다.

---

## 2단계 — URL · 주석 전수 (완료)

### 2-1 URL 정합 — 결함 2건을 더 고쳤다

#### 결함 3 — `UserController` 하나가 base 가 다른 두 갈래를 담고 있었다

```
전   @RequestMapping("/api/v1/sys")
     @GetMapping("/code/user-management/list")   화면
     @GetMapping("/users/me/sign")               공용 서명
형제  @RequestMapping("/api/v1/sys/code/{화면}")
     @GetMapping("/list")
```

형제 5화면을 보고 `@GetMapping("/list")` 를 적으면 `/api/v1/sys/list` 가 된다.
**둘로 갈랐다** — `UserController`(화면 4개) · `UserSignController`(서명 6개).
서명은 결재·문서 여러 화면이 함께 쓰므로 화면 경로가 아닌 게 맞다.

#### 결함 4 — 개선조치관리 화면 API 가 남의 패키지에 있었다

`TaskController`(`com.haccp.tsk`) 하나가 **URL 4계열**을 서빙하고 있었다.

```
/api/v1/tsk/today-tasks/*                     오늘 할 일
/api/v1/tsk/notifications/*                   알림 (셸 공용)
/api/v1/flow/ca/corrective-action-management/*   ← 화면인데 패키지가 tsk
/api/v1/docs/documents/{docIdx}/relations     문서 관계
/api/v1/docs/audit-export/*                   감사자료 (G-14 동결)
```

「URL = DB = 폴더 = 패키지」가 깨져 있었다. 개선조치를 제 자리로 옮겼다.

| 신설 | |
|---|---|
| `flow/ca/CorrectiveActionController.java` | `@RequestMapping("/api/v1/flow/ca/corrective-action-management")` |
| `flow/ca/CorrectiveActionService.java` | JWT·업무 판단·camelCase 변환 |
| `flow/ca/CorrectiveActionMapper.java` | |
| `mapper/flow/ca/CorrectiveActionMapper.xml` | namespace 도 함께 옮겼다 |

`tsk` 에서 컨트롤러 59줄 · 서비스 36줄 · 매퍼 3문을 걷어냈다.

**MyBatis 는 컴파일에 안 잡힌다 — 기동해서 확인했다.**

```
기동 성공 · GET  /api/v1/flow/ca/corrective-action-management/list      → 200, 1건
            camelCase 키: caNo, occurDt, actionDesc, tmplNm, deviationDesc ...
            POST /validate-delete  []                                   → 400 "삭제할 개선조치를 선택하세요."
```

#### 그대로 둔 것

| | 왜 |
|---|---|
| `/api/v1/docs/audit-export/*` | 코드에 `@Deprecated(forRemoval = false)` 와 **"절대 혼동·삭제하지 않는다"** 는 결정이 명시돼 있다. 규제·감사 대비 경로다 |
| `/api/v1/docs/documents/{docIdx}/relations` | FE 에 API 함수는 있으나 **부르는 화면이 없다.** 지우려면 판단이 필요해 `tsk/README.md` 에 적어 두었다 |

#### 최종 정합

**자기 컨트롤러가 있는 18개 화면 전부 URL = 폴더 = 패키지 일치, 어긋남 0.**
나머지 10개(양식 5본·결재 3본·문서함·오늘 할 일)는 공용 허브가 맡는다.

### 2-2 주석 전수

| 층 | 전 | 후 |
|---|---|---|
| 자바 | 137 / 138 | **142 / 142** |
| FE | 200 / 210 | **208 / 210** |

남은 2본은 `tanstack-table.d.ts` · `vite-env.d.ts` — 타입 선언이라 코드가 없다.
**규칙에 예외로 못 박았다**(`05-handoff-comments.mdc`). 앞으로 이 자리에서 다시 흔들리지 않는다.

고친 8본은 헤더가 없던 게 아니라 **옛 형식**(`주요 역할:`)이었다.
`개발자 / 일자 / 코멘트 1)2)3)` 로 통일했다. 테스트 파일도 형제 4/6 이 이미 달고 있어 같이 맞췄다.

### 규칙 파일이 죽은 곳을 가리키고 있었다 — 41건 훑어 13건 수정

규칙은 **모든 에이전트가 읽는 정본**인데 `00-bootstrap.mdc` 의 읽기 순서가
없어진 문서 6본을 가리키고 있었다.

```
docs/5_PIPELINE_색인.md              docs/15_HACCP_FE_BE_통합_상세스펙.md
docs/4_명명과_경로.md   docs/20_배포_런북.md
.cursor/rules/09-haccp-frontend.mdc        .cursor/rules/08-haccp-backend.mdc
```

25본→8본 정리 때 문서만 옮기고 규칙을 안 고쳤다. 새 8본과 `PIPELINE.md` 2본으로 다시 썼다.

그 밖에 고친 것

| 규칙 | 무엇 |
|---|---|
| `05-handoff-comments` | 본보기 파일 3곳이 지운 경로(`ColdMonitorPage` · `docs/document/`)였다 |
| `07-haccp-db` | 없는 `tools/ApplyHaccpDb.java` · `q.sh` · `e.sh` 를 실제 `Q.java` · `ApplyOneSql.java` · `Bc.java` 로 |
| `02·09-frontend` | 본보기 `ColdMonitorPage.tsx`(삭제됨) → `HwpTemplateManagementPage.tsx` |
| `01-project-core` | `SqlUserMessage.java` 경로 생략표기(`.../`)를 실제 경로로 |
| `04-deploy` | `docker-compose.override.yml`(서버 전용) → 저장소에 있는 `.example.yml` 을 가리키게 |

**규칙 파일 죽은 링크 0건.**

### 2-3 하드코딩

| 갈래 | 결과 |
|---|---|
| 회사코드(`0000`·`0001`)·계정(`admin`) | 운영 코드에 **0건** — 시드·E2E 에만 있다 |
| 호스트·포트 | 4곳 전부 `${ENV:기본값}` · `import.meta.env ?? 기본값` — 규약대로다 |
| 양식코드 | 리터럴은 **주석뿐**이다. 코드는 접두로 판정한다 |
| 문서상태 | `lib/docStatus.ts` 한 곳이 정본. 9개 파일이 여기서 가져다 쓴다 |

### 관문

| 검사 | 결과 |
|---|---|
| `./mvnw -o compile` · `test` | 통과 |
| 백엔드 기동 | **성공** — 매퍼 XML 이동 확인 |
| `npx tsc --noEmit` | 오류 0 |
| `npx vitest run` | **130건 통과** |

---

## 3단계 — E2E 28화면 격자 (진행)

### 기준선

기존 71건을 먼저 돌려 4건 실패를 잡았다.

| 실패 | 원인 |
|---|---|
| 스모크 28화면 · document-flow · flow-approval · scenario | **내가 개선조치를 떼어낼 때 `recent-docs`·`notifications` 까지 지웠다** |
| document-flow | `getByText("문서 목록")` 이 2개에 걸렸다 — 셸이 닫은 탭을 DOM 에 남긴다 |

복원 후 4개 엔드포인트 200 확인, **71건 전부 통과**.

스모크가 28화면 전부에서 404 를 잡아냈다. 이걸 잡으려고 E2E 를 돈다.

### 새로 채운 격자

| spec | 건수 | 무엇 |
|---|---|---|
| `today-tasks.spec.ts` | **8** | KPI 5장 · 미완료 체크 · 기한경과 빨강·정렬 · 클릭/더블클릭 구분 · `?add=1` 소비 · DB 건수 대조 |
| `flow-corrective.spec.ts` | **5** | 개선조치 조회·수정(DB 대조)·삭제 차단 · **옮긴 URL 정본, 옛 경로 404** |
| `shell-grid.spec.ts` | **5** | 방향키 비제어 3화면·제어 1화면 · 패널 초록 4케이스(그리드·중첩분할·단패널 트리) |
| `docs-hwp-template` +2 | **2** | 자사양식 삭제 왕복 · 화면 우회 API 직격 차단 |
| `docs-html-template` +5 | **5** | 양식 5본 복사본 삭제 왕복 |
| `company-provisioning.spec.ts` | **6** | 신규 업체 개설 · 재실행 안전 · 로그인 · 회사 격리 |

### 결함 5 — 삭제 확인창을 아무도 안 누르고 있었다

```ts
await btn(page, "삭제").click();
const ok = btn(page, "확인");
if (await ok.count()) await ok.click();   // 없어서 안 눌림
expect(...).toBe("1");                     // "안 지워졌다" → 공짜로 통과
```

**확인창의 긍정 버튼 글자가 「확인」이 아니라 「삭제」다.** 툴바 버튼과 글자가 같아
다이얼로그 안으로 범위를 좁혀야 한다.

**결함 1(HWP 삭제 500)이 이것 때문에 안 잡혔다.** 부정만 보는 시험은 기능이 죽어도 통과한다.

`confirmDeleteBtn()` 헬퍼를 만들고 그 이유를 주석에 못 박았다.

### 화면마다 삭제 방식이 다르다 — 시험이 그걸 알아야 한다

| 화면 | 고르는 법 | 지우는 법 |
|---|---|---|
| HWP 사용양식 | 체크박스 | `tbl_company_template` · `tbl_template` **하드 삭제** |
| HTML 양식 5본 | **활성 행** (체크박스 없음) | `tbl_company_template`·`tbl_schedule_rule` 하드 + 지면 버전 `use_yn='N'` **소프트** |

양식 5본이 소프트 삭제인 데에는 까닭이 있다 — **이미 그 양식으로 쓴 문서가 지면을 잃으면
기록의 근거가 사라진다.** 시험도 그 계약을 그대로 본다(카탈로그·지면 항목이 남는 것까지).

### 내가 만든 약한 시험도 걷어냈다

처음 쓴 오늘 할 일 8건 중 3건이 「자료 없음」으로 건너뛰었다.
확인해 보니 **자료는 4건 있었고 내 시험이 조회 완료를 안 기다린 것**이었다.
건수 검사도 `shown <= 4` 라 0행에도 통과했다.

**건너뛰는 시험은 검증이 아니다.** 전부 조여서 지금은 건너뜀 0이다.

### psql 없이도 업체 개설을 검증한다

`06_company_seed.sql` 은 psql 메타명령(`\if`·`\set`·`\gset`)을 **머리말 25줄에만** 쓴다.
본문은 순수 SQL 이다. 그래서 `seedCompany()` 헬퍼가 파일을 읽어 변수만 psql 과 같은 규칙으로
바꿔 넣고 돌린다 — **시드를 베끼지 않으므로 정본은 계속 그 파일 하나다.**

기대치도 손으로 적지 않는다. 표준 업체(0000)와 **같은 수인지**만 본다.
화면이 하나 늘면 두 곳이 같이 늘어야 하고, 한쪽만 늘면 여기서 걸린다.

### 신규 업체 개설 — 6건 전부 통과

```
시드 실행           메뉴·화면권한이 표준 업체(0000)와 같은 수
관리자·부서·결재선   전부 깔림. 비밀번호는 BCrypt 해시
재실행              메뉴 수 그대로 — 두 배가 되지 않는다
로그인              새 계정으로 랜딩·사용양식관리가 열린다
회사 격리           0000 문서가 한 건도 안 보인다
회사코드 위조        본문에 0000 을 실어도 JWT 회사에만 부서가 생긴다
```

### 결재 첨부 화면 — 전송이 미검증이었다

이 화면에도 전송(상신)이 있는데 시험이 없었다. 3건을 넣었다.

```
전송      WRK → REQ (DB 로 확인)
재전송    화면 우회 API 직격도 막힌다. 400 대이지 500 이 아니다
전송취소  REQ → WRK
```

### 결함 6 — 전체 실행에서만 깨지던 시험

권한 게이트 시험이 **단독으로는 통과하고 전체 실행에서만** 깨졌다.

```
Error: page.goto: Navigation to ".../login" is interrupted by another navigation to ".../login"
```

로그아웃 버튼이 스스로 `/login` 으로 보내는데, 그 이동이 도는 중에 `login()` 이
`page.goto` 를 부른다. 앞 시험이 남긴 상태에 따라 타이밍이 갈렸다.

공용 헬퍼 `login()` 을 고쳤다 — **이미 로그인 화면이면 다시 이동하지 않는다.**
계정을 갈아타는 모든 시험이 같은 자리를 지난다.

> 단독으로 통과하고 전체에서 깨지는 시험은 그대로 두면 안 된다.
> 다음에 진짜 결함이 나도 「또 그거겠지」로 넘어간다.

### 결함 7 — 순서에 매인 시험

개선조치 수정 시험이 **앞 spec 이 만든 자료를 기다려** 건너뛰었다.
실행 순서가 바뀌면 조용히 안 돈다.

이 시험이 보려는 것은 「화면이 읽고 고치는가」이지 「개선조치가 어떻게 태어나는가」가 아니다 —
그건 `draft-all.spec` 이 본다. **시험이 스스로 한 건을 깔고 시작하게** 바꿨다.

### 28화면 × 5축 격자

R 조회 · CU 저장 · D 삭제 · X 위반 · P 권한

| 화면 | R | CU | D | X | P |
|---|:-:|:-:|:-:|:-:|:-:|
| today-tasks | O | — | — | O | O |
| schedule-cycle-management | O | O | O | O | O |
| hwp-template-management | O | O | **O** | **O** | O |
| html-form 양식 5본 | O | O | **O** | O | O |
| hyg-process · ccp-verify | O | O | O | O | O |
| ccp-pkg · htg · mtl | O | O | O | O | O |
| hwp-write | O | O | O | O | O |
| attach | O | **O** | — | **O** | O |
| sign-ready · sign-ok | O | O | — | O | O |
| document-inbox | O | 조회전용 | 조회전용 | O | O |
| corrective-action-management | **O** | **O** | **O** | **O** | O |
| sys 코드 6화면 | O | O | O | O | O |
| sys 로그 3화면 | O | 조회전용 | 조회전용 | O | O |

굵은 표시가 이번에 채운 칸이다. `—` 는 그 화면에 그 동작이 없다(오늘 할 일·결재는 문서를 만들지 않는다).

### 그 밖에 도는 것

| | |
|---|---|
| 문서 한 바퀴 | 작성 → 전송 → 반려 → 재작성 → 재전송 → 승인 → 문서함 |
| 계정 전환 | 권한 있는 계정이 연 탭이 계정을 갈아타면 닫힌다 |
| 신규 업체 | 시드 → 로그인 → 화면 → 회사 격리 |
| 셸 공통 | 방향키(제어·비제어) · 패널 활성 4케이스 |

### 3단계 관문

```
npx playwright test    →  105 passed  ·  실패 0  ·  건너뜀 0   (13.2분)
```

**71 → 105건.** 건너뛰는 시험이 하나도 없다 — 105건 전부 실제로 돈다.

| | 전 | 후 |
|---|---|---|
| E2E | 71 | **105** |
| 건너뜀 | 3 | **0** |
| spec 파일 | 16 | 21 |

---

## 4단계 — 문서 최종 정본 (완료)

### 「손으로 안 고친다」고 적혀 있었는데 생성기가 없었다

`3_화면_지도.md` 와 `5_PIPELINE_색인.md` 는 **「소스에서 뽑는다」**고 적혀 있었지만
뽑는 스크립트가 없었다. 그래서 손으로 적힌 채 굳어 있었다.

| 결함 | 무엇이 어긋났나 |
|---|---|
| PIPELINE 색인이 1:1 | 태그 하나를 여러 파일이 다는데 표는 한 줄만 실었다. `HF74` 는 파일 5곳인데 `main.tsx` 만 있었고 **새로 넣은 `mesSec.ts` 가 빠져 있었다** |
| 화면 지도가 낡음 | 이탈·개선조치를 `TaskController` 로 적고 있었다 (이번에 옮겼다) |

**생성기 2본을 만들었다.**

```sh
node scripts/gen_screen_map.mjs         # 화면 지도
node scripts/gen_pipeline_index.mjs     # PIPELINE 색인
    --check                             # 어긋나면 실패 (CI 에 걸 수 있다)
```

Node 만 있으면 된다 — FE 빌드에 이미 필요하다. **DB 접속이 없어도 돈다**:
메뉴 이름·계층은 `02_seed.sql` 의 `tbl_menu`, 화면 컴포넌트는 `screenRegistry.tsx`,
URL 은 `tabRoute.ts`, BE 는 컨트롤러 `@RequestMapping`, SP 는 `01_sp.sql` 에서 읽는다.

| | 전 | 후 |
|---|---|---|
| PIPELINE 색인 | 197줄 · 태그당 파일 1 | **570줄 · HF 85태그/267파일 · HB 79태그/272파일** |
| 화면 지도 | 손으로 적음 | **28화면 · 자기 컨트롤러 18개** 자동 생성 |

두 생성기 모두 `--check` 가 통과한다 — 지금 문서가 소스와 같다는 뜻이다.

### 낡은 수치를 실측으로 맞췄다

| 문서 | 고친 것 |
|---|---|
| `6_테스트.md` | E2E 68 → **105**, vitest 122 → **130**, 스펙 표 21본으로 다시 |
| `4_명명과_경로.md` | SP 154 → **152** + 인자·컬럼 점검 결과 |
| `README.md` · `CLAUDE.md` | E2E 68 → 105 |
| `backend/haccp-api/README.md` | E2E 71 → 105 |
| `backend/haccp-api/PIPELINE.md` | SP 154 → 152 · E2E 68 → 105 · 점검 축 추가 |
| `frontend/haccp-web/PIPELINE.md` | vitest 122 → 130 · E2E 68 → 105 |

DB 실측과 대조했다 — **53 표 / 152 SP / 43 메뉴 / 86 코드 / 47 사용양식**,
`1_시작하기.md` 에 적힌 기대치와 정확히 같다.

### 시험이 빠지기 쉬운 자리를 문서에 남겼다

`6_테스트.md` 에 이번에 겪은 함정 6가지를 적었다 — 부정만 보는 시험,
확인창 버튼 글자, 화면마다 다른 삭제 방식, 조회 완료 대기, 순서 의존, 전체에서만 깨짐.
다음 사람이 같은 자리에서 안 넘어지게 하려는 것이다.

### 결정 이력

`8_결정_이력.md` 에 이번 판단 4건을 남겼다 — 화면 API 는 자기 경로에,
없는 경로는 404, 시험은 부정만 보지 않는다, 생성 문서에는 생성기를 둔다.

### nginx — 파일 업로드 경로가 죽어 있었다

`haccp.conf.template` 의 파일 전송 location 정규식이 **없어진 경로**를 가리키고 있었다.

```
doc/documents/…  doc/templates/…  doc/audit-export/…     ← docs/ 로 바뀌었다
hyg/health-cert/…  bas/equipment/…  bas/company-templates/…  ← 지운 화면
```

여기 안 걸리면 그 요청은 일반 API 로 떨어져 **기본 70s** 로 처리된다 —
운영에서 큰 HWP 업로드가 끊겼을 것이다. 지금 URL 7개로 맞추고 정규식을 대조했다.

### 검사

| | |
|---|---|
| 문서 224본 죽은 링크 | **0건** |
| 폴더당 README 누락 | **0건** |
| 이모지 | 규칙이 예외로 적어 둔 원문 인용 2줄뿐 |
| 생성기 `--check` | 화면 지도·PIPELINE 색인 **둘 다 최신** |

