# pages/flow/ca — 이탈·개선조치

URL `/flow/ca`. 작성 화면에서 **이탈로 등록한 문서**를 모아 조치를 적는다.

| 하위 | 화면 |
|---|---|
| [`corrective/`](corrective/README.md) | `corrective-action-management` — 그리드 1개 |

이탈 등록 경로는 둘이고 둘 다 `tbl_corrective_action` 한 표로 모인다.

- HTML 작성 5화면 — 지면 하단 이탈 시그널 (`HtmlFormDeviationSignal`)
- HWP 작성 — 좌측 목록의 「이탈여부」 칸 (우측이 rhwp 편집기라 지면에 둘 자리가 없다)
