# 세션 인수인계 — 2026-08-27

다음 세션이 이 문서만 읽고 이어갈 수 있게 적는다.
**정본은 각 문서다.** 여기는 「지금 어디까지 왔고, 무엇을 조심해야 하나」만 담는다.

| 항목 | 값 |
|---|---|
| `main` | **f53c4b2** (`Merge pull request #53`) — 이 회차 작업분은 **미커밋** |
| 열린 PULL REQUEST | 없음 |
| 작업 트리 | **변경 있음** — 실무 검증 반영(아래 1절) |
| 배포 서버 | http://180.71.58.87/haccp/ — **운영 개시 전** |

---

## 1. 지금 상태 한 줄

**실무 검증 보고 두 본을 받아 재현하고, 재현된 것만 고쳤다 (2026-08-27 4회차).**

「P0·치명적」으로 올라온 12건은 **재현되지 않았다** — 그리드 클릭·더블클릭은 정상이다.
대신 결재가 담당자에게 안 가고, 승인해도 종이에 안 남고, 금속검출 일지를 아예 못 쓰던 것을 찾아 고쳤다.

**운영 DB 에는 적용했고 운영에서 결과까지 확인했다. 프론트는 Jenkins Build Now 가 남았다.**
자세한 것은 `grokbot/test/클로드종합의견.md` (git 미포함).

### 검증 스택

| 도구 | 건수 | 무엇을 보나 |
|---|---:|---|
| `tsc --noEmit` | — | 타입 |
| `eslint src e2e` | — | 0 error (warning 12) |
| `vitest run` | **144** | 순수 함수·규칙 모듈 |
| `mvnw test` | **104** | 서비스 분기·권한 판정 |
| `playwright test` | **151** | 화면→API→SP→DB 한 줄 |
| 문서 생성기 `--check` | — | 화면지도·PIPELINE 색인 |
| 죽은 링크 | **0** | 전 저장소 `.md` |

E2E 스펙은 23본.

### 이번 회차(4회차)에 고친 것

| | 무엇 | 어디 |
|---|---|---|
| 1 | 승인해도 **지면 도장칸에 결재자가 안 찍혔다** | `01_sp.sql` 상세 SP 2본이 `tbl_document_approval` 을 먼저 읽는다 |
| 2 | 지면 승인자 칸에 **`'3'`** 같은 값이 저장됐다 | 지면 서명 SP 2본이 `tbl_user` 에 없는 이름을 `45000` 으로 막는다 |
| 3 | **금속검출 일지를 한 장도 못 썼다** | `tbl_ccp_metal_sens_row.check_time` `varchar(4)`→`(10)`. `09:10` 이 5자였다 |
| 4 | 개선조치 **조치일 저장 불가** | `MesEditableGrid` 가 `type:"date"` 를 `YYYYMMDD` 로 되돌린다 |
| 5 | `OPEN` 이 화면마다 다른 이름 | 개선조치가 `CA_STATUS` 공통코드를 읽는다 |
| 6 | 마감이 `20260827 1800` 생숫자 | 기존 `toDisplayDate`·`toInputTime` 재사용 |

**결재선 `DEFAULT` 승인자를 `haccpm` 으로 바꿨다(운영 데이터).** 양식 47본 백필은 **안 했다** —
`appr_line_cd NULL → COALESCE(…,'DEFAULT')` 는 고장이 아니라 설계된 길이고, 그 끝만 바로잡으면 된다.

---

## 2. Jenkins — 지금 눌러야 하나

**눌러도 되고 안 눌러도 앱은 최신이다.** 배포 뒤 바뀐 건 전부 CI·시험·문서다.

```
Jenkinsfile · tools/q.mjs · e2e/*.spec.ts · docs/6_테스트.md · 배포후_개선점.md
```

운영 코드(`frontend/haccp-web/src/**`, `backend/haccp-api/src/main/**`)는 **한 줄도 안 바뀌었다.**

그래도 **한 번은 누르는 게 좋다.** Jenkinsfile 에 넣은 `clean` 이 실제로 도는지,
그리고 BE 집계가 **117 → 104** 로 내려오는지 확인할 수 있다.

### 「BE 117 vs 로컬 104」의 정체

실패는 0건이었고 **집계만 달랐다.** 파이프라인이 `clean` 없이 `test` 만 돌려서,
시험 클래스를 옮기면 `target/test-classes` 에 **옛 `.class` 가 남고 surefire 가 그것까지 돌린다.**
Jenkins 워크스페이스는 빌드마다 지워지지 않는다.

실제로 옮긴 시험이 셋 있다 (패키지를 URL 과 맞추면서).

| 옛 자리 | 지금 |
|---|---|
| `com.haccp.hwp.doccycle.CycleScheduleGeneratorTest` | `com.haccp.docs.sch.…` |
| `com.haccp.docs.doccycle.CycleScheduleGeneratorTest` | 〃 |
| `com.haccp.draft.hwp.HwpDraftServiceDetailTest` | `com.haccp.draft.hwpdoc.…` |

**숫자가 부푸는 것보다 이미 없는 시험이 「통과」로 잡히는 게 문제다.**
지운 기능을 보던 시험이 초록으로 남으면 그게 죽어도 아무도 모른다.
→ `./mvnw -q -B -DskipITs clean test` (PULL REQUEST #50)

---

## 3. 이번 회차에 넣은 것 (PULL REQUEST #47~#52)

### #47 — 서명 이미지 MIME · 백엔드 시험 58 → 104

**보안 결함 하나.** `uploadSign` 이 올린 쪽이 준 `Content-Type` 을 그대로 저장했다가
`inline` 으로 되돌려 줬다. 검사도 「확장자 **또는** 타입」이라 한쪽만 맞으면 통과했다.
`sign.png` 를 `Content-Type: text/html` 로 올리면 우리 도메인에서 열리는 HTML 이 됐다.

- 저장 MIME 을 **확장자에서만** 정한다 (`guessImageMime`)
- 검사를 「확장자 **그리고** 타입」으로
- `SecurityHeadersFilter` — 전 응답에 `nosniff` · `Referrer-Policy`.
  파일을 되돌려 주는 컨트롤러가 넷이라(문서첨부·양식·서명·감사PDF) **한 곳에서** 건다

시험 넷을 더했다 — `AuthServiceGuardTest`(9) · `UserServiceSignTest`(10) ·
`HtmlTemplateServiceGuardTest`(13) · `CcpLogDraftServiceSaveTest`(14).
**22가지로 깨뜨려 22건 모두 물었다.**

### #48 — 그리드 Escape 포커스 · 그리드 전 기능 시험

**Escape 로 편집을 닫으면 포커스가 `body` 로 빠져 방향키·F2·Delete 가 전부 죽었다.**
다시 마우스로 눌러야 살아났다. 키보드로 기록 치는 현장에서 손을 멈추게 하는 종류다.

- `cancelEdit` — 셀 선택을 비편집 상태로 유지하고 포커스를 그리드로 되돌린다
- 되돌리는 시점은 **`useLayoutEffect`**. `requestAnimationFrame` 으로 미뤘더니
  한 프레임이 비어 그 사이 Delete·F2 가 새 나갔다

`e2e/grid-features.spec.ts` **35건** — 정렬·다중정렬·필터·검색·열 숨김/너비/순서/틀고정·
CSV 내용·가상스크롤 · **아이디별 열 저장** · 편집 키보드 · **단축키 예의**.

**단축키는 애초에 잘 골라져 있었다** — `Ctrl`·`Meta` 조합이 코드에 하나도 없다.
쓰는 키는 화살표·Tab·Enter·F2·Delete·Backspace·Escape 뿐이다.

### #49 — 작성 5화면 판정 기본값 적합 · 「모두 적합」

현장 기록은 대부분이 적합인데 판정이 빈 값으로 시작해 행마다 라디오를 한 번 더 눌러야 했다.

- 지면을 열 때 **비어 있는 판정만** 채운다. 저장된 부적합은 안 건드린다
- 행추가로 만든 새 행도 적합으로 시작
- 지면 4본에 **「모두 적합」** 버튼 (ccp-verify 가 HygPrcPaper 를 그대로 써서 5화면 커버)

**금속검출만 화면이 미리 안 칠한다** — `sp_tbl_ccp_metal_monitor_c_000` 이
감도 5칸으로 판정을 계산한다. 미리 칠하면 저장에서 뒤집혀 보이는 값과 저장되는 값이
달라지고, 감도 확인도 안 한 행이 적합으로 남는다.
「모두 적합」은 금속에도 있고 `judgeModYn=Y` 를 같이 남긴다 — 라디오를 직접 누른 것과 같다.

### #50 · #51 · #52 — CI·시험 안정화

- **#50** Jenkins `clean` (위 2절)
- **#51** `tools/q.mjs` 가 `E2E_DB_NAME` 을 본다 — 배포 서버로 돌릴 때
  브라우저는 운영을, 검증은 시험 DB 를 읽던 것을 맞췄다
- **#52** 운영 규모·앞 회차 잔재에서만 터지던 시험 셋을 고쳤다

---

## 4. 다음 세션이 꼭 알아야 할 함정

### E2E 는 `dist/` 를 본다 — **소스만 고치고 돌리면 뜻이 없다**

`e2e/.env` 의 `E2E_BASE_URL` 이 `http://localhost:4173/haccp/` — `vite preview` 가 서빙하는 `dist/`.
**프론트를 고쳤으면 반드시 `npm run build` 하고 E2E 를 돌린다.**
이걸 몰라서 한참 헤맸다. `CLAUDE.md` 검증 절에 못 박아 뒀다.

### 배포 서버로 돌릴 때

```sh
cd frontend/haccp-web
E2E_BASE_URL=http://180.71.58.87/haccp/ \
E2E_API_BASE_URL=http://180.71.58.87 \
E2E_DB_NAME=sasshaccp \
E2E_ALLOW_PROD_WRITES=1 \
npx playwright test
```

**`E2E_DB_NAME` 을 빼면 안 된다.** 로컬 `.env` 는 시험 DB(`sasshaccp_test`)를 가리키는데
배포 서버는 운영(`sasshaccp`)을 쓴다 — 화면은 잘 되는데 검증만 「자료가 없다」로
무더기 실패한다. 그렇게 20건 넘게 헛나갔다.

`E2E_ALLOW_PROD_WRITES=1` 은 **운영 문서를 지운다.** 운영 개시 전에만 쓴다.

### 셸이 앞 탭을 mount 한 채 숨긴다

오늘 할 일 탭이 늘 남아 있어서 `page.locator(".mes-grid-wrap").first()` 가
**안 보이는 탭**의 그리드를 집는다. `:visible` 로 거른다.

### 자리를 숫자로 박으면 안 된다

- 번호 열(rownum) `th` 에는 `.mes-th-inner` 가 없어 헤더 번호가 하나 밀린다 —
  **「로그아웃 일시로 정렬하고 로그인 일시를 읽는」** 일이 실제로 벌어졌다
- 필터 행도 번호 열에는 입력이 없다
- `td:has(.mes-cell-editable)` 은 편집이 열리면 그 칸이 빠져 `.first()` 가 옆 칸을 가리킨다

전부 **열 이름으로** 찾는다.

### 그 밖

| 함정 | |
|---|---|
| 가상 스크롤 | 총 550건이어도 `tbody tr` 은 스물 몇 개. 푸터의 총/표시 건수나 DB 로 판정한다 |
| 서로 상쇄되는 조작 | 「방향키가 새는가」를 ↓↑ 로 보면 제자리라 늘 통과한다. 한 방향으로만 민다 |
| 지면이 잠깐 읽기 전용 | 저장 직후 라디오가 `disabled`. `force` 로 눌러도 아무 일이 없다 |
| 운영 규모 SQL | 코드 목록을 `IN (…)` 에 박으면 명령줄 길이를 넘겨 잘린다. 부분질의를 넘긴다 |
| 앞 회차 잔재 | 회차가 끊기면 뒷정리가 안 돈다. **시작할 때도 지운다** |
| MyBatis | 컴파일로 안 잡힌다. 패키지를 옮기면 **반드시 기동해서** 확인한다 |

---

## 5. 로컬 환경

| 포트 | 무엇 | 띄우는 법 |
|---|---|---|
| 4173 | `vite preview` (dist) — **E2E 대상** | `npx vite preview --host localhost --port 4173 --strictPort` |
| 7070 | 백엔드 API | `backend/haccp-api` 에서 `./mvnw spring-boot:run` |

**`./mvnw -q -o test`(오프라인) 은 지금 된다** — 2026-08-27 확인, 104건 전부 통과.
앞 회차에 없던 surefire·spring-beans 가 로컬 저장소에 채워졌다.

**`dist/` 잠김** — `vite preview` 가 켜져 있으면 `npm run build` 가 `dist` 를 못 비운다.
빌드 전에 4173 을 끄고, 끝나면 다시 켠다.
**빌드가 `tsc` 오류로 멈춰도 앞 `dist` 가 그대로 남는다** — 그걸 모르면 고치기 전 화면을 시험하게 된다.
시험 결과가 이상하면 `dist/assets/*.js` 시각과 바꾼 문구를 직접 확인한다.

**`netstat` 으로 4173 을 볼 때는 IPv6 도 본다** — `vite preview` 가 `[::1]:4173` 로 붙어서
`netstat -p tcp | grep LISTENING` 만 보면 「안 떠 있다」로 잘못 읽는다.

---

## 6. DB

| | |
|---|---|
| 호스트 | `180.71.58.87:5432`, 스키마 `sasshaccp` |
| 운영 | `sasshaccp` |
| 시험 | `sasshaccp_test` ← 로컬 `backend/haccp-api/.env` 가 이걸 가리킨다 |

`tools/q.mjs` 로 읽는다. 우선순위는 `--db` > `E2E_DB_NAME` > `.env` 의 `DB_NAME`.

```sh
node tools/q.mjs "SELECT count(*) FROM tbl_document"
E2E_DB_NAME=sasshaccp node tools/q.mjs "…"     # 운영을 볼 때
```

**자료를 지우는 헬퍼는 DB 이름이 `_test` 로 끝나야 돈다.**
`resetDocuments` · `purgeCompany` · `seedCompany` — 아니면 멈춘다.
`E2E_ALLOW_PROD_WRITES=1` 로만 열린다.

### 시험 DB 를 새로 깔 때

```sh
node tools/seed_test_db.mjs        # 6본 + tools/seed_test_forms.sql
```

시드 6본은 **표준 지면**까지만 깐다. HTML 작성 5화면은 **회사 지면 버전**(`tbl_*_ver`)을
읽는데 그건 안 만든다 — `seed_test_forms.sql` 이 복사 SP 를 불러 그 상태까지 만든다.

---

## 7. 백업

```sh
bash scripts/backup_export.sh /mnt/backup            # 로컬 경로
bash scripts/backup_export.sh user@host:/srv/backup  # 원격
KEEP_DAYS=14  DRY_RUN=1                              # 보관 일수 · 시늉만
```

**DB 덤프와 파일 tar 를 짝으로 뺀다.** 하나만 있으면 복구가 반쪽이다.

되돌리기는 `scripts/db_restore.sh` — `RESTORE_CONFIRM=<dbname>` 이 있어야 돈다.
**빼기 → 되돌리기 → 앱 기동**을 실제로 해봤다 (`운영.md` 12.1·12.2).

---

## 8. 남은 것 — 판단이 필요하다

### 지금 당장 남은 것 (4회차 뒤)

| | 무엇 |
|---|---|
| 1 | **Jenkins Build Now** — 이번엔 운영 코드를 고쳤다. 안 누르면 배포 서버에 프론트가 반영 안 된다 |
| 2 | `haccpm`·`hacppt` **서명 등록** (사용자관리 → 서명). 없으면 종이에 이름 글자만 나온다 |
| 3 | 시험 계정 정리 — `e2erw`·`master`·`smoke`·`test`·`write` 5개 전부 `use_yn='Y'` |
| 4 | 운영 문서 587 의 지면 승인자 `'3'` — 화면 표시는 고쳐졌지만 저장값은 그대로다. 다시 저장하면 이제 막힌다 |
| 5 | **막 테스트** — 팀원 작성 → 전송 → 팀장 결재대기 → 승인 → 종이에 서명. 이 고리는 아직 한 번도 안 닫혔다 |

### 안 고치고 남긴 것

| | |
|---|---|
| **그리드 `Escape` 는 「취소」가 아니라 「닫기」** | 타이핑이 곧바로 행 버퍼에 들어가고 Escape 는 편집칸만 닫는다. **친 값은 남고** 행은 「변경」으로 표시된다. 엑셀과 다르다. 되돌리려면 편집 열 때 원래 값을 떠 놔야 한다 — 동작이 바뀌는 일이라 **실제 동작을 시험으로 못 박고** README 에 적어만 뒀다 |
| **번들 분할** | 사용자가 「안 해도 된다」고 했다 |
| **Let's Encrypt** | 사용자가 나중에 직접 한다고 했다. 지금은 자체서명이라 rhwp 편집기 ServiceWorker 가 등록 안 된다 |

### 완성도 (개발자 관점, 2026-08-26 평가)

| | |
|---|---|
| 코드·구조 | 85% |
| 시험 체계 | 70% → 지금은 더 높다 (BE 104 · E2E 150) |
| 운영 준비 | 50% → 백업 되돌리기까지 해봐서 더 높다 |
| **업무 검증** | **10%** — 현장 절차대로 써 본 적이 없다 |

**업무 검증이 제일 낮다.** 시험은 「기능이 도는가」를 보지 「업무가 맞는가」를 못 본다.
HACCP 은 서류가 법으로 정해져 있어서, 실제 담당자가 한 달 치를 쳐 보기 전엔 모른다.

### 시험이 아직 얇은 곳

서비스 24본 중 **9본**에 단위 시험이 닿는다. 남은 15본은 대부분 SP 를 그대로 부르는
얇은 통로고 E2E 150건이 그 길을 지난다.

---

## 9. 이 저장소에서 특히 자주 어긋나는 것

`CLAUDE.md` 에 있는 것이지만 다시 적는다.

- **한 화면 = 여러 층.** FE 라우트·레지스트리, BE 패키지·매퍼, `ScreenAuthResolver`,
  DB `tbl_screen`·`tbl_role_screen`·`tbl_menu` 가 같이 움직인다
- **`scrnCd`·`persistId` 는 고정.** 폴더·URL 이 바뀌어도 안 바꾼다.
  `persistId` 를 바꾸면 그 화면 사용자 전원의 열 너비·숨김이 초기화된다 (`grid_id` 30자 제한)
- **공통코드 `sub_cd` 는 저장값과 같은 표기.** 코드만 올리고 데이터를 안 올리면 콤보가 빈다
- **이모지 금지 · 한국어 · git commit/push 는 사용자가 말할 때만**
- **폴더를 새로 만들면 `README.md` 를 같이 만든다**

---

## 10. 사용자 선호

| | |
|---|---|
| 호칭 | **"PR" 로 줄이지 말고 "PULL REQUEST" 로 풀어 쓴다** |
| 시험 | **「시험 넘기지마라」** — 통과만 보고 넘기지 말고 **깨뜨려서** 무는지 확인한다 |
| 진행 | 중간에 멈춰 보고하지 말고 **끝까지 이어서** 한다 |
| `.env` | 실사용 값으로 쓴다. 보안은 지금 신경 안 쓰고 **git 에만 안 올린다** |

---

## 11. 어디를 먼저 보나

| 하려는 일 | 볼 곳 |
|---|---|
| 작업 순서 | [`CLAUDE.md`](CLAUDE.md) → `.cursor/rules/00-bootstrap.mdc` |
| 이 프로젝트가 뭔지 | [`docs/1_시작하기.md`](docs/1_시작하기.md) |
| 코드가 도는 순서 | `backend/haccp-api/PIPELINE.md` · `frontend/haccp-web/PIPELINE.md` |
| 화면 하나 만들기 | [`docs/2_화면_추가하기.md`](docs/2_화면_추가하기.md) |
| 지금 있는 화면 | [`docs/3_화면_지도.md`](docs/3_화면_지도.md) — **생성기가 만든다** |
| 시험 · 함정 | [`docs/6_테스트.md`](docs/6_테스트.md) |
| 보안·파일·서명 | [`docs/7_보안과_파일.md`](docs/7_보안과_파일.md) |
| 왜 이렇게 돼 있나 | [`docs/8_결정_이력.md`](docs/8_결정_이력.md) |
| 이번 회차에 고친 것 | [`배포후_개선점.md`](배포후_개선점.md) — A~M 절 |
| 배포·운영·백업 | [`운영.md`](운영.md) |
| 그리드 | `frontend/haccp-web/src/components/grid/README.md` |
| 지면·판정 | `frontend/haccp-web/src/components/form/README.md` |
