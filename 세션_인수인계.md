# 세션 인수인계 — 2026-09-03

다음 세션이 이 문서만 읽고 이어갈 수 있게 적는다.
**정본은 각 문서다.** 여기는 「지금 어디까지 왔고, 무엇을 조심해야 하나」만 담는다.

| 항목 | 값 |
|---|---|
| `main` | **c8c4b15** (`Merge pull request #94` — 문서주기 시작일·E2E 회사) |
| 마지막 회차 | [#94](https://github.com/k971230/haccp/pull/94) (#93 board 분할·일정 캘린더·비밀번호 변경 뒤). 운영 반영은 Jenkins `haccp-deploy` **Build Now**(사용자) |
| 작업 트리 | 깨끗 |
| 배포 서버 | **`http://180.71.58.87/haccp/`** — 운영 개시 전. **`https` 로 열지 않는다**(아래 9절) |
| 지금 단계 | **개발은 어느 정도 끝났다.** 다음은 매뉴얼과 현장 검증이다 |

---

## 1. 한 줄

**팀원이 쓰고 팀장이 결재하고 종이에 결재자가 남는 고리가 두 업체에서 돈다.**
데모식품(0000)·별담푸드(0001) 둘 다 작성 → 전송 → 결재대기 → 승인 → 문서함까지 실제로 돌렸다.

남은 것은 **매뉴얼**과 **현장 담당자가 한 달 치를 쳐 보는 것**이다.

---

## 2. 접속

| | |
|---|---|
| 주소 | http://180.71.58.87/haccp/ |
| 로컬 | 4173 (`vite preview` — dist) · 7070 (API) |

### 계정 (비밀번호 전부 `1234`)

| 회사 | 아이디 | 이름 | 권한그룹 | 무엇을 하나 |
|---|---|---|---|---|
| 0000 데모식품 | `admin` | 시스템관리자 | ADMIN | 전권. 시스템 메뉴까지 |
| | `haccpm` | 해썹팀장 | HACCP_MASTER | **결재한다** |
| | `hacppt` | 해썹팀원 | HACCP_TEAM | **작성한다.** 시스템 메뉴 없음(403) |
| | `smoke` | 스모크조회 | VIEWER | 조회 전용 — Jenkins 배포 스모크가 쓴다. **끄지 말 것** |
| 0001 별담푸드 | `admin0001` | 시스템관리자 | ADMIN | 개설 관리자 |
| | `bdsys` | 별담팀장 | HACCP_MASTER | 결재 |
| | `bdusr` | 별담팀원 | HACCP_TEAM | 작성 |

**권한그룹이 화면을 가른다.** 팀원으로 로그인하면 사이드바에 시스템이 없다 —
매뉴얼에서 「관리자만 보이는 화면」을 가를 때 이 계정으로 확인한다.

> `e2erw`·`master`·`test`·`write` 는 껐다(`use_yn='N'`). 지우지 않은 이유는 감사로그 참조 때문이다.
> `Jenkinsfile.e2e`(배포 서버 상대 E2E)를 돌리려면 `write`·`e2erw` 를 되살려야 한다.

---

## 3. 화면 전수 — 매뉴얼용 URL

**29화면.** 주소는 전부 `http://180.71.58.87/haccp/` 뒤에 붙는다
(예: `.../haccp/board/today-tasks`). 라우터 경로에 `/haccp` 를 **또 넣지 않는다.**

`scrnCd` 는 **화면의 고정 식별자**다. 폴더·URL 이 바뀌어도 이 값은 안 바꾼다 —
권한(`tbl_role_screen`)·메뉴(`tbl_menu`)·이용통계가 이 값으로 붙어 있다.

비밀번호는 푸터에서 **본인만** 바꾼다 — `PasswordChangeModal` → `POST /api/v1/auth/change-password`.

### 게시판 (2)

| 중분류 | 화면 | `scrnCd` | URL |
|---|---|---|---|
| — | **오늘 할 일·대시보드** | `today-tasks` | `/board/today-tasks` |
| — | **일정 캘린더** | `calendar` | `/board/calendar` |

### 양식작성 (6)

| 중분류 | 화면 | `scrnCd` | URL |
|---|---|---|---|
| HTML 양식 | **일반위생·공정점검 작성** | `hyg-process` | `/draft/html/hyg-process` |
| HTML 양식 | **CCP 검증점검표 작성** | `ccp-verify` | `/draft/html/ccp-verify` |
| CCP 공정모니터링 | **CCP 포장공정 작성** | `ccp-pkg` | `/draft/ccp-monitoring/ccp-pkg` |
| CCP 공정모니터링 | **CCP 가열공정 작성** | `ccp-htg` | `/draft/ccp-monitoring/ccp-htg` |
| CCP 공정모니터링 | **CCP 금속검출공정 작성** | `ccp-mtl` | `/draft/ccp-monitoring/ccp-mtl` |
| HWP 양식 | **HWP 작성** | `hwp-write` | `/draft/hwp-doc/hwp-write` |

### 문서 현황·결재 (5)

| 중분류 | 화면 | `scrnCd` | URL |
|---|---|---|---|
| 결재 | **결재대기** | `sign-ready` | `/flow/appr/sign-ready` |
| 결재 | **결재완료** | `sign-ok` | `/flow/appr/sign-ok` |
| 결재 | **결재첨부** | `attach` | `/flow/appr/attach` |
| 문서함 | **문서함** | `document-inbox` | `/flow/box/document-inbox` |
| 이탈·개선조치 | **이탈·개선조치** | `corrective-action-management` | `/flow/ca/corrective-action-management` |

### 문서 (7)

| 중분류 | 화면 | `scrnCd` | URL |
|---|---|---|---|
| 작성 문서·주기 | **문서주기관리** | `schedule-cycle-management` | `/docs/sch/schedule-cycle-management` |
| HTML·양식 원본 | **일반위생·공정점검 양식관리** | `hyg-process-template` | `/docs/html-form/hyg-process-template` |
| HTML·양식 원본 | **CCP 검증점검표 양식관리** | `ccp-verify-template` | `/docs/html-form/ccp-verify-template` |
| HTML·양식 원본 | **CCP 포장공정 일지관리** | `ccp-pkg-template` | `/docs/html-form/ccp-pkg-template` |
| HTML·양식 원본 | **CCP 가열공정 일지관리** | `ccp-htg-template` | `/docs/html-form/ccp-htg-template` |
| HTML·양식 원본 | **CCP 금속검출공정 일지관리** | `ccp-mtl-template` | `/docs/html-form/ccp-mtl-template` |
| HWP·양식 원본 | **사용양식관리** | `hwp-template-management` | `/docs/hwp/hwp-template-management` |

### 시스템 (9) — 관리자만 보인다

| 중분류 | 화면 | `scrnCd` | URL |
|---|---|---|---|
| 권한·사용자·코드 | **사용자관리** | `user-management` | `/sys/code/user-management` |
| 권한·사용자·코드 | **부서관리** | `department-management` | `/sys/code/department-management` |
| 권한·사용자·코드 | **권한그룹관리** | `role-management` | `/sys/code/role-management` |
| 권한·사용자·코드 | **메뉴관리** | `menu-management` | `/sys/code/menu-management` |
| 권한·사용자·코드 | **공통코드관리** | `common-code-management` | `/sys/code/common-code-management` |
| 권한·사용자·코드 | **결재선관리** | `approval-line-management` | `/sys/code/approval-line-management` |
| 이력·통계 | **로그인 이력** | `login-history` | `/sys/logs/login-history` |
| 이력·통계 | **변경 감사 로그** | `audit-log` | `/sys/logs/audit-log` |
| 이력·통계 | **화면 이용 통계** | `screen-usage-statistics` | `/sys/logs/screen-usage-statistics` |

> 이 표는 [`docs/3_화면_지도.md`](docs/3_화면_지도.md) 에서 뽑았다.
> 그 문서는 **생성기가 만든다** — FE 파일·BE 컨트롤러·전용 SP 까지 한 줄에 있다.
> 화면이 늘면 `node scripts/gen_screen_map.mjs` 를 돌린다. **손으로 고치지 않는다.**

---

## 4. 업무 한 바퀴 — 매뉴얼 뼈대

매뉴얼은 화면 순서가 아니라 **일하는 순서**로 쓰는 편이 읽힌다.

### 준비 (관리자, 처음 한 번)

| # | 무엇 | 어디 |
|---|---|---|
| 1 | 부서·사용자 만들기 | `/sys/code/department-management` · `/sys/code/user-management` |
| 2 | **서명 등록** | `/sys/code/user-management` → 서명 열 |
| 3 | **결재선 승인자를 팀장으로** | `/sys/code/approval-line-management` |
| 4 | 쓸 양식 고르기 | `/docs/hwp/hwp-template-management` · `/docs/html-form/*-template` |
| 5 | 작성 주기 걸기 | `/docs/sch/schedule-cycle-management` |

**2·3 을 빼먹으면 나중에 반드시 막힌다.** 서명이 없으면 종이에 이름 글자만 나오고,
결재선이 개설 관리자로 남아 있으면 팀원이 올린 일지가 팀장 함에 안 온다.
결재선관리 화면이 그 상태를 경고한다.

### 매일 (팀원)

| # | 무엇 | 어디 |
|---|---|---|
| 1 | 오늘 할 일 확인 | `/board/today-tasks` — 행을 **더블클릭**하면 그 작성 화면으로 간다 |
| 1b | 월간 일정 | `/board/calendar` — 담당·밀림·완료를 칸에서 본다. 주말·공휴일을 영업일로 바꿀 수 있다 |
| 2 | 행추가 → 양식 선택 → **좌측 저장** | 작성 화면. 저장해야 우측 지면이 열린다 |
| 3 | 지면 채우기 · 「모두 적합」 | **판정만 채운다.** 온도·시각은 직접 넣는다 |
| 4 | 작성 후 저장 → 전송 | 필수값이 비면 **그 칸으로 커서가 간다** |

### 결재 (팀장)

| # | 무엇 | 어디 |
|---|---|---|
| 1 | 결재대기에서 문서 고르기 | `/flow/appr/sign-ready` |
| 2 | 미리보기로 지면 확인 → 승인·반려 | 같은 화면 |
| 3 | 승인한 문서 보기 | `/flow/appr/sign-ok` · `/flow/box/document-inbox` |

**문서함은 승인완료만 담는다.** 작성중·승인요청 문서는 작성 화면이나 결재첨부에 있다.
비어 있으면 화면이 그 이유를 적어 준다.

### 이탈이 났을 때

지면 하단 **「한계기준 이탈내용」**(일반위생은 「특이사항」)에 글자를 쓰면
**개선조치가 자동으로 생긴다.** 이탈이 없으면 비워 둔다 —
「없음」이라고 써도 한 건으로 잡힌다. 칸에 마우스를 올리면 그 규칙이 뜬다.
조치는 `/flow/ca/corrective-action-management` 에서 적는다.

---

## 5. 검증

```sh
# 프론트 — E2E 는 dist 를 본다. 소스를 고쳤으면 반드시 다시 빌드한다
cd frontend/haccp-web
npx tsc --noEmit ; npx eslint src e2e ; npx vitest run ; npm run build
npm run preview &            # 4173 에 dist/ 를 띄운다 — 빼면 전부 접속 실패
npx playwright test

# 백엔드
cd backend/haccp-api ; ./mvnw -q -o test

# 생성 문서 — 소스를 고쳤으면 표도 다시 뽑는다
bash scripts/audit_generated_docs.sh
```

| 도구 | 무엇을 보나 | 건수 |
|---|---|---|
| `vitest run` | 순수 함수·규칙 모듈 | 러너로 센다 |
| `playwright test` | 화면→API→SP→DB 한 줄 (스펙 23본) | `--list` 로 센다 |
| `mvnw -o test` | 서비스 분기·권한 판정 | 러너로 센다 |
| 죽은 링크 · 생성 문서 | `audit_docs_links` · `audit_generated_docs` | 0 · 최신 |

**시험을 통과만 보고 넘기지 않는다.** 새 시험은 반드시 **깨뜨려서 무는지** 확인한다.
이번 회차에 그렇게 해서 「통과하는데 실은 안 잡는 시험」을 여러 번 걸렀다.

---

## 6. 이번 회차에 고친 것 (PULL REQUEST #54~#67)

실사용자 관점 검증 보고 네 본(`따가리`·`부하`·`따까리RE`·`부하RE`)을 재현해서
**재현된 것만** 고쳤다.

| | 무엇 |
|---|---|
| #54 | 결재가 담당자에게 안 감 · 승인해도 종이에 안 남음 · 저장이 막히던 컬럼 폭 셋 |
| #55 | 전송 필수값이 그 칸으로 이동 · 활성 탭 따라오기 · `Invalid Date` · 결재 차수 |
| #56 | 배포 뒤 배포 서버 전수 검증 기록 |
| #57 | **SP 색인 생성기** · 없는 문서를 가리키던 참조 정리 |
| #59 | **업체 개설 시 회사 지면까지** (`07_company_forms.sql`) |
| #60 | **테이블 레이아웃 생성기** — 표 53 · 엑셀 55시트 |
| #61 | 사용양식관리 검색에 구분·사용여부 |
| #62 | HWP 편집기 **도구함 노출** · 늦은 응답이 문서를 덮는 것 차단 |
| #63 | **숨은 탭이 남의 URL 쿼리를 먹던 것** · 날짜 검증 · 저장 범위 · 오늘 할 일 상태 |
| #64 | 그리드 **「열 초기화」** — 열 순서를 되돌릴 길이 없었다 |
| #65 | **알림을 만드는 곳을 둘에서 하나로.** 지연분이 날마다 다시 쌓이고, 한 문장이 중복 행을 넣고, 조회가 쓰기를 하던 것 |
| #66 | **공용 양식 목록을 화면 권한에서 뗐다** — 조회 전용이 이탈·개선조치에서 양식 콤보가 비었다 |
| #67 | **지면 자리 번호를 보내온 순서로** — 겹친 `sortNo` 가 409 「동시 처리 중」으로 둔갑했다 (병합 대기) |

### #67 뒤에 들어간 것 (#75~#84 · 직접 병합 3본)

여기부터는 **현장에서 나온 단발 신고를 그때그때 고친 것**이다. 배포가 필요하다.

| 병합 | 무엇 | 사용자에게 보이는 변화 |
|---|---|---|
| #75 · #84 | 감사 로그가 **화면코드로 남는다** | 문서 한 장이 여러 화면에 걸릴 때 승인이 엉뚱하게 문서함에 붙던 것이 사라진다 |
| #76 | 양식을 복사해도 **주기를 만들지 않는다** | 시작일 없는 주기 행이 00:05 배치를 죽이던 것. 주기는 문서주기 화면에서만 만든다 |
| #78 | CCP 지면 도장칸에 **작성자·승인자**가 찍힌다 | 포장·가열·금속 문서함 미리보기가 비어 보이던 것. HWP 표준양식은 **38종 → 27종** (볼륨에 실물 없던 11종을 걷어냈다) |
| #79 | 팀원 403 해소 · 양식 목록이 **자사만** · 문서함·결재 검색줄 통일 | 작성 계정이 사용자관리 권한 없이 서명·양식·주기 콤보를 쓴다 |
| #81 | 주기 말일 · 이탈 체크 · HWP 기본양식 불러오기·초기화 | 체크만 켜고 저장하면 이탈내용이 깜빡이던 것 |
| #83 · 7db3596 | 생성 문서를 소스에 다시 맞춤 | 없음 (nightly audit 초록) |
| `ccp-draft-ops` | **「모두 적합」이 부적합 O/X 까지 덮게** · 통과시간 시·분 콤보가 한쪽만 골라도 안 리셋 · 반려·취소 사유를 첨부 화면에 노출 · 작성 목록 비고·노란 행 | 매뉴얼에 없는 새 동작이다 |
| `rhwp-editor-0.8.4` | `@rhwp/editor` 0.8.4 | PDF CLI 볼륨은 이 커밋에 없다 |
| `draft-title-and-split` | 작성 목록 칸이 **`tbl_document.title`** · 본문 저장이 제목을 안 덮음 · **결재 4화면 좌우 분할** · 시각을 `type=time` 으로 | 목록 제목·검색이 맞아지고, 결재 화면에 드래그 핸들이 생긴다 |

**시각 칸 AM/PM 세그먼트 문제는 여기서 닫혔다** — `type=time` 으로 되돌려 키패드 입력이 된다.

### #93 — board 분할 · 일정 캘린더 · 비밀번호 변경 (2026-09-03)

`today-tasks` 를 `/board/{scrnCd}` 로 맞추고 일정 캘린더를 붙였다. `scrnCd`·`persistId` 는 그대로.
비밀번호 변경은 푸터에서 본인만. 라이브·로컬 SP 는 `01_sp.sql` 해당 본만 `CREATE OR REPLACE`. `apply-all` 은 빈 DB 전용.
운영 반영은 Jenkins `haccp-deploy` **Build Now**. API 는 매퍼·경로가 바뀌었으면 IntelliJ 재시작.

### 알림은 이제 한 곳에서만 만든다

`DocumentAlarmScheduler`(10분) → `sp_tbl_notification_task_c_000` **하나뿐이다.**
과제당 한 번(`alarm_send_yn` 으로 잠근다)이고, **30일 로그인 없는 회사는 건너뛴다.**
일일 배치·화면 조회는 이제 알림을 만들지 않는다 — **여기 말고 다른 데서 `tbl_notification` 에 넣지 않는다.**

**보는 화면은 아직 없다.** `listNotifications` API 는 있고 부르는 화면이 0건이다.
알림함을 만들지, 텔레그램을 붙일지, 스케줄러를 끌지 **정해야 한다.**

### 재현되지 않아 **안 고친 것**

그리드 클릭·더블클릭 계열은 로컬·배포 서버 양쪽에서 **좌표 클릭까지** 다시 재도
한 번도 재현되지 않았다. 고치면 통과 중인 `grid-features` 35건을 깨면서
없는 병을 고치는 셈이라 그대로 뒀다.
근거는 `grokbot/test/클로드종합의견.md` (git 미포함).

---

## 7. 남은 일

| | 무엇 | 누가 |
|---|---|---|
| 1 | **매뉴얼 작성** | 이 문서 3·4절이 뼈대다 |
| 2 | `haccpm`·`hacppt`·`bdsys`·`bdusr` **서명 등록** | 사용자. 실제 사람의 서명이라 대신 못 만든다 |
| 3 | **현장 담당자가 한 달 치를 쳐 본다** | 사람. 완성도가 여기서 제일 낮다 |
| 4 | Let's Encrypt | 사용자가 직접. 지금은 자체서명이라 rhwp ServiceWorker 가 안 붙는다 |
| 5 | `E2E_ALLOW_PROD_WRITES` 차단 장치 | 운영 개시 시점에 끄는 게 지금은 사람 기억뿐이다 |
| 6 | **알림을 어떻게 할지 정한다** | 사용자. 알림함·텔레그램·스케줄러 끄기 셋 중 하나 |

### 다음 회차에 볼 만한 것

- 전송 필수값 **토스트가 2.6초** 만에 사라진다 — 사람이 세 번 못 봤다 (`VITE_TOAST_DURATION_MS`)
- 「모두 적합」이 **이름이 「다 됐다」로 읽힌다** (판정 + 부적합 O/X 까지 채우게 바뀌었다)
- HWP 양식 27종 중 실제로 써 본 것은 두어 종뿐이다
- **알림 크론이 `LATE` 과제를 영영 안 집는다.** 조건이 `status IN ('TODO','ING')` 이라
  서버가 마감 전후로 오래 죽어 있으면 그 과제는 알림 없이 지나가고 `alarm_send_yn='N'` 으로 남는다.
  운영에 실물 1건(idx 693, 8/24). 해롭진 않고 「보낸 적 없는데 앞으로도 안 보낼 것」이 쌓인다

### 2026-09-02 전수 감사에서 나온 것

정본·시험 DB·운영 DB 를 **본문 단위로 대조**했다. SP 155본 md5 일치 · 스키마 53표 769컬럼 일치 ·
인덱스 146본 일치 · 참조 무결성 9종 0건 · 매퍼 XML↔인터페이스 짝 전부 맞음 · 화면 28본 DB↔FE 일치.
**뼈대는 어긋나지 않았다.** 남은 셋은 판단이 필요해 안 고쳤다.

0. **(2026-09-03 처리함) 쌍둥이 클래스 3쌍을 합쳤다** — 아래 1번은 이제 옛 기록이다.
   `HtmlDraftControllerBase` + `HtmlDraftService`(Family HYG·CHK) + `HtmlDraftMapper` 하나로 접고,
   두 컨트롤러는 URL 과 양식군만 선언하는 껍데기로 남겼다. 옛 파일 6개(Service·Mapper·XML ×2)는 지웠다.
   **SP 이름과 목록 인자 형태 차이는 XML `<choose>` 안에만** 둔다 — `CcpLogDraftMapper` 와 같은 방식이다.
   검증: 기동해서 두 화면 엔드포인트 6개를 직접 호출(조회·상세·저장·삭제까지) · `mvnw` 114 ·
   해당 E2E 6본 **30/30**. 자세한 것은 `com/haccp/draft/html/README.md`.

1. **(옛 기록) 쌍둥이 클래스 3쌍** — `draft/html` 의 위생공정 ↔ CCP검증점검이 이름을 치환하면
   Service 224줄 중 17줄, Controller 146줄 중 15줄, Mapper 161줄 중 20줄만 다르다.
   실제 차이는 상수 둘(`STD_TMPL_CD`·`USR_TMPL_PREFIX`)과 주석뿐이다.
   **지금은 둘이 동기 상태라 물지 않았다.** 다만 이 저장소는 같은 상황에 이미 답을 써 뒀다 —
   `CcpLogDraftControllerBase` 머리 주석: *"화면마다 138줄을 복제하면 한 곳을 고칠 때 다른 하나가 조용히 어긋난다."*
   그 규칙을 CCP 계열엔 적용하고 여기엔 안 했다. **단발 수정이 갈라질 자리가 여기다.**

   합칠 때 알아 둘 것 — **자바는 거의 같은데 SQL 은 갈린다.**
   목록 SP 인자가 `sp_tbl_hyg_process_r_000` 10개 · `sp_ccp_verify_r_000` 8개로 다르다.
   그래서 매퍼는 한 인터페이스로 묶되 XML 은 문장마다 `<choose>` 가지를 둬야 한다 —
   `CcpLogDraftMapper.xml` 이 포장·가열에 이미 그렇게 해 뒀으니 그 모양을 그대로 따르면 된다.
   두 화면의 저장 경로를 건드리므로 **따로 한 건으로 올리고 E2E 를 한 바퀴 돌린다.**
   지금 이 두 화면의 E2E(`draft-all`·`draft-judge-default`)는 초록이라 기준선은 있다.
2. **자동 제목 모양이 화면마다 다르다** (사용자 눈에 보인다) — 목록 제목을 비우고 저장하면
   위생·검증·한글은 `양식명 (2026-09-02)`, **금속검출은 `양식명 (20260902)`**,
   **포장·가열은 일자가 아예 안 붙는다.** 운영에 세 모양이 같이 있다.
   고칠 자리는 SP 두 곳뿐이다 — `sp_tbl_ccp_metal_monitor_c_000` 의
   `v_auto := v_name || ' (' || p_base_dt || ')'` 를 `substr` 로 끊고,
   `sp_tbl_ccp_generic_monitor_c_000` 은 `v_title` 에 일자를 안 붙인다.
   나머지 셋(`sp_tbl_hyg_process_c_000`·`sp_ccp_verify_c_000`·`sp_tbl_hwp_document_c_000`)이 정본 모양이다
3. **죽은 파일** — 없다. `components/form/DocFormLayout.tsx`(178줄, 아무 데서도 안 부르던 옛 작성 셸)는
   지웠다. `components/form/` 에 남은 것은 전부 쓰인다
4. **잔털** — `lib/docKind.ts` 만 PIPELINE 태그가 없다.
   `sp_hwp_template_management_ensure_default_000` 만 명명규칙(`_{r|c|d|u}_NNN`) 밖이다(내부 헬퍼라 무해)

`tbl_company_template_file` 에 부모 없는 3행이 있는데 **결함이 아니다** —
`sp_tbl_company_template_d_000` 이 *"파일 이력은 논리 삭제 — 물리 파일과 감사 추적을 남긴다"* 로 일부러 남긴다.

### 2026-09-03 E2E 로 나온 것 — 전부 닫았다

8-29 6회차는 153/153 초록이었다. 그 뒤 병합 10본이 들어가며 **23건이 깨졌다.**
23 → 12 → 6 → 5 → **0**. 최종 **153 전부 통과 · 실패 0 · 건너뜀 0 (14.7분).**

**고쳤다**

1. **잠긴 셀 버튼이 클릭을 삼켰다** (`MesEditableGrid`) — 저장된 행 한가운데에 있는
   잠긴 「양식 선택」 버튼이 `disabled` 라 클릭을 먹어 **행 선택이 안 됐다.**
   상세 요청도 콘솔 오류도 없이 조용히 아무 일도 안 났다.
   `document.elementFromPoint` 로 행 가운데를 찍어 확인했다 — `BUTTON.mes-cell-btn-locked`.
   **제목 칸이 생기며 행 가운데가 그 버튼 위로 옮겨간 것**이 여러 시험이 한꺼번에 깨진 이유다.
   잠겼을 때 `pointer-events-none` 을 줘서 클릭이 셀로 통과하게 했다.
   > 처음엔 `disabled` 를 빼고 잠금 안내를 띄우게 고쳤는데, 그 **모달 backdrop 이 뒤 클릭을 전부 막아**
   > 실패가 6 → 10 으로 늘었다. 되돌리고 통과 방식으로 바꿨다. 안내는 띄우지 않는다
2. **rhwp 호스트 가드를 공용으로** — `waitForHostSize` 가 `HwpEditorPane` 에만 있고
   사용양식관리는 자기 호스트라 빠져 있었다. `lib/rhwpStudio.ts` 로 옮겨 둘이 같이 쓴다
3. **E2E 도우미가 낡았다** (`e2e/helpers.ts`) — 양식 선택 팝업은 `onRowClick` 으로
   **한 번 클릭에 확정·닫힘**인데 `dblclick` 을 했다. 두 번째 클릭이 닫힌 팝업 아래
   좌측 목록 첫 행의 셀 버튼에 떨어져 팝업이 다시 열렸다. 목록에 행이 둘 이상일 때만 났다.
   또 행추가가 스스로 여는 팝업 위에서 셀 버튼을 다시 눌러 대상이 옛 행으로 바뀌던 것도 막았다

**고쳤다 (이어서)**

4. **지면이 적재되는 동안 친 값이 조용히 사라졌다** (진짜 결함, 사용자에게 보인다).
   좌측 저장 직후 행 키가 임시키(`__new_*`)에서 서버키로 바뀌며 지면 버퍼가 갈린다.
   그 사이(1~3초) 지면 입력칸은 **이미 열려 있는데** 친 값은 어디에도 안 남는다.
   재현: 저장 직후 곧바로 시각을 치고 「작성 후 저장」 → `tbl_ccp_generic_monitor_row.check_time` 이 NULL.
   같은 절차에서 `networkidle` + 3초를 두고 치면 `checkTime` 이 정상으로 실린다(요청 본문으로 확인).
   `draft-all` 「지면에 넣은 값은 새로고침해도 남는다」 외 4본이 여기 걸렸다.
   **HACCP 기록의 시각이 말없이 비는 경로였다.**
   → **선택이 끝날 때까지 지면을 잠그는 쪽으로 막았다** (`HtmlFormDraftPage` 의 `selecting`).
   값을 뒤늦게 되살리는 대신 애초에 못 치게 한다 — 잃는 입력이 없다.
   덩달아 E2E 도우미 `fillPaperRequired` 도 지면이 열릴 때까지 기다리게 고쳤다.
   안 기다리면 `:not([disabled])` 가 한 칸도 못 잡아 **조용히 아무것도 안 채우고** 지나간다

5. **전송 필수값이 늘어난 걸 시험이 몰랐다** — `09ca3d8` 이 포장·가열에 **온도(`cells.temp`)를
   전송 필수**로 더했는데 `document-flow` 는 시각만 채우고 있었다. 그래서 전송이 계속 토스트로 막혀
   `waitForResponse` 가 영문 모를 타임아웃으로 터졌다. 화면 필수값은 `fillPaperRequired` 한 곳에서 채우게 했다 —
   규칙이 또 늘어도 이 시험은 안 고친다

6. **숫자 칸은 `type="number"` 가 아니다** — 한글 IME 로 치면 값이 조용히 사라져서
   지면은 `type="text"` 로 받는다(`HtmlFormCellInput` 머리 주석). 도우미는 아직 `input[type=number]` 를
   채우고 있어 **한 칸도 안 잡혔다.** `input.html-form-sign-input[type="text"]` 로 고쳤다
**남았다 — 판단이 필요하다**

7. **`[CanvasView] 페이지 0 정보가 없습니다`** — `@rhwp/editor` 0.8.4 가 **빈 캔버스**에서 남기는
   SDK 로그다. 사용양식관리는 화면에 들어오는 즉시 편집기를 만들어 두어 이 상태가 된다.
   화면·저장에는 영향이 없다. 지금은 `screens.smoke` 에서 **그 문구 하나만** 예외로 뺐다.
   진짜로 없애려면 양식을 고를 때 `createEditor` 하도록 늦추거나 SDK 를 고쳐야 한다

### 이번에 훑어서 **안 나온 것**

배포 서버를 `http://` 로 전수 훑었다 — **관리자 28화면 0건, 조회 전용 17화면 0건**(4xx·콘솔오류).
운영에서 업무 한 바퀴도 닫았다: `bdusr` 저장 → 전송 → 팀장 함 → `bdsys` 승인 → 문서함,
종이에 작성자·승인자가 찍힌다. **지금 화면에서 보이는 결함은 없다.**

---

## 8. 이 저장소에서 특히 자주 어긋나는 것

- **한 화면 = 여러 층.** FE 라우트·레지스트리, BE 패키지·매퍼, `ScreenAuthResolver`,
  DB `tbl_screen`·`tbl_role_screen`·`tbl_menu` 가 같이 움직인다
- **`scrnCd`·`persistId` 는 고정.** `persistId` 를 바꾸면 그 화면 사용자 전원의 열 설정이 초기화된다
- **저장형과 표시형이 다르다.** 날짜는 DB `varchar(8)` `20260828` · 화면 `2026-08-28`,
  시각은 `HH:MM` 5자. 컬럼을 좁게 잡으면 `22001` 로 저장이 막힌다 —
  실제로 네 번 났다 ([`docs/4_명명과_경로.md`](docs/4_명명과_경로.md) 10절)
- **셸이 앞 탭을 mount 한 채 숨긴다.** 숨은 탭에서도 effect 가 돈다.
  URL 쿼리 같은 전역 상태를 읽을 때는 **지금 보고 있는 탭인지** 먼저 본다
- **E2E 는 `dist/` 를 본다.** 프론트를 고쳤으면 반드시 다시 빌드한다.
  `tsc` 오류로 빌드가 멈추면 **옛 `dist` 가 그대로 남아** 고치기 전 화면을 시험하게 된다
- **MyBatis 는 컴파일로 안 잡힌다.** 패키지를 옮기면 반드시 기동해서 확인한다
- **공통코드 `sub_cd` 는 저장값과 같은 표기.** 화면이 라벨을 만들지 않는다
- **여러 화면이 쓰는 조회를 화면 권한에 묶지 않는다.** 부르는 곳이 둘 이상이면
  `ScreenAuthResolver.isWhitelisted()` 를 본다. 안 그러면 「내가 볼 권한을 가진 화면인데
  남의 화면 권한 때문에 403」이 난다 (#66 에서 실제로 났다)
- **유니크가 걸린 자리 번호를 화면 값으로 받지 않는다.** `23505` 가 **409 「다른 사용자가
  동시에 처리 중입니다」**로 둔갑해 사람을 동시성 쪽으로 잘못 보낸다 (#67)
- **배포 서버를 훑을 때는 `http://` 로 본다.** `https` 는 자체서명이라 rhwp ServiceWorker
  오류가 70건 쌓여 진짜 신호를 덮는다. `ignoreHTTPSErrors` 로도 안 없어진다 ([`운영.md`](운영.md) 10.2)
- **운영에 전체 E2E 를 돌리지 않는다.** `resetDocuments()` 가 **문서를 전량 삭제**하고 시작한다.
  배포 서버 상대 시험은 `Jenkinsfile.e2e` 가 전용 계정으로 한다
- **이모지 금지 · 한국어 · git commit/push 는 사용자가 말할 때만**

---

## 9. 로컬 환경

| 포트 | 무엇 | 띄우는 법 |
|---|---|---|
| 4173 | `vite preview` (dist) — **E2E 대상** | `npx vite preview --host localhost --port 4173 --strictPort` |
| 7070 | 백엔드 API | `backend/haccp-api` 에서 `./mvnw spring-boot:run` |

`./mvnw -q -o test`(오프라인)는 **된다.**

**`netstat` 으로 4173 을 볼 때는 IPv6 도 본다** — `vite preview` 가 `[::1]:4173` 로 붙어서
`netstat -p tcp | grep LISTENING` 만 보면 「안 떠 있다」로 잘못 읽는다.

**`dist/` 잠김** — `vite preview` 가 켜져 있으면 빌드가 `dist` 를 못 비운다. 끄고 빌드한다.

프로세스를 끌 때는 PowerShell 을 쓴다 — `taskkill /PID` 는 Git Bash 가 경로로 해석해 실패한다.

```powershell
Stop-Process -Id (Get-NetTCPConnection -LocalPort 7070 -State Listen).OwningProcess -Force
```

---

## 10. DB

| | |
|---|---|
| 호스트 | `180.71.58.87:5432`, 스키마 `sasshaccp` |
| 운영 | `sasshaccp` |
| 시험 | `sasshaccp_test` ← 로컬 `backend/haccp-api/.env` 가 이걸 가리킨다 |

```sh
node tools/q.mjs "SELECT ..."                  # 시험 DB
node tools/q.mjs --db sasshaccp "SELECT ..."   # 운영
node tools/q.mjs --db sasshaccp "@파일.sql"     # 파일 적용
```

**이 PC 에 psql 이 없다.** `apply-all.sh` 는 psql 을 쓰므로 여기서는 못 돌린다 —
psql 전용 문법(`\if`·`:'co_cd'`)을 치환해서 `q.mjs` 로 넣는 방법을 썼다.

**자료를 지우는 헬퍼는 DB 이름이 `_test` 로 끝나야 돈다.**
`E2E_ALLOW_PROD_WRITES=1` 로만 열린다. **운영 개시 전에만 쓴다.**

### 정본 7본 (`apply-all.sh` 순서)

`00_ddl` → `01_sp` → `02_seed` → `03_code_seed` → `05_form_seed` → `06_company_seed` → `07_company_forms`

**`07` 까지 돌아야 쓸 수 있다.** `06` 까지만 돌리면 로그인·메뉴·권한은 되는데
작성 화면의 양식 선택이 **0건**이라 일지를 한 장도 못 쓴다.

새 업체를 열면 **사람이 둘을 더 해야 한다** — 실무 계정 만들기, 결재선 승인자를 팀장으로.
자세한 것은 [`db_sasshaccp/README.md`](db_sasshaccp/README.md).

---

## 11. 사용자 선호

| | |
|---|---|
| 호칭 | **"PR" 로 줄이지 말고 "PULL REQUEST" 로 풀어 쓴다** |
| 시험 | **「시험 넘기지마라」** — 통과만 보지 말고 **깨뜨려서** 무는지 확인한다 |
| 진행 | 중간에 멈춰 보고하지 말고 **끝까지 이어서** 한다 |
| 수정 | **모아서 한 번에.** 재현부터 하고 재현된 것만 고친다 |
| `.env` | 실사용 값으로 쓴다. git 에만 안 올린다 |

---

## 12. 어디를 먼저 보나

| 하려는 일 | 볼 곳 |
|---|---|
| 작업 순서 | [`CLAUDE.md`](CLAUDE.md) → `.cursor/rules/00-bootstrap.mdc` |
| 이 프로젝트가 뭔지·업무 흐름 | [`docs/1_시작하기.md`](docs/1_시작하기.md) |
| **화면·URL 전수** | [`docs/3_화면_지도.md`](docs/3_화면_지도.md) — 생성기 |
| **표에 어떤 컬럼이 있나** | [`docs/10_테이블_레이아웃.md`](docs/10_테이블_레이아웃.md) — 생성기 · 엑셀본 동봉 |
| 이 표를 고치면 어느 SP 가 걸리나 | [`docs/9_SP_색인.md`](docs/9_SP_색인.md) — 생성기 |
| 이름·값의 모양 | [`docs/4_명명과_경로.md`](docs/4_명명과_경로.md) |
| 코드가 도는 순서 | [`backend/haccp-api/PIPELINE.md`](backend/haccp-api/PIPELINE.md) · [`frontend/haccp-web/PIPELINE.md`](frontend/haccp-web/PIPELINE.md) |
| 화면 하나 만들기 | [`docs/2_화면_추가하기.md`](docs/2_화면_추가하기.md) |
| 시험 · 함정 | [`docs/6_테스트.md`](docs/6_테스트.md) · [`E2E.md`](E2E.md) |
| DB 를 깔거나 새 업체를 연다 | [`db_sasshaccp/README.md`](db_sasshaccp/README.md) |
| 배포·운영·백업 | [`DEPLOY.md`](DEPLOY.md) · [`운영.md`](운영.md) |
| 왜 이렇게 돼 있나 | [`docs/8_결정_이력.md`](docs/8_결정_이력.md) |
