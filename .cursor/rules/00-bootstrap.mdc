---
description: 에이전트 부트스트랩 — 모든 작업 전 최우선 적용 (HACCP)
alwaysApply: true
---

# 부트스트랩 (최우선)

에이전트(Agent)는 **어떤 도구 호출·파일 수정 전에도** 아래 순서를 따른다:

1. 루트 `README.md` — E2E 절·읽기 순서
2. `docs/23_PIPELINE.md` — 태그→파일 색인 (전수 표는 README에 없음)
3. `docs/15_HACCP_FE_BE_통합_상세스펙.md` — 유형별 이야기 (로그인·CRUD·DocForm·삭제)
4. 해당 도메인 README (`pages/sys/`, `pages/docs/`, `pages/ccp/` …) → 화면 README가 있으면 그것
5. `01-project-core.mdc`
6. `05-handoff-comments.mdc` — FE·BE **동일 밀도** 인수인계 주석
7. `06-operations.mdc` — 삭제·타임아웃·전역 env
8. 작업 영역 scoped rule
   - DB: `07-haccp-db.mdc`
   - 백엔드: `08-haccp-backend.mdc`
   - 프론트: `09-haccp-frontend.mdc` · UI: `02-frontend-ui.mdc`
   - 배포: `04-deploy.mdc` · 런북 `docs/20_배포_런북.md`
9. 에이전트 가이드 — `docs/7_에이전트_가이드_FE.md` · `docs/8_에이전트_가이드_BE.md`

## 필수 행동

- **한국어**로 응답
- 요청된 작업만 수행 — 범위 밖 대규모 리팩터·테스트 프레임워크 추가 금지
- **손대는 메뉴는 분할** — 아직 평탄하면 그 작업에서 sys와 같이 폴더 1개(Page+Rule+패키지)로 나눈다. 미리 전 메뉴를 나누지 않는다 (`01-project-core`)
- 기존 코드 스타일·패턴을 따른다
- **git commit/push**는 사용자 명시 요청 시에만
- **이모지 금지** — `01-project-core.mdc`
- **인수인계 주석** — FE·BE 동일 밀도 (`05-handoff-comments.mdc`)
- Windows: 명령 연결은 `;` (`&&` 지양)

## 작업 순서

1. 위 규칙·가이드 읽기
2. 최소 diff로 구현
3. 빌드/린트 가능 시 검증
