---
description: HACCP 프론트엔드(haccp-web) 컨벤션
globs: frontend/haccp-web/src/**/*.{ts,tsx}
alwaysApply: false
---

# 프론트엔드 (haccp-web)

| 항목 | 값 |
|------|-----|
| Vite | **4173** (`strictPort`) |
| API | `VITE_API_BASE_URL` → **7070** |
| 셸 | `HaccpShell` 단일 (터치PC 없음) |
| 로그인 | 아이디만 |
| 권한 | `readYn`·`writeYn`·`modifyYn`·`deleteYn`·`printYn` |
| storage | `haccp-*` (`authKeys.ts`) |
| 메뉴 | 대분류·화면 2단 (`h_menu_cd`) |

UI 마감: [`21-frontend-ui.mdc`](21-frontend-ui.mdc) · 운영: [`50-operations.mdc`](50-operations.mdc)

## 이름 접두사

`.mes-*` · `MesButton` · `mesToast` · `mesConfirm` · `MES` 문구 상수는 **유지**한다.  
앱 고유 셸·로고만 `Haccp*` (`HaccpShell`, `HaccpLogo`).

## 디렉터리

| 경로 | 역할 |
|------|------|
| `api/` | 도메인 API → `http.ts` (Axios 3계층) |
| `shell/` | `HaccpShell`, 메뉴·탭·세션·에러·레지스트리 |
| `pages/{모듈}/` | 화면 (ccp, hyg, doc, bas, sys …) |
| `components/{ui,grid,layout,form,document}/` | 공용 UI |
| `config/` | `envConfig.ts` — 매직넘버 금지 |
| `hooks/`, `stores/`, `types/`, `lib/`, `styles/`, `utils/` | 공통 |

## 네이밍

| 대상 | 규칙 |
|------|------|
| 컴포넌트·페이지 파일 | PascalCase = export명 |
| 훅 | `use` 접두사 |
| Props | `[ComponentName]Props` — `Props` 단독 금지 |
| default export | **금지** — named export만 |
| Row/API JSON | camelCase |
| 편집 메타 | `_rowState`, `_key` |
| 상수·화면코드 | UPPER_SNAKE |

## API 레이어

- `http` / `httpBatch` / `httpFile` — `VITE_API_TIMEOUT_*` + `envConfig.ts`
- 삭제: validate-delete → `mesConfirm` → delete
- 401: 인터셉터 → 세션 정리 → `/login`

## 화면 등록

1. `shell/screenRegistry.tsx`에 `scrn_cd`와 **동일** kebab-case 키 등록
2. 라우트는 `tabRoute`가 자동 생성
3. 레지스트리 누락 화면을 메뉴에서 숨기지 말 것(비활성 표시)

## 인수인계·PIPELINE

- 주석: [`40-handoff-comments.mdc`](40-handoff-comments.mdc)
- 골드: `shell/HaccpShell.tsx` · `pages/ccp/ColdMonitorPage.tsx` · `pages/auth/LoginPage.tsx`
- 태그 접두 **`HF`**. 루트 `README.md` PIPELINE 표와 동기화
