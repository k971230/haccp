# HACCP 개발 핸드북

> 대상: 기능을 추가·수정하는 개발자·에이전트  
> 전제: [`환경구축.md`](환경구축.md)로 로컬 4173/7070 기동 완료  
> 배포·Jenkins 일상 조작: [`운영.md`](운영.md)  
> 시크릿 실값 금지 · UI/커밋에 이모지 금지

---

## 1. 개발 루프 (정본)

```text
1) main 최신화
2) feature/<slug> 또는 hotfix/<slug> 분기   ← main 직커밋 금지
3) 구현 + 인수인계 주석(FE·BE 동일 밀도)
4) 로컬 검증 (tsc / mvnw compile·test)
5) 커밋 Conventional Commits
6) push → PR → 리뷰·머지
7) 운영 반영이 필요하면 Jenkins haccp-deploy → Build Now
```

머지만 하고 Build Now를 안 누르면 **서버는 안 바뀐다** ([`운영.md`](운영.md)).

---

## 2. 브랜치 · 커밋 · PR

| 구분 | 규약 |
|------|------|
| 브랜치 | `main` (배포) · `feature/<slug>` · `hotfix/<slug>` |
| 커밋 | `<type>(<scope>): <subject>` |
| type | `feat` · `fix` · `chore` · `docs` · `refactor` · `build` · `ci` · `db` |
| scope | `fe` · `be` · `db` · `infra` · `docs` |
| PR 제목 | 가능하면 `[STEP nn] type(scope): subject` 또는 동일 Conventional |

예:

```text
feat(fe): 로그인 MES 스플릿·HACCP 로고·로컬 4173
fix(fe): 로그아웃 시 Path basename /haccp/login 정합
docs(docs): 루트 환경구축·개발·운영 핸드북
```

리뷰어 없는 self-merge는 규약상 지양. 긴급 hotfix는 팀에 공유.

---

## 3. 스택 · Two-Tier 명명

| 층 | 스택 |
|----|------|
| FE | React 18, Vite 5, TypeScript, Tailwind, Zustand, React Query |
| BE | Spring Boot 3.3.4, Java 17, MyBatis 3.0.3, JWT |
| DB | PostgreSQL · DB/스키마 `sasshaccp` · SP `sp_tbl_*` |

| 영역 | 표기 |
|------|------|
| DB·Mapper·SP | `lower_snake` (`co_cd`, `sp_tbl_*`) |
| DTO·JSON·그리드 | `camelCase` (`coCd`, `docNo`) |
| 화면코드·상수 | UPPER (팀 관례) |

MES(`metis` / `sp_sk_*`)와 **테이블·SP 공유 금지**.

---

## 4. 프론트엔드 구조

루트: [`frontend/haccp-web/`](frontend/haccp-web/).

| 영역 | 경로 | 역할 |
|------|------|------|
| 진입 | `src/main.tsx` | `BrowserRouter` `basename={import.meta.env.BASE_URL}` |
| 라우트 | `src/routes/AppRoutes.tsx` | `/login` 공개 · 나머지 토큰 보호 |
| 셸 | `src/shell/HaccpShell.tsx` | 사이드·탭·로그아웃 |
| API | `src/api/*` · `http.ts` | axios 인스턴스 3계층 타임아웃 |
| 상태 | `src/stores/*` | auth · tabs |
| 화면 | `src/pages/**` | 업무 화면 |
| 그리드 | `src/components/grid/*` | MesEditableGrid 등 |
| 설정 | `src/config/envConfig.ts` | VITE_* 숫자 상수 |

로컬: `npm run dev` → `http://localhost:4173`  
API: `VITE_API_BASE_URL=http://localhost:7070`  
운영 빌드: Docker `ARG VITE_BASE=/haccp/` → basename `/haccp/`.

### 4.1 인수인계 주석 (FE = BE 동일 밀도)

정본: [`.cursor/rules/40-handoff-comments.mdc`](.cursor/rules/40-handoff-comments.mdc)  
(문서 모음 PR이 머지되면 규칙 번호가 바뀔 수 있다 — **내용 원칙은 동일**.)

- import 위: `// 역할 — …`
- 공개 핸들러: 개발자 / 일자 / 코멘트 3줄
- JSX **의미 있는 prop마다** 여러 줄 주석 (`//` only)
- 골드 샘플: 콜드모니터·문서 화면 등 (FE docs 참고)

### 4.2 검증

```bash
cd frontend/haccp-web
npx tsc --noEmit
npm run lint
npm test -- --run
```

---

## 5. 백엔드 구조

루트: [`backend/haccp-api/`](backend/haccp-api/).

```text
Controller → Service (@Transactional) → Mapper → SP (sp_tbl_*)
```

| 주제 | 위치 |
|------|------|
| JWT·LoginUser | `auth/` |
| 공통 예외 | `common/exception/` · `SqlUserMessage` (사용자 문구) |
| Mapper XML | `resources/mapper/**` |
| 설정 | `application.yml` + `.env` |

로컬:

```bash
cd backend/haccp-api
./mvnw -q -DskipTests compile
./mvnw -q -B -DskipITs test
./mvnw spring-boot:run
```

포트 **7070**. CORS 기본 `http://localhost:4173`.

사용자에게는 업무 문구만, 서버 로그에는 기술 상세 (`GlobalExceptionHandler`).

---

## 6. 인증 · 세션 · Path basename

### 6.1 로그인

- API: `POST /api/v1/auth/login` — **회사 콤보 없음** (아이디 전역 유일 → 서버가 `coCd` 판정).
- FE: prefs(아이디 저장·자동 로그인) → `applyAuthPersistStorage` → `setAuth` → navigate.
- 화면: [`LoginPage.tsx`](frontend/haccp-web/src/pages/auth/LoginPage.tsx)  
  MES 스플릿 · `login_bg.png` · `haccp_logo.png`.

### 6.2 로그아웃 · 401

- 수동 로그아웃 ([`HaccpShell`](frontend/haccp-web/src/shell/HaccpShell.tsx)):  
  `logoutApi` → `clearAuthSession` → **`location.replace(loginBrowserPath())`**  
  → 로컬 `/login`, 운영 `/haccp/login`.
- 401·멀티탭: [`handleUnauthorized`](frontend/haccp-web/src/shell/authSession.ts) 가 동일하게 `loginBrowserPath()` 사용.
- Path 헬퍼: [`authPaths.ts`](frontend/haccp-web/src/shell/authPaths.ts) (`normalizeAppBaseUrl`, `isLoginBrowserPath`, `toRouterPath`).
- 멀티탭: `haccp-auth-logout-signal` localStorage ([`authCrossTab.ts`](frontend/haccp-web/src/shell/authCrossTab.ts)).

**금지:** `window.location`에 `"/login"` 하드코딩 (운영에서 `/haccp` 누락).

### 6.3 favicon

[`index.html`](frontend/haccp-web/index.html): `%BASE_URL%logo2.svg` (절대 `/logo2.svg` 금지 — Path 깨짐).

더 깊은 JWT 문서: FE [`docs/04_인증_보안_JWT.md`](frontend/haccp-web/docs/04_인증_보안_JWT.md) · BE [`docs/04_인증_보안_JWT.md`](backend/haccp-api/docs/04_인증_보안_JWT.md).

---

## 7. 운영 규약 (개발 시 필수)

정본 요약: [`.cursor/rules/50-operations.mdc`](.cursor/rules/50-operations.mdc) · FE/BE `01_운영규칙.md`.

### 7.1 OPS_DELETE

- HTTP **DELETE 금지** — `POST .../validate-delete` → `POST .../delete`.
- Body: 복합키 **객체 배열** `[{...}]`.
- BE: validate·delete 양쪽 `assertDeletable` Double Check.
- FE: `validateDeleteApi` → `mesConfirm` → `deleteApi` → 재조회.

### 7.2 타임아웃 3계층

| 인스턴스 | env | 기본 | 용도 |
|----------|-----|------|------|
| `http` | `VITE_API_TIMEOUT_DEFAULT` | 10s | CRUD |
| `httpBatch` | `VITE_API_TIMEOUT_BATCH` | 60s | 대량 |
| `httpFile` | `VITE_API_TIMEOUT_FILE` | 120s | HWPX·PDF |

매직 넘버 금지 → `.env` / `envConfig.ts` / `application.yml`.

### 7.3 전역 env (FE)

`VITE_GRID_VIRTUAL_THRESHOLD`, `VITE_SEARCH_DEBOUNCE_MS`, `VITE_DASHBOARD_POLLING_MS` 등 — 변경 후 **Vite 재시작**.

---

## 8. 업무 화면 · CRUD 진입

화면·API·DB 매핑 전수와 상세 스펙은 아래에 있다. **화면 하나 추가 전** 해당 절을 연다.

| 문서 | 내용 |
|------|------|
| [`frontend/.../07_메뉴_화면_API_DB_전수.md`](frontend/haccp-web/docs/07_메뉴_화면_API_DB_전수.md) | 메뉴·API·테이블 |
| [`frontend/.../08_HACCP_FE_BE_통합_상세스펙.md`](frontend/haccp-web/docs/08_HACCP_FE_BE_통합_상세스펙.md) | 통합 스펙 |
| [`frontend/.../06_업무_CRUD.md`](frontend/haccp-web/docs/06_업무_CRUD.md) | FE CRUD 패턴 |
| [`backend/.../06_업무_CRUD.md`](backend/haccp-api/docs/06_업무_CRUD.md) | BE CRUD 패턴 |
| [`frontend/.../05_메인_셸.md`](frontend/haccp-web/docs/05_메인_셸.md) | 셸·탭 |
| [`frontend/.../10_파일구조_컴포넌트_함수지도.md`](frontend/haccp-web/docs/10_파일구조_컴포넌트_함수지도.md) | 파일 지도 |
| [`frontend/.../11_프레임워크_파일_보안_작성규칙.md`](frontend/haccp-web/docs/11_프레임워크_파일_보안_작성규칙.md) | 파일·보안 |

DB SQL 추가 시 `db_sasshaccp/` 번호 규칙·SP 명명 준수 ([`.cursor/rules/60-haccp-db.mdc`](.cursor/rules/60-haccp-db.mdc)).

---

## 9. 로컬 검증 · E2E

### 9.1 최소

```bash
# FE
cd frontend/haccp-web
npx tsc --noEmit

# BE
cd backend/haccp-api
./mvnw -q -DskipTests compile
```

### 9.2 Playwright (있을 때)

`frontend/haccp-web` 의 `test:e2e` · `e2e/.env` (git 주의).  
운영/스테이징 URL·계정은 로컬 env로만.

스모크 매트릭스: [`frontend/haccp-web/docs/13_스모크_매트릭스.md`](frontend/haccp-web/docs/13_스모크_매트릭스.md).

---

## 10. Cursor / 에이전트 규칙

부트스트랩: [`.cursor/rules/00-bootstrap.mdc`](.cursor/rules/00-bootstrap.mdc)

| 규칙 (현행 main 파일명) | 내용 |
|-------------------------|------|
| `01-project-core` | 스택·시크릿·이모지 금지 |
| `30-branching` | 브랜치·커밋·PR |
| `40-handoff-comments` | FE·BE 주석 밀도 |
| `50-operations` | DELETE·타임아웃·env |
| `60-haccp-db` | DB 규약 |
| `61-haccp-backend` | BE 스코프 |
| `62-haccp-frontend` | FE 스코프 |
| `20-frontend-react` · `21-frontend-ui` | FE/UI |
| `10-backend-java` | BE Java |

에이전트 가이드: FE [`docs/03_에이전트_가이드.md`](frontend/haccp-web/docs/03_에이전트_가이드.md) · BE [`docs/03_에이전트_가이드.md`](backend/haccp-api/docs/03_에이전트_가이드.md).

요청 범위 밖 대규모 리팩터·테스트 프레임워크 추가 금지. ERP·바코드는 의도적 미구현.

---

## 11. 금지 목록 (개발)

1. `main` 직접 커밋  
2. MES DB/SP 공유  
3. HTTP DELETE 로 업무 삭제  
4. 타임아웃·페이지 크기 등 매직 넘버  
5. `/login` 하드코딩 (Path 배포 깨짐)  
6. `.env` · PAT · JWT 실값 커밋  
7. UI·커밋·주석에 이모지  
8. FE만 validate-delete 추가하고 BE 미구현 (404)

---

## 12. 최근 반영된 UI/인프라 (참고)

| 항목 | 내용 |
|------|------|
| 로그인 UI | MES 스플릿 · `haccp_logo.png` · RMA favicon |
| 로컬 포트 | Vite 4173 · API 7070 |
| 로그아웃 | `loginBrowserPath()` → `/haccp/login` |
| PR 예 | [#16](https://github.com/k971230/haccp/pull/16) |

문서 번호 `1_`~`21_` 루트 `docs/` 모음은 PR [#15](https://github.com/k971230/haccp/pull/15) 등으로 진행 중일 수 있다. **링크는 현재 main에 있는 경로**를 우선한다.

---

## 13. 다음 단계

- 로컬에서 기능 완료 → PR 머지 → [`운영.md`](운영.md) 의 Build Now  
- 환경이 무너졌으면 [`환경구축.md`](환경구축.md) 체크리스트
