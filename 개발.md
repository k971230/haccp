# 개발 핸드북

> 개발자: 박승우 · 최종 갱신: 2026-08-26
> 대상: 기능을 추가·수정하는 사람
> 전제: [`환경구축.md`](환경구축.md) 로 로컬 4173/7070 이 떠 있다

**이 문서는 「브랜치에서 main 까지」만 다룬다.**

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 처음 세팅 | [`환경구축.md`](환경구축.md) |
| 프로젝트가 뭔지·업무 흐름 | [`docs/1_시작하기.md`](docs/1_시작하기.md) |
| 코드가 어떤 순서로 도는지 | [`backend/haccp-api/PIPELINE.md`](backend/haccp-api/PIPELINE.md) · [`frontend/haccp-web/PIPELINE.md`](frontend/haccp-web/PIPELINE.md) |
| 화면 하나 만들기 | [`docs/2_화면_추가하기.md`](docs/2_화면_추가하기.md) |
| 이름·경로 규칙 | [`docs/4_명명과_경로.md`](docs/4_명명과_경로.md) |
| Git 명령 실습 | [`깃.md`](깃.md) |
| 배포 | [`DEPLOY.md`](DEPLOY.md) · [`운영.md`](운영.md) |

---

## 1. 하루 루프

```sh
# 1. 최신을 받는다
git checkout main && git pull

# 2. 브랜치를 판다 — main 에 직접 커밋하지 않는다
git checkout -b feature/ccp-htg-judge-required

# 3. 고친다

# 4. 확인한다 (아래 3장)

# 5. 커밋한다 — 성격별로 나눈다
git add <파일들>
git commit

# 6. 올린다
git push -u origin feature/ccp-htg-judge-required

# 7. PR 을 만든다
gh pr create --base main --fill
```

머지는 리뷰 후. **Jenkins Build Now 는 main 이 정리된 뒤에 누른다.**

---

## 2. 브랜치·커밋·PR

### 브랜치 이름

| 접두 | 언제 |
|---|---|
| `feature/` | 기능 추가·개선 |
| `fix/` | 버그 |
| `refactor/` | 동작은 그대로, 구조만 |
| `docs/` | 문서만 |
| `test/` | 시험만 |
| `hotfix/` | 운영 급한 것 |

뒤는 **무엇을 하는지** 짧게. 화면코드를 쓰면 찾기 쉽다 — `fix/ccp-htg-judge-required`.

### 커밋 메시지

```
fix(fe): HWP 이탈 처리를 되살린다

HWP 작성의 「이탈여부」 체크가 눌리지도, 서버로 가지도 않았다.
두 결함이 겹쳐 이탈 처리가 처음부터 끝까지 죽어 있었다.

- onCellChange 가 첫 줄에서 일자 외 모든 셀 변경을 버렸다
- hwpDraftApi.save 가 deviationYn 을 안 실어 보냈다
```

| 지키는 것 | 왜 |
|---|---|
| 제목은 **무엇을 했는지** 한 줄 | 목록에서 보인다 |
| 본문은 **왜 그랬는지** | 무엇을 했는지는 diff 에 있다 |
| 한 커밋 = 한 가지 | 되돌릴 때 갈라진다 |
| 한국어 · 이모지 없음 | 규칙 |

타입: `feat` `fix` `refactor` `docs` `test` `chore`
범위: `fe` `be` `db` `e2e` (생략 가능)

### PR

- 제목은 커밋 제목과 같은 규칙
- 본문에 **무엇을 고쳤고 어떻게 확인했는지**
- DB 를 바꿨으면 **PR 본문에 반드시 적는다** — 배포 담당이 따로 돌려야 한다

---

## 3. 커밋 전에 확인하는 것

```sh
# 프론트
cd frontend/haccp-web
npx tsc --noEmit          # 타입
npx eslint src e2e        # 규칙
npx vitest run            # 단위 144건
npm run build             # 번들
npx playwright test       # E2E 152건 — 화면·API·SP·DB

# 백엔드
cd backend/haccp-api
./mvnw -q -o test         # 단위 104건

# 시크릿
bash scripts/pre-commit-check-secrets.sh
```

**`mvn test` 통과가 기동 성공을 뜻하지 않는다.**
MyBatis 매퍼 XML 은 컴파일에 안 잡힌다 — 패키지를 옮겼거나 XML 을 고쳤으면
**반드시 기동해서** 화면을 열어 본다.

DB 를 고쳤으면:

```sh
# 파일과 라이브가 같은지
psql -c "SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
         WHERE n.nspname='sasshaccp' AND p.proname LIKE 'sp\_%'"
grep -c "CREATE OR REPLACE" db_sasshaccp/01_sp.sql
```

---

## 4. 무엇을 고치면 무엇이 같이 움직이나

| 고친 것 | 같이 봐야 하는 것 |
|---|---|
| 화면 추가·이동 | `tabRoute.ts` · `screenRegistry.tsx` · `ScreenAuthResolver` · `tbl_screen`·`tbl_role_screen`·`tbl_menu` · 각 README |
| 자바 패키지 이동 | 매퍼 XML 폴더 · `namespace` · `resultType` · **기동 확인** |
| SP 시그니처 | 부르는 매퍼 XML 의 인자 개수 · `01_sp.sql` |
| 공통코드 추가 | `03_code_seed.sql` + **저장되는 데이터 표기** (둘이 같아야 콤보가 뜬다) |
| 표 컬럼 | `00_ddl.sql` · 그 표를 읽는 SP 전부 |
| 새 폴더 | 그 폴더의 `README.md` (규칙) |

---

## 5. 금지

| 하지 않는다 | 왜 |
|---|---|
| `main` 에 직접 커밋·푸시 | 리뷰를 건너뛴다 |
| `.env` 커밋 | 시크릿 |
| 이모지 | 터미널·diff·검색에서 깨진다 |
| `scrnCd`·`persistId` 변경 | 권한·메뉴·사용자 열 설정이 끊긴다 |
| 매퍼 XML 에 직접 SQL | 업무 SQL 은 SP 에 둔다 |
| SP 를 `CREATE PROCEDURE` 로 | 재적용이 막힌다 |
| 양식·화면코드를 조건문에 나열 | 새 값이 조용히 빠진다 |
| `git commit` 을 시키지 않았는데 하기 | 사용자가 말할 때만 |

---

## 6. 에이전트로 작업할 때

규칙 정본은 `.cursor/rules/` 하나다. 도구가 달라도 결과가 같아야 한다.

| 도구 | 읽는 법 |
|---|---|
| Cursor | `.cursor/rules/*.mdc` 자동 |
| Claude Code | `CLAUDE.md` → `.cursor/rules/` |
| 그 밖 | `AGENTS.md` → 같은 곳 |

읽는 순서는 `.cursor/rules/00-bootstrap.mdc` 가 정한다.
**규칙을 `CLAUDE.md`·`AGENTS.md` 에 복제하지 않는다** — 한쪽만 고쳐지면 도구마다 다른 답을 준다.

---

## 7. 자주 밟는 것

| 증상 | 원인 |
|---|---|
| 기동이 안 된다 | `.env` 의 `JWT_SECRET`·`DB_PASSWORD` |
| 컴파일은 되는데 기동에서 터진다 | 매퍼 XML `namespace`·`resultType` 이 패키지와 다르다 |
| 화면이 안 열린다 | `tbl_role_screen` 에 권한 행이 없다 |
| 콤보가 비었다 | 공통코드 `sub_cd` 표기와 저장값이 다르다 |
| 저장은 되는데 목록에 없다 | 회사코드가 다르다. 서비스가 JWT 값으로 덮어쓰는지 본다 |
| E2E 가 간헐 실패 | 숨은 탭의 요소를 잡았다. `helpers.ts` 의 `btn`·`grids`·`rowOfDoc` 을 쓴다 |

---

## 관련

- Git 명령 실습: [`깃.md`](깃.md)
- 배포: [`DEPLOY.md`](DEPLOY.md) · [`운영.md`](운영.md)
- 문서 지도: [`docs/README.md`](docs/README.md)
