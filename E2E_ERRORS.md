# E2E Error Log

2026-08-25 전체 E2E 수행 중 발견한 오류를 발견 즉시 누적한다.
수정 뒤에도 기록을 지우지 않는다 — 무엇이 있었고 어떻게 고쳤는지 남긴다.

집계는 문서 맨 아래.

---

## E2E-001 — 전송(결재요청)이 500 으로 죽는다

| 항목 | 내용 |
|---|---|
| 발견 | 문서 흐름 E2E — CCP 가열일지 작성 후 「전송」 |
| 증상 | `PUT /api/v1/docs/documents/approval` → 500 `DB_ERROR`. 문서가 전송대기(WRK)에 그대로 남는다 |
| 원인 | `sp_tbl_document_approval_c_000` 이 `tbl_user.sign_path` · `tbl_document_approval.sign_path` 를 읽고 쓴다. 서명이 파일 경로에서 바이너리(`sign_img`)로 바뀌었는데 이 SP 만 따라오지 못했다 |
| 영향 | **전송·검토·승인·반려·결재취소 전 경로 불능.** 결재 기능 전체가 죽어 있었다 |
| 조치 | `db_sasshaccp/01_sp.sql` — `v_sign_path varchar(300)` → `v_sign_img bytea`, 읽기·쓰기 5곳을 `sign_img` 로 바꿨다 |
| 확인 | SP 직접 호출로 `WRK → REQ` 전이 확인. 화면 전송도 통과 |

## E2E-002 — SP 본을 다시 돌리면 실패한다

| 항목 | 내용 |
|---|---|
| 발견 | E2E-001 수정본을 라이브에 다시 올릴 때 |
| 증상 | `ERROR: function "sp_..." already exists with same argument types` |
| 원인 | `01_sp.sql` 152본이 전부 `CREATE PROCEDURE` / `CREATE FUNCTION` 이었다. `apply-all.sh` 주석은 "몇 번을 다시 돌려도 결과가 같다"고 적혀 있어 실제와 어긋났다 |
| 영향 | 배포 재실행·SP 수정 반영이 막힌다. 운영에서 SP 하나 고치려면 손으로 DROP 해야 했다 |
| 조치 | 152본 전부 `CREATE OR REPLACE` 로 바꿨다 |
| 확인 | 같은 파일을 두 번 적용해 오류 없음 |

## E2E-003 — 결재선 변경이 감사로그에 안 남는다

| 항목 | 내용 |
|---|---|
| 발견 | 이력 3화면 점검 — 감사로그에 `tbl_approval_line` 이 한 건도 없었다 |
| 증상 | 공통코드·메뉴·권한그룹·부서·사용자 5화면은 저장·삭제마다 감사 이력을 남기는데 결재선관리만 안 남긴다 |
| 원인 | `ApprovalLineService` 에 `AuditWriter` 주입 자체가 없었다. 형제 5화면과 달리 처음부터 빠져 있었다 |
| 영향 | 결재선은 「누가 결재하는가」를 정한다. 몰래 바꿔 자기 문서를 자기가 승인하게 만들어도 흔적이 없다 |
| 조치 | `save` 는 `U`, `delete` 는 `D` 로 `auditWriter.record` 를 넣었다. 결재선 저장은 UPSERT 한 건이라 등록·수정을 못 갈라 `U` 로 통일했다 |
| 확인 | 결재선 등록 전 0건 → 후 1건. 스펙에 고정했다 |

## E2E-004 — HWP 작성의 「이탈여부」 체크가 눌리지 않는다

| 항목 | 내용 |
|---|---|
| 발견 | 작성 6화면 점검 — HWP 작성 목록의 이탈여부 칸 |
| 증상 | 체크박스를 눌러도 상태가 안 바뀐다. 잠김 안내도 없이 아무 일도 안 일어난다 |
| 원인 | `HtmlFormDraftPage` 의 `onCellChange` 가 첫 줄에서 `if (field !== "baseDtDisp") return;` 로 일자 외 모든 셀 변경을 버렸다. 이탈여부 칸은 이 화면에만 있어 아무도 안 밟았다 |
| 영향 | 이탈 표시가 화면상 불가능. HWP 문서는 이탈·개선조치로 올릴 방법이 없었다 |
| 조치 | `deviationYn` 분기를 앞에 넣어 버퍼와 목록 메타를 함께 갱신한다. 한쪽만 고치면 체크는 보이는데 저장이 안 된다 |
| 확인 | 체크 → 저장 → `tbl_corrective_action` 1건 생성 |

## E2E-005 — 이탈여부를 켜도 서버로 안 간다

| 항목 | 내용 |
|---|---|
| 발견 | E2E-004 를 고친 직후 — 체크는 되는데 개선조치 행이 안 생겼다 |
| 증상 | `PUT /draft/hwp-doc/hwp-write/save` 본문에 `deviationYn` 이 아예 없다 |
| 원인 | `hwpDraftApi.save` 가 `tmplCd·docIdx·baseDt` 세 칸만 골라 보낸다. 서버(`HwpDraftService.applyDeviation`)는 `deviationYn` 을 읽을 준비가 돼 있었는데 화면이 안 보냈다 |
| 영향 | E2E-004 와 겹쳐 이탈 처리가 처음부터 끝까지 죽어 있었다 |
| 조치 | 저장 본문에 `deviationYn` 을 실어 보낸다 (미지정은 `N`) |
| 확인 | 체크 → 저장 → 개선조치 행 생성 → 이탈·개선조치 화면에 문서번호로 조회됨 |

## E2E-006 — 길이를 넘긴 입력이 500 으로 터진다

| 항목 | 내용 |
|---|---|
| 발견 | 제약 위반 시험 — 부서명에 300자를 넣고 저장 API 를 직접 호출 |
| 증상 | `500 DB_ERROR "데이터 처리 중 오류가 발생했습니다."` |
| 원인 | PostgreSQL 이 `22001`(값이 칸 길이 초과)로 거절하는데 `GlobalExceptionHandler.classify` 가 45000·23505·40P01 만 분기하고 나머지는 전부 500 으로 내렸다 |
| 영향 | 사용자는 무엇을 고쳐야 할지 못 본다. 로그에는 서버 오류로 쌓여 진짜 장애와 구분이 안 된다. 길이 외에 `23502`(필수값)·`23503`(참조 중)·`22P02`(형식)도 같은 문제였다 |
| 조치 | 입력이 틀려 DB 가 거절한 SQLSTATE 4종을 400 `BAD_INPUT` 과 안내 문구로 내린다. 화면마다 길이 검증을 흩뿌리는 대신 한곳에서 잡는다 |
| 확인 | 재기동 후 400 `BAD_INPUT` 확인. 부서명 300자 → 저장 거절, DB 무변화 |

## E2E-007 — HTML 양식 원본 5화면 중 2화면의 복사가 죽어 있었다

| 항목 | 내용 |
|---|---|
| 발견 | 정본화 작업 중 — 삭제된 화면에 딸린 양식을 걷어낸 뒤 |
| 증상 | 「일반위생·공정점검 양식관리」·「CCP 검증점검표 양식관리」에서 행추가→저장이 400 으로 막힌다 |
| 원인 | 복사 SP 가 `html_sys_001`(일반위생)·`html_sys_006`(검증)을 **표준 원본**으로 읽는다. 이름이 `html_sys_*` 라 레거시로 오해해 지웠다 |
| 왜 안 잡혔나 | 스펙이 5화면 중 CCP 가열 하나만 복사를 시험했다. 같은 프레임을 쓰지만 **복사 SP 는 화면마다 다르다** |
| 조치 | 두 양식을 씨앗·라이브에 되살렸다. 스펙을 5화면 전부 도는 반복문으로 바꿨다 |
| 재발 방지 | 양식코드를 지우기 전에 `01_sp.sql` 에서 그 코드를 참조하는 SP 가 있는지 본다. 표준 원본은 `db_sasshaccp/README.md` 에 적었다 |

---

# 2회차 — 배포 전 정본화 (2026-08-26)

계획 [`배포전_최종검증_계획.md`](배포전_최종검증_계획.md) · 결과 [`배포전_최종검증_결과.md`](배포전_최종검증_결과.md)

## E2E-008 — HWP 사용양식 삭제가 통째로 죽어 있었다

| 항목 | 내용 |
|---|---|
| 발견 | 0단계 관문에서 `/api/v1/bas/` 를 서빙하는 컨트롤러가 없는 것을 보고 실호출 |
| 증상 | 화면에서 양식을 지우면 `500`. **한 번도 동작한 적이 없다** |
| 원인 | FE 가 `/api/v1/bas/company-templates/*` 를 부르는데 그 URL 을 서빙하던 `MasterController` 가 28화면 정리(`190fe6d`)에서 사라졌다. 게다가 `company-templates` 는 그 컨트롤러의 `MasterType` enum 에 **애초에 없어서** 지워지기 전에도 실패했다 |
| 왜 안 잡혔나 | FE 문자열이라 컴파일에 안 잡힌다. E2E 가 이 화면의 삭제를 안 봤다. 주석·README 3곳이 「법적서류와 공유해 Workflow 에 잔류」라고 적어 두었는데 그 Workflow 도 없었다 |
| 조치 | 형제 5화면처럼 화면 자기 경로에 붙였다. 차단 SP 신설(시스템 제공분·작성된 문서), `AuditWriter` 추가, 죽은 `/api/v1/bas` 예외 맵 제거 |
| 확인 | 자사양식 생성 → validate 200 → delete 200 → `tbl_company_template` 0행 · `tbl_template` 0행 · 감사로그 `D`. 시스템양식은 400 |

## E2E-009 — 없는 경로가 500 UNKNOWN + 스택트레이스로 나갔다

| 항목 | 내용 |
|---|---|
| 발견 | E2E-008 을 쫓다가 응답 코드가 404 가 아닌 것을 보고 |
| 증상 | 죽은 URL 을 부르면 `500 UNKNOWN` 과 서버 로그에 스택트레이스 |
| 원인 | `NoResourceFoundException` 이 `@ExceptionHandler(Exception.class)` 로 떨어졌다 |
| 영향 | **이 결함이 E2E-008 을 가렸다.** 404 였으면 「없는 주소」로 바로 보였을 것을 「서버 오류」로 읽었다 |
| 조치 | 404 `NOT_FOUND` 로 내리고 스택트레이스를 남기지 않는다. 잘못된 주소는 서버 결함이 아니다 |

## E2E-010 — 한 컨트롤러가 base 가 다른 두 갈래를 담고 있었다

| 항목 | 내용 |
|---|---|
| 발견 | 2단계 URL 전수 대조 — 형제 5화면과 형태가 다른 컨트롤러 |
| 증상 | `UserController` 가 `@RequestMapping("/api/v1/sys")` 하나로 화면(`/code/user-management/…`)과 서명(`/users/…`)을 함께 받았다 |
| 영향 | 형제를 보고 `@GetMapping("/list")` 를 적으면 `/api/v1/sys/list` 가 된다. 아직 안 터졌을 뿐인 함정 |
| 조치 | `UserController`(화면 4개) · `UserSignController`(서명 6개)로 갈랐다. 서명은 여러 화면 공용이라 화면 경로가 아닌 게 맞다 |

## E2E-011 — 화면 API 가 남의 패키지에 있었다

| 항목 | 내용 |
|---|---|
| 발견 | 같은 대조에서 `TaskController` 하나가 URL 4계열을 서빙하는 것을 보고 |
| 증상 | 개선조치관리 URL 은 `/flow/ca/…` 인데 컨트롤러가 `com.haccp.tsk` 에 있었다 |
| 영향 | 「URL = DB = 폴더 = 패키지」가 깨져 있었다. 화면을 찾아가는 사람이 패키지에서 못 찾는다 |
| 조치 | `flow/ca` 로 옮겼다 (컨트롤러·서비스·매퍼·XML namespace). **MyBatis XML 은 컴파일에 안 잡혀 기동해서 확인했다** |
| 곁들여 | 옮기며 `recent-docs`·`notifications` 를 함께 지웠는데 **스모크가 28화면 전부에서 404 로 잡아냈다.** 복원함 |

## E2E-012 — 삭제 확인창을 아무도 안 누르고 있었다

| 항목 | 내용 |
|---|---|
| 발견 | E2E-008 을 고친 뒤 「그럼 왜 시험이 통과했나」를 되짚다가 |
| 증상 | `btn(page,"확인")` 이 아무것도 못 찾고 조용히 지나갔다. 시험은 초록 |
| 원인 | 확인창의 긍정 버튼 글자가 **「확인」이 아니라 「삭제」** 다. 게다가 시험이 「시스템 양식이 안 지워졌다」만 봤다 — **삭제가 통째로 죽어도 참이 되는 단언** |
| 영향 | E2E-008 이 이것 때문에 안 잡혔다 |
| 조치 | `confirmDeleteBtn()` 헬퍼로 다이얼로그 안에서 고른다. 막는 것을 볼 때는 **되는 것도 같이** 본다 — 복사본이 실제로 지워지는지를 전 화면에서 확인 |
| 재발 방지 | 함정 6가지를 `e2e/README.md` 와 `docs/6_테스트.md` 에 적었다 |

## E2E-013 — 전체 실행에서만 깨지던 시험

| 항목 | 내용 |
|---|---|
| 증상 | 권한 게이트 시험이 단독으로는 통과, 전체 실행에서만 실패 |
| 원인 | 로그아웃 버튼이 스스로 `/login` 으로 보내는데 `login()` 이 그 위에 `page.goto` 를 겹쳐 쳤다 (`interrupted by another navigation`). 앞 시험이 남긴 상태에 따라 타이밍이 갈렸다 |
| 조치 | 공용 헬퍼 `login()` — 이미 로그인 화면이면 다시 이동하지 않는다 |
| 왜 중요한가 | 단독 통과·전체 실패를 두면 다음에 진짜 결함이 나도 「또 그거겠지」로 넘어간다 |

## E2E-014 — nginx 파일 업로드 경로가 죽어 있었다

| 항목 | 내용 |
|---|---|
| 발견 | 파비콘 작업을 확인하다 `haccp.conf.template` 을 보고 |
| 증상 | 파일 전송 location 정규식이 없어진 경로(`doc/`·`bas/`·`hyg/`)를 가리켰다 |
| 영향 | 여기 안 걸리면 일반 API 로 떨어져 **기본 70s** 로 처리된다 — 운영에서 큰 HWP 업로드가 끊긴다. 로컬에서는 안 드러난다 |
| 조치 | 지금 URL 7개로 맞추고, 일반 API 가 안 걸리는 것까지 정규식으로 대조했다 |
| 재발 방지 | 이 목록은 **백엔드 컨트롤러의 multipart·download·pdf 매핑과 같아야 한다** — `nginx/README.md` 에 적었다 |
