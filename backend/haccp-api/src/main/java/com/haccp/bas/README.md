# com.haccp.bas

정본: FE `pages/bas/README.md`. 마스터는 `com.haccp.bas.master` · XML `mapper/bas/master/`.  
공유 URL `/api/v1/bas/{type}` · `/api/v1/bas/company-templates`. 경로 정본 `docs/24_URL_DB_폴더_패키지_정본.md`.

결재선 HTTP는 `/api/v1/sys/code/approval-line-management` — 패키지 `com.haccp.sys.code.approvalline`.
설비·방충 이력 패키지는 `com.haccp.docs.prp`, URL은 `/api/v1/docs/prp/equipment-history` · `/api/v1/docs/prp/pest-device-history`.
