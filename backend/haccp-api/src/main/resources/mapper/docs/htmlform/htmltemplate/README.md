# mapper/docs/html/htmltemplate

`HtmlTemplateMapper.xml` — `com.haccp.docs.htmlform.htmltemplate.HtmlTemplateMapper`

목록 `sp_tbl_html_hyg_prc_ver_r_000`. 복사 FUNCTION이 `html_hyg_prc_NNN`을 반환. 검증점검은 `ccpverifytemplate`, 포장일지는 `ccppkgtemplate`, 가열일지는 `ccphtgtemplate`, 금속검출일지는 `ccpmtltemplate`.
