<?xml version="1.0" encoding="UTF-8"?>
<!--
DocCycleMapper.xml — 문서주기관리 sp_schedule_cycle_management_* 호출.

개발자: 박승우
일자: 2026-08-20
코멘트:
  1) 조회는 TABLE FUNCTION, 저장·삭제·재생성은 PostgreSQL PROCEDURE를 호출한다
  2) DTO 매핑. formTy 는 sys_yn 별칭, details 는 jsonb::text — 빼면 FE/파싱이 깨진다
  3) details·dates는 jsonb라 조회는 ::text, 전달은 CAST(... AS jsonb)로 다룬다
  4) 결재선(apprLineCd)은 114 SP. 주기 없는 양식은 목록 행에서 채운다

PIPELINE[HB99] 문서주기 MyBatis XML
PIPELINE[HB94, HB98] 연관 모듈
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haccp.docs.sch.DocCycleMapper">

    <!--
      좌측 양식 목록 — 사용여부 검색 + 구분(formTy) + 주기 등록여부(ruleYn) + 결재선.
      tmplCd·tmplNm은 SP 내부에서 LIKE 부분일치로 쓰며 공백이면 전체다.
      useYn은 Y/N 등가, 공백이면 전체다.
      apprLineCd는 주기 없는 양식의 우측 폼이 사용양식 결재선을 채울 때 쓴다.
    -->
    <select id="selectForms" resultType="com.haccp.docs.sch.dto.DocCycleFormRow">
        SELECT tmpl_cd       AS "tmplCd",
               tmpl_nm       AS "tmplNm",
               sys_yn        AS "formTy",
               doc_kind      AS "docKind",
               cycle_cd      AS "cycleCd",
               rule_yn       AS "ruleYn",
               use_yn        AS "useYn",
               appr_line_cd  AS "apprLineCd",
               appr_line_nm  AS "apprLineNm"
          FROM sp_schedule_cycle_management_form_r_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{tmplCd, mode=IN, jdbcType=VARCHAR},
            #{tmplNm, mode=IN, jdbcType=VARCHAR},
            #{useYn,  mode=IN, jdbcType=VARCHAR}
          )
    </select>

    <!--
      주기 단건 — 반복 상세는 details jsonb 배열 1컬럼으로 함께 내려온다.
      부서명·담당자명은 표시 전용이며 저장은 deptCd·userId로만 한다.
      결재선은 사용양식(tbl_company_template.appr_line_cd) 조인. 저장은 apprLineCd.
    -->
    <select id="selectCycle" resultType="com.haccp.docs.sch.dto.DocCycleRow">
        SELECT tmpl_cd        AS "tmplCd",
               tmpl_nm        AS "tmplNm",
               base_dt        AS "baseDt",
               cycle_cd       AS "cycleCd",
               nonwork_rule   AS "nonworkRule",
               due_time       AS "dueTime",
               dept_cd        AS "deptCd",
               dept_nm        AS "deptNm",
               user_id        AS "userId",
               user_nm        AS "userNm",
               use_yn         AS "useYn",
               appr_line_cd   AS "apprLineCd",
               appr_line_nm   AS "apprLineNm",
               details::text  AS "details"
          FROM sp_schedule_cycle_management_r_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{tmplCd, mode=IN, jdbcType=VARCHAR}
          )
    </select>

    <!--
      주기 저장 — payload는 camelCase JSON 폼 1건이다.
      양식당 1건 업서트이며 상세는 SP가 전량 교체한다.
      apprLineCd는 사용양식 결재선을 갱신한다. 빈값이면 NULL.
    -->
    <update id="saveCycle" statementType="CALLABLE">
        CALL sp_schedule_cycle_management_c_000(
            #{coCd, mode=IN, jdbcType=VARCHAR},
            CAST(#{payload, mode=IN, jdbcType=VARCHAR} AS jsonb),
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!--
      주기 삭제 — Service Double Check 뒤에 호출한다.
      과거·진행분 예정일은 감사 자료라 남기고 미래 미작성분만 정리된다.
    -->
    <update id="deleteCycle" statementType="CALLABLE">
        CALL sp_schedule_cycle_management_d_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{tmplCd, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!--
      예정일 재생성 — dates는 CycleScheduleGenerator 결과 ["yyyyMMdd", …]이다.
      날짜 계산은 Java가 끝냈고 SP는 반영·알림시각 계산만 한다.
    -->
    <update id="regenerateTasks" statementType="CALLABLE">
        CALL sp_tbl_schedule_task_regen_c_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{tmplCd, mode=IN, jdbcType=VARCHAR},
            CAST(#{dates, mode=IN, jdbcType=VARCHAR} AS jsonb),
            #{dueTime,      mode=IN, jdbcType=VARCHAR},
            #{deptCd,       mode=IN, jdbcType=VARCHAR},
            #{assignUserId, mode=IN, jdbcType=VARCHAR},
            #{alarmMinutes, mode=IN, jdbcType=INTEGER},
            #{userId,       mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!--
      배치 재생성 대상 — 회사 전체의 사용 중 주기 + 상세.
      화면 조회와 달리 테넌트 파라미터가 없어 별도 쿼리로 둔다(사용양식이 미사용이면 제외).
    -->
    <select id="selectActiveCycles" resultType="com.haccp.docs.sch.dto.DocCycleRow">
        <!--
          DTO 매핑. details jsonb 별칭은 유지한다.
          업무 SQL 은 SP 가 갖는다 — 예전에는 이 자리에 조인·jsonb_agg 가 그대로 적혀 있었다.
        -->
        SELECT co_cd        AS "coCd",
               tmpl_cd      AS "tmplCd",
               cycle_cd     AS "cycleCd",
               nonwork_rule AS "nonworkRule",
               base_dt      AS "baseDt",
               due_time     AS "dueTime",
               dept_cd      AS "deptCd",
               user_id      AS "userId",
               details      AS "details"
          FROM sp_tbl_schedule_rule_active_r_000()
    </select>

    <!--
      마감 임박 알림 적재 — 발송 후 alarm_send_yn='Y'로 올려 재발송을 막는다.
      대상 선별·플래그 갱신을 한 SP에서 처리해 배치가 행을 돌지 않는다.

      **알림을 만드는 유일한 자리다.** 예전에는 일일 배치(sp_tbl_schedule_task_generate_c_000)도
      같이 넣어 지연분이 날마다 다시 쌓였다. 그쪽 INSERT는 걷어냈다.

      dormantDays 동안 로그인이 없는 회사는 적재를 건너뛴다 — 플래그는 그래도 닫아
      나중에 깨어났을 때 지난 마감이 한꺼번에 터지지 않게 한다.
    -->
    <!-- 영업일 전환 — 캘린더 화면과 같은 SP. 예정일 이동이 이 목록을 본다 -->
    <select id="selectWorkdays" resultType="string">
        SELECT ymd FROM sp_calendar_workday_r_000(
            #{coCd,    jdbcType=VARCHAR},
            #{fromYmd, jdbcType=VARCHAR},
            #{toYmd,   jdbcType=VARCHAR}
        )
    </select>

    <update id="sendTaskAlarms" statementType="CALLABLE">
        CALL sp_tbl_notification_task_c_000(
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{dormantDays, mode=IN, jdbcType=INTEGER}
        )
    </update>

</mapper>
