<?xml version="1.0" encoding="UTF-8"?>
<!--
DocumentMapper.xml — 문서 허브 sp_tbl_document_* 호출 매퍼.

개발자: 박승우
일자: 2026-08-25
코멘트:
  1) 목록·상세·결재·첨부·버전은 FUNCTION 조회로 통일한다
  2) 파일 메타·결재 처리는 PROCEDURE/FUNCTION으로 DB 잠금·테넌트 조건을 강제한다
  3) 삭제 차단은 전송·결재완료 상태만 IN 단일 쿼리로 검사한다. 보존기간은 공식 기록 보관용이라 쓰지 않는다

PIPELINE[HB84] MyBatis XML
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haccp.docs.documents.DocumentMapper">

    <!--
      회사 사용 템플릿 목록 — sp_tbl_document_template_r_000이 impl/use/company use 조건을 모두 강제한다.
      form_path는 서버 내부에서만 쓰고, Service가 인증된 form URL로 치환해 외부에 물리 경로를 노출하지 않는다.
    -->
    <select id="selectTemplates" resultType="com.haccp.docs.documents.dto.DocumentTemplateRow">
        SELECT * FROM sp_tbl_document_template_r_000(
            #{coCd, mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!-- 양식 파일 업로드 — 이력 append + 현재 적용본 갱신. 기본 제공본은 없을 때만 채운다 -->
    <update id="insertCompanyTemplateFile" statementType="CALLABLE">
        CALL sp_hwp_template_management_file_c_000(
            #{coCd, jdbcType=VARCHAR},
            #{tmplCd, jdbcType=VARCHAR},
            #{fileNm, jdbcType=VARCHAR},
            #{formPath, jdbcType=VARCHAR},
            #{fileSize, jdbcType=BIGINT},
            #{userId, jdbcType=VARCHAR}
        )
    </update>

    <!--
      템플릿 원본 단건 — sp_tbl_document_template_r_001이 회사 사용여부와 구현여부를 다시 확인한다.
      tmplCd는 URL 경로의 업무키이며 form_path는 TemplateFileStorage만 해석한다.
    -->
    <select id="selectTemplate" resultType="com.haccp.docs.documents.dto.DocumentTemplateRow">
        SELECT * FROM sp_tbl_document_template_r_001(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{tmplCd, mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!--
      문서함·결재첨부 목록 — sp_tbl_document_r_000.
      keyword=문서번호·제목, writerId=작성자 ID·이름. 빈 값은 SP가 전체로 본다.
    -->
    <select id="selectDocuments" resultType="com.haccp.docs.documents.dto.DocumentListRow">
        SELECT * FROM sp_tbl_document_r_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{fromDt,   mode=IN, jdbcType=VARCHAR},
            #{toDt,     mode=IN, jdbcType=VARCHAR},
            #{tmplCd,   mode=IN, jdbcType=VARCHAR},
            #{status,   mode=IN, jdbcType=VARCHAR},
            #{keyword,  mode=IN, jdbcType=VARCHAR},
            #{writerId, mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!--
      결재대기(sign-ready) — sp_sign_ready_r_000.
      내 APPROVE 단계가 대기(W)인 REQ 문서. writerId는 작성자 칸.
    -->
    <select id="selectSignReady" resultType="com.haccp.docs.documents.dto.DocumentListRow">
        SELECT * FROM sp_sign_ready_r_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR},
            #{fromDt,   mode=IN, jdbcType=VARCHAR},
            #{toDt,     mode=IN, jdbcType=VARCHAR},
            #{keyword,  mode=IN, jdbcType=VARCHAR},
            #{writerId, mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!--
      결재완료(sign-ok) — sp_sign_ok_r_000.
      내가 APPROVE 단계로 승인·반려한 문서. 작성자 자동승인은 빼 둔다.
    -->
    <select id="selectSignOk" resultType="com.haccp.docs.documents.dto.DocumentListRow">
        SELECT * FROM sp_sign_ok_r_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR},
            #{fromDt,   mode=IN, jdbcType=VARCHAR},
            #{toDt,     mode=IN, jdbcType=VARCHAR},
            #{keyword,  mode=IN, jdbcType=VARCHAR},
            #{writerId, mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!-- 문서 공통 헤더 단건 — 테넌트 조건은 SP가 강제 -->
    <select id="selectDocument" resultType="com.haccp.docs.documents.dto.DocumentHeaderRow">
        SELECT * FROM sp_tbl_document_r_001(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT}
        )
    </select>

    <!-- 결재 단계 목록 — WRITE·APPROVE 순. 검토 단계는 없다 -->
    <select id="selectApprovals" resultType="com.haccp.docs.documents.dto.DocumentApprovalRow">
        SELECT * FROM sp_tbl_document_approval_r_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT}
        )
    </select>

    <!-- 첨부 목록 — 내부 filePath는 Service에서만 사용하고 API 응답에서는 제거 -->
    <select id="selectFiles" resultType="com.haccp.docs.documents.dto.DocumentFileRow">
        SELECT * FROM sp_tbl_document_file_r_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT}
        )
    </select>

    <!-- 파일 단건 — 다운로드·물리 삭제 전에 회사 범위와 문서 존재를 함께 확인 -->
    <select id="selectFile" resultType="com.haccp.docs.documents.dto.DocumentFileRow">
        SELECT * FROM sp_tbl_document_file_r_001(
            #{coCd,    mode=IN, jdbcType=VARCHAR},
            #{fileIdx, mode=IN, jdbcType=BIGINT}
        )
    </select>

    <!-- 버전 목록 — 파일 경로는 내부 다운로드에서만 사용 -->
    <select id="selectVersions" resultType="com.haccp.docs.documents.dto.DocumentVersionRow">
        SELECT * FROM sp_tbl_document_version_r_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT}
        )
    </select>

    <!-- 파일 물리 저장이 성공한 뒤 메타 행 등록 — 반환값은 파일 idx -->
    <select id="insertFile" resultType="long">
        SELECT sp_tbl_document_file_c_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{docIdx,   mode=IN, jdbcType=BIGINT},
            #{fileKind, mode=IN, jdbcType=VARCHAR},
            #{fileNm,   mode=IN, jdbcType=VARCHAR},
            #{filePath, mode=IN, jdbcType=VARCHAR},
            #{fileSize, mode=IN, jdbcType=BIGINT},
            #{mimeType, mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!-- HWP 문서형 헤더 저장 — 문서번호·보존기간은 SP가 신규 시 자동 계산 -->
    <select id="saveHwpDocument" resultType="long">
        SELECT sp_tbl_hwp_document_c_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{docIdx,   mode=IN, jdbcType=BIGINT},
            #{tmplCd,   mode=IN, jdbcType=VARCHAR},
            #{baseDt,   mode=IN, jdbcType=VARCHAR},
            #{baseDtTo, mode=IN, jdbcType=VARCHAR},
            #{title,    mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR}
        )
    </select>

    <!-- 파일 삭제 — SP가 결재 잠금을 확인한 뒤 메타 행만 제거 -->
    <update id="deleteFile" statementType="CALLABLE">
        CALL sp_tbl_document_file_d_000(
            #{coCd,    mode=IN, jdbcType=VARCHAR},
            #{fileIdx, mode=IN, jdbcType=BIGINT},
            #{userId,  mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!-- HWP_SRC 등 종류별 일괄 삭제 — 물리 파일은 Service가 먼저 지운 뒤 호출 -->
    <update id="deleteFilesByKind" statementType="CALLABLE">
        CALL sp_tbl_document_file_d_001(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{docIdx,   mode=IN, jdbcType=BIGINT},
            #{fileKind, mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!-- 문서 비고 저장 — 결재 첨부 화면. APV 이후는 SP가 막는다 -->
    <update id="saveRemark" statementType="CALLABLE">
        CALL sp_tbl_document_u_001(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT},
            #{remark, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!-- 작성 목록 제목 — tbl_document.title. 상태와 무관 -->
    <update id="saveTitle" statementType="CALLABLE">
        CALL sp_tbl_document_title_u_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT},
            #{title,  mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!-- 결재취소 — 본인이 처리한 마지막 단계를 되돌린다 -->
    <update id="undoApproval" statementType="CALLABLE">
        CALL sp_tbl_document_approval_u_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{docIdx,   mode=IN, jdbcType=BIGINT},
            #{userId,   mode=IN, jdbcType=VARCHAR},
            #{opinion,  mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!-- 결재 상태 전이 — REQUEST/CANCEL/APPROVE/REJECT -->
    <update id="processApproval" statementType="CALLABLE">
        CALL sp_tbl_document_approval_c_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{docIdx,   mode=IN, jdbcType=BIGINT},
            #{actionCd, mode=IN, jdbcType=VARCHAR},
            #{opinion,  mode=IN, jdbcType=VARCHAR},
            #{userId,   mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!--
      문서 삭제 사전 차단 — 전송(REQ)·결재완료(APV)의 첫 건만 반환한다.
      전송대기(WRK)·반려(RJT)는 초안이라 막지 않는다. 보존기간은 공식 기록 보관용이라 여기 쓰지 않는다.
      docIdxs는 객체 배열에서 추출한 대리키 목록이고, 서비스 루프 COUNT를 쓰지 않는다.
    -->
    <select id="selectDocumentDeleteBlocker" resultType="com.haccp.common.validation.DeleteBlocker">
        <!--
          삭제 차단 — 전송(REQ)·결재완료(APV) 문서 첫 건.
          검사와 문구는 sp_tbl_document_delete_blocker_r_000 한곳에 있다.
          예전에는 매퍼마다 같은 SQL 을 복제해 두어 상태가 하나 늘면 여섯 곳을 고쳐야 했다.
        -->
        SELECT * FROM sp_tbl_document_delete_blocker_r_000(
            #{coCd, mode=IN, jdbcType=VARCHAR},
            <foreach collection="docIdxs" item="id" open="ARRAY[" separator="," close="]::bigint[]">#{id}</foreach>
        )
    </select>

    <!-- 문서형 임시·반려 문서 삭제 — 파일 물리 삭제 전 메타·결재·버전을 제거 -->
    <update id="deleteDocument" statementType="CALLABLE">
        CALL sp_tbl_document_d_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{docIdx, mode=IN, jdbcType=BIGINT},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

</mapper>
