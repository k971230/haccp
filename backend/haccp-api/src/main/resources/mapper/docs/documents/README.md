# mapper/docs/document

골드 XML. `DocumentMapper.xml` — `com.haccp.docs.documents.DocumentMapper`

문서 허브 `sp_tbl_document_*`. 목록·상세·결재·첨부·삭제 차단. `form_path`는 서버 내부만. Service가 인증 URL로 바꾼다.

Jenkins는 이 XML만 이미지에 넣고 DB migrate는 안 돌린다.
