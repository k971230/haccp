<?xml version="1.0" encoding="UTF-8"?>
<!--
EquipmentHistMapper.xml — 설비 이력 sp_tbl_equipment_hist_* 호출.

개발자: 박승우
일자: 2026-08-07
코멘트:
  1) 목록은 JSONB FUNCTION, 저장·삭제는 PostgreSQL PROCEDURE를 호출한다
  2) equipIdx·idx는 파라미터 바인딩만 하며 SQL 식별자로 조립하지 않는다
  3) coCd는 JWT에서 온 테넌트 키다

PIPELINE[HB94] 설비이력 MyBatis XML
PIPELINE[HB95, HB96] 연관 모듈
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haccp.docs.prp.EquipmentHistMapper">

    <!--
      설비 이력 목록 — 선택 설비의 이력일 역순 JSON 행.
      coCd·equipIdx로 테넌트·설비 범위를 고정한다.
    -->
    <select id="selectList" resultType="string">
        SELECT sp_tbl_equipment_hist_r_000(
            #{coCd,     mode=IN, jdbcType=VARCHAR},
            #{equipIdx, mode=IN, jdbcType=BIGINT}
        )::text
    </select>

    <!--
      설비 이력 저장 — payload는 camelCase JSON 행이다.
      SP가 신규·수정을 판별하고 감사 컬럼은 JWT userId로만 기록한다.
    -->
    <update id="save" statementType="CALLABLE">
        CALL sp_tbl_equipment_hist_c_000(
            #{coCd,    mode=IN, jdbcType=VARCHAR},
            CAST(#{payload, mode=IN, jdbcType=VARCHAR} AS jsonb),
            #{userId,  mode=IN, jdbcType=VARCHAR}
        )
    </update>

    <!--
      설비 이력 삭제 — Service Double Check 뒤에 호출한다.
      SP가 idx + co_cd로 테넌트 경계를 강제한다.
    -->
    <update id="delete" statementType="CALLABLE">
        CALL sp_tbl_equipment_hist_d_000(
            #{coCd,   mode=IN, jdbcType=VARCHAR},
            #{idx,    mode=IN, jdbcType=BIGINT},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )
    </update>

</mapper>
