# mapper/draft/ccp

`CcpVerifyDraftMapper.xml` — `com.haccp.draft.html.CcpVerifyDraftMapper`

작성 SP 는 화면 전용 `sp_ccp_verify_r_000/r_001/c_000/d_000` (123).
양식 목록은 양식관리와 같은 `sp_tbl_html_ccp_chk_ver_r_000` 을 감싸 사용여부 Y·예시(000) 제외로 좁힌다.
저장 직후 `sp_ccp_verify_sign_u_000` — 점검자·승인자·확인 서명 스냅샷.
