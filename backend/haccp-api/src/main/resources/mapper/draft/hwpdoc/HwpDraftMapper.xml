<?xml version="1.0" encoding="UTF-8"?>
<!--
HwpDraftMapper.xml — HWP 양식 작성 조회 SP.

개발자: 박승우
일자: 2026-08-25
코멘트:
  1) 양식 목록은 사용양식 관리와 같은 sp_tbl_document_template_r_000 을 감싸 HWP 만 남긴다
  2) 작성 목록·오늘 할일은 125에서 만든 화면 전용 SP 를 그대로 부른다
  3) 저장·삭제·상세 문은 여기 없다 — 문서 허브(DocumentMapper)를 쓴다

PIPELINE[HB144] HWP 작성 XML
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haccp.draft.hwpdoc.HwpDraftMapper">

    <!--
      작성 가능 양식 — 사용양식 관리 목록과 같은 SP.
      그 SP 가 이미 회사 사용여부 Y·양식 파일 보유만 내리므로 여기서는 HWP 만 남긴다.
      ver_no 는 자사 양식 1 고정 — 다른 작성 화면과 팝업 컬럼을 맞추기 위한 값이다.
    -->
    <select id="selectForms" resultType="com.haccp.draft.dto.DraftFormRow">
        SELECT t.tmpl_cd,
               t.tmpl_nm AS ver_nm,
               1 AS ver_no,
               'Y' AS use_yn,
               <!-- 양식 등록일자 칸 — HWP 는 양식 파일명을 대신 보여 준다 -->
               t.form_file_nm AS ins_dt
          FROM sp_tbl_document_template_r_000(#{coCd}) t
         WHERE t.doc_kind = 'HWP'
         ORDER BY t.tmpl_cd
    </select>

    <!-- 좌측 작성 목록 — doc_kind=HWP 문서만. 결재 여부는 화면이 거른다 -->
    <select id="selectList" resultType="com.haccp.draft.dto.DraftListRow">
        SELECT * FROM sp_draft_hwp_r_000(
            #{coCd,     jdbcType=VARCHAR},
            #{tmplCd,   jdbcType=VARCHAR},
            #{tmplNm,   jdbcType=VARCHAR},
            #{fromDt,   jdbcType=VARCHAR},
            #{toDt,     jdbcType=VARCHAR},
            #{writerId, jdbcType=VARCHAR},
            #{writerNm, jdbcType=VARCHAR},
            #{remark,   jdbcType=VARCHAR}
        )
    </select>

    <!-- 행 추가 팝업 — 오늘 할일 중 HWP 문서주기만 -->
    <select id="selectTasks" resultType="com.haccp.draft.dto.DraftTaskRow">
        SELECT * FROM sp_draft_hwp_task_r_000(
            #{coCd,   jdbcType=VARCHAR},
            #{userId, jdbcType=VARCHAR},
            #{baseDt, jdbcType=VARCHAR}
        )
    </select>
</mapper>
