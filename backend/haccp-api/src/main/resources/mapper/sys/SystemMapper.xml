<?xml version="1.0" encoding="UTF-8"?>
<!--
SystemMapper.xml — 시스템 관리 화면의 고정 SP 조회 매퍼.

개발자: 박승우
일자: 2026-08-06
코멘트:
  1) choose 분기로 화면별 호출 SP를 고정해 사용자 입력으로 테이블명·컬럼명을 만들지 않는다
  2) coCd는 JWT에서 온 테넌트 키이며 모든 조회 SP의 첫 인자로 전달한다
  3) 이력·통계 기본 기간은 오늘이며 기간 필터 저장 API가 추가되기 전에도 안전하게 최신 데이터를 확인할 수 있다

PIPELINE[HB92] 시스템 관리 MyBatis XML
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metis.haccp.sys.SystemMapper">
    <!-- 시스템 관리 목록 — type은 Controller 허용 목록을 통과한 고정 분기값이다 -->
    <select id="selectRows" resultType="map">
        <choose>
            <when test="type == 'company-management'">
                SELECT * FROM sp_tbl_company_r_000(#{coCd, jdbcType=VARCHAR})
            </when>
            <when test="type == 'user-management'">
                SELECT * FROM sp_tbl_user_r_000(#{coCd, jdbcType=VARCHAR}, #{keyword, jdbcType=VARCHAR}, '', '', '')
            </when>
            <when test="type == 'department-management'">
                SELECT * FROM sp_tbl_dept_r_000(#{coCd, jdbcType=VARCHAR}, #{keyword, jdbcType=VARCHAR}, '')
            </when>
            <when test="type == 'role-management'">
                SELECT * FROM sp_tbl_role_r_000(#{coCd, jdbcType=VARCHAR}, '')
            </when>
            <when test="type == 'menu-management'">
                SELECT * FROM sp_tbl_menu_admin_r_000(#{coCd, jdbcType=VARCHAR})
            </when>
            <when test="type == 'common-code-management'">
                SELECT * FROM sp_tbl_code_r_000(#{coCd, jdbcType=VARCHAR}, #{keyword, jdbcType=VARCHAR}, '')
            </when>
            <when test="type == 'login-history'">
                SELECT * FROM sp_tbl_login_log_r_000(#{coCd, jdbcType=VARCHAR}, #{fromDt, jdbcType=VARCHAR}, #{toDt, jdbcType=VARCHAR}, #{keyword, jdbcType=VARCHAR}, '')
            </when>
            <when test="type == 'screen-usage-statistics'">
                SELECT * FROM sp_tbl_view_stat_daily_r_000(#{coCd, jdbcType=VARCHAR}, #{fromDt, jdbcType=VARCHAR}, #{toDt, jdbcType=VARCHAR}, #{keyword, jdbcType=VARCHAR})
            </when>
            <otherwise>
                SELECT * FROM sp_tbl_audit_log_r_000(#{coCd, jdbcType=VARCHAR}, #{fromDt, jdbcType=VARCHAR}, #{toDt, jdbcType=VARCHAR}, '', #{keyword, jdbcType=VARCHAR}, '')
            </otherwise>
        </choose>
    </select>

    <!-- 시스템 관리 저장 — JSON payload는 Service가 평문 비밀번호 해시·테넌트 제거 뒤 만든다 -->
    <update id="save" statementType="CALLABLE">
        CALL sp_tbl_system_c_000(
            #{coCd, jdbcType=VARCHAR},
            #{type, jdbcType=VARCHAR},
            CAST(#{payload, jdbcType=VARCHAR} AS jsonb),
            #{userId, jdbcType=VARCHAR}
        )
    </update>

    <!-- 삭제 참조 검증 — 모든 대상 idx를 bigint 배열 하나로 전달해 DB에서 한 번에 검사한다 -->
    <select id="selectDeleteBlocker" resultType="com.metis.haccp.common.validation.DeleteBlocker">
        SELECT ref_key, target
          FROM sp_tbl_system_delete_blocker_r_000(
              #{coCd, jdbcType=VARCHAR},
              #{type, jdbcType=VARCHAR},
              ARRAY[<foreach collection="idxs" item="idx" separator=",">#{idx, jdbcType=BIGINT}</foreach>]::bigint[]
          )
         ORDER BY ref_key
         LIMIT 1
    </select>

    <!-- 시스템 관리 삭제 — Service의 validate-delete와 동일한 Double Check 뒤에만 호출한다 -->
    <update id="delete" statementType="CALLABLE">
        CALL sp_tbl_system_d_000(
            #{coCd, jdbcType=VARCHAR},
            #{type, jdbcType=VARCHAR},
            #{idx, jdbcType=BIGINT},
            #{userId, jdbcType=VARCHAR}
        )
    </update>

    <!-- 사용자 서명 경로 조회 — JWT 회사·대상 user_id 범위 -->
    <select id="selectSignPath" resultType="string">
        SELECT sign_path
          FROM tbl_user
         WHERE co_cd = #{coCd, jdbcType=VARCHAR}
           AND user_id = #{userId, jdbcType=VARCHAR}
         LIMIT 1
    </select>

    <!-- 사용자 서명 경로 갱신 — 업로드 직후 상대경로만 저장 -->
    <update id="updateSignPath">
        UPDATE tbl_user
           SET sign_path = #{signPath, jdbcType=VARCHAR},
               upd_id = #{actorId, jdbcType=VARCHAR},
               upd_dt = now()
         WHERE co_cd = #{coCd, jdbcType=VARCHAR}
           AND user_id = #{userId, jdbcType=VARCHAR}
    </update>
</mapper>
