# .run

IntelliJ 실행 구성. **팀 공용이라 커밋한다.**

| 파일 | 무엇 |
|---|---|
| `HaccpApiApplication.run.xml` | 서버 기동 구성. IntelliJ 에서 Run 목록에 바로 뜬다 |

새로 받은 사람이 실행 구성을 손으로 만들지 않게 하려고 둔다.
`.idea/` 는 개인 설정이라 git 에 안 올린다 — 이 폴더만 예외다.

## 관련

- 띄우는 법: [`../README.md`](../README.md)
- IDE 규칙: `.cursor/rules/10-ide-workflow.mdc` (백엔드 IntelliJ · 프론트 VSCode)
