# HACCP 사용자 매뉴얼 (코워크 작성용 원고)

> **마지막 갱신 2026-09-03** — 오늘 할 일 URL 을 `/board/today-tasks` 로 맞추고
> 일정 캘린더·비밀번호 변경을 넣었다. 그 앞 다섯 자리(금속검출 「모두 적합」 ·
> 기록표 품명 필수 · 문서주기 결재선·담당자 필수 · 양식 점검항목 0건 차단 · 열 초기화)는 그대로다.
> 배포된 HTML 매뉴얼(v0.1)과 다른 자리는 `grokbot/test/메뉴얼_보완.md` 6·7장에 정리돼 있다.

주소는 전부 `http://180.71.58.87/haccp/` 뒤에 붙는다. 예: `.../haccp/board/today-tasks`
**`https` 로 열지 않는다** — 지금 인증서가 자체서명이라 브라우저 경고가 뜨고
한글 편집기 관련 콘솔 오류가 쌓인다(화면·저장은 정상).

이 원고는 **누르는 순서**로 썼다. 화면마다 「1 터치 → 2 터치 → 3 터치」가 본문이고,
그 아래 「막히는 자리」에 실제로 막히는 것만 짧게 적었다.

읽는 순서는 일하는 순서와 같다.

| 부 | 누가 | 무엇 |
|---|---|---|
| 0부 | 모두 | 로그인과 공통 조작 |
| 1부 | 관리자 | 기초 세팅 — 처음 한 번 (부서 → 사용자 → 권한 → 결재선 → 양식 → 주기) |
| 2부 | 팀원 | 매일 작성 — 오늘 할 일 → 작성 → 전송 |
| 3부 | 팀장 | 결재 — 결재대기 → 승인 → 문서함 |
| 4부 | 모두 | 이탈이 났을 때 |
| 5부 | 관리자 | 이력·통계 |

---

# 0부. 로그인과 공통 조작

## 0-1. 로그인

주소 `/haccp/login`

| 터치 | 무엇 |
|---|---|
| 1 | **아이디** 칸에 아이디를 넣는다 |
| 2 | **비밀번호** 칸에 비밀번호를 넣는다 |
| 3 | 필요하면 **아이디 저장** · **자동 로그인**을 켠다 |
| 4 | **로그인** |

회사를 고르는 칸은 없다. 아이디가 회사를 안다.

## 0-2. 화면 구조

로그인하면 화면이 셋으로 나뉜다.

```
┌ 왼쪽 사이드바 ┬ 위 탭 줄 ────────────────────┐
│ 메뉴 트리     │ [오늘 할 일][CCP 가열공정] … │
│               ├──────────────────────────────┤
│               │  지금 보고 있는 화면          │
└───────────────┴──────────────────────────────┘
```

- 사이드바 위 **접기/펴기** 버튼으로 메뉴를 접는다. 화면이 좁을 때 쓴다
- 메뉴를 누르면 **탭이 하나 생긴다.** 이미 열려 있으면 그 탭으로 간다
- 탭에 **오른쪽 클릭** — 이 탭 닫기 · 다른 탭 모두 닫기 · 왼쪽 탭 모두 닫기 · 오른쪽 탭 모두 닫기 · 모든 탭 닫기
- 오른쪽 위 **로그아웃**
- 화면 **맨 아래 줄(푸터)** 의 열쇠 아이콘으로 **비밀번호를 바꾼다** — 아래 0-2a

**권한그룹이 메뉴를 가른다.** 팀원 계정으로 들어가면 사이드바에 「시스템」이 없다.

### 좌우로 나뉜 화면은 가운데를 끌 수 있다

작성 5화면·양식관리·문서주기, 그리고 **결재 4화면(결재첨부·결재대기·결재완료·문서함)** 은
왼쪽 목록과 오른쪽 지면으로 나뉜다. **가운데 경계선을 끌면 비율이 바뀌고**,
그 화면에 한해 **다음에 열 때도 그대로 기억한다.**

## 0-2a. 비밀번호 변경

로그인한 **본인 비밀번호만** 바꾼다. 남의 비밀번호는 여기서 못 바꾼다.

| 터치 | 무엇 |
|---|---|
| 1 | 화면 **맨 아래 줄**에서 열쇠 아이콘을 누른다 |
| 2 | **현재 비밀번호**를 넣는다 |
| 3 | **새 비밀번호**와 **새 비밀번호 확인**을 같게 넣는다. 지금 쓰는 비밀번호와는 달라야 한다 |
| 4 | **변경** |

바꾼 뒤에도 로그인은 유지된다. 다음 로그인부터 새 비밀번호를 쓴다.

## 0-3. 표(그리드) 공통 조작 — 표가 있는 화면은 같다

거의 모든 화면이 같은 표를 쓴다. 한 번 익히면 전 화면에서 통한다.

**표 위 버튼**

| 버튼 | 색 | 무엇 |
|---|---|---|
| 행추가 | 주황 | 빈 줄 하나를 만든다. **아직 저장 아님** |
| 저장 | 파랑 | 고친 줄을 DB 에 넣는다 |
| 삭제 | 빨강 | 체크한 줄, 체크가 없으면 지금 줄 |
| 조회 | — | 검색 조건대로 다시 읽는다 |

**키보드** — `Ctrl` 조합은 하나도 안 쓴다. 브라우저 단축키를 뺏지 않는다.

| 키 | 무엇 |
|---|---|
| ↑ ↓ | 줄 이동 (끝에서 멈춘다. 처음으로 돌지 않는다) |
| Enter · F2 | 칸 편집 열기 (엑셀 F2 와 같다) |
| Tab · Shift+Tab | 옆 칸으로 이동 |
| Delete · Backspace | 칸 비우기 |
| Escape | 편집칸만 닫는다 |
| 열 제목 클릭 | 정렬. Shift+클릭이면 두 번째 정렬 |

> **Escape 는 「취소」가 아니라 「닫기」다.** 엑셀과 다르다.
> 친 값은 그대로 남고 줄은 「변경」으로 표시된다. 되돌리려면 **저장하지 말고 조회를 다시** 누른다.
> DB 에 들어가는 시점은 언제나 **저장**을 눌렀을 때다.

**열 꾸미기** — 열 너비·순서·숨김은 **아이디별로 저장된다.** 내가 맞춰 놓은 표는 다음에 들어와도 그대로다.
되돌리고 싶으면 **열 → 열 초기화**를 누른다. 그 화면의 열 꾸미기가 처음 상태로 돌아간다.
정렬·필터·검색어는 저장하지 않는다 (어제 걸어 둔 정렬 때문에 오늘 조회가 이상해 보이지 않게).

**건수** — 표 아래에 「총 n건 · 표시 n건」이 나온다. 걸러도 **총 건수는 줄지 않는다.**
화면에 줄이 스물 몇 개만 보여도 정상이다 — 스크롤할 때 더 그린다.

**CSV 내보내기** — 지금 **보이는 열**과 **거른 뒤 줄**만 나간다. 숨긴 열은 안 나간다.

## 0-4. 삭제는 늘 두 단계다

「삭제」를 누르면 곧바로 지우지 않는다. 서버가 먼저 **지워도 되는지** 보고,
걸리는 게 있으면 이유를 알려 준다 (예: 하위 부서가 있다, 소속 사용자가 있다).

## 0-5. 날짜와 시각

- 날짜 칸은 달력으로 고른다. 화면에는 `2026-08-28` 로 보인다
- 시각 칸은 `09:10` 처럼 5자다
- 달력에 없는 날은 화면이 막는다. 윤년은 정상으로 본다

---

# 1부. 기초 세팅 — 관리자, 처음 한 번

**이 순서대로 한다.** 앞 단계를 건너뛰면 뒤에서 반드시 막힌다.

```
부서 → 사용자 → 서명 → 권한그룹 → (공통코드) → 결재선 → 양식 → 문서주기
```

## 1-1. 부서관리 `/sys/code/department-management`

왼쪽 부서 트리, 오른쪽 부서 표.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 트리에서 **전체** 또는 상위 부서를 고른다 |
| 2 | 오른쪽 표 위 **행추가** |
| 3 | **부서코드**를 친다 |
| 4 | **부서명**을 친다 |
| 5 | **상위부서** 칸의 버튼을 눌러 팝업에서 고른다. 최상위면 **(없음)** |
| 6 | **사용여부**를 사용으로 둔다 |
| 7 | **저장** |

**막히는 자리**

- 부서코드는 **새 줄에서만** 칠 수 있다. 저장한 뒤에는 못 바꾼다
- 상위부서는 직접 못 친다. 반드시 팝업으로 고른다 (자기 자신·자기 하위는 팝업에 안 나온다)
- 하위 부서나 소속 사용자가 있으면 **삭제가 막힌다.** 아래부터 정리한다

## 1-2. 사용자관리 `/sys/code/user-management`

| 터치 | 무엇 |
|---|---|
| 1 | **행추가** |
| 2 | **사용자 ID** — 로그인에 쓸 아이디 |
| 3 | **사용자명** |
| 4 | **권한그룹** 칸의 버튼 → 팝업에서 고른다 (ADMIN · HACCP_MASTER · HACCP_TEAM · VIEWER …) |
| 5 | **부서** 칸의 버튼 → 팝업에서 고른다. 비워도 된다 |
| 6 | **비밀번호**를 넣는다 |
| 7 | **저장** |

필수는 **사용자 ID · 사용자명 · 권한그룹** 셋이다. 부서는 비워도 된다.

**비밀번호를 바꿀 때** — 비밀번호 칸에 새 값을 넣고 저장한다.
**비워 두면 지금 비밀번호가 그대로 유지된다.** 빈칸으로 저장해도 지워지지 않는다.

### 1-2-1. 서명 등록 — 빼먹으면 안 되는 것

종이(지면)의 결재 도장칸에 **그림 서명**이 찍히게 하려면 사람마다 서명을 올려야 한다.
안 올리면 이름 글자가 대신 찍힌다 — 고장이 아니라 설계지만, 서류로는 서명이 낫다.

| 터치 | 무엇 |
|---|---|
| 1 | 표에서 그 사람 줄을 고른다 |
| 2 | **서명** 칸의 버튼을 누른다 |
| 3 | **업로드** (이미 있으면 「서명 교체」) |
| 4 | 파일을 고른다 — **PNG 또는 JPG, 10MB 이하** |
| 5 | **닫기** |

**막히는 자리**

- **저장하지 않은 새 줄에는 서명을 못 올린다.** 먼저 저장하고 그 줄을 다시 고른다
- PNG·JPG 가 아니면 막힌다
- 지울 때는 같은 팝업의 **삭제**

## 1-3. 권한그룹관리 `/sys/code/role-management`

왼쪽은 화면 트리(체크박스), 오른쪽은 권한그룹 표다.

**그룹을 만든다**

| 터치 | 무엇 |
|---|---|
| 1 | 오른쪽 **행추가** |
| 2 | **그룹코드** · **그룹명** |
| 3 | 오른쪽 **저장** |

**그 그룹이 볼 화면을 정한다**

| 터치 | 무엇 |
|---|---|
| 4 | 오른쪽 표에서 **저장된 그룹**을 클릭한다 |
| 5 | 왼쪽 트리에서 보여 줄 화면을 체크한다. 폴더를 체크하면 그 아래가 전부 켜진다 |
| 6 | 왼쪽 트리 쪽 **저장** |

**막히는 자리**

- **저장 버튼이 둘이다.** 그룹 저장과 화면권한 저장은 별개다. 그룹을 저장하지 않으면 왼쪽 트리가 잠긴 채고 「저장된 권한그룹을 선택하세요」가 뜬다
- 그룹코드는 새 줄에서만 칠 수 있다
- 그 그룹에 속한 사용자가 있으면 삭제가 막힌다

## 1-4. 공통코드관리 `/sys/code/common-code-management` (필요할 때만)

표가 셋이다.

| 자리 | 무엇 | 고칠 수 있나 |
|---|---|---|
| 왼쪽 | 대분류 | 조회만 |
| 오른쪽 위 | 시스템 코드 | 조회만 — 플랫폼 표준이라 못 고친다 |
| 오른쪽 아래 | 사용자 코드 | 행추가·저장·삭제 |

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽에서 대분류를 고른다 |
| 2 | 오른쪽 아래 표 **행추가** |
| 3 | **대분류코드** · **세부코드** · **코드명** · 정렬 |
| 4 | **저장** |

여기서 고친 값은 **전 화면 콤보에 바로 반영된다.** 필요 없는 코드를 늘리지 않는다.

## 1-5. 결재선관리 `/sys/code/approval-line-management`

**이걸 안 고치면 팀원이 올린 일지가 팀장 결재함에 안 온다.** 세팅에서 가장 자주 놓치는 자리다.

왼쪽은 결재선 목록, 오른쪽은 그 결재선의 **1 작성 · 2 승인** 두 줄이다 (고정).

**기본 결재선(DEFAULT)의 승인자를 팀장으로 바꾼다**

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽에서 **DEFAULT** 줄을 고른다 |
| 2 | 오른쪽 **2 승인** 줄의 **결재자** 칸 버튼을 누른다 |
| 3 | 팝업에서 팀장을 고른다 — **부서는 따라 들어온다** |
| 4 | 오른쪽 **저장** |

**결재선을 새로 만든다**

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 **행추가** |
| 2 | **결재선코드** · **결재선명** |
| 3 | 왼쪽 **저장** |
| 4 | 그 줄을 고르고 오른쪽에서 작성·승인 결재자를 고른 뒤 오른쪽 **저장** |

**막히는 자리**

- 오른쪽 두 줄은 **행추가·삭제가 없다.** 저장만 있다
- **검토 단계는 없다.** 결재는 작성 → 승인 둘로만 흐른다
- **작성과 승인은 항상 사용이다.** 끄려고 하면 「작성과 승인은 항상 사용입니다」가 뜬다
- **DEFAULT 결재선은 지울 수 없다**
- **결재자를 안 넣어도 결재선은 저장된다** — 코드·이름을 먼저 만들고 결재자를 넣는 순서라서다.
  안 넣은 채 저장하면 「결재자를 아직 안 넣었습니다. 이 결재선으로는 결재가 가지 않습니다」가 뜬다.
  그대로 두면 **문서주기관리에서 그 결재선을 고를 때 막힌다**

## 1-6. 쓸 양식 고르기

양식은 두 종류다. **한글(HWP) 양식**과 **화면 입력(HTML) 양식**.

### 1-6-1. 사용양식관리 (한글) `/docs/hwp/hwp-template-management`

왼쪽 목록, 오른쪽 한글 미리보기.

**쓸 양식을 켠다**

| 터치 | 무엇 |
|---|---|
| 1 | 검색줄에 **양식명**을 넣고 **조회** (구분·사용여부로도 거른다) |
| 2 | 목록에서 쓸 양식 줄을 고른다 — 오른쪽에 미리보기가 뜬다 |
| 3 | **사용여부**를 **사용**으로 바꾼다 |
| 4 | **저장** |

**우리 양식을 새로 올린다**

| 터치 | 무엇 |
|---|---|
| 1 | **행추가** |
| 2 | **양식명**을 넣는다 |
| 3 | **저장** — 여기까지 해야 파일을 붙일 수 있다 |
| 4 | **업로드** → 한글 파일을 고른다 |
| 5 | 사용여부를 **사용**으로 두고 **저장** |

**나머지 버튼**

| 버튼 | 무엇 |
|---|---|
| 내보내기 | 지금 양식 파일을 내려받는다 |
| 불러오기 | **파일 이력** 팝업 — 예전 버전을 골라 되돌린다 |
| 초기화 | 기본 제공 양식으로 되돌린다 |

**막히는 자리**

- **저장하기 전에는 업로드가 안 된다.** 「아직 저장하지 않은 사용자추가 양식입니다」가 뜬다
- **시스템 제공 양식은 지울 수 없다.** 안 쓰면 사용여부를 미사용으로 둔다
- 구분(시스템/자사)은 만든 뒤 못 바꾼다
- 파일은 덮어쓰지 않고 쌓인다. 그래서 「불러오기」로 언제든 되돌릴 수 있다
- 이 화면의 사용여부 검색은 **기본이 전체**다 — 미사용 양식을 다시 켜야 하는 곳이라 일부러 그렇게 뒀다

### 1-6-2. HTML 양식관리 5화면 `/docs/html-form/…`

| 화면 | URL |
|---|---|
| 일반위생·공정점검 양식관리 | `/docs/html-form/hyg-process-template` |
| CCP 검증점검표 양식관리 | `/docs/html-form/ccp-verify-template` |
| CCP 포장공정 일지관리 | `/docs/html-form/ccp-pkg-template` |
| CCP 가열공정 일지관리 | `/docs/html-form/ccp-htg-template` |
| CCP 금속검출공정 일지관리 | `/docs/html-form/ccp-mtl-template` |

다섯 화면 조작이 같다. 왼쪽 양식 목록, 오른쪽 지면(종이 모양).

**표준 양식을 우리 것으로 복사해 고친다**

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 **행추가** — 표준 양식을 복사한 새 양식이 생긴다 |
| 2 | **양식명**을 우리 이름으로 바꾼다 |
| 3 | 왼쪽 **저장** |
| 4 | 오른쪽 지면 위 **수정** |
| 5 | 지면의 제목·점검항목·한계기준을 고친다. 항목을 늘릴 때는 표 오른쪽 끝 **행추가** |
| 6 | 오른쪽 **저장** (되돌리려면 **취소**) |
| 7 | 왼쪽 목록에서 **사용여부**를 사용으로 두고 왼쪽 **저장** |

**막히는 자리**

- **표준 양식(코드가 `_000` 으로 끝나는 것)은 못 고치고 못 지운다.** 항상 미사용이다. 반드시 복사해서 쓴다
- 새로 만든 양식은 기본이 **사용**이다
- **양식명은 필수**다
- **점검항목이 하나도 없으면 저장이 막힌다** — 「점검항목을 한 줄 이상 넣으세요」.
  항목 없는 양식으로는 일지를 전송할 수 없어서 **만드는 자리에서** 막는다
- 저장 안 한 새 줄이 있으면 또 행추가할 수 없다 — 저장하거나 지운다
- 여기서 **사용여부를 사용으로 두지 않으면 작성 화면의 양식 선택 팝업에 안 나온다**

## 1-7. 문서주기관리 `/docs/sch/schedule-cycle-management`

「어느 양식을 언제 써야 하는지」를 정한다. 여기서 건 주기가 **오늘 할 일**에 뜬다.

왼쪽은 사용양식 목록(조회 전용), 오른쪽은 그 양식의 주기 한 벌이다.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽에서 주기를 걸 양식을 고른다 |
| 2 | **관리 시작일** — 이 날짜 앞으로는 예정일을 만들지 않는다 |
| 3 | **주기** — 매일 · 매주 · 매월 · 분기 · 반기 · 매년 · 비정기 |
| 4 | 아래 **반복 설정**이 주기에 따라 바뀐다 — 매주면 요일, 매월이면 실행일(1~31) 또는 **말일 실행**, 분기·반기·매년이면 월과 일 |
| 5 | **비영업일 처리** — 그대로 · 이전 영업일 · 다음 영업일 |
| 6 | **마감시간** — 기본 18:00. 이 시각이 지나면 오늘 할 일에서 **빨간 줄(기한 경과)** 로 올라온다 |
| 7 | **결재선** 옆 **선택** → 결재선을 고른다. 작성 단계에 사람이 있으면 담당자·담당부서가 자동으로 찬다 |
| 8 | **담당자** 옆 **선택** → 사람을 고른다 (담당부서는 자동) |
| 9 | **사용여부**를 사용으로 |
| 10 | **저장** — 저장하는 순간 예정일이 다시 만들어진다 |

**막히는 자리**

- **관리 시작일은 필수**다
- 매주인데 요일을 안 고르면 「반복할 요일을 선택하세요」
- 매월인데 실행일도 말일도 안 고르면 「실행일 또는 말일을 선택하세요」
- **결재선은 필수**다 — 안 고르면 「결재선을 선택하세요」.
  비워 두면 여기서 만든 일지가 **아무 결재함에도 가지 않는다**
- **담당자도 필수**다 — 안 고르면 「담당자를 선택하세요」.
  비워 두면 그 과제가 **누구의 「오늘 할 일」에도 뜨지 않는다**
- 고른 결재선에 승인자가 없으면 「고른 결재선에 승인 결재자가 없습니다」 —
  **결재선관리에서 그 결재선에 승인자를 먼저 넣는다**
- 양식 하나에 주기는 하나다. 저장은 덮어쓰기다
- 저장하지 않고 왼쪽에서 다른 양식을 고르면 확인 창이 뜬다
- 양식 등록·삭제는 여기가 아니라 **사용양식관리**에서 한다

## 1-8. 메뉴관리 `/sys/code/menu-management` (거의 안 건드린다)

**행추가가 없다.** 메뉴는 시스템이 깔아 둔 것이고, 여기서 고칠 수 있는 것은 **메뉴명**과 **사용여부** 둘뿐이다.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 트리에서 메뉴를 고른다 |
| 2 | 오른쪽에서 메뉴명이나 사용여부를 고친다 |
| 3 | **저장** |

---

# 2부. 매일 작성 — 팀원

## 2-1. 오늘 할 일 `/board/today-tasks`

로그인하면 처음 보는 화면이다.

**맨 위 다섯 장**

| 카드 | 무엇 |
|---|---|
| 오늘 작성 과제 | 오늘 써야 할 일지 수 |
| 과제 완료 | 그중 끝난 것 |
| 미결재 | 올렸는데 아직 결재가 안 난 것 |
| 이탈·개선조치 | 조치가 남은 것 |
| 최근 문서 | 최근에 오간 문서 |

**아래 두 표**

| 표 | 무엇 |
|---|---|
| 오늘 할 일 | 오늘 써야 할 과제 · 개선조치 |
| 최근 문서 | 최근 문서 목록. **이전 / 다음 / 새로고침** 으로 넘긴다 |

**일을 시작한다**

| 터치 | 무엇 |
|---|---|
| 1 | 「오늘 할 일」 표에서 쓸 줄을 찾는다 |
| 2 | 그 줄을 **더블클릭** |
| 3 | 그 양식의 작성 화면이 열리고 **줄이 이미 깔려 있다** — 양식과 날짜가 채워진 채로 |

- 이미 쓴 문서가 있는 과제면 **그 문서**가 열린다
- 아직 안 쓴 과제면 **새 줄**이 깔린다 (예정이든 지연이든 같다)
- 개선조치 줄이면 **이탈·개선조치 화면**이 열린다

**한 번 클릭은 선택만 한다. 이동은 더블클릭이다.**

**빨간 줄은 기한이 지난 것**이고 맨 위로 올라온다.
**「미완료 과제만 보기」는 기본으로 켜져 있다.** 끝난 것까지 보려면 체크를 푼다.

## 2-1a. 일정 캘린더 `/board/calendar`

회사 전체 과제를 **한 달 칸**에서 본다. 색은 내 담당·밀림·완료·오늘 할 일·그 외로 갈린다.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 메뉴 **일정 캘린더** |
| 2 | 화살표로 달을 옮긴다 |
| 3 | 칸의 과제를 **더블클릭**하면 작성 화면으로 간다 |

**더블클릭이 열리는 때**

- 담당이 없거나, 담당이 나라면 연다 (밀린 것·오늘 할 일도 같다)
- 이미 끝난 것은 **문서가 있을 때만** 연다. 문서가 없으면 작성 화면으로 안 보낸다
- 다른 사람 담당은 안 연다

**주말·공휴일을 영업일로 바꿀 때** (쓰기 권한이 있을 때)

| 터치 | 무엇 |
|---|---|
| 1 | 그 날 칸의 체크를 켠다 |
| 2 | **저장** |

저장하면 그 날을 영업일로 보고 예정일을 다시 잡는다.

## 2-2. 일지 작성 — HTML 5화면 공통

| 화면 | URL |
|---|---|
| 일반위생·공정점검 작성 | `/draft/html/hyg-process` |
| CCP 검증점검표 작성 | `/draft/html/ccp-verify` |
| CCP 포장공정 작성 | `/draft/ccp-monitoring/ccp-pkg` |
| CCP 가열공정 작성 | `/draft/ccp-monitoring/ccp-htg` |
| CCP 금속검출공정 작성 | `/draft/ccp-monitoring/ccp-mtl` |

**다섯 화면이 똑같이 움직인다.** 하나만 익히면 된다.

```
왼쪽 = 작성 목록 (어느 날, 어느 양식)      오른쪽 = 지면 (실제로 채우는 종이)
```

### 순서

| 터치 | 어디 | 무엇 |
|---|---|---|
| 1 | 왼쪽 위 | **행추가** — 오늘 날짜로 빈 줄이 생기고 양식 선택 팝업이 뜬다 |
| 2 | 팝업 | 쓸 양식을 고른다 |
| 3 | 왼쪽 표 | 날짜가 오늘이 아니면 **일자** 칸을 열어 고친다 |
| 4 | 왼쪽 위 | **저장** ← **여기까지 해야 오른쪽에 글씨를 쓸 수 있다** |
| 5 | 오른쪽 지면 | 점검값을 채운다 |
| 6 | 오른쪽 지면 | **모두 적합**을 눌러 판정을 한 번에 채우고, 부적합인 줄만 다시 누른다 |
| 7 | 오른쪽 위 | **작성 후 저장** |
| 8 | 오른쪽 위 | **전송** → 저장이 안 됐으면 「저장 후 전송」을 먼저 묻고, 확인 창에서 예 |

**4번을 빼먹는 사람이 가장 많다.** 저장 전에는 오른쪽 지면이 전부 잠겨 있고,
지면 위에 「왼쪽 목록에서 **저장**을 먼저 눌러야 이 지면에 값을 쓸 수 있습니다」가 뜬다.

### 왼쪽 버튼 넷

| 버튼 | 대상 |
|---|---|
| **행추가** | 새 줄 하나 |
| **저장** | 날짜와 양식코드를 등록한다. 이걸 해야 문서번호가 생긴다 |
| **삭제** | 체크한 줄들. 체크가 없으면 지금 줄 |
| **모두 전송** | 체크한 줄 중 **보낼 수 있는 것만** 한 번에 보낸다 |

### 왼쪽 목록에서 알아 둘 것

- **제목** 칸은 언제든 고칠 수 있다. 비워 두면 **양식명이 자동으로** 들어가고,
  **한 번 넣은 제목은 지면을 저장해도 지워지지 않는다.** 목록 검색이 이 칸을 본다
  - 자동 제목에 일자가 붙는 모양이 **화면마다 다르다** — 위생·검증·한글은 `양식명 (2026-09-02)`,
    금속검출은 `양식명 (20260902)`, 포장·가열은 일자가 붙지 않는다.
    보기 싫으면 **직접 쳐서 덮으면 된다** (알려진 어긋남, 인수인계 7절)
- **반려된 줄은 노란색**으로 칠해진다. 상태 배지는 「전송대기」로 돌아와 있어
  색이 없으면 반려된 것인지 처음 쓰는 것인지 구분되지 않는다.
  **사유는 결재첨부 화면(2-4)에서 본다**

### 오른쪽 버튼 넷

| 버튼 | 대상 |
|---|---|
| **작성 후 저장** | 지면에 쓴 값만 저장한다. **전송하지 않는다** |
| **전송** | **이 문서 한 건**을 결재로 올린다 |
| **삭제** | 이 문서 한 건 |
| **전송취소** | 올린 것을 되돌린다. 전송대기로 돌아와 다시 고칠 수 있다 |

왼쪽 「저장·삭제」와 오른쪽 「저장·삭제」는 **이름이 같아도 대상이 다르다.**
왼쪽은 목록 줄, 오른쪽은 지금 열려 있는 문서 하나다.

**목록 접기** — 오른쪽 맨 왼쪽 버튼으로 왼쪽 목록을 접으면 지면이 넓어진다.

### 지면(종이)에서

- 맨 위 **결재란** — 작성자·승인자 도장칸. 서명을 올려 뒀으면 그림이, 아니면 이름이 찍힌다
- 가운데 **점검 항목** 또는 **기록 표**
- 맨 아래 **한계기준 이탈내용**(일반위생은 「특이사항」) **· 개선조치 및 결과 · 조치자 · 확인**

**기록 표가 있는 화면(포장·가열·금속검출)은 줄을 늘릴 수 있다.**
영역마다 버튼이 따로다 — 포장·가열은 「작업 전 행 추가」 「작업 종료 행 추가」,
금속검출은 거기에 「제품 통과 행 추가」가 더 있다. 각 버튼은 **자기 영역 끝에만** 줄을 붙인다.
작업 전 끝에 붙이면 작업 종료 줄 **바로 위**에 보인다.
영역 첫 줄은 라벨이라 지우는 버튼이 없다. 내가 더한 줄만 오른쪽 `×` 로 지운다.

### 「모두 적합」이 하는 일

**판정만 적합으로 채운다.** 온도·시각 같은 실측값은 채우지 않는다 — 그건 직접 넣어야 한다.
이름 때문에 「다 됐다」로 읽혀서 온도를 안 채우고 전송하다 막히는 일이 있었다.

**금속검출은 감도 5칸까지 같이 채운다.** 감도점검이 정상이면
철·스테인리스 시편은 **검출(O)**, **제품만 통과는 미검출(X)** 이다.

**이미 찍어 둔 칸도 덮는다.** 부적합으로 찍었던 O/X 를 적합 모양으로 되돌리는 게 이 버튼의 일이라
「모두 적합」을 누르면 그 줄의 감도 5칸은 전부 적합 모양이 된다.
**손으로 넣은 부적합 기록이 있으면 누르지 않는다.**

「해당 없음」인 칸은 그대로 둔다 — 단 **영역 첫 줄(작업 전·작업 종료)에서만** 그렇다.
사람이 더한 줄에는 그 열에도 입력칸이 있어 같이 채워진다.

### 전송이 막힐 때

전송을 누르면 빈칸을 검사하고, **막힌 칸으로 화면이 자동으로 이동한다.**

| 안내 | 뜻 |
|---|---|
| 「일자를 입력하세요」 | 날짜가 비었다 |
| 「n번째 기록 행의 시각을 입력하세요」 | 기록 표 시각 칸 |
| 「n번째 기록 행의 판정을 선택하세요」 | 기록 표 판정 칸 |
| 「n번째 기록 행의 품명을 입력하세요」 | 기록 표 품명 칸 |
| 「(항목명)을 입력하세요」 | 그 점검항목의 판정이나 값 |
| 「점검 행이 없습니다」 | 양식에 점검 항목이 없다 — 양식관리에서 확인한다 |

기록 표에서 필수는 **시각·판정·품명** 셋이다.
다만 **영역 첫 줄**(작업 전·작업 종료)은 품명 자리에 라벨이 들어가 있어 비워 둔다 —
**내가 더한 줄만** 품명을 채우면 된다.
금속 제품통과표는 **첫 줄 품명**이 필수다.

### 상태에 따라 되는 일

| 표시 | 고치기 | 지우기 | 전송 | 전송취소 |
|---|---|---|---|---|
| **전송대기** | O | O | O | — |
| **전송** | X | X | — | O |
| **결재완료** | X | X | — | X (결재 화면에서 취소) |

전송한 문서를 고치려면 **전송취소 → 수정 → 다시 전송**이다.

## 2-3. 한글(HWP) 일지 작성 `/draft/hwp-doc/hwp-write`

왼쪽은 앞의 5화면과 **똑같다.** 오른쪽만 한글 편집기다.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 **행추가** |
| 2 | **「오늘 할일 — HWP 문서주기」 팝업**이 뜬다. 오늘 쓸 HWP가 없으면 이 팝업 없이 바로 양식 선택 |
| 3 | 주기와 상관없이 아무 양식이나 쓰려면 **취소 (양식 직접 선택)** → 양식 선택 팝업으로 넘어간다 |
| 4 | 왼쪽 **저장** |
| 5 | 오른쪽 한글 편집기에서 문서를 채운다 — 글꼴·표·정렬 도구가 다 있다 |
| 6 | 오른쪽 **작성 후 저장** — 본문이 함께 올라간다 |
| 7 | 오른쪽 **전송** |

**막히는 자리**

- 문서를 여는 동안 상태줄이 **「문서를 여는 중입니다…」**로 바뀐다. 이게 끝나기 전에 다른 줄을 누르지 않는다
- 한글 문서는 **일자만 맞으면 전송된다** — 본문 점검 항목을 서버가 볼 수 없어서다
- **이탈여부**는 지면이 아니라 **왼쪽 목록의 체크 칸**이다. 켜고 저장하면 개선조치가 생긴다.
  실제 조치 내용은 이탈·개선조치 화면에서 적는다. 다시 끄면 조치가 지워지지만,
  **이미 조치를 적어 둔 건은 지워지지 않는다**

## 2-4. 결재첨부 `/flow/appr/attach`

**내가 쓴 문서**에 사진·파일을 붙이고, 결재가 어디까지 갔는지 본다.

| 터치 | 무엇 |
|---|---|
| 1 | 검색 조건(기간·문서번호)을 넣고 **조회** |
| 2 | 왼쪽 목록에서 문서를 고른다 |
| 3 | 오른쪽 위에서 **결재 진행상태**를 본다 — 작성 · 전송 · 결재가 누구인지 |
| 4 | **파일선택** → 파일을 고른다 |
| 5 | **첨부** |
| 6 | **비고**에 결재자에게 남길 메모를 쓰고 **저장** |
| 7 | 아직 안 보냈으면 **전송**, 되돌리려면 **전송취소** |

**나머지 버튼**

| 버튼 | 무엇 |
|---|---|
| 작성화면 | 그 문서의 작성 화면으로 간다 |
| 다운로드 | 첨부 파일을 내려받는다 |
| 삭제 | 그 첨부 하나를 지운다 |
| 초기화 | **저장하지 않은 화면 변경만** 되돌린다. 저장된 첨부는 안 지운다 |

**막히는 자리**

- 첨부는 문서당 **5개**까지
- **전송한 뒤에는 첨부를 못 붙이고 못 지운다.** 결재자가 본 것과 최종본이 달라지면 안 되기 때문이다. 고치려면 전송취소부터
- **비고는 결재완료 직전까지 고칠 수 있다** — 메모라서 기준이 다르다
- 이 화면에는 **문서 본문 미리보기가 없다.** 본문은 작성 화면에서 본다
- 목록에는 **내가 쓴 문서만** 나온다

**반려·취소 사유를 보는 자리**

결재자가 되돌린 이유는 **여기서 본다.** 문서를 고르면 오른쪽에 뜬다.

| 무엇 | 언제 보이나 |
|---|---|
| **반려 사유** | 그 문서가 반려(RJT) 상태이고 결재자가 사유를 남겼을 때 |
| **결재 취소 사유** | 결재자가 승인을 취소했을 때. **다시 전송하면 비워진다** |

작성자는 이 글을 **고칠 수 없다.** 읽고 고쳐서 다시 전송하는 자리다.

---

# 3부. 결재 — 팀장

## 3-1. 결재대기 `/flow/appr/sign-ready`

**내 차례인 문서만** 나온다.

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 목록에서 문서를 고른다 |
| 2 | 오른쪽 **문서 미리보기**로 내용을 확인한다 (한글 문서면 한글 그대로, 화면 입력형이면 지면 그대로) |
| 3 | 아래 **결재 이력**과 **첨부 파일**을 본다 |
| 4 | 문제 없으면 **승인** → 확인 창에서 예 |
| 5 | 되돌려 보내려면 **반려 사유**를 쓰고 **반려** |

**막히는 자리**

- **반려는 사유가 필수**다. 비우면 「반려 사유를 입력하세요」로 막힌다
- 버튼은 **「승인」 하나**다. 검토 단계는 없다 — 결재선은 작성·승인 둘로만 흐른다
- 결재자는 문서를 **고칠 수 없다.** 그래서 이 화면에는 저장·첨부 버튼이 없다
- 승인하면 목록에서 사라진다. **결재완료** 화면에서 볼 수 있다

## 3-2. 결재완료 `/flow/appr/sign-ok`

**내가 처리한 결재**가 모인다. 잘못 눌렀을 때 되돌리는 자리다.

| 터치 | 무엇 |
|---|---|
| 1 | 목록에서 문서를 고른다 |
| 2 | **취소 사유**를 쓴다 |
| 3 | **취소** |

**막히는 자리**

- **사유 없이는 취소되지 않는다.** 감사 이력에 남는다
- **다음 결재자가 이미 처리했으면 취소되지 않는다**
- 승인을 되돌리면 승인 완료본 사본도 같이 걷힌다

## 3-3. 문서함 `/flow/box/document-inbox`

**결재까지 끝난 문서(승인완료)만** 모아 보는 보관함이다. **조회 전용**이다.

| 터치 | 무엇 |
|---|---|
| 1 | 기간·타입(DB 입력형 / 한글 문서형)으로 **조회** |
| 2 | 문서를 고른다 |
| 3 | 오른쪽에서 본문·결재 이력·첨부를 본다 |

여기에는 신규·저장·삭제·첨부 버튼이 없다. 상태 필터도 없다 — 항상 승인완료다.

**비어 있으면** 아직 승인된 문서가 없다는 뜻이다. 작성중·승인요청 문서는
작성 화면이나 결재첨부 화면에 있다.

---

# 4부. 이탈이 났을 때

## 4-1. 개선조치는 어떻게 생기나

**따로 만들지 않는다.** 작성 화면에서 이탈을 적으면 자동으로 생긴다.

| 어디 | 무엇을 하면 |
|---|---|
| HTML 5화면 | 지면 아래 **「한계기준 이탈내용」**(일반위생은 「특이사항」)에 글자를 쓰면 |
| HWP 작성 | 왼쪽 목록의 **이탈여부** 체크를 켜면 |
| 어느 화면이든 | 판정을 **부적합**으로 하면 |

지면 맨 아래 **「이탈·개선조치 문서」** 줄이 「있음」으로 바뀐다.
근거가 있을 때는 자동으로 켜지고 **끌 수 없다.**

> **이탈이 없으면 그 칸을 비워 둔다.** 「없음」이라고 써도 이탈 한 건으로 잡힌다.
> 칸에 마우스를 올리면 그 규칙이 뜬다.

## 4-2. 이탈·개선조치 `/flow/ca/corrective-action-management`

이탈로 잡힌 문서가 전부 모인다. 표에서 바로 친다 — 따로 상세 폼이 없다.

| 터치 | 무엇 |
|---|---|
| 1 | **일자 구간 · 양식 · 작성자**로 **조회** |
| 2 | 줄을 고른다 |
| 3 | **이탈내용 · 발생장소 · 조치내용 · 조치자**를 친다 |
| 4 | **조치일 · 기한**을 달력에서 고른다 |
| 5 | **상태**를 고른다 |
| 6 | **저장** |

**막히는 자리**

- 일자·양식·문서번호·작성자 칸은 **잠겨 있다.** 원문서에서 온 값이라 작성 화면에서 고친다
- 검색의 일자는 이탈이 난 날이 아니라 **그 일지의 기준일**이다
- 완료된 건은 서버가 삭제를 막는다

---

# 5부. 이력·통계 — 관리자

세 화면 다 **조회 전용**이다. 행추가·저장·삭제가 없다.

| 화면 | URL | 무엇을 보나 |
|---|---|---|
| 로그인 이력 | `/sys/logs/login-history` | 누가 언제 로그인·로그아웃했나. 실패도 남는다 |
| 변경 감사 로그 | `/sys/logs/audit-log` | 시스템 화면에서 누가 무엇을 저장·삭제했나 |
| 화면 이용 통계 | `/sys/logs/screen-usage-statistics` | 어느 화면을 얼마나 썼나 |

| 터치 | 무엇 |
|---|---|
| 1 | 왼쪽 트리에서 대상을 고른다 (로그인 이력은 사용자, 나머지는 메뉴) |
| 2 | 기간을 넣는다 |
| 3 | **조회** |

집계 배치가 돌기 전이면 **오늘 값은 비어 있을 수 있다.**

---

# 부록 A. 문서 상태 한눈에

```
작성중 ──전송──→ 승인요청 ──승인──→ 승인완료 ──→ 문서함
  ↑                  │
  ├──전송취소────────┤
  └──반려────────────┘
```

| 화면에 보이는 말 | 뜻 | 이때 할 수 있는 것 |
|---|---|---|
| **전송대기** | 아직 안 올렸다 (작성중·반려) | 고치기 · 지우기 · 전송 |
| **전송** | 결재에 올라가 있다 | 전송취소만 |
| **결재완료** | 승인이 끝났다 | 없음 (결재자가 결재취소) |

# 부록 B. 자주 막히는 자리

| 증상 | 까닭 | 어떻게 |
|---|---|---|
| 오른쪽 지면에 글씨가 안 써진다 | 왼쪽 저장을 안 했다 | 왼쪽 **저장**을 먼저 |
| 양식 선택 팝업이 비어 있다 | 그 양식군에 「사용」인 양식이 없다 | 양식관리에서 **사용여부 = 사용** |
| 전송했는데 팀장 결재함에 안 온다 | 결재선 승인자가 팀장이 아니다 | 결재선관리에서 승인자 지정 |
| 종이에 이름 글자만 찍힌다 | 그 사람 서명이 없다 | 사용자관리 → 서명 → 업로드 |
| 전송이 안 되고 어디론가 스크롤된다 | 그 칸이 비었다 | 옮겨 간 칸을 채운다 |
| 오늘 할 일이 비어 있다 | 문서주기를 안 걸었다 | 문서주기관리에서 주기 설정 |
| 문서를 고칠 수 없다 | 이미 전송했다 | **전송취소** 후 수정 |
| Escape 를 눌렀는데 값이 남아 있다 | Escape 는 「닫기」다 | 저장하지 말고 **조회**를 다시 |
| 첨부를 못 붙인다 | 전송한 뒤다 (또는 5개를 다 썼다) | 전송취소 후 첨부 |
| 시스템 메뉴가 안 보인다 | 팀원 권한이다 | 정상. 관리자 계정으로 |

# 부록 C. 화면 전수 (29)

| 대분류 | 화면 | URL |
|---|---|---|
| 게시판 | 오늘 할 일·대시보드 | `/board/today-tasks` |
| 게시판 | 일정 캘린더 | `/board/calendar` |
| 양식작성 | 일반위생·공정점검 작성 | `/draft/html/hyg-process` |
| 양식작성 | CCP 검증점검표 작성 | `/draft/html/ccp-verify` |
| 양식작성 | CCP 포장공정 작성 | `/draft/ccp-monitoring/ccp-pkg` |
| 양식작성 | CCP 가열공정 작성 | `/draft/ccp-monitoring/ccp-htg` |
| 양식작성 | CCP 금속검출공정 작성 | `/draft/ccp-monitoring/ccp-mtl` |
| 양식작성 | HWP 작성 | `/draft/hwp-doc/hwp-write` |
| 문서 현황·결재 | 결재대기 | `/flow/appr/sign-ready` |
| 문서 현황·결재 | 결재완료 | `/flow/appr/sign-ok` |
| 문서 현황·결재 | 결재첨부 | `/flow/appr/attach` |
| 문서 현황·결재 | 문서함 | `/flow/box/document-inbox` |
| 문서 현황·결재 | 이탈·개선조치 | `/flow/ca/corrective-action-management` |
| 문서 | 문서주기관리 | `/docs/sch/schedule-cycle-management` |
| 문서 | 일반위생·공정점검 양식관리 | `/docs/html-form/hyg-process-template` |
| 문서 | CCP 검증점검표 양식관리 | `/docs/html-form/ccp-verify-template` |
| 문서 | CCP 포장공정 일지관리 | `/docs/html-form/ccp-pkg-template` |
| 문서 | CCP 가열공정 일지관리 | `/docs/html-form/ccp-htg-template` |
| 문서 | CCP 금속검출공정 일지관리 | `/docs/html-form/ccp-mtl-template` |
| 문서 | 사용양식관리 | `/docs/hwp/hwp-template-management` |
| 시스템 | 사용자관리 | `/sys/code/user-management` |
| 시스템 | 부서관리 | `/sys/code/department-management` |
| 시스템 | 권한그룹관리 | `/sys/code/role-management` |
| 시스템 | 메뉴관리 | `/sys/code/menu-management` |
| 시스템 | 공통코드관리 | `/sys/code/common-code-management` |
| 시스템 | 결재선관리 | `/sys/code/approval-line-management` |
| 시스템 | 로그인 이력 | `/sys/logs/login-history` |
| 시스템 | 변경 감사 로그 | `/sys/logs/audit-log` |
| 시스템 | 화면 이용 통계 | `/sys/logs/screen-usage-statistics` |
