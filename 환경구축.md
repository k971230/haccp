# HACCP 환경구축 핸드북

> 대상: 신규 개발자·에이전트·본인이 PC를 다시 잡을 때  
> 목표: 이 문서만으로 **로컬 기동 + Jenkins Job 등록**까지 끝낸다  
> 시크릿 실값·비밀번호·PAT·SSH 키는 **절대 이 문서에 적지 않는다**  
> 관련: [`개발.md`](개발.md) · [`운영.md`](운영.md) · [`docs/12_배포_런북.md`](docs/12_배포_런북.md)

---

## 1. 목적·소요

| 단계 | 내용 | 대략 소요 |
|------|------|-----------|
| A | 도구 설치 (Node/Java/Docker/Git) | 30–60분 |
| B | clone · `.env` · DB · API · FE 기동 | 30–60분 |
| C | Jenkins 설치 · Credentials · Job 2개 · 첫 Build Now | 1–2시간 |

완료 기준:

1. 브라우저에서 `http://localhost:4173/login` 이 열린다 (MES 스플릿 + HACCP 로고).
2. API `http://localhost:7070/actuator/health` 가 DB 연결 시 `UP` 이다.
3. Jenkins에 `haccp-deploy` · `haccp-audit` Job이 있고, Credentials ID가 런북과 **문자열 일치**한다.

---

## 2. 권장 OS · MES와 공존

- 개발 PC: **Windows 10/11 + Git Bash** (Jenkinsfile·스크립트도 bash 전제).
- MES(`D:\MES`, Vite **5173**, mes-api **8080**)와 HACCP는 **별도 저장소·별도 DB**다.
- HACCP 로컬 포트 (현행 main):

| 용도 | 포트 | 비고 |
|------|------|------|
| haccp-web (Vite) | **4173** | `strictPort: true` — 점유 시 다른 포트로 안 빠짐 |
| haccp-api | **7070** | 운영 컨테이너 listen과 동일 |
| MES Vite (참고) | 5173 | 충돌 없음 |
| MES API (참고) | 8080 | 충돌 없음 |

PowerShell/CMD에서 명령을 이을 때 `&&` 대신 `;` 를 쓰는 편이 Git Bash·에이전트 규약과 맞다.

---

## 3. 필수 도구

| 도구 | 버전·메모 | 확인 명령 |
|------|-----------|-----------|
| Node.js | 20+ (`frontend/haccp-web/.nvmrc`) | `node -v` |
| npm | Node와 함께 | `npm -v` |
| JDK | 17 (`backend/haccp-api/.java-version`) | `java -version` |
| Docker Desktop | 24+ | `docker version` |
| Git | 최신 | `git --version` |
| Git Bash | Windows 필수 | — |
| DBeaver 등 | PostgreSQL 클라이언트 | — |
| Jenkins | LTS 권장 | 브라우저로 UI 접속 |
| (선택) gh CLI | audit 실패 시 GitHub 이슈 | `gh --version` |

버전 요약은 루트 [`requirements.txt`](requirements.txt) · 스택 설명은 [`.cursor/rules/01-project-core.mdc`](.cursor/rules/01-project-core.mdc).

---

## 4. 저장소 clone · 디렉터리 맵

```bash
git clone https://github.com/k971230/haccp.git
cd haccp
git checkout main
git pull origin main
```

| 경로 | 역할 |
|------|------|
| `frontend/haccp-web/` | React 18 + Vite 5 SPA |
| `backend/haccp-api/` | Spring Boot 3.3 + MyBatis API |
| `db_sasshaccp/` | PostgreSQL 스키마·SP 정본 (`00_`… 번호순) |
| `nginx/` | edge TLS·리버스 프록시 템플릿 |
| `scripts/` | 볼륨·빌드·배포·스모크·감시 |
| `docs/` | 배포 런북·후속 STEP (및 FE/BE 문서 인덱스는 각 `*/docs/`) |
| `.cursor/rules/` | 에이전트·운영 규약 |
| `Jenkinsfile` | 배포 Job (`haccp-deploy`) |
| `Jenkinsfile.audit` | 야간 감시 Job (`haccp-audit`) |
| `환경구축.md` · `개발.md` · `운영.md` | 루트 핸드북 (본 문서군) |

---

## 5. 시크릿 규칙 (Zero Tolerance)

| 해도 됨 | 하면 안 됨 |
|---------|-----------|
| `*.env.example` · `.env.docker.example` | `.env` · `.env.docker` · `*_secret*` |
| Jenkins Credentials (`haccp-*`) | Jenkinsfile·compose·문서에 실값 하드코딩 |
| 서버의 `.env.docker` (권한 0600) | git commit / PR 첨부 |

커밋 전:

```bash
bash scripts/pre-commit-check-secrets.sh
# 또는
ln -sf ../../scripts/pre-commit-check-secrets.sh .git/hooks/pre-commit
```

---

## 6. DB (PostgreSQL · 스키마 `sasshaccp`)

### 6.1 원칙

- DB·스키마 이름: **`sasshaccp`** (MES `metis`와 **상호 참조·공유 금지**).
- SP 접두: `sp_tbl_*` (lower_snake).
- 저장소 SQL: [`db_sasshaccp/`](db_sasshaccp/) 번호 파일 (`00_schema.sql` …).
- 당분간 스키마 정본을 DBeaver로 운영 DB에 맞추는 팀 관행이 있다. 저장소 SQL과 운영이 어긋나면 **운영·팀 합의**를 우선하고, 저장소는 따라 맞춘다.

### 6.2 문법만 검증 (로컬 PG 임시)

```bash
bash scripts/db_migrate_dryrun.sh
```

### 6.3 적용

- DBeaver 등에서 `00_`부터 순서 적용, 또는
- compose profile `migrate` (런북·compose 참고),
- `db_sasshaccp/apply-all.sh` (환경에 맞게).

데모 계정·시드: `09_seed_platform.sql` 등 — **비밀번호는 시드/운영 채널로만** 전달.

### 6.4 compose 로컬 DB (선택)

```bash
# 루트에서 — 프로필·파일명은 compose 현행을 따른다
docker compose --profile with-db up -d
```

---

## 7. 백엔드 (haccp-api · 7070)

```bash
cd backend/haccp-api
cp .env.example .env
# .env 편집 — DB_* · JWT_SECRET(64바이트 이상) · CORS
./mvnw spring-boot:run
```

### 7.1 `.env` 핵심 키 (값 예시는 example만)

| 키 | 의미 |
|----|------|
| `SERVER_PORT` | 기본 `7070` |
| `DB_URL` / `DB_*` | `currentSchema=sasshaccp` 포함 JDBC |
| `JWT_SECRET` | HS512용 64바이트 이상 (`openssl rand -hex 48`) |
| `JWT_EXPIRE_MINUTES` | 기본 480 |
| `CORS_ALLOWED_ORIGINS` | **`http://localhost:4173`** (쉼표로 추가 Origin 가능) |
| `APP_FILE_ROOT` | 업로드 루트 (로컬 `./data/haccp-files` 등) |

정본 목록: [`backend/haccp-api/.env.example`](backend/haccp-api/.env.example).

### 7.2 확인

```bash
curl -s http://localhost:7070/actuator/health
```

DB가 붙으면 `UP`. DB가 없으면 `DOWN`/503이 나올 수 있다 — 연결 정보를 먼저 고친다.

Windows에서 `MAVEN_HOME`/`M2_HOME`이 깨져 있으면 `mvnw`가 실패할 수 있다. Jenkinsfile처럼 `unset MAVEN_HOME M2_HOME` 후 재시도.

---

## 8. 프론트엔드 (haccp-web · 4173)

```bash
cd frontend/haccp-web
cp .env.example .env
# VITE_API_BASE_URL=http://localhost:7070
npm ci
npm run dev
```

기대 출력:

```text
➜  Local:   http://localhost:4173/
```

`5174`가 뜨면 **옛 설정/옛 프로세스**다. 이 저장소 현행은 **4173**이다. 기존 Vite를 종료하고 `vite.config.ts`의 `port: 4173`을 확인한 뒤 재기동한다.

`Port 4173 is already in use` → 점유 프로세스 종료 후 재실행 (`strictPort: true`).

### 8.1 로그인 화면 (현행)

- 레이아웃: MES와 같은 **스플릿** (좌 `login_bg.png` / 우 흰 폼).
- 우측 상단: HACCP 공식 마크 [`src/static/img/haccp_logo.png`](frontend/haccp-web/src/static/img/haccp_logo.png).
- 브라우저 탭 아이콘: RMA [`public/logo2.svg`](frontend/haccp-web/public/logo2.svg) (`%BASE_URL%logo2.svg`).
- 회사 콤보 **없음** — 아이디만으로 테넌트 판정.

---

## 9. 첫 로그인 · 흔한 실패

1. API·FE 둘 다 기동.
2. `http://localhost:4173/login` 접속.
3. 시드/팀 계정으로 로그인.
4. 실패 시 FE 폼 아래 업무 문구 · BE 로그의기술 상세)를 본다.

| 증상 | 점검 |
|------|------|
| CORS 에러 | API `CORS_ALLOWED_ORIGINS`에 `http://localhost:4173` |
| Network Error | `VITE_API_BASE_URL` · API 기동 여부 |
| 4173 in use | 다른 Vite/프로세스 종료 |
| 로그인 후 흰 화면 | 콘솔·토큰·메뉴 API |
| JWT 기동 실패 | `JWT_SECRET` 길이 |

---

## 10. (선택) Docker 로컬 스택

이미지·compose·볼륨·edge `127.0.0.1:17070:7070` 은 [`docs/12_배포_런북.md`](docs/12_배포_런북.md) §8–§10이 정본이다.

요약:

```bash
# 이미지 로컬 태그 예
docker build -f backend/haccp-api/Dockerfile  -t haccp-api:local   backend/haccp-api
docker build -f frontend/haccp-web/Dockerfile -t haccp-web:local   frontend/haccp-web
docker build -f nginx/Dockerfile              -t haccp-nginx:local nginx
```

운영 웹 Dockerfile은 `VITE_BASE=/haccp/` 로 빌드한다 — Path 배포와 basename 정합.

---

## 11. Jenkins 환경 구축 (필수 — 운영 전 선행)

일상 배포 조작법은 [`운영.md`](운영.md). **여기 절은 최초 1회 구축**이다.  
정본 보강: [`docs/12_배포_런북.md`](docs/12_배포_런북.md) §12–§13.

### 11.1 설치·기동

1. Jenkins LTS 설치 (Windows 서비스 또는 Docker — 팀 표준을 따른다).
2. 초기 Admin 계정 생성.
3. UI 접속 예: `http://localhost:9090/` (포트는 설치본 설정).
4. **GitHub webhook을 localhost Jenkins에 붙이지 않는다** — GitHub 클라우드가 `localhost`에 도달할 수 없다.

### 11.2 권장 플러그인

- Pipeline
- Git
- Credentials Binding
- Docker (에이전트에 Docker CLI가 있으면 shell 빌드로도 충분)
- (선택) GitHub / GitHub Branch Source — webhook 자동화는 **외부 HTTPS URL이 생긴 뒤**

### 11.3 에이전트(빌드 머신) 전제

에이전트에서 아래가 PATH에 있어야 `haccp-deploy`가 통과한다.

| 도구 | 용도 |
|------|------|
| `git` | Checkout |
| `bash` / Git Bash | `sh '''...'''` 단계 |
| JDK 17 · `./mvnw` | BE test/package |
| Node 20 · npm | FE ci/tsc/lint/test/build |
| Docker | 이미지 빌드·push |
| (선택) `gh` | `haccp-audit` 실패 시 이슈 생성 |

Windows 에이전트 주의:

- 깨진 `MAVEN_HOME`/`M2_HOME` → `mvnw` 실패. Jenkinsfile은 unset 한다.
- `MSYS_NO_PATHCONV`를 **전역으로 켜면** Windows `mvnw` 클래스패스가 깨질 수 있다. SSH 원격 경로·스모크 등 **필요한 단계만** export (Jenkinsfile 주석 참고).
- Git Bash가 `/haccp` 를 `C:/Program Files/Git/haccp` 로 바꿔 smoke가 깨질 수 있다 → 해당 단계에서 `MSYS_NO_PATHCONV=1`.

### 11.4 Credentials 등록 (ID 문자열 고정)

Manage Jenkins → Credentials → (global) → Add.  
**ID는 아래와 一字一句 동일**해야 Jenkinsfile이 찾는다. **값은 UI에만** 넣고 git/문서에 쓰지 않는다.

| ID | 종류 | 용도 |
|----|------|------|
| `haccp-registry-cred` | Username/password | GHCR 로그인·이미지 push (PAT) |
| `haccp-deploy-ssh-key` | SSH Username with private key | prod SSH |
| `haccp-deploy-host` | Secret text | `user@host` 또는 host |
| `haccp-github-token` | Secret text | nightly 이슈·(이후) webhook |
| `haccp-env-docker` | Secret file | 서버 `.env.docker` 원본(정책에 따라 사용) |
| `haccp-smoke-user` | Username/password | prod 스모크 로그인 |

정본 표: 런북 §12.1.

### 11.5 Pipeline Job 생성

#### A) `haccp-deploy` (배포)

1. New Item → Pipeline → 이름 `haccp-deploy`.
2. Pipeline → Definition: Pipeline script from SCM.
3. SCM: Git → 이 저장소 URL · credentials(필요 시) · Branch `*/main`.
4. Script Path: **`Jenkinsfile`** (루트).
5. 저장.
6. 역할: BE/FE 검증 → (현행) DB dry-run → 이미지 빌드·GHCR push → Prod migrate → Deploy → Prod smoke.  
   서버에 반영되는 **유일한 일상 Job**.

#### B) `haccp-audit` (야간 감시)

1. New Item → Pipeline → 이름 `haccp-audit`.
2. SCM 동일 · Branch `*/main`.
3. Script Path: **`Jenkinsfile.audit`**.
4. 역할: 버전 drift · OPS_DELETE · 파일크기 · 문서 링크 검사. **배포하지 않는다**. cron(UTC 18:00 ≈ KST 03:00).

### 11.6 트리거 정책 (지금)

| 방식 | 현재 |
|------|------|
| GitHub webhook → 자동 빌드 | **사용 안 함** (localhost 도달 불가) |
| `Jenkinsfile`의 `githubPush()` | 파일에 있어도 **큐가 안 쌓일 수 있음** |
| **Build Now** | **정본 트리거** |

main에 머지만 하고 Build Now를 안 누르면 **운영 서버 이미지는 그대로**다.

이후 Jenkins에 HTTPS(또는 터널) 공인 URL이 생기면  
`https://<jenkins>/github-webhook/` + Push events 로 자동화한다 (런북 §13.1).

### 11.7 첫 검증 (Build Now 1회)

1. `haccp-deploy` → **Build Now**.
2. 콘솔에서 스테이지 순서·`TAG=1.0.<BUILD_NUMBER>` 확인.
3. 성공 시 운영 URL (예: `https://180.71.58.87/haccp/login`) 스모크.
4. 실패 시 우선 확인:
   - Credentials ID 오타
   - GHCR login / PAT 권한
   - SSH host·키·`DEPLOY_DIR` (`Jenkinsfile` 기본 `/home/ubuntu/haccp`)
   - Windows path 변환 · Docker daemon

`haccp-audit`는 수동 Build Now로 한 번 돌려 스크립트 경로·권한을 확인해도 된다.

### 11.8 구축 완료 후

- 매일/배포 시 조작 → [`운영.md`](운영.md)
- 코드 수정 루프 → [`개발.md`](개발.md)

---

## 12. 검증 체크리스트

- [ ] Node 20 · JDK 17 · Docker · Git Bash
- [ ] `backend/haccp-api/.env` (git 무시) · API `7070` health
- [ ] `frontend/haccp-web/.env` · Vite `4173` · 로그인 UI
- [ ] CORS `http://localhost:4173`
- [ ] Jenkins Credentials 6종 ID 일치
- [ ] Job `haccp-deploy` / `haccp-audit` Script Path 정확
- [ ] `haccp-deploy` Build Now 1회 경험 (또는 팀과 확인)

---

## 13. 더 깊은 정본

| 주제 | 링크 |
|------|------|
| 배포·시크릿·파이프라인 | [`docs/12_배포_런북.md`](docs/12_배포_런북.md) |
| FE 운영규칙 | [`frontend/haccp-web/docs/01_운영규칙.md`](frontend/haccp-web/docs/01_운영규칙.md) |
| BE 운영규칙 | [`backend/haccp-api/docs/01_운영규칙.md`](backend/haccp-api/docs/01_운영규칙.md) |
| 통합 스펙 | [`frontend/haccp-web/docs/08_HACCP_FE_BE_통합_상세스펙.md`](frontend/haccp-web/docs/08_HACCP_FE_BE_통합_상세스펙.md) |
