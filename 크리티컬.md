# 크리티컬 — 고치지 않기로 한 치명 결함

> 개발자: 박승우 · 일자: 2026-09-08
> 핸드북이다. `docs/` 정본 10본에 넣지 않는다.
> **이번에 고친 것**은 아래 「이번에 고친 SP」와 코드 diff 다. 여기는 **안 고친 것**만 적는다.

같은 목록을 다시 꺼내지 않으려고 남긴다. 이후 작업으로 미룬 것은 그때 **이 절을 읽고** 시작한다. 코드를 추측하지 말 것.

---

## 제외 — 사용자가 운영·정책으로 두기로 한 것

고치지 않는다. 재논의하려면 이 절을 먼저 읽는다.

| 항목 | 왜 두나 | 근거 |
|---|---|---|
| 운영 HTTP + 기본 비밀번호 `1234` | 운영 환경·시크릿 정책. 코드로 막을 자리가 아니다 | 배포 env · `DEPLOY.md` · `docs/7_보안과_파일.md` · `UserService.DEFAULT_NEW_USER_PW` |
| 동시 저장 | `seenUpdDt` 충돌과 HWP `hwp/save` 경합은 알고 둔다 | `DocumentService` 저장 · HTML/CCP `seenUpdDt` |
| 자기승인 | 결재선 작성자=승인자 허용. 소규모 사업장 흐름 | `sp_tbl_document_approval_c_000` APPROVE |
| 서명 GET 화이트리스트 | 서명 이미지는 로그인 사용자가 연다. 공개 URL 이 아니다 | `UserController` 서명 조회 · `docs/7_보안과_파일.md` |
| `lockYn` stale 해제 | 잠금 플래그가 남아 화면이 잠긴 것처럼 보이는 잔여. 운영으로 푼다 | 작성 화면 `lockYn` |
| JWT ADMIN 8시간 | 관리자 토큰 TTL. 정책 값이다 | JWT 설정 · `application.yml` `JWT_EXPIRE_MINUTES` |

---

## 보류 — 이후에 고친다 (이번 작업에서 손대지 않음)

아래 5건은 **전면 수정·과설계·의도적 보류**라 이번 커밋에 넣지 않았다.
다음에 손댈 때는 한 건씩, 아래 「어떻게」를 그대로 따른다. 새 훅·새 권한 모델을 먼저 만들지 말 것.

---

### 1. HTML·CCP 초안 `writer_id` 미검사

**증상.** 같은 회사 동료가 남의 작성중(WRK)·반려(RJT) HTML/CCP 문서를 저장·삭제할 수 있다. 목록에 남의 행이 보이면 API만 치면 된다. 화면 필터는 우회된다.

**왜 HWP만 막혀 있나.** HWP 저장은 이미 작성자만 고친다.

```5183:5194:db_sasshaccp/01_sp.sql
        SELECT idx INTO v_idx
          FROM tbl_document
         WHERE idx = p_doc_idx
           AND co_cd = p_co_cd
           AND doc_kind = 'HWP'
           AND writer_id = p_id
           AND status IN ('WRK', 'RJT')
           AND del_yn = 'N'
         FOR UPDATE;

        IF NOT FOUND THEN
            RAISE EXCEPTION '작성자 본인의 작성중 또는 반려 문서만 수정할 수 있습니다.' USING ERRCODE = '45000';
```

HTML·CCP 저장 SP 는 `co_cd` + `doc_idx` + 상태만 본다. `writer_id = p_id` 가 없다.

**손댈 SP (저장·삭제 양쪽).**

| 가족 | 저장 | 삭제 |
|---|---|---|
| 공정점검 | `sp_tbl_hyg_process_c_000` | `sp_tbl_hyg_process_d_000` |
| CCP 검증 | `sp_ccp_verify_c_000` | `sp_ccp_verify_d_000` |
| 포장 | `sp_tbl_ccp_pkg_monitor_c_000` | `sp_tbl_ccp_pkg_monitor_d_000` |
| 가열 | `sp_tbl_ccp_htg_monitor_c_000` | `sp_tbl_ccp_htg_monitor_d_000` |
| 금속 | `sp_tbl_ccp_metal_monitor_c_000` | `sp_tbl_ccp_metal_monitor_d_000` |

**어떻게.** HWP 와 같은 문: `FOR UPDATE` 뒤에 `writer_id = p_id` 가 아니면 45000 「작성자 본인의 작성중 또는 반려 문서만 …」. 관리자 예외를 둘지는 **그때 사용자에게 물어라.** 지금 정책에 관리자 우회는 없다. Java는 SP가 막으면 충분하다. FE 목록 필터만 고치면 안 된다.

**확인.** 작성자 A 문서를 동료 B 토큰으로 PUT/삭제 → 45000. A 본인은 WRK/RJT 저장 가능. REQ/APV 는 기존 잠금이 먼저 막는다.

---

### 2. 권한그룹 조회 하나 = 쓰기·수정·삭제·인쇄 다섯 (전면 수정)

**증상.** 권한그룹 화면 트리는 `readYn` 체크 하나다. 조회만 켜도 서버가 쓰기·수정·삭제·인쇄를 전부 Y 로 넣는다. 조회만 있는 그룹이 저장·삭제를 한다.

**구멍 위치 (FE 가 아니라 BE 정책).**

```191:193:backend/haccp-api/src/main/java/com/haccp/sys/code/role/RoleMgmtService.java
            // 조회권한이 Y가 아니면 전부 닫는다 — 트리 체크 하나로 5권한을 함께 움직인다
            String yn = "Y".equalsIgnoreCase(text(row.getReadYn())) ? "Y" : "N";
            roleMgmtMapper.upsertScreen(coCd, grp, scrn, yn, yn, yn, yn, yn, actor);
```

화면: `RoleManagementPage.handleSaveTree` 가 `{ scrnCd, readYn }` 만 보낸다.
DTO: `RoleScreenSaveRow` 에 readYn 만 있다.
SP `sp_role_management_screen_c_000` 자체는 5칸을 따로 받는다. **SP는 이미 분리돼 있다.** 서버가 같은 값을 다섯 번 넣는 것이 문제다.

주석에도 「1차 정책」이라고 적혀 있다 (`RoleMgmtService` 파일 머리).

**어떻게 (전면).**

1. FE 트리에 조회·쓰기·수정·삭제·인쇄 5 체크 (또는 조회 + 나머지 4). `scrnCd`·`persistId` 는 바꾸지 않는다.
2. `RoleScreenSaveRow` / `saveRoleScreens` body 에 5칸.
3. `RoleMgmtService.saveScreens` 는 받은 값을 그대로 `upsertScreen` 에 넘긴다. `yn, yn, yn, yn, yn` 삭제.
4. `authStore.can(scrnCd, "write"|"modify"|"delete"|"print")` 와 BE `ScreenAuthInterceptor.granted` 가 이미 칸별로 본다 — 데이터가 맞으면 동작한다.
5. 기존 DB 행: 조회만 주려고 YYYYY 로 들어가 있는 그룹을 **마이그레이션할지** 정해야 한다. 코드만 고치면 예전 저장분이 계속 5칸 Y 다.
6. E2E·권한 시험이 트리 체크 하나 전제면 같이 고친다.

**하지 말 것.** SP 시그니처를 줄이거나 `tbl_role_screen` 컬럼을 합치지 말 것. 표는 이미 5칸이다.

---

### 3. 문서 허브 OR 권한 (과설계로 판단 — 문서별 관리 하지 말 것)

**증상.** `/api/v1/docs/documents/**` 같은 허브 API 는 화면 하나가 아니라 **허브에 묶인 화면 중 하나라도** 해당 칸(쓰기 등)이 Y 이면 통과한다.

```96:99:backend/haccp-api/src/main/java/com/haccp/common/auth/ScreenAuthInterceptor.java
        if (m.hubKind() == ScreenAuthMatch.HubKind.DOC) {
            ok = grantedAny(rows, ScreenAuthResolver.DOC_HUB_SCREENS, m.action());
        } else if (m.hubKind() == ScreenAuthMatch.HubKind.HWP) {
            ok = grantedAny(rows, ScreenAuthResolver.HWP_HUB_SCREENS, m.action());
```

묶음은 `ScreenAuthResolver.DOC_HUB_SCREENS` / `HWP_HUB_SCREENS` — 문서함·첨부·결재대기·결재완료·오늘할일·HTML/CCP 작성·HWP 작성.

예: `attach` 쓰기만 있어도 문서 허브 결재·파일 API 가 열린다. HTML 작성 화면 권한이 없어도 된다.

**왜 보류.** 문서마다 작성 화면 권한을 AND 로 묶으면, 결재자가 자기 차례 문서를 열 때마다 그 양식 화면 권한이 필요해진다. 사용자는 「문서별 관리는 과설계」라고 했다.

**이후에 손댄다면 (최소).**

- 허브 **쓰기**만 호출 화면(`X-Haccp-Screen` 헤더)의 칸을 본다. 헤더가 없거나 묶음 밖이면 거부.
- `ScreenAuthResolver.applyHubScreen` 이 헤더를 이미 읽는다. 조회(목록)는 OR 을 유지하고 CUD 만 헤더 화면 AND — 이 절충도 그때 물어라.
- 문서별 ACL 표·결재선 밖 권한은 만들지 말 것.

---

### 4. 결재완료 가짜 PDF

**증상.** 결재완료(APV)·승인요청(REQ) 문서에 `file_kind=PDF` 를 올리면, 인쇄·변환 API 가 **idx가 가장 큰 PDF** 를 공식 완료본처럼 돌려준다. rhwp 변환을 다시 하지 않는다.

```359:364:backend/haccp-api/src/main/java/com/haccp/docs/documents/DocumentService.java
        if (approvalLocked(headerStatus(header))) {
            DocumentFileRow storedPdf = latestFile(files, "PDF");
            if (storedPdf != null) {
                return storedPdf;
            }
        }
```

`latestFile` 은 종류가 같고 idx 가 큰 쪽. 사용자가 올린 PDF 가 변환본보다 늦게 들어가면 그게 이긴다.

업로드: `POST /api/v1/docs/documents/{docIdx}/files` + `fileKind=PDF`. `sp_tbl_document_file_c_000` 은 문서함 변환용으로 REQ/APV 에서도 PDF 등록을 허용한다. 허브 권한이 OR 이라(3번) 작성 화면 권한이 없어도 된다.

**어떻게.**

1. 사용자 업로드는 `ATTACH`/`PHOTO`/`HWP_SRC`(작성중만) 만. `fileKind=PDF` 는 변환 API(`registerGeneratedPdf`) 전용.
2. `DocumentService.upload` 에서 PDF 면 45000 「완료본 PDF는 올릴 수 없습니다」.
3. 인쇄는 「승인 시 남긴 변환본」만. 구분 컬럼을 새로 만들기보다, 변환만 `PDF` 를 쓰게 하고 사용자 업로드를 막는 쪽이 작다.
4. 이미 올라간 가짜 PDF 가 있으면 운영에서 지울지 그때 정한다. 이번 작업의 UNDO PDF 삭제는 **결재취소 시 완료본 메타**만 지운다. 가짜 업로드 차단이 아니다.

**하지 말 것.** 다운로드를 inline 으로 바꿔 XSS 를 키지 말 것. 다운로드는 이미 attachment.

---

### 5. 결재첨부 K12 경합

**증상.** 결재첨부(`ApprovalAttachPage`)에서 왼쪽 문서를 빨리 바꾸면, 늦게 온 옛 상세가 오른쪽을 덮는다. 그 상태에서 전송하면 **지금 강조된 행이 아니라 덮인 문서**가 나간다.

금본은 문서함이 이미 고친 코드다. 새 훅을 만들지 말 것.

```222:246:frontend/haccp-web/src/pages/flow/box/documentbox/DocumentBoxPage.tsx
  const beginDetail = useLatestOnly();
  ...
    const isLatest = beginDetail();
    setSelected(row);
    setListActiveKey(String(row.docIdx));
    setDetail(null);
    try {
      const next = await getDocumentDetail(row.docIdx);
      if (!isLatest()) return;
      setDetail(next);
```

결재첨부는 아직 이 가드가 없다.

```181:183:frontend/haccp-web/src/pages/flow/appr/attach/ApprovalAttachPage.tsx
      setListActiveKey(String(row.docIdx));
      const next = await getDocumentDetail(row.docIdx);
      setDetail(next);
```

**어떻게 (복붙 수준).**

1. `import { useLatestOnly } from "@/hooks/useLatestOnly";`
2. 상세 적재 함수에 `const isLatest = beginDetail();` → 시작 시 `setDetail(null)` → await 뒤 `if (!isLatest()) return;` 아니면 `setDetail`.
3. `DocumentApprovalToolbar` 의 `docIdx` 는 `selected.docIdx === detail.header.docIdx` 일 때만 넘긴다. 둘이 다르면 버튼을 숨기거나 비활성. 문서함 주석과 같은 이유: 툴바가 `detail.header.docIdx` 로 전송한다.
4. `onBeforeAction` / 첨부 업로드 / 비고 저장도 같은 일치 조건. 적재 중(`detail == null`)에는 전송하지 못하게.
5. 훅 시험은 `useLatestOnly.test.ts` 가 이미 있다. 화면 E2E 한 줄: 두 문서를 연속 클릭 후 전송이 마지막 선택만 나가는지.

**하지 말 것.** `useAsyncAction` 키 잠금으로 두 번째 클릭을 버리지 말 것. 훅 주석: 선택이 바뀌는 적재에 쓰면 새 선택이 사라진다. `AbortController` 도 필요 없다 — 낡은 응답이 상태를 안 덮으면 된다.

---

## 의도된 동작 — 버그가 아니다

| 항목 | 왜 이렇게 됐나 | 근거 |
|---|---|---|
| 양식 삭제가 할일·주기를 같이 지운다 | 양식만 지우면 TODO·주기가 고아로 남는다 | 양식 삭제 SP · `tbl_todo` · `tbl_schedule_rule` |
| 문서주기 저장이 미래 TODO 를 갈아끼운다 | 규칙을 바꾸면 아직 안 온 할일은 새 규칙이어야 한다 | 주기 저장 SP · TODO 재생성 |

---

## 이번에 고친 SP (라이브 반영)

정본은 `db_sasshaccp/01_sp.sql`. 이미 깔린 DB 는 `01_sp.sql` 만 `psql -v ON_ERROR_STOP=1 -f` (전체 7본 `apply-all` 이 아니다 — 시드를 다시 돌리지 않는다). 서명 스냅샷 둘은 파일 안에 `DROP PROCEDURE` 옛 5인자가 있다.

**적용 (2026-09-08):** 호스트의 `sasshaccp`(라이브) · `sasshaccp_test` 둘 다 `01_sp.sql` 을 돌렸다. 서명 SP 인자에 `p_id` 가 있는 것까지 확인했다. API 컨테이너를 안 재기동해도 SP 는 바로 먹는다. Java·프론트 변경은 배포(이미지/빌드)가 나가야 한다.

| SP | 한 일 |
|---|---|
| `sp_tbl_document_title_u_000` | REQ/APV 제목 잠금 |
| `sp_tbl_document_approval_c_000` | HTML 빈 본문 · CCP 빈 판정 · 부적합 이탈 공란 차단 |
| `sp_tbl_ccp_metal_monitor_c_000` | 감도행 0건 거부 |
| `sp_tbl_document_approval_u_000` | UNDO 시 `file_kind=PDF` 메타 삭제 |
| `sp_ccp_verify_sign_u_000` | `p_id` 추가. 본인 도장만 복사 |
| `sp_tbl_hyg_process_sign_u_000` | 위와 같음 |
| `sp_tbl_ccp_pkg_monitor_c_000` | 행 도장: `checkerId = p_id` 일 때만 |
| `sp_tbl_ccp_htg_monitor_c_000` | 위와 같음 |
| `sp_tbl_approval_line_c_000` | APPROVE 단계 2개 이상 거부 |
| `sp_tbl_corrective_action_c_000` | DONE 건 UPDATE 거부 |

확인:

```sql
SELECT p.proname, pg_get_function_identity_arguments(p.oid)
  FROM pg_proc p
  JOIN pg_namespace n ON n.oid = p.pronamespace
 WHERE n.nspname = 'sasshaccp'
   AND p.proname IN (
     'sp_ccp_verify_sign_u_000',
     'sp_tbl_hyg_process_sign_u_000'
   );
-- 인자에 p_id character varying 이 있어야 한다. 옛 5인자만 있으면 DROP 이 안 된 것.
```

빈 DB 는 7본 `apply-all.sh` 그대로.
