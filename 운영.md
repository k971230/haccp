# HACCP 운영 핸드북

> 대상: main 반영 후 배포·감시·장애 대응하는 사람  
> Jenkins **최초 설치·Credentials·Job 생성**은 [`환경구축.md`](환경구축.md) §11  
> 코드 수정 루프는 [`개발.md`](개발.md)  
> 시크릿 실값 금지 · 정본 보강 [`DEPLOY.md`](DEPLOY.md)

---

## 1. 운영 토폴로지

```text
Browser
  → Apache (TLS, Path)
       /haccp/  → edge(haccp-nginx) → web(정적 SPA, VITE_BASE=/haccp/)
       /api/    → edge → api(haccp-api :7070)
       /rhwp/   → (설정에 따라) 스튜디오/프록시
```

| 구성요소 | 역할 |
|----------|------|
| Apache | 공인 진입 · Path 분기 · (self-signed 또는 공인서) |
| haccp-nginx (edge) | 루프백 publish 예: `127.0.0.1:17070:7070` |
| haccp-api | JWT API · 파일 볼륨 · SP 호출 |
| haccp-web | Vite 빌드 정적 · basename `/haccp/` |
| GHCR | `ghcr.io/k971230/haccp-{api,web,nginx}` |
| PostgreSQL | DB/스키마 `sasshaccp` (외부 또는 compose) |

운영 URL 예: `https://180.71.58.87/haccp/login`  
(`Jenkinsfile`의 `SMOKE_BASE_URL` · `SMOKE_WEB_PREFIX=/haccp` 와 정합.)

상세 다이어그램·이미지 크기: 런북 §8–§10.

---

## 2. Jenkins Job 두 개

| Job | Script Path | 역할 |
|-----|-------------|------|
| **haccp-deploy** | `Jenkinsfile` | 빌드·이미지·(migrate)·배포·스모크 — **서버를 바꿈** |
| **haccp-audit** | `Jenkinsfile.audit` | 야간 규약 검사 — **배포 안 함** |

설치·Credentials·첫 등록: [`환경구축.md`](환경구축.md) §11.

### 2.1 Credentials (이름만 — 값은 Jenkins UI)

| ID | 종류 | 용도 |
|----|------|------|
| `haccp-registry-cred` | Username/password | GHCR |
| `haccp-deploy-ssh-key` | SSH private key | prod SSH |
| `haccp-deploy-host` | Secret text | `user@host` |
| `haccp-github-token` | Secret text | audit 이슈 등 |
| `haccp-env-docker` | Secret file | `.env.docker` 원본 |
| `haccp-smoke-user` | Username/password | prod smoke 로그인 |

---

## 3. 일상 트리거 — Build Now

**현재(개발/테스트):** Jenkins가 localhost이면 GitHub webhook이 동작하지 않는다.

| 오해 | 사실 |
|------|------|
| main 머지하면 자동 배포 | **아니오** |
| Reload Configuration만 하면 됨 | **빌드 시작 안 함** |
| 대기열이 비어 있다 | webhook 미연결이면 **정상** |
| 할 일 | `haccp-deploy` → **Build Now** |

`Jenkinsfile`의 `githubPush()`는 외부 URL이 생기기 전에는 사실상 무력하다.  
이후: `https://<jenkins>/github-webhook/` + Push events (런북 §13.1).

**안 누르면 운영 이미지·컨테이너 상태가 안 바뀐다.**

---

## 4. haccp-deploy 스테이지 (현행 Jenkinsfile)

정본: 루트 [`Jenkinsfile`](Jenkinsfile).

```text
Checkout
  → BE test & compile
  → FE test & build
  → DB migrate dry-run
  → Build images
  → Push images (TAG=1.0.${BUILD_NUMBER} + latest)
  → Prod DB migrate
  → Deploy to prod
  → Prod smoke
```

| 스테이지 | 하는 일 |
|----------|---------|
| Checkout | `main` SCM · `git log -1` |
| BE | `mvnw` test · package (`MAVEN_HOME` unset) |
| FE | `npm ci` · `tsc` · lint · test · `npm run build` |
| DB dry-run | `scripts/db_migrate_dryrun.sh` |
| Build/Push | `build_images.sh` · GHCR login · push api/web/nginx |
| Prod migrate | SSH 후 compose `--profile migrate run --rm migrate` |
| Deploy | `scripts/deploy_remote.sh` · 원격 pull/up · 배포 디렉터리 `DEPLOY_DIR` |
| Smoke | `scripts/prod_smoke.sh` · `SMOKE_BASE_URL` + `/haccp` |

환경 변수(파일에 하드코딩된 비시크릿):

| 변수 | 현행 값·의미 |
|------|----------------|
| `REGISTRY` | `ghcr.io/k971230` |
| `TAG` | `1.0.${BUILD_NUMBER}` |
| `DEPLOY_DIR` | `/home/ubuntu/haccp` (sudo 없이 홈 배포 — `/opt/haccp` root 권한 이슈 회피) |
| `SMOKE_BASE_URL` | `https://180.71.58.87` |
| `SMOKE_WEB_PREFIX` | `/haccp` |
| `SMOKE_INSECURE` | `1` (self-signed 시) |

`options`: `disableConcurrentBuilds()` · 45분 timeout · 로그 30개 보관.

### 4.1 DB 는 파이프라인이 안 건드린다

**Jenkins 는 DB 를 만지지 않는다.** 스키마 변경은 되돌리기 어렵고
배포와 같은 트랜잭션으로 묶을 수 없어 실패하면 반쪽 상태가 남는다.

DB 반영은 배포 담당이 따로 돌린다.

```bash
PGHOST=호스트 PGUSER=계정 PGPASSWORD=*** bash db_sasshaccp/apply-all.sh
```

**파이프라인과 이 핸드북이 다르면 `Jenkinsfile` 을 따른다.**

### 4.2 배포 디렉터리

- Jenkins 원격 작업 기준: **`/home/ubuntu/haccp`** (`DEPLOY_DIR`).
- 런북 일부 절에 `/opt/haccp/.env.docker` 서술이 남아 있을 수 있다.  
  실제 서버 경로가 홈인지 `/opt`인지는 **서버·Credentials·compose 실행 cwd**로 확인한다.
- SSH 경로를 Git Bash가 깨뜨리지 않게 `//home/ubuntu/haccp` 리터럴·`MSYS_NO_PATHCONV`를 쓰는 단계가 있다.

---

## 5. haccp-audit (야간 감시)

정본: [`Jenkinsfile.audit`](Jenkinsfile.audit).

- cron: `H 18 * * *` (UTC) ≈ KST 03:00.
- 타임아웃 5분.
- 스크립트:

| 스크립트 | 검사 |
|----------|------|
| `scripts/audit_version_drift.sh` | README 스택 vs package/pom |
| `scripts/audit_ops_delete.sh` | HTTP DELETE·validate/delete 짝 |
| `scripts/audit_file_size_alignment.sh` | 파일 크기 키 정합 |
| `scripts/audit_docs_links.sh` | docs 상대 링크 |

실패 시 `haccp-github-token` + `gh issue create` (없으면 스킵 로그).  
**이미지를 빌드·배포하지 않는다.** 배포는 항상 `haccp-deploy`.

---

## 6. 태그 · 롤백

- 매 성공 빌드: `1.0.<BUILD_NUMBER>` 와 `latest` 동시 push.
- 롤백: 이전 TAG로 원격 `pull`/`up` (절차 상세는 런북 §12 롤백·§14).
- 실패 시 콘솔 로그 → Credentials/Docker/SSH/smoke 순으로 좁힌다.

---

## 7. 스모크

### 7.1 파이프라인

`scripts/prod_smoke.sh` — 배포 직후 자동 (로그인·핵심 API 등, 스크립트 단계 수 기준).

### 7.2 수동

[`docs/6_테스트.md`](docs/6_테스트.md).

배포 후 사람이 볼 URL:

- `https://180.71.58.87/haccp/login` — 스플릿·HACCP 로고·RMA favicon
- 로그인 성공 · 메뉴 · 로그아웃 후 주소가 **`/haccp/login`** 인지 ( `/login` 만 되면 Path/basename/`VITE_BASE` 문제)

---

## 8. Path · 로그아웃 · favicon

| 항목 | 기대 |
|------|------|
| SPA base | Docker 빌드 `VITE_BASE=/haccp/` |
| Router basename | `import.meta.env.BASE_URL` |
| 로그아웃 | `loginBrowserPath()` → `/haccp/login` |
| favicon | `%BASE_URL%logo2.svg` |

증상: 로그아웃 후 `https://호스트/login` (haccp 없음)  
→ 옛 이미지(베이스 `/`) 또는 잘못된 하드코딩. **새 web 이미지 배포** + FE `loginBrowserPath` 코드 확인.

---

## 9. 시크릿 회전 · 서버 파일

| 작업 | 요약 |
|------|------|
| JWT 회전 | `openssl rand -hex 48` → 서버 `.env.docker`의 `JWT_SECRET` → api 재기동 → **기존 토큰 전부** |
| GHCR PAT | Jenkins `haccp-registry-cred` 갱신 |
| SSH 키 | `haccp-deploy-ssh-key` 갱신 · 서버 `authorized_keys` |
| smoke 계정 | `haccp-smoke-user` |

서버 `.env.docker`는 git에 없고 권한 `0600`급으로 관리한다.  
상세: 런북 §12.

---

## 10. 장애 대응 빠른표

| 증상 | 볼 곳 |
|------|-------|
| 빌드 큐 없음 | Build Now 했는지 · webhook 기대하지 말 것 |
| BE mvnw 실패 | `MAVEN_HOME` · JDK 17 · Git Bash |
| FE npm 실패 | Node 20 · lockfile · 네트워크 |
| GHCR push 거부 | PAT 권한 · `haccp-registry-cred` |
| SSH/deploy 실패 | host·키·`DEPLOY_DIR` · Windows path |
| migrate 실패 | DB 권한 · SQL · OUT 시그니처(과거 이슈) |
| smoke 로그인 실패 | **먼저 계정이 아니라 Jenkins credential 을 본다** — 아래 10.1 |
| rhwp 편집기 ServiceWorker 오류 | 자체서명 인증서. 코드로는 못 고친다 — 아래 10.2 |
| smoke 가 「화면 부재」로 실패 | 스모크가 개명·삭제된 화면코드를 보고 있다 — 아래 10.3 |
| CORS (로컬) | API `CORS_ALLOWED_ORIGINS=http://localhost:4173` |
| 401 루프 | 토큰·시계·JWT_SECRET 불일치(회전 직후) |
| 로그아웃 Path 누락 | web 이미지 `VITE_BASE` · FE 재배포 |

### 10.1 smoke 로그인 실패 — 계정보다 자격증명을 먼저 본다

2026-08-26 에 실제로 겪었다. 파이프라인 6단계 중 배포까지 다 통과하고
`Prod smoke` 만 `LOGIN_FAIL` 로 떨어졌다. **배포는 성공한 상태였다.**

가르는 법 — 운영에 직접 쳐 본다.

```sh
curl -sk -X POST https://<운영>/api/v1/auth/login \
  -H "Content-Type: application/json" -d '{"userId":"smoke","password":"1234"}'
```

| 결과 | 뜻 |
|---|---|
| **200** | 계정·서버 정상. **Jenkins credential `haccp-smoke-user` 의 비밀번호가 틀렸다** |
| 400 `LOGIN_FAIL` | 계정 쪽이다 — 아래 DB 를 본다 |

```sql
SELECT user_id, use_yn, lock_yn, login_fail_cnt FROM tbl_user WHERE user_id='smoke';
```

`lock_yn='Y'` 면 잠긴 것이다. 임계값은 `LOGIN_MAX_FAIL_COUNT`(기본 **5**) —
비밀번호를 안 고치고 Build 를 다섯 번 누르면 잠긴다.

`prod_smoke.sh` 는 값 앞뒤 공백과 CR 을 이미 지운다(`trim`). Windows Jenkins 가
붙이는 개행 때문이 아니라면 저장된 값 자체가 다른 것이다.

### 10.2 rhwp 편집기 ServiceWorker 오류

```
Failed to register a ServiceWorker for scope ('https://<운영>/rhwp/')
An SSL certificate error occurred when fetching the script
```

**프록시 잘못이 아니다.** `/rhwp/` 와 `/rhwp/sw.js` 둘 다 200 을 준다.

브라우저는 **인증서 오류가 있는 연결에서는 ServiceWorker 등록을 거부한다.**
「고급 → 계속 진행」을 눌러 화면을 열어도 이 규칙은 안 풀린다.

지금 운영은 자체서명이다.

```
subject=CN = 180.71.58.87
issuer =CN = 180.71.58.87     ← 자기가 자기를 발급
```

**코드로는 못 고친다.** 셋 중 하나다.

| 방법 | 언제 |
|---|---|
| 도메인 + Let's Encrypt | 정공법. `HACCP_SERVER_NAME` 이 이미 도메인을 받게 돼 있다 |
| 사설 CA 를 클라이언트 PC 에 신뢰 등록 | 사내망만 쓰고 PC 대수가 적을 때 |
| 그냥 둔다 | 편집기가 SW 없이도 돌면 콘솔 경고일 뿐이다 — **HWP 열기·저장을 한 번 해 보고 판단한다** |

### 10.3 스모크가 「화면 부재」로 실패할 때

```
==> 6) approval-history leaf (STEP 03 회귀, scrnCd)
approval-history 화면(scrnCd) 메뉴 부재 — STEP 03 회귀
```

**배포는 이미 끝난 뒤다.** 스모크가 없어진 화면을 보고 있는 것이다.

2026-08-26 에 실제로 그랬다. 28화면 정리 때 화면이 바뀌었는데 스모크만 옛 이름을 봤다.

| 스모크가 보던 것 | 실제 |
|---|---|
| `approval-history` | `sign-ok`(결재완료)로 개명 |
| `/api/v1/docs/ccp/ccp-cold-monitor/list` | 화면이 사라졌다 — 404 |
| `/api/v1/bas/company-templates/list` | 죽은 `bas` 경로 — 404 |

**고친 방식:** 화면 이름 하나를 박지 않는다. leaf 에 `scrnCd` 가 실려 오는지와
랜딩(`today-tasks`)만 본다 — 개명에 안 흔들린다.

고를 때 기준이 하나 더 있다. **smoke(VIEWER) 계정이 실제로 읽을 수 있어야 한다.**
사용양식관리는 VIEWER 에게 read 권한이 없어 403 이다.

```sh
# 손으로 돌려 보기
SMOKE_USER=smoke SMOKE_PASS=1234 SMOKE_INSECURE=1 SMOKE_WEB_PREFIX=/haccp   bash scripts/prod_smoke.sh https://<운영>
```

---

## 11. 배포 체크리스트 (매 릴리스)

1. [ ] feature PR이 **main에 머지**됨 (`git log origin/main -1`)
2. [ ] Jenkins `haccp-deploy` → **Build Now**
3. [ ] 콘솔 전 스테이지 초록 · `TAG=1.0.x` 기록
4. [ ] `https://…/haccp/login` UI·로그인·로그아웃 Path
5. [ ] (필요 시) 스모크 매트릭스 수동 항목
6. [ ] 실패 시 롤백 또는 핫픽스 브랜치 (`hotfix/<slug>`)

`haccp-audit`는 배포 체크리스트에 **넣지 않는다** (감시 Job).

---

## 12. 백업과 되돌리기

### 12.1 뜨는 것은 둘이다

```sh
cd /opt/haccp
docker compose --profile backup run --rm backup        # DB   -> haccp-db-backup 볼륨
docker compose --profile backup run --rm backup-files  # 파일 -> 같은 볼륨에 tar.gz
```

**둘을 같이 뜬다.** DB 만 되돌리면 문서 행은 살아나고 그 문서의 첨부는 사라진다.
규제 문서를 다루는 시스템에서 그건 복구가 아니다.

덤프는 `haccp-db-backup` 볼륨에 쌓인다 — **같은 호스트 안이다.**
호스트가 통째로 죽으면 백업도 같이 죽는다. 그래서 밖으로 뺀다.

```sh
bash scripts/backup_export.sh /mnt/backup            # 로컬 경로
bash scripts/backup_export.sh user@host:/srv/backup  # 원격 (scp)
```

**DB 덤프와 파일 tar 를 짝으로 뺀다.** 하나만 있으면 복구가 반쪽이다 —
파일 tar 가 없으면 스크립트가 그렇다고 알린다.

지우는 것도 같이 한다. 안 지우면 무한히 쌓여 디스크가 차고, 그러면 배포가 멈춘다.

```sh
KEEP_DAYS=14 bash scripts/backup_export.sh /mnt/backup   # 기본 14일
DRY_RUN=1    bash scripts/backup_export.sh /mnt/backup   # 지우지 않고 보여만 준다
```

### 매일 자동으로 — cron

```
10 3 * * * cd /opt/haccp && \
  docker compose --profile backup run --rm backup && \
  docker compose --profile backup run --rm backup-files && \
  bash scripts/backup_export.sh /mnt/backup >> /var/log/haccp-backup.log 2>&1
```

**아직 손이 필요한 것** — `/mnt/backup` 자체가 이 호스트에 있으면 뺀 게 아니다.
다른 장비·NAS·오브젝트 스토리지로 두거나 원격 경로(`user@host:`)를 쓴다.


### 12.2 되돌리기 — 실제로 해봤다 (2026-08-26 · 2026-08-27)

```sh
RESTORE_CONFIRM=<대상DB> bash scripts/db_restore.sh <덤프파일> <대상DB>
```

대상 DB 를 **지우고 다시 만든다.** 그래서 이름을 두 번 확인시킨다.

실측 (시험용 DB · 표 53 · SP 152)

| 단계 | |
|---|---|
| 덤프 크기 | 777 KB |
| 복구 | **17초** |
| 결과 | 표 53 · SP 152 · 문서 1 · 사용자 5 — 원본과 같음 |
| 앱 기동 | 3.3초 · 로그인·메뉴·문서·작성양식 API 전부 200 |

2026-08-27 에는 **볼륨 밖으로 뺀 덤프**로 한 번 더 했다 —
`backup_export.sh` 로 빼고 `db_restore.sh` 로 되돌린 뒤 앱까지 띄웠다.
빼기 → 되돌리기 → 앱 기동, 고리가 끊기지 않는다.

**겪은 것 하나** — 접속이 하나라도 남아 있으면 `DROP DATABASE` 가 거절된다.

```
database "..." is being accessed by other users
```

스크립트가 먼저 `pg_terminate_backend` 로 끊는다. 손으로 할 때도 그래야 한다.

### 12.3 표 수가 맞는 것과 앱이 도는 것은 다르다

되돌린 뒤 **반드시 앱을 띄워 로그인까지 해본다.**

```sh
curl -X POST http://<API>/api/v1/auth/login \
  -H "Content-Type: application/json" -d '{"userId":"admin","password":"..."}'
```

시퀀스가 안 따라오면 표는 멀쩡한데 다음 INSERT 가 중복키로 죽는다.
`-Fc` 덤프는 시퀀스를 같이 담지만, 확인은 공짜다.

---

## 13. DB · 스키마 운영 메모

- **정본은 [`db_sasshaccp/`](db_sasshaccp/) 6본**이다. 6본 모두 재실행 안전하다.
- 파이프라인은 DB 를 만지지 않는다 (위 4.1). 배포 담당이 `apply-all.sh` 를 따로 돌린다.
- 새 업체 개설은 `CO_CD=0001 ... bash db_sasshaccp/apply-all.sh`.
- **롤백 SQL 은 없다.** 스키마를 바꾸기 전에 되돌릴 방법을 먼저 정한다 —
  `docker compose --profile backup run --rm backup` 으로 dump 를 떠 둔다.
**파일도 같이 떠야 한다** — 아래 12.1.

---

## 14. 더 깊은 정본

| 주제 | 링크 |
|------|------|
| 전체 런북 | [`DEPLOY.md`](DEPLOY.md) |
| 후속 STEP | [`docs/8_결정_이력.md`](docs/8_결정_이력.md) |
| 환경 구축·Jenkins 설치 | [`환경구축.md`](환경구축.md) |
| 개발 규약 | [`개발.md`](개발.md) |
| 통합 스펙 | [`docs/1_시작하기.md`](docs/1_시작하기.md) |
